# TASK ENVELOPE — P11
id: P11   branch: task/P11   worktree: /root/ngfw-wt/P11   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave B (day 10-12): strongSwan+VPP build (staging sysroot) + IPsec S2S + tunnel dashboards
prompt: prompts/P11-strongswan-vpp.md   (template: none)   wbs: D6.1, D6.2, D6.10
scope: strongSwan+VPP build (staging sysroot) + IPsec S2S + tunnel dashboards — the RF-2 renderer, DF-5 descriptors and the P02c schema exist: build the vrx-strongswan package (kernel-vpp) and wire them end to end
merged deps you can rely on: P08 (vertical slice; `subsystems.Wiring.IPsecOptions()` = persisted BootStore + D-096 keyer + globals flag), RF-2 (renderers/strongswan + swantest harness), DF-5 (descriptors/{ipsec,vpn}, `ipsec.NewCharonSweeper`) (+ pre-wave merges TD-2, TD-3: TD-3's interface sanitizer is in `ipsec.itf` Create)
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI)
daemon-owner: strongswan (charon) — slot test instances only: your built charon (kernel-vpp) and the stock charon from debs unpacked under /run/vrx-test/w<SLOT>/ (RF-2 README), in slot namespaces, VICI sockets under /run/vrx-test/w<SLOT>/swan/; strongSwan is NOT installed on the host (D-083) and must stay so; stop every charon you started by PID
files you own exclusively: deploy/strongswan/** deploy/debian/vrx-strongswan/** apps/agent/internal/renderers/strongswan/** (gap-only: `AckRestart(ctx, since)` per DF-5 Q13, kernel-vpp plugin list/paths, the IPsec state message; keep RF-2's owner-prefix scoping) apps/agent/internal/descriptors/ipsec/** (gap-only; keep TD-3's sanitizer hunk in itf.go) apps/agent/internal/charon/** (charon lifecycle: stop → sweep → start → AckRestart, SA state/event glue) apps/agent/internal/desired/ipsec*.go apps/agent/internal/subsystems/ipsec*.go apps/agent/internal/agent/rpc_ipsec*.go packages/schema/src/semantic/ipsec*.ts apps/api/src/features/ipsec/** apps/api/test/e2e/ipsec* apps/web/src/domains/vpn/ipsec/** apps/web/src/locales/{en,fa}/ipsec.json docs/user/vpn/ipsec.md docs/agent/renderers/strongswan.md docs/agent/descriptors/ipsec.md test/topology/ipsec/** docs/status/tasks/P11* — plus apps/agent/internal/secrets/** ONLY if the manager assigns the API→agent secret channel to P11 (see coordination)
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["vpn"]` — shared with F-wireguard and later F-ikev2-native: whoever lands first adds the key, the others append; one Register line; new `*Wiring` methods in your subsystems/ipsec.go) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (RPC methods live in your rpc_ipsec.go) · C1 packages/schema/src/domains/vpn.ts (tunnel/profile names max(60), D-089 — one line) · C2 packages/schema/src/semantic/index.ts (one spread) · C5 packages/proto/vrx/v1/dataplane.proto (IpsecState rpc + messages in a `// ----- P11 -----` section, SA event kind) · C6 docs/contracts/proto.md · C7 generated files (regen, never hand-merge) · P1 apps/api/src/app.module.ts · P4 apps/api/src/agent/agent.client.ts · P5 apps/api/src/testing/fake-agent.ts (stub handler under the anchor; real fake in features/ipsec/fake.ts) · P6 apps/api/src/infra/bus.ts (ipsec SA topic) · W1 apps/web/src/router.tsx · W2 apps/web/src/nav/nav.ts (+nav.test.ts: BUILT_DOMAINS 'vpn') · W3 apps/web/src/i18n.ts · vpn domain page + tab registry apps/web/src/domains/vpn/{VpnPage.tsx,tabs.ts} (shared with F-wireguard; the manager seeds it before spawn — if it is not on main, do NOT create it (two parallel creators = add/add conflict): register your screen as its own route + NavItem under W1/W2 and the manager folds it into the shell at merge) · apps/agent/internal/renderers/ALLOWLIST.md (rows only if you add an exec) · subsystems.Env event sink — seeded by the manager in the anchor commit (the agent bus is unexported; shared with F-neighbors-ra, F-object-model, P11, P12, F-wireguard); never add it yourself in agent.go (A5) — if it is absent, write the question and keep the publisher behind an interface in your own files · A7 docs/vpp-code-track.md
contract numbers (wave-A-hotspots §2, binding since D-109 e; restated in docs/status/wave-BC-numbers.md "Batch-2 follow-ons"): **EventKind 12** (IPsec SA state; up/down/rekey go in an attribute — a second kind would take F-wireguard's 13), **ActionRequest 8** (initiate/terminate, optional), `IpsecTunnel` 27–29 only for a proven gap (30–31 are F-ikev2-native's), rpc `IpsecState` + messages in your section — nothing else, never "next free"; commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/P11-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp (read-only: copy extras/strongswan/vpp_sswan out, never build or `make` in place), apps/agent/binapi (P04/manager-owned), apps/agent/internal/descriptors/{vpn,ikev2,wireguard,lcp}/** (DF-5 shared helpers / F-ikev2-native / F-wireguard / P12), apps/agent/internal/agent/{agent,service,state,ifstate}.go (A5), apps/api/src/{secrets,commit}/** (a need → questions file), tools/ci.sh, tools/lab
host facts: strongSwan not installed; no package installs (D-083) — `apt-get download` + `dpkg -x` under /run/vrx-test/w<SLOT>/ only · staging sysroot from /root/vpp/build-root/{vpp-dev,libvppinfra-dev}_26.06-release_amd64.deb via `dpkg -x` (never dpkg -i) · libsystemd-dev, libssl-dev, autotools present; bison/flex/gperf absent → release tarball, not the GitHub archive · kernel-vpp (vpp_sswan) is out-of-tree, policy-based only (no tunnel protect/if_id), tested upstream with 5.9.5/5.9.6, expects IKE to reach charon through a linux-cp pair · linux_cp/linux_nl loaded since D-060, no `linux-cp { default netns }` → linux_nl listens in the root netns · VPP's ikev2 plugin claims UDP 500/4500 inside VPP once any IKEv2 profile exists → IKE/NAT-T on slot ports (proposal 20000+100×<SLOT>+20/+21; DF-5 uses +0…+2 and +10/+11) · no data NICs bound (D-026): af_packet rig only (D-010)
obligations accumulated from decisions (all must be honoured and listed in P11.md with evidence): D-096 charon gets its own SPD/SA id range disjoint from the descriptors' (`WithIDRange` in tests), order stop charon → `Sweep` → start → `AckRestart`, keyed `hmac:` fingerprints only · DF-5 Q13 / D-089: `AckRestart(ctx, since)` acks only the charon start time you observed (not option (b)); flush orphaned SAs after every charon restart · D-089 owner-prefix scoping on a shared charon (`WithOwnerPrefix`), names max(60) · D-083 IPsec state/event messages are your contract addition · D-071 `ipsec.backend`/`async-mode` only in the globals owner (slot agents VRX_GLOBALS_OWNER=0 require, never set) · D-082 globals lock for tests reading VPP-wide IPsec settings · D-051 PSKs are `psk/<name>` refs, never inline, never in logs/GET/fixtures (test literal `VRX_TEST_PSK_P11_*`) · RF-2 review M1 (old CHILD_SAs keep running after a selector/PSK change — close it or document) and M3 (charon crash residue in VPP → sweep) are yours
coordination: (1) renderer stage — no renderer is called by the agent today (`service.go` runs only the scheduler); default = wrap RF-2 in one singleton scheduler descriptor under `Domains["vpn"]` (the F-host-acl-nftables default (a)) unless the manager has put a shared renderer stage on main; log the choice with options · (2) secret channel — nothing carries secret material API→agent (D-040 strips secret leaves; `vpn.Resolver` is an interface only); P08 says "P11 adds the secret resolver": before building it write the design with options (API commit path vs agent pull RPC; never persisted in desired.pb; opaque to fmt/slog) in P11-questions.md and keep going on everything else — it is a security-boundary / secret-storage decision (decision-policy #4), so the manager files PENDING-secret-channel and you do not implement a channel before the answer; tests use a slot-local `vpn.MapResolver` fixture with `VRX_TEST_PSK_P11_*`; F-wireguard and P12 consume it · (3) F-wireguard (parallel) shares `Domains["vpn"]`, the vpn page and `descriptors/vpn` (read-only for both) · (4) F-tunnels (parallel) owns the `tunnels.ipip` projection that `routeBased.ipipInterface` names · (5) P12 (parallel) owns the linux-cp pair model; your IKE path uses a test-fixture pair through DF-8's `lcp.itf-pair` on a slot interface (never ens*) · (6) F-ikev2-native and F-pki depend on you (D-099)
V19/V24 SAFETY (D-095, D-101): before ANY ping/ESP through the rig, run TD-3's preflight on the rig interfaces' sw_if_index (no classify/ACL/foreign SPD binding) and never send packets through an unchecked interface; every af_packet delete with its veth down; the kernel-vpp charon talks to the shared VPP — it may bind SPDs and add routes only on your slot's interfaces and tables <SLOT>000–<SLOT>999; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b (kept outside the repo) for screenshots; paste `vppctl show ipsec sa` with keys redacted
time box: 24 h — when exceeded: stop, commit WIP, write docs/status/tasks/P11.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/P11-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/P11.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
questions: docs/status/tasks/P11-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
