import { UnboundChronySyslogController } from './controller.js';

/** F-unbound-chrony-syslog: controllers and providers, spread into AppModule under the feature's anchors (P1). */
export const unboundChronySyslogFeature = {
  controllers: [UnboundChronySyslogController],
  providers: [],
};
