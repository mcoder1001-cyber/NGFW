# TASK ENVELOPE — F-dashboard-prom-alarms
id: F-dashboard-prom-alarms   branch: task/F-dashboard-prom-alarms   worktree: /root/ngfw-wt/F-dashboard-prom-alarms   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave C (day 13-15): dashboard, Prometheus exporter/prom plugin, alarms/events, tunnel dashboards
prompt: prompts/features/F-dashboard-prom-alarms.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main + P08 in wave-B/C prep)   wbs: D8.1, D8.3, D8.4, D6.10
scope: agent-side exporter families (stats segment) on the existing `/metrics` + a configurable external listener, the `management.prometheus`/`management.alarms` contract, an API alarm engine (thresholds + hysteresis, PostgreSQL table, webhook delivery), `/state/dashboard` + `/state/alarms`, the dashboard page with tiles/charts and a tunnel card for merged tunnel features only, Grafana JSON, docs; the VPP prom plugin (startup.conf, V18) is documented, not enabled
merged deps you can rely on: P08 (desired/subsystems/projection patterns; InterfaceState), DF-8 (D-077: prom has no binary API, dropped from DF-8) (+ through P08: P05 agent `/metrics` (`agent/metrics.go`, `vrx_agent_*` incl. `retrieve_unsupported`), `StreamStats`/`StreamEvents`/link watcher; P06 telemetry relay, bus `TOPICS`, `/state/system`, `/state/events`, `system_event` table; P07a `DashboardPage.tsx` placeholder) (+ on main by then: TD-2, TD-3, W-seed; wave-A/B features — incl. P11, F-wireguard, F-tunnels for the tunnel card and F-unbound-chrony-syslog's `Domains["management"]` — only if merged when you start: check `git log main`)
read first: prompts/features/F-dashboard-prom-alarms.md · docs/status/wave-A-hotspots.md (§0 rules; A1 A2 A5 C1–C7 P1 P3 P4 P5 P6 W1–W3) · docs/status/wave-BC-numbers.md § F-dashboard-prom-alarms (incl. the DB migration protocol) · apps/agent/internal/agent/{metrics,telemetry,events}.go (read-only) · apps/api/src/telemetry/relay.service.ts, apps/api/src/infra/bus.ts, apps/api/src/audit/system-events.service.ts, apps/api/src/db/schema.ts · docs/vpp-code-track.md V18 · docs/decisions/LOG.md D-025, D-046, D-051, D-063, D-077, D-081, D-091, D-094, D-106, D-109
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; the configured external exporter listener in tests 127.0.0.1:3<SLOT>82, webhook receiver 127.0.0.1:3<SLOT>81; the loopback agent endpoint stays on VRX_METRICS_PORT)
daemon-owner: none (no Prometheus/Grafana/SMTP on the host; `prometheus-node-exporter` on :9100 is the **host's own** active service — never touch, scrape-test or reconfigure it)
files you own exclusively: apps/agent/internal/promexport/** deploy/grafana/** apps/agent/internal/desired/prometheus*.go apps/agent/internal/subsystems/dashboard_prom_alarms*.go apps/agent/internal/agent/rpc_dashboard*.go packages/schema/src/domains/ext/dashboard-prom-alarms*.ts packages/schema/src/semantic/dashboard-prom-alarms*.ts packages/schema/examples/dashboard-prom-alarms-*.json packages/proto/test/fixtures/dashboard-prom-alarms-*.json apps/api/src/features/dashboard-prom-alarms/** apps/api/test/e2e/dashboard-prom-alarms* apps/web/src/domains/dashboard/dashboard-prom-alarms/** apps/web/src/pages/DashboardPage.tsx apps/web/src/locales/{en,fa}/dashboard-prom-alarms.json docs/user/dashboard/dashboard-prom-alarms.md test/topology/dashboard-prom-alarms/** docs/status/tasks/F-dashboard-prom-alarms*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-dashboard-prom-alarms` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-dashboard-prom-alarms.md and say so in the questions file); never reorder or reformat other lines): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["management"]` — F-unbound-chrony-syslog adds it in wave B; if it is not on main, whoever lands first adds the key — the external-listener singleton descriptor name, one Register line; exporter background work starts from your own subsystems/dashboard_prom_alarms.go, never from agent.go) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A5 agent metrics collector hook — `internal/agent/metrics.go` is agent core: the manager seeds a registration seam so your families are served by the existing `/metrics` handler; if it is not on main, write the question and keep promexport behind a `Collector` interface in your own package (the one-line hookup is the manager's at merge) — never edit agent.go/metrics.go · C1 packages/schema/src/domains/management.ts (one key line each for `prometheus` and `alarms`; sub-schemas in your ext file — F-unbound-chrony-syslog edits other lines of this file) · C2 packages/schema/src/semantic/index.ts (one spread) · C3 packages/schema/src/index.ts (export of your ext file) · C4 new `dashboard-prom-alarms-*.json` fixtures only · C5 packages/proto/vrx/v1/dataplane.proto (`prometheus = 5`, `alarms = 6` one line each in `ManagementConfig`; EITHER `StatsBatch` 6–7 + `StreamStatsRequest` 4–5 as one-line insertions OR the `DashboardSnapshot` RPC under the service anchor — one path, logged with options; messages in a `// ----- F-dashboard-prom-alarms -----` section) · C6 docs/contracts/proto.md (`### F-dashboard-prom-alarms: …`) · C7 generated files (regen, never hand-merged: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`) · P1 apps/api/src/app.module.ts · P3 `POST /api/v1/actions/alarms/{id}/ack` as a static route in your own controller (never edit apps/api/src/actions/**) · P4 apps/api/src/agent/agent.client.ts (only with `DashboardSnapshot`; `streamStats`/`streamEvents` exist — the alarm engine opens its own streams, no relay edit) · P5 apps/api/src/testing/fake-agent.ts (only with a new RPC) · P6 apps/api/src/infra/bus.ts `TOPICS` (one `alarm.events` line) · DB apps/api/src/db/schema.ts (alarm tables under your anchor) + apps/api/migrations/** (generated by `pnpm -C apps/api db:generate --name dashboard-prom-alarms`; regenerated by the manager at merge if main has a newer migration — wave-BC-numbers.md DB protocol) · W1 apps/web/src/router.tsx (only for an alarms route; the dashboard index route exists) · W2 apps/web/src/nav/nav.ts + nav.test.ts (only for an Alarms NavItem, labelKey in your namespace) · W3 apps/web/src/i18n.ts
contract numbers: docs/status/wave-BC-numbers.md § F-dashboard-prom-alarms — **ManagementConfig 5 `prometheus`, 6 `alarms`**; `StatsBatch` 6 `buffers` / 7 `node_errors` + `StreamStatsRequest` 4 `include_buffers` / 5 `include_node_errors` **or** RPC `DashboardSnapshot` (one of the two); new messages as listed there; nothing else (no ActionRequest member, no EventKind — alarms are raised in the API); commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-dashboard-prom-alarms-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp (incl. the startup.conf `prom` stanza), /root/vpp, apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned), apps/agent/internal/agent/{agent,service,state,ifstate,metrics,telemetry,events}.go + apps/agent/cmd/** (A5 core), apps/agent/internal/renderers/** (vppstartup = start-up generator; rsyslog/snmpd = other tasks), apps/agent/internal/desired/interfaces.go (A3), apps/api/src/telemetry/** and apps/api/src/state/** (P06 — read-only; your routes live in your controller), apps/api/src/{auth,users,secrets,commit,datastore,audit}/** (use the services, do not edit them), apps/web/src/shell/** and apps/web/src/HealthCard.tsx (reuse), tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (charts come from ui-kit; a need → questions file)
host facts: no Prometheus, promtool, Grafana, SMTP relay or `sendmail` installed (and nothing gets installed): exposition format checked by a parser test, Grafana JSON is import-ready only, webhook is the tested delivery · vrx-a has **no VPP worker threads** (main core only): worker CPU shows `vpp_main` only · stats segment `/run/vpp/stats.sock` (root:vpp); the P05 stats reader is unexported core — open your own govpp stats connection in promexport · VPP's `prom_plugin.so` is on disk and loaded but has no binary API — enabling it is a startup.conf `prom { … }` stanza (manager/handover), never this task
obligations accumulated from decisions (all must be honoured and listed in F-dashboard-prom-alarms.md with evidence): reuse, do not fork: the `vrx_agent_*` families (incl. `vrx_agent_retrieve_unsupported_*`, D-063) stay in `agent/metrics.go` and are served next to yours; stdlib only (no `prometheus/client_golang`, D4) · D-025 slot metrics port `9100 + 10·N + 1`; ports 3000/8080/9101/9100 are not yours · D-109 (d) `management.prometheus` = one singleton scheduler descriptor under `Domains["management"]` running the external listener with the allow-list enforced (Create/Update = (re)bind, Delete = close, Retrieve = what is bound) — log it with options · D-051/D-046/D-091 webhook tokens are `token/<name>` refs resolved by the API through its secrets service (API-side: no API→agent channel needed); never in GET, audit, logs or status files · the agent persists only implemented domains (`agent/state.go` `mergeDomains`): with `management` implemented, API-only leaves (users, aaa, tls, alarms) must not become noisy `agent.unsupported-field` warnings — coordinate with F-unbound-chrony-syslog's handling, questions file if unclear · D-094 no long-lived dev stack holding the lab lock · FAST MODE: rates are displayed, never benchmarked or claimed
coordination: (1) F-unbound-chrony-syslog (wave B) adds `Domains["management"]` and edits `management.ts` (syslog) — append, never re-add · (2) tunnel card: read only the state routes of P11 / F-wireguard / F-tunnels that are merged when you start, add none; otherwise "not available" · (3) F-licensing records its expiry event in `system_event` (subsystem `licensing`) — you may raise an alarm from it, no new topic for it · (4) F-snmp owns the snmpd renderer; SNMP trap targets are out of scope · (5) F-licensing may also add PostgreSQL tables — DB migration protocol, the manager regenerates at merge
V19/V24 SAFETY (D-095, D-101): before ANY traffic through the rig (counter evidence), run TD-3's preflight on the rig interfaces' sw_if_index and never send packets through an unchecked interface; every af_packet delete with its veth down; the link-down alarm test toggles only your own w<SLOT> interface; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots of the dashboard (en + fa/RTL); paste `curl` of the slot `/metrics` and the webhook receiver log
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-dashboard-prom-alarms.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-dashboard-prom-alarms-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-dashboard-prom-alarms.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite/webhook receiver/exporter listener), by PID · lab lock released · vrx_w<SLOT> dropped · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-dashboard-prom-alarms-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
