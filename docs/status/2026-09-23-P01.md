# P01 monorepo scaffold — done 2026-09-23

Host: root@172.30.126.195:/root/ngfw (Ubuntu 26.04, Node 22.23, pnpm 12.5, Go 1.26, buf 1.73).

Verified: `pnpm install && pnpm gen && pnpm typecheck && pnpm lint && pnpm test && pnpm build` all green
(16 tasks, 5 tests); `cd apps/agent && make test build` green; `bin/vrx-agent` starts, logs JSON, stops on SIGTERM.

Decisions made in P01 (state them, do not re-litigate):
- pnpm 12 needs `allowBuilds` in pnpm-workspace.yaml (old onlyBuiltDependencies is ignored).
- gRPC on the Node side: @grpc/grpc-js + ts-proto v2 (runtime @bufbuild/protobuf); Go side: protoc-gen-go(-grpc). buf drives both.
- Zod 4 native `z.toJSONSchema` produces JSON Schema 2020-12 and OpenAPI 3.1 components — no extra library.
- API tests: vitest + unplugin-swc (NestJS needs emitDecoratorMetadata).
- buf lint STANDARD with SERVICE_SUFFIX excepted (service is `Dataplane`, per contract).
- golangci-lint not installed yet (go vet only) — add in P09.

Not done / next: P02 schema, P03 proto, P04 lab (this host is vrx-a; VPP bring-up owned by another agent), P05–P09.

Dev smoke (2026-09-23): `node apps/api/dist/main.js` → GET /api/v1/health 200 `{"status":"ok","service":"vrx-api"}`,
Swagger UI /api/docs 200, /api/docs-json valid; Vite dev server 200 and proxies /api to the API. Fix needed: `@fastify/static`
is required by SwaggerModule under FastifyAdapter — added to apps/api.
