# TASK ENVELOPE — F-wireguard
id: F-wireguard   branch: task/F-wireguard   worktree: /root/ngfw-wt/F-wireguard   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave B (day 10-12): WireGuard peers/keys
prompt: prompts/features/F-wireguard.md   (template: prompts/FEATURE-TEMPLATE.md)   wbs: D6.5
scope: WireGuard peers/keys — DF-5's wireguard descriptors and the P02c schema/rules exist: projection, key-pair action, state/events, API, UI, docs
merged deps you can rely on: P08 (vertical slice: desired/subsystems/projection patterns, `Wiring.VPNKeyer()`/`BootStore()`), DF-5 (descriptors/wireguard + descriptors/vpn helpers) (+ pre-wave merges TD-2, TD-3: TD-3's interface sanitizer is in `wireguard.interface` Create)
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI)
daemon-owner: none (an optional kernel `wg` peer in ns-w<SLOT>-wan is a test fixture, not a daemon)
files you own exclusively: apps/agent/internal/descriptors/wireguard/** (gap-only; keep TD-3's sanitizer hunk in interface.go) docs/agent/descriptors/wireguard.md apps/agent/internal/desired/wireguard*.go apps/agent/internal/subsystems/wireguard*.go apps/agent/internal/agent/rpc_wireguard*.go apps/agent/internal/actions/wireguard/** packages/schema/src/semantic/wireguard*.ts apps/api/src/features/wireguard/** apps/api/test/e2e/wireguard* apps/web/src/domains/vpn/wireguard/** apps/web/src/locales/{en,fa}/wireguard.json docs/user/vpn/wireguard.md test/topology/wireguard/** docs/status/tasks/F-wireguard*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["vpn"]` — shared with P11 and later F-ikev2-native: whoever lands first adds the key, the others append; one Register line; `WireguardOptions()` in your subsystems/wireguard.go) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (WireguardState in your rpc_wireguard.go) · C1 packages/schema/src/domains/vpn.ts (`routeAllowedIps` — one key line) · C2 packages/schema/src/semantic/index.ts (one spread) · C5 packages/proto/vrx/v1/dataplane.proto (`WireguardInterface.route_allowed_ips`, WireguardState rpc + messages in a `// ----- F-wireguard -----` section, peer-state EventKind) · C6 docs/contracts/proto.md · C7 generated files · P1 apps/api/src/app.module.ts · P3 key-pair action = static route `POST /actions/vpn/wireguard/keypair` in your own controller · P4 agent.client.ts · P5 fake-agent.ts · P6 apps/api/src/infra/bus.ts (wireguard peer topic) · W1 router.tsx · W2 nav.ts (+nav.test.ts: BUILT_DOMAINS 'vpn') · W3 i18n.ts · vpn domain page + tab registry apps/web/src/domains/vpn/{VpnPage.tsx,tabs.ts} (shared with P11; the manager seeds it before spawn — if it is not on main, do NOT create it (two parallel creators = add/add conflict): register your screen as its own route + NavItem under W1/W2 and the manager folds it into the shell at merge) · subsystems.Env event sink for `peer.Events()` — seeded by the manager in the anchor commit (the agent bus is unexported; shared with F-neighbors-ra, F-object-model, P11, P12, F-wireguard); never add it yourself in agent.go (A5) — if it is absent, write the question and keep the publisher behind an interface in your own files · A7 docs/vpp-code-track.md
contract numbers: needs `WireguardInterface.route_allowed_ips` (next after 11 — the manager confirms), WireguardState rpc (+ messages), EventKind ×1 (peer handshake state) — from the manager's wave-B allocation table, never "next free"; commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-wireguard-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi (P04/manager-owned), apps/agent/internal/descriptors/{vpn,ipsec,ikev2}/** (DF-5 shared helpers / P11 / F-ikev2-native — a change goes to the questions file), apps/agent/internal/renderers/strongswan/**, apps/agent/internal/secrets/** and apps/agent/internal/charon/** (P11), apps/api/src/{secrets,commit}/** (use the secrets service, do not edit it), apps/agent/internal/agent/{agent,service,state,ifstate}.go (A5), tools/ci.sh, tools/lab
host facts: wireguard kernel module and wireguard-tools v1.0.20250521 present (optional kernel peer in the slot netns) · test listen ports 20000+100×<SLOT>+10/+11, never 51820 (DF-5 port scheme, docs/agent/descriptors/wireguard.md) · VPP allows several wg interfaces on one UDP port (the existing socket rule keys on underlayVrf + listenAddress + port) · `vppctl show wireguard interface` prints the private key (base64 AND hex) and the mac-key — every pasted output goes through a redaction filter · no data NICs bound (D-026): af_packet rig only (D-010)
obligations accumulated from decisions (all must be honoured and listed in F-wireguard.md with evidence): D-051 key/psk references only, never material in GET/logs/fixtures/status files · D-063 `wireguard.async-mode` write-only · D-065/D-069 `interface/wg<N>` alias, logical names · D-071 async mode only in the globals owner (slot agents VRX_GLOBALS_OWNER=0) · D-082 globals lock in tests reading VPP-wide settings · D-096 PSK fingerprints are keyed `hmac:` via `Wiring.VPNKeyer()`, never plain sha256 · DF-5 Q3 default (a) no src_ip dependency unless ordering fails · DF-5 Q8 you wire `peer.Events()` into StreamEvents · `generate_key` stays false (keys are generated API-side and stored through the secrets service)
coordination: (1) secret channel — nothing carries secret material API→agent today (D-040 strips secret leaves; `vpn.Resolver` is an interface + test MapResolver); P11 is the default owner (P08: "P11 adds the secret resolver"). If it is not on main when you reach the end-to-end step: finish everything else, run the host check with a slot-local `vpn.MapResolver` fixture, keep the E2E step open and write it in F-wireguard-questions.md (the channel is a security-boundary decision, decision-policy #4 → PENDING-secret-channel; only this E2E step waits on it, the task does not) — never put key material into DesiredState, a status file or a log · (2) P11 (parallel) shares `Domains["vpn"]`, the vpn page and `descriptors/vpn` (read-only for both) · (3) F-ikev2-native, F-ra-vpn later append to the same vpn domain/page
V19/V24 SAFETY (D-095, D-101): if you run the optional handshake packet test through the rig, run TD-3's preflight on the rig interfaces' sw_if_index first and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b (kept outside the repo) for screenshots
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-wireguard.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-wireguard-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-wireguard.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
questions: docs/status/tasks/F-wireguard-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
