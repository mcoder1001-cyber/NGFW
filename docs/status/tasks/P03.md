# P03 — gRPC contract agent↔api (`vrx.v1.Dataplane`) — status report

branch `task/P03` · worktree `/root/ngfw-wt/P03` · base `main@2de6c2f` · worker P03 (respawn after the usage-limit kill;
the first incarnation produced only the envelope commit) · slot 7 (no VPP/DB/daemon objects were needed or created)

## What was built

- **`packages/proto/vrx/v1/dataplane.proto`** (2004 lines, 918 comment lines, 146 messages, 6 enums, 7 RPCs, 603 fields —
  every field commented):
  - `service Dataplane { Apply, Retrieve, DryRun, StreamStats, StreamEvents, Action, Health }` — `Health` unchanged
    (fields 1–3 identical to main, 8 fields added).
  - Transactions: `ApplyRequest{txn_id, desired_state, subsystems[], confirm_timeout_sec, confirm_txn_id, owner}`,
    `ApplyResponse{txn_id, status APPLIED|FAILED|ROLLED_BACK|DEGRADED|CONFIRMED, results[] ObjectResult{key, op, code,
    message, pointer, subsystem}, summary, validation, applied_at, confirm_deadline, message}`, `DryRunRequest`,
    `ValidationReport{txn_id, ok, errors[]{pointer, message, severity, rule}, plan[], summary}`, `RetrieveRequest{subsystems,
    owner}` → `RetrieveResponse{desired_state, subsystems, owner, retrieved_at}`.
  - Telemetry: `StreamStatsRequest{interval_ms, interfaces[], include_worker_cpu}` → `stream StatsBatch{ts, seq,
    interface_counters[]{name, sw_if_index, rx/tx packets/bytes, drops, errors, punts, rx_misses}, worker_cpu[], interval_ms}`
    (absolute counters, 1 Hz default, ≤ 1000 interfaces); `StreamEventsRequest{kinds[], interfaces[]}` → `stream Event{ts,
    seq, kind LINK_UP|LINK_DOWN|RECONCILE_START|RECONCILE_DONE|ERROR|CONFIRM_REVERTED|VPP_CONNECTED|VPP_DISCONNECTED|DEGRADED,
    interface, message, txn_id, summary, attributes}`.
  - Actions: `ActionRequest{oneof ping|traceroute|capture}` (`PingAction{target, vrf, count, size, interval_ms, timeout_ms,
    source}`, `TracerouteAction{…}`, `CaptureAction{interface, bpf, max_packets, seconds, direction, snaplen}`) →
    `stream ActionOutput{oneof line|pcap_chunk|done ActionDone{summary, exit_code, stats}}`.
  - **`DesiredState`**: one field per root key in `ROOT_KEYS` order (1–13). Fully modelled per docs/04 + P02 §2:
    `SystemConfig`, `DataplaneConfig`, `Interface`/`Subinterface` (`map<string, Interface> interfaces`), `Vrf`
    (`map<string, Vrf> vrfs`), `RoutingConfig.static[] StaticRoute{prefix, next_hops[] NextHop{address, interface?, weight},
    vrf}`, `ManagementConfig.users[] ManagementUser`. Mirrored field-by-field from the committed P02b/P02c WIP models:
    `NatConfig` (NAT44 + pools/static/identity/LB mappings, timeouts, ipfix, nat64, nat66, nptv6, det44, dslite, map, cnat),
    `ObjectsConfig` (addresses, address_groups, services, service_groups, schedules, zones, tags), `AclConfig` (lists, macip,
    host + three attachment kinds), `VpnConfig` (ipsec settings/proposals/tunnels, wireguard, pki, remote_access).
    Target-shape shells with empty sub-messages: `routing.{prefix_lists, route_maps, bgp, ospf, isis, rip, bfd}`,
    `system.{ntp, dns}`, `management.{aaa, tls, syslog[]}`, `TunnelsConfig{gre, vxlan, ipip}`, `ServicesConfig{dhcp, dns,
    snmp, lldp, ipfix}`, `HaConfig{vrrp[]}`.
  - Mirror rules (documented in the file header and `docs/contracts/proto.md` §1): snake_case of the Zod names (protobuf
    JSON name == Zod name), records → `map<string, X>`, Zod enums → `string` with the values in the comment, `.optional()`
    scalars → proto3 `optional` (since the review fixes: **every** scalar, D-039), discriminated unions flattened.
    Property checked by the tests: the protobuf JSON of a valid configuration document (the schema examples and the
    proto fixtures, loaded as-is — none is run through `RootConfig.parse()` here; that drift guard is P03b's) is a
    `DesiredState`: strict `protojson.Unmarshal` (unknown field = error) on the Go side; structurally on the TS side
    (`toJSON(fromJSON(doc))` deep-equals `doc`), because ts-proto `fromJSON` is lenient (see "Review fixes").
- **Tooling** (`packages/proto/`): `buf.yaml` (STANDARD − SERVICE_SUFFIX − RPC_RESPONSE_STANDARD_NAME, breaking FILE),
  `buf.gen.yaml` (ts-proto now `include_imports` + `include_wkt` for `google.protobuf.Timestamp`; Go uses `timestamppb`),
  `gen.sh` (full wipe of both generated trees since the review fixes — the Go test moved out), `package.json`
  (`test: vitest run`, `vitest ^3` devDependency → `pnpm-lock.yaml` updated; since the review fixes a real `build` to
  `dist/` with `exports` pointing there), `tsconfig.json` includes `test/`, `tsconfig.build.json` emits `dist/`.
- **Generated stubs committed**: `apps/agent/gen/vrx/v1/dataplane.pb.go` (13047 lines), `dataplane_grpc.pb.go` (414),
  `packages/proto/gen/ts/vrx/v1/dataplane.ts` (24835), `packages/proto/gen/ts/google/protobuf/timestamp.ts` (235).
- **Compile-check tests** (paths/counts as of the review fixes): `apps/agent/internal/contracttest/desiredstate_test.go`
  (9 tests: root-key/field-number pinning; every valid `packages/schema/examples/*.json` + `packages/proto/test/fixtures/*.json`
  through strict protojson + binary and JSON round trips + key-set fidelity; negative strictness; F3 presence probe;
  D-039/D-040 structural walks; value assertions; typed construction across all 13 domains + envelopes) and
  `packages/proto/test/desired-state.test.ts` (13 tests: same corpus with ts-proto `fromJSON/encode/decode/toJSON/fromPartial`,
  `toJSON∘fromJSON == doc`, leniency documented, 64-bit strings, JSON names == Zod names).
- **`docs/contracts/proto.md`**: DesiredState projection rules, Apply (idempotency: declarative + `txn_id` retry, subsystem
  authority, outcome/gRPC-status table), DryRun, confirm-timeout self-revert (6 rules incl. agent-restart persistence),
  what Retrieve must include (7 rules), ownership scoping (`owner` guard), stream ordering guarantees (stats/events/action),
  Health, compatibility rules for consumers.
- `docs/status/tasks/P03-questions.md` (8 items), `P03-wip.md`.

## How it was verified (real output, host `ngfw`, worktree `/root/ngfw-wt/P03`, HEAD `633cdf6`)

### buf lint / buf breaking
```
$ cd packages/proto && buf --version
1.73.0
$ buf lint; echo exit=$?
exit=0
$ buf breaking --against "../../.git#branch=main,subdir=packages/proto"; echo exit=$?
exit=0
```
(The bare `--against ../../.git#branch=main` form reports the file as deleted because the module root is `packages/proto`;
the `subdir=` form is the correct invocation and is what `docs/contracts/proto.md` documents.)

### Generation is deterministic
```
$ sha256sum apps/agent/gen/vrx/v1/*.pb.go packages/proto/gen/ts/vrx/v1/dataplane.ts packages/proto/gen/ts/google/protobuf/timestamp.ts
2ef921962656568aa96338e49717419e0beb2419977bb58d1e79a30bc807a292  apps/agent/gen/vrx/v1/dataplane_grpc.pb.go
c83592ac2a22bcd956b4ea0bb30bdababe1c86f9673fa04be29c498f84582053  apps/agent/gen/vrx/v1/dataplane.pb.go
b87d93e9bd0a1b2aa758dc5838c2c2f97474984a5b80fb048ff943dd73d7faff  packages/proto/gen/ts/vrx/v1/dataplane.ts
2984b4f8d4ba4269e36ce5e829899f6344bbdf5e35882d2117c4fa77a6fd6acf  packages/proto/gen/ts/google/protobuf/timestamp.ts
$ pnpm gen >/dev/null && echo gen1-ok; pnpm gen >/dev/null && echo gen2-ok
gen1-ok
gen2-ok
$ sha256sum … | diff /tmp/p03-sums-1 - && echo "checksums identical"
checksums identical
$ git status --porcelain
(empty)
```

### Go compile-check test, vet, golangci-lint
```
$ cd apps/agent && go test -race -count=1 -v ./gen/...
=== RUN   TestDesiredStateMirrorsRootKeys
--- PASS: TestDesiredStateMirrorsRootKeys (0.01s)
=== RUN   TestDesiredStateFromSchemaExamples
=== RUN   TestDesiredStateFromSchemaExamples/minimal.json
=== RUN   TestDesiredStateFromSchemaExamples/two-interfaces.json
--- PASS: TestDesiredStateFromSchemaExamples (0.03s)
=== RUN   TestTwoInterfacesExampleValues
--- PASS: TestTwoInterfacesExampleValues (0.00s)
=== RUN   TestTypedConstruction
--- PASS: TestTypedConstruction (0.02s)
PASS
ok  	ngfw/agent/gen/vrx/v1	1.162s
$ golangci-lint run ./gen/...
0 issues.
```
(first run flagged gosec G304 on `os.ReadFile(filepath.Join(dir, name))` — fixed by reading through `os.DirFS` + `fs.ReadFile`.)

### TS compile-check test + typecheck
```
$ cd packages/proto && pnpm typecheck
$ tsc -p tsconfig.json --noEmit
$ pnpm test
 RUN  v3.2.7 /root/ngfw-wt/P03/packages/proto
 ✓ test/desired-state.test.ts (7 tests) 65ms
 Test Files  1 passed (1)
      Tests  7 passed (7)
```

### CI gate
```
$ tools/ci.sh --base main        (full log: /root/ngfw-wt/logs/P03-ci.log)
== install (frozen lockfile) ==
== generated output must be clean ==
== contract guard vs main ==
== forbidden patterns ==
== typecheck ==
 WARNING  no output files found for task @ngfw/proto#build. Please check your `outputs` key in `turbo.json`
== lint ==
== test ==
== build ==
== agent ==
CI GATE PASSED
```
The turbo warning is pre-existing (`@ngfw/proto` `build` is an `echo`; `turbo.json` is not a P03 file).

### Branch
```
$ git log --oneline main..HEAD
633cdf6 contract(proto): compile-check tests (Go+TS), docs/contracts/proto.md, questions; align NOOP/plan comments
f85e8e7 contract(proto): full Dataplane service + DesiredState mirror of RootConfig (WIP: tests/docs follow)
1c433ff chore(P03): task envelope
$ git diff --stat main...HEAD | tail -1
 17 files changed, 40717 insertions(+), 127 deletions(-)
```

## Acceptance checklist (prompt)
- [x] `buf lint` and `buf breaking --against .git#branch=main` pass (subdir form; see above)
- [x] `pnpm gen` regenerates both stubs deterministically (two runs, identical checksums, clean tree)
- [x] A Go and a TS compile-check that construct a `DesiredState` from the P02 example documents (Go: strict protojson;
      TS: ts-proto `fromJSON` with a structural `toJSON∘fromJSON == doc` check since `fromJSON` itself is lenient; round
      trips, value assertions, typed literals across all 13 domains)
- [x] `docs/contracts/proto.md` — Apply idempotency, Retrieve contents, stream ordering, confirm-timeout self-revert,
      ownership scoping

## Out of scope / not done
- Implementing the service (P05), any descriptor/renderer, the API client mapper (P06).
- The follow-up **proto sync after P02a/b/c merge** (mechanical: fill the empty sub-messages, add fields P02x added
  after `ec0ccda`/`b815d15`; est. 1–2 h) — needs an owner, see questions #4.
- A schema⊆proto drift-guard test (proposed in questions #7).
- `packages/proto` has no ESLint (its `lint` script is `buf lint`, as before); the test file is Prettier-formatted by hand.

## Open questions
See `docs/status/tasks/P03-questions.md` (8 items). None blocks P05/P06: they can build against this branch now.

## Decisions taken (for docs/decisions/LOG.md)

| id | decision | options considered | why | reversal cost |
|---|---|---|---|---|
| D-P03-1 | `DesiredState` is a strict 1:1 JSON-compatible projection of `RootConfig` (snake_case names, records→maps, arrays→repeated, Zod enums→`string`, every scalar proto3 `optional` per D-039, unions flattened); checked by strict protojson (Go) and structural `toJSON∘fromJSON` (TS) tests on the schema examples and proto fixtures | (a) proto enums + hand-written per-domain mapper in the API (b) JSON-compatible mirror, strings for enums | (b) zero mapping code in P06 and the tests, new Zod enum values need no proto change, `diff(running, actual)` works on `toJSON()`; Go loses enum exhaustiveness (descriptors switch on documented strings) | medium after `contracts-v1` (retyping fields), minutes before |
| D-P03-2 | Record-shaped domains `interfaces`/`vrfs` are `map<string, Interface>` / `map<string, Vrf>` fields on `DesiredState`; the other 11 domains are `<Key>Config` messages | (a) `InterfacesConfig{map entries}` wrappers for all 13 (b) maps for the two records | (a) breaks the JSON projection (`{interfaces:{interfaces:…}}`) and needs a transform in every consumer | low |
| D-P03-3 | buf lint = STANDARD − SERVICE_SUFFIX (D-008) − `RPC_RESPONSE_STANDARD_NAME`; stream element types keep docs/04 names (`StatsBatch`, `Event`, `ActionOutput`, `ValidationReport`); request types are standard (`DryRunRequest`, `StreamStatsRequest`, `StreamEventsRequest`) | (a) `StreamStatsResponse{batch}` wrappers (b) rename to `<Rpc>Response` (c) except the rule | (c) keeps the names P05/P06 prompts use and avoids an indirection in every stream loop; same rationale as D-008 | trivial (pre-v1 rename) |
| D-P03-4 | `Retrieve` returns `RetrieveResponse{desired_state, subsystems, owner, retrieved_at}`; `DryRun` takes its own `DryRunRequest` (no confirm fields) | (a) bare `DesiredState` / reuse `ApplyRequest` as sketched | metadata says which subsystems were really dumped; `RPC_REQUEST_RESPONSE_UNIQUE`; a dry run is not a transaction | trivial |
| D-P03-5 | Unmodelled domains mirror the **committed P02b/P02c WIP** (`task/P02b@ec0ccda`, `task/P02c@b815d15`) rather than docs/04's literal top-level sketch; P02a domains and tunnels/services/ha carry the documented target shapes with empty sub-messages | (a) docs/04 literal (`nat.static/portForwards/cgnat`) (b) empty `<Key>Config{}` shells only (c) mirror WIP | (a) would be dead on arrival (P02b's model has none of those names); (b) gives DF-*/P11 nothing to code against; (c) most likely to survive, follow-up is a mechanical diff | low (pre-v1) |
| D-P03-6 | No proto `reserved` ranges for planned fields; numbering convention in comments (Zod fields from 1 in declaration order, agent-only annotations from 100) | (a) `reserved 1 to 99` per shell (b) comments | `reserved` means never-use; buf `RESERVED_FIELD_NO_DELETE` (FILE) would make P02b/P02c filling them in a breaking change | trivial |
| D-P03-7 | `owner` on Apply/DryRun/Retrieve is a guard: empty = agent's `VRX_OWNER`; any other value → INVALID_ARGUMENT; one owner per agent process | (a) per-request owner selecting the tag to stamp (multi-owner agent) (b) guard | (a) needs one desired.pb/confirm timer/resync per owner in P05; (b) catches a request hitting the wrong slot socket loudly | low |
| D-P03-8 | Timestamps are `google.protobuf.Timestamp` (ts-proto → `Date`, Go → `timestamppb`); ts-proto generates the WKT (`include_wkt`) for TS only | (a) `int64 unix_ms` (b) WKT | (b) idiomatic, both stacks handle it; Go must not generate the WKT (duplicate registration) | trivial |
| D-P03-9 | Apply outcomes (FAILED/ROLLED_BACK/DEGRADED) are returned with gRPC OK inside `ApplyResponse`; gRPC codes only for INVALID_ARGUMENT / FAILED_PRECONDITION / ABORTED / UNAVAILABLE / UNIMPLEMENTED; `txn_id` is a retry-idempotency key (≥ 16 remembered responses) | (a) gRPC errors for application failures (b) status in the response | (b) keeps per-object results and the validation report on the failure path; the API maps them to RFC 9457 | low |
| D-P03-10 | StatsBatch counters are absolute (not deltas); slow consumers get dropped batches visible as `seq` gaps; events have no replay and a bounded buffer with an explicit `ERROR "dropped N events"` | (a) deltas + unbounded buffering (b) absolutes + drop | (b) never blocks the data plane, loss is harmless/visible | low |
| D-P03-11 | ~~`gen.sh` deletes only `*.pb.go` so the hand-written compile-check test can live in `apps/agent/gen/vrx/v1`~~ **superseded by the review fixes (F8)**: the Go test lives in `apps/agent/internal/contracttest/` and `gen.sh` wipes `apps/agent/gen` completely; `vitest` added to `packages/proto` (lockfile updated) | (a) test in another package (b) keep test beside stubs | a hand-written file in a generated, contract-guarded tree needs `contract(` commits for test edits and is one `rm -rf` away from vanishing | trivial |

## Review fixes (2026-09-23, worker P03-fix — review verdict APPROVE WITH CHANGES, `P03-review.md` F1–F11)

Design questions were decided by the manager (D-039 explicit presence + `forceLong=string`, D-040 drop `password_hash`,
D-041 unset domain = skip / explicit empty = delete all, D-042 mirror P02a HEAD now). Contract summary with the reasoning:
`docs/status/tasks/P03-contract.md`. Same branch, no redesign beyond the findings.

| finding | fix | where |
|---|---|---|
| F1 HIGH — `uint64` → `number` throws above 2^53 | ts-proto `forceLong=string` (D-039); `string` is the canonical protobuf JSON form, needs no `long` dependency and matches Go's `protojson` output byte-for-byte — `bigint` would have forced a JSON-string↔bigint conversion in every consumer and in the diff path. TS tests cover 2^64−1 through encode/decode. proto.md §9 corrected: 2^53 bytes = 9.0 PB ≈ 8.3 days at 100 Gbit/s | `buf.gen.yaml`, `proto.md` §1/§9, TS test |
| F2 HIGH — four P02a mismatches | `system.banner` → `SystemBanner{login?, motd?}`; `dataplane.corelist` → `repeated uint32`; `Interface.rx_mode`, `NextHop.address` → `optional`. Verified against `task/P02a@df554dc`; also took the flat additive leaves while there: `Interface.promiscuous`, `Subinterface.dot1ad`, `DataplaneConfig.tx_queues`, `StaticRoute.distance/description`, `ManagementUser.ssh_keys/full_name/disabled`. `management.syslog` confirmed an array (matches). P02b/P02c re-checked for renames since `ec0ccda`/`b815d15`: none (only additions → P03b) | `dataplane.proto` |
| F3 HIGH — `toJSON` drops proto3 defaults | every scalar under `DesiredState` is `optional` (182 declarations; a Go test walks the descriptor and fails on any implicit-presence scalar). The F3 probe (`id: 0`, `enabled: false`) is a test in Go and TS; `toJSON(fromJSON(doc))` must deep-equal `doc` for every corpus document | `dataplane.proto`, both tests, proto.md §1 "Presence" |
| F4 MEDIUM — `password_hash` on the wire | field removed, `reserved 4; reserved "password_hash";` (D-040); rule "secret-flagged leaves have no proto field" in the file header and proto.md §1; Go test walks the tree for forbidden leaf names; strict decode of a document carrying `passwordHash` now fails | `dataplane.proto`, proto.md, Go test |
| F5 MEDIUM — tests overstated | ts-proto `fromJSON` leniency documented in the TS test header and proto.md; strictness is asserted structurally in TS and natively in Go (`protojson.UnmarshalOptions{DiscardUnknown: false}` spelled out) over **every** valid `packages/schema/examples/*.json` plus a proto-local `packages/proto/test/fixtures/all-domains.json` that exercises nat/objects/acl/vpn/management/banner/corelist (the schema examples touch ~15 fields); negative tests for the invalid example, a nested unknown leaf, `passwordHash`, the old `corelist`/`banner` shapes. `RootConfig.parse()` is **not** applied (the branch's `@ngfw/schema` domains are `looseObject({})` placeholders; the parse→strict drift guard is P03b's). Wording in this file corrected | `packages/proto/test/`, `apps/agent/internal/contracttest/` |
| F6 MEDIUM — P03b size | recorded in `P03-questions.md` #9 (half a day + drift guard; start from branch heads) | questions |
| F7 MEDIUM — destructive unset domain | D-041 written into `ApplyRequest.desired_state`/`subsystems` comments, the file header and proto.md §2 (decision table: unset+unnamed = skip, present or named = authoritative, `subsystems` narrows) | `dataplane.proto`, proto.md §2 |
| F8 LOW — test in generated dir | moved to `apps/agent/internal/contracttest/` (`doc.go` + `desiredstate_test.go`; inside the Go module so `make test`/`make lint` cover it; not P05's package layout); `gen.sh` back to a full wipe | `apps/agent/internal/contracttest/`, `gen.sh` |
| F9 LOW — `@ngfw/proto` not importable | real build: `tsconfig.build.json` (`rootDir gen/ts` → `dist/`), `build: tsc -p tsconfig.build.json`, `main/types/exports` → `dist/vrx/v1/dataplane.*`; turbo `build` already lists `dist/**` (the "no output files" warning goes away). Verified with a Node ESM import from `apps/api` (below). P06 note in questions #11 | `package.json`, `tsconfig.build.json` |
| F10 LOW — nits | buf breaking command form (`../../.git#…`); `Event.interface` → `optional`; numbering sentence now "append-only, next free number"; `rx_misses` comment; enums renamed `Operation`→`ApplyOperation`, `ResultCode`→`ObjectResultCode`, `Severity`→`IssueSeverity` (values re-prefixed per buf `ENUM_VALUE_PREFIX`); `HealthResponse` 1–3 untouched | `dataplane.proto`, proto.md |
| F11 LOW — no `-contract.md` | written | `docs/status/tasks/P03-contract.md` |

### Verification (real output, host `ngfw`, `/root/ngfw-wt/P03`, after commit `13e1c6f`)

```
$ python3 fix_proto.py packages/proto/vrx/v1/dataplane.proto
optional added to 182 scalar fields
$ awk '/^message DesiredState \{/{f=1} f' dataplane.proto | grep -cE '^\s+(bool|string|uint32|uint64|int32|double|bytes) [a-z_0-9]+ = [0-9]+;'
0
$ cd packages/proto && buf lint; echo exit=$?
exit=0
$ buf breaking --against "../../.git#branch=main,subdir=packages/proto"; echo exit=$?
exit=0
```
(`buf breaking` vs `main` is expected to pass: `main` still has only `Health`, whose fields 1–3 are byte-identical; the
review proved on a scratch copy that each F2/F3 change *would* have tripped `FIELD_SAME_CARDINALITY`/`FIELD_SAME_TYPE`
against this branch once tagged — which is why they land now.)

```
$ cd apps/agent && go test -count=1 -v ./internal/contracttest/...
--- PASS: TestDesiredStateMirrorsRootKeys (0.00s)
--- PASS: TestStrictDecodeOfEveryDocument (0.04s)        # examples/minimal.json, examples/two-interfaces.json, fixtures/all-domains.json
--- PASS: TestStrictDecodeRejectsUnknownFields (0.00s)   # invalid-unknown-root-key.json, nested leaf, passwordHash, old corelist/banner
--- PASS: TestExplicitPresenceSurvivesJSON (0.00s)       # the F3 probe
--- PASS: TestNoSecretLeaves (0.00s)                     # D-040 walk + ManagementUser field 4 reserved
--- PASS: TestExplicitPresenceEverywhere (0.00s)         # D-039 walk over >100 messages
--- PASS: TestTwoInterfacesExampleValues (0.00s)
--- PASS: TestAllDomainsFixtureValues (0.00s)
--- PASS: TestTypedConstruction (0.00s)
PASS
ok  	ngfw/agent/internal/contracttest	0.076s
$ go test -race -count=1 ./internal/contracttest/...
ok  	ngfw/agent/internal/contracttest	1.396s
$ golangci-lint run ./internal/contracttest/...
0 issues.

$ cd packages/proto && pnpm typecheck
$ tsc -p tsconfig.json --noEmit
$ pnpm test
 RUN  v3.2.7 /root/ngfw-wt/P03/packages/proto
 ✓ test/desired-state.test.ts (13 tests) 379ms
 Test Files  1 passed (1)
      Tests  13 passed (13)

$ pnpm build
$ tsc -p tsconfig.build.json
$ ls dist/vrx/v1/
dataplane.d.ts  dataplane.js  dataplane.js.map
$ cd apps/api && node --input-type=module -e 'const m = await import("@ngfw/proto"); console.log("exports:", Object.keys(m).length, "DesiredState.fromJSON:", typeof m.DesiredState.fromJSON, "ApplyOperation.APPLY_OPERATION_CREATE =", m.ApplyOperation.APPLY_OPERATION_CREATE)'
exports: 195 DesiredState.fromJSON: function ApplyOperation.APPLY_OPERATION_CREATE = 1
exit=0

$ sha256sum … > /tmp/p03fix-sums-1; pnpm --filter @ngfw/proto gen >/dev/null && echo gen1-ok; pnpm --filter @ngfw/proto gen >/dev/null && echo gen2-ok
gen1-ok
gen2-ok
$ sha256sum … | diff /tmp/p03fix-sums-1 - && echo "checksums identical"
checksums identical
0a9327ceadd08ec34b363aeabaefa325ee68f9acb3ce0a5820dc8446b429ca90  apps/agent/gen/vrx/v1/dataplane_grpc.pb.go
7d2c85d325b49346f96d49a0c2c9d7a394c4421e652b1d9a8bbe782f7d5cbde8  apps/agent/gen/vrx/v1/dataplane.pb.go
90ea132549b529c5fc485c8bb780c7282e6b07f1be0fc28f26ede16945682fb5  packages/proto/gen/ts/vrx/v1/dataplane.ts
198d518f8599f225528c284e11331f851f5d8b8e903281d6c1ba1c7cdebe8443  packages/proto/gen/ts/google/protobuf/timestamp.ts
```
