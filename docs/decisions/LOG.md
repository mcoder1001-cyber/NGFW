# Decision log

One line per decision taken by an agent (or recorded from chat), **with the options that were considered**.
The product owner reviews; to overturn, append `OVERTURNED → …` to the line or say so in chat.

| date | id | decision | options considered | why | reversal cost | tasks |
|---|---|---|---|---|---|---|
| 2026-09-23 | D-001 | Appliance OS = Ubuntu 26.04; VPP 26.06 built from source in /root/vpp, shipped as our .debs | (a) 24.04 + FD.io packages (b) 26.04 + source build | product owner's choice; FD.io has no 26.04 build; dev host already 26.04 | high (os.md) | all |
| 2026-09-23 | D-002 | Lab = VMware VMs, VPP with DPDK on vmxnet3; no Docker, no libvirt/nested KVM | (a) Docker VPP (b) nested KVM (c) VMware VMs directly | host has no nested virt; (c) is the hardware code path | medium | P04, P08+ |
| 2026-09-23 | D-003 | VDOM/multi-tenancy deferred; guardrails in vdom.md | (a) tenants in schema day 1 (+10%) (b) defer with guardrails (~1.5× later) (c) defer without (~3×) | product owner: time | high later | P02*, P06, P07* |
| 2026-09-23 | D-004 | pnpm 12 `allowBuilds` in pnpm-workspace.yaml | (a) legacy onlyBuiltDependencies (ignored) (b) allowBuilds (c) approve-builds interactive | only (b) works unattended | trivial | P01 |
| 2026-09-23 | D-005 | Node↔agent gRPC = @grpc/grpc-js + ts-proto v2 (@bufbuild/protobuf runtime); Go = protoc-gen-go/-grpc; buf drives both | (a) grpc-js + ts-proto (b) Connect (connect-node/connect-go) (c) protobufjs | (a) proven over unix sockets, mature | medium (contract layer) | P01, P03, P05, P06 |
| 2026-09-23 | D-006 | Zod 4 native `toJSONSchema` for JSON Schema 2020-12 + OpenAPI 3.1 | (a) zod-to-json-schema lib (b) Zod 4 native | zero deps, one source of truth | low | P01, P02* |
| 2026-09-23 | D-007 | NestJS tests on vitest + unplugin-swc | (a) jest (b) vitest+swc | esbuild lacks emitDecoratorMetadata; vitest shared with web | low | P01, P06 |
| 2026-09-23 | D-008 | buf lint STANDARD, SERVICE_SUFFIX excepted (service stays `Dataplane`) | (a) rename DataplaneService (b) except rule | matches docs/04 naming | trivial | P03 |
| 2026-09-23 | D-009 | golangci-lint deferred to P09 (go vet only now) | (a) install now (b) defer | binary not present; not blocking | trivial | P09 |
| 2026-09-23 | D-010 | Until data NICs exist on vrx-a, packet tests use af_packet host-interfaces on veth+netns; DPDK path when NICs arrive | (a) wait for NICs (b) af_packet rig now | (b) keeps work moving; path recorded per test | low | P04, P08, F-* |
| 2026-09-23 | D-011 | Decision policy = 2× rule; one manager orchestrates all workers; options logged | — | product owner's instruction | — | all |
| 2026-09-23 | D-012 | No VPP restarts by anyone before handover; restart-safety proven by agent-restart simulation; after handover manager-only under flock | (a) allow restarts under lock now (b) forbid until handover | VPP bring-up agent may be mid-work; (b) is safe and still proves reconcile | low | P05, P08, F-*, docs |
| 2026-09-23 | D-013 | Board rows split so one task = one worker: P02s/P02a/P02b/P02c, P07a/P07b; P05a own task (deps P01) | (a) multi-worker rows (b) split rows | worktree/branch/merge are per task | trivial | S1, S2 |
| 2026-09-23 | D-014 | `apps/agent/binapi/` generated for ALL plugins on the host, owned by P04 then the manager; factories never regenerate | (a) per-factory regen (b) single owner, generate all | (a) guarantees merge conflicts | low | P04, DF-* |
| 2026-09-23 | D-015 | Operating model until Claude Code is authenticated on the host: manager = Claude session on the operator desktop; workers edit via rsync and run via SSH; all git on the host | (a) wait for server login (b) desktop-driven | (a) stops work | trivial | all |
| 2026-09-23 | D-016 | This Claude session acts as manager (product owner's instruction); S1 started with 4 parallel workers; P02s skeleton first, then P02a/b/c + P03 | (a) wait for a server-side manager agent (needs Claude login on host) (b) start now from the desktop | (a) idles the programme | trivial | S1 |
| 2026-09-23 | D-017 | Root keys use `<Key>Schema.prefault({})`; every domain schema must accept `{}` | (a) `.default({})` (b) `.prefault({})` | (b) fills nested defaults on parse; same JSON Schema | trivial | P02* (worker P02s) |
| 2026-09-23 | D-018 | Semantic validators in `semantic/<key>.ts` (one array per domain) aggregated by `semantic/index.ts` | (a) one shared registry file (b) per-domain arrays | (b) avoids three groups editing one file | trivial | P02*, P06 (worker P02s) |
| 2026-09-23 | D-019 | `x-vrx-ui` hints via `withUi()` → Zod `.meta()`, copied into JSON Schema/OpenAPI by Zod 4 | (a) side table (b) `.meta()` | one source, zero plumbing | low | P02*, P07* (worker P02s) |
| 2026-09-23 | D-020 | Placeholders `z.looseObject({})`, root `z.strictObject` (Zod 4 names) | (a) deprecated `.passthrough()/.strict()` (b) Zod 4 functions | not deprecated, same semantics | trivial | P02s |
| 2026-09-23 | D-021 | pointer/diff/merge-patch shipped as small working implementations (RFC 6901/6902/7386); arrays are diff leaves | (a) throwing stubs (b) minimal working code | usable by P02a/b/c immediately | low | P02a (worker P02s) |
| 2026-09-23 | D-022 | S1 skeleton/infra tasks ≤ 4 h (P02s) are reviewed directly by the manager (scope, forbidden patterns, gate); all other tasks get a review agent | (a) review agent for every task (b) manager-direct for tiny critical-path tasks | (b) saves ~10 min on the critical path with no data-plane risk | trivial | P02s |
| 2026-09-23 | D-023 | `wt.sh push` no longer preserves owner/group (`-rlptDz --no-o --no-g` + chown root); `safe.directory=*` for root on the host | (a) chown after every push (b) rsync flags + safe.directory | (b) removes the dubious-ownership failure for every worker | trivial | all workers |
| 2026-09-23 | D-024 | `docs/contracts/schema.md` is created by P02a (index); P02b/P02c write `schema-nat-objects-acl.md` / `schema-vpn-tunnels-services-ha.md`; `@vitest/coverage-v8` added by P02a | (a) one shared doc all groups edit (b) per-group docs + index | (b) no cross-group edits | trivial | P02a/b/c |
| 2026-09-23 | D-025 | Metrics port per slot = 9100 + 10·N + 1 (9111…9191, 9201/9211/9221) — replaces the invalid `91<N>1` pattern for N ≥ 10 | (a) keep pattern, cap slots at 9 (b) formula | (b) keeps 12 slots | trivial | all workers (from P04 Q3) |
| 2026-09-23 | D-026 | Six data NICs recorded in host-vrx-a.md and test/topology/vrx-a.yml as `unassigned`; DPDK binding waits for handover + the port-group mapping from the product owner; rig remains the default test path meanwhile | (a) bind now via startup.conf (violates D-012) (b) record, wait for mapping + handover | (b) safe; nothing blocked | low | P04, P08, F-* |
| 2026-09-23 | D-027 | P04 decisions adopted: slot/address derivation from the prefix; python3-yaml inventory; binapi MANIFEST provenance with `--no-source-path-info`; per-create DB password only under /run; smoke test as its own Go module; kill/restart only under `flock -x` after handover | see docs/status/tasks/P04.md (D-P04-1…6) | worker's rationale accepted | low | P04 |
| 2026-09-23 | D-028 | PostgreSQL on the host is 18.6 (Ubuntu 26.04) — requirement is "≥ 16"; docs said 16 | (a) pin 16 (b) ≥16 | (b) no reason to downgrade | trivial | P06, P10 |
| 2026-09-23 | D-029 | Interface-only tasks ≤ 6 h with no VPP mutation (P05a) are also reviewed directly by the manager (extends D-022) | (a) review agent (b) manager-direct | P05a gates 12 factories; review agent would cost ~15 min on the critical path with no data-plane risk | trivial | P05a |
| 2026-09-23 | D-030 | P05a decisions adopted: `vpp.Client` ⊇ govpp `api.Connection`; keys `<descriptor>/<id>`; `Register` panics on duplicates, `Add` returns error; strict mandatory-dependency failure (no Ligato-style pending); owner stamp = interface tag `<owner>:<id>`; `File.Secret` flag; allowlist enforced by a source-scanning test; `vpptest` helper; govpp v0.13.0 dependency | see docs/status/tasks/P05a.md (11 rows with options) | worker's rationale accepted; all additive | low | P05a, DF-*, RF-*, P05 |
| 2026-09-23 | D-031 | `apps/agent/Makefile` lint target no longer swallows golangci-lint's exit code | (a) leave (b) if/else | (b) a failing linter must fail the gate | trivial | P05a finding |

| 2026-09-23 | D-032 | Usage-limit interruption (resets 14:50 Tehran) killed 7 workers + 1 reviewer; manager salvaged unpushed desktop edits onto each branch (never generated files or lockfiles) and respawned all with CONTINUE envelopes; P04 review verdict BLOCK (F1–F4) goes back to a P04 fix worker | (a) restart tasks from scratch (b) salvage + continue | (b) preserves ~1–2 h of work per task | trivial | S1 |
| 2026-09-23 | D-033 | P04 merged after the review BLOCK was cleared: the reviewer stated F1–F4 as the approval condition; the manager verified the pasted F1 re-test (rig down/gc anchored) and the gate instead of running a second review round | (a) second full review (b) manager verifies the stated condition | (b) saves ~15 min; condition was explicit and evidenced | low | P04 |

| 2026-09-23 | D-034 | P03 decisions adopted: DesiredState is a JSON-compatible mirror of the Zod schema (strings for enums, no mapper); `owner` is a guard with one owner per agent; no `reserved` ranges for planned fields (buf RESERVED_FIELD_NO_DELETE trap) — comments + numbering convention; Apply outcomes app-level with txn_id retry-idempotency; absolute counters; lint exception RPC_RESPONSE_STANDARD_NAME accepted | see docs/status/tasks/P03.md D-P03-1…11 with options | rationale accepted; keeps P06 free of per-domain mappers | medium (contract layer) | P03, P05, P06 |
| 2026-09-23 | D-035 | New board task P03b (proto sync after P02a/b/c merge, ~3 h) gates the contracts-v1 tag | (a) tag v1 on P03 as-is and sync later as contract branches (b) sync first, then tag | (b) avoids a breaking change right after the tag | trivial | S1 |
| 2026-09-23 | D-036 | Root schema test asserts each domain parses to an object (not to `{}`), since domains fill nested defaults (D-017); manager applied the one-line fix on main | (a) wait for P02a to fix its file (b) manager one-liner now | (b) unblocks P02b/P02c gates today | trivial | P02a/b/c |
| 2026-09-23 | D-037 | Known merge hazard noted: P02a and P02c both export a `secretRef` symbol via `export *`; the second one merged must alias or the index must use explicit exports — the manager resolves at merge time | (a) rename now in a running branch (b) resolve at merge | (b) workers are one-shot; cheap at merge | trivial | P02a, P02c |
| 2026-09-23 | D-038 | `tools/ci.sh full` takes the exclusive lab lock only as a barrier (waits for running tests) then downgrades to shared so the rig/harnesses can take their own shared locks; shared-host-rules §1b amended accordingly; P09 merged after manager-direct review (infra, gate is self-testing) | (a) keep exclusive during full (rig would time out) (b) barrier-then-shared | (b) works with P04 rig as built | trivial | P09, P04 |
| 2026-09-23 | D-039 | DesiredState scalars use explicit presence (`optional`) so proto3 defaults are not dropped in Retrieve/diff; ts-proto `forceLong=string` for all 64-bit counters | (a) implicit presence + special-casing in diff (b) optional everywhere (c) withdraw the diff-on-DesiredState promise | (b) makes running-vs-actual diff honest; (a) hides drift on default values | medium before tag, high after | P03, P05, P06 |
| 2026-09-23 | D-040 | `ManagementUser.password_hash` removed from the agent contract — authentication material never crosses the API↔agent boundary; the agent gets only what VPP/daemons need | (a) keep with "unless needed" loophole (b) drop | (b) secrets rule #10; agent persists desired.pb to disk | low | P03, P06 |
| 2026-09-23 | D-041 | Apply semantics: a domain **unset** in DesiredState means "not managed in this txn" (skip); an explicitly empty domain means "delete all owned objects of that domain"; `subsystems` may still narrow further | (a) unset = delete all (destructive partials) (b) unset = skip | (b) partial documents cannot wipe the data plane | low | P03, P05 |
| 2026-09-23 | D-042 | P03 mirrors P02a HEAD shapes now (banner object, corelist array, optional rx_mode/next-hop address) before contracts-v1; P03b remains the post-merge sync for P02b/P02c leaf keys + a strict drift-guard test (RootConfig.parse → protojson strict) | (a) tag now, fix later (breaking) (b) fix before tag | (b) the review proved each fix trips buf breaking after the tag | trivial | P03, P03b |
| 2026-09-23 | D-043 | P02b fix round before merge: M1 pools may reference an interface address (nat44 interface-addr), M2 MAP interface binding + mode tied to ipv6Source, M3 drop cnat `side` (no VPP counterpart) in favour of table selection, M4 network-prefix fields reject host bits, M5 refresh evidence. H1 (`withUi` shallow-merge losing leaf hints) is fixed on main right after P02a merges (P02a owns ui.ts) | (a) merge now, fix as contract branches later (reshape after tag) (b) fix before tag | (b) reshapes after the tag would break the guard | trivial | P02b, P02a |
| 2026-09-23 | D-044 | Schema export collisions at merge (P02a↔P02c: secretRef, DnsSchema, NtpSchema, NtpServerSchema) are resolved by P02c aliasing its symbols (P02a merges first, owns index) | (a) P02a renames (b) P02c aliases | (b) P02a is merged first and owns index exports | trivial | P02c |
| 2026-09-23 | D-045 | Routing shapes follow the consumer P12 §2: `routing.bgp.neighbors` is a record keyed by neighbor address, `routing.policy{prefixLists, routeMaps}` nesting, `passwordRef`, `afi`, `redistribute` names as in P12; static routes allow blackhole (empty next-hops with `blackhole: true`) | (a) keep arrays + itemKey (b) records keyed by natural key per P12 | (b) is what the FRR renderer and UI expect; reshape after the tag is always-PENDING | trivial now | P02a, P12 |
| 2026-09-23 | D-046 | Secrets in the config document: `passwordHash` stays write-only (`secret: true` meta); `packages/schema` exports `redactSecrets(doc)` driven by that meta; the API (P06) stores hashes in `app_user`, strips them from revisions/diff/audit/GET via redactSecrets, and GET→PUT round-trips never clear a hash (absent = unchanged) | (a) leave to P06 ad hoc (b) schema-level redaction helper + explicit round-trip rule | (b) one mechanism for every secret leaf | low | P02a, P06 |
| 2026-09-23 | D-047 | Amends D-044: P02a renames its `DnsSchema/NtpSchema/NtpServerSchema` to `System*` (it merges first); P02c aliases `secretRef` and any remaining duplicate exports | (a) P02c renames everything (b) split by owner | (b) minimal edits per branch | trivial | P02a, P02c |
| 2026-09-23 | D-048 | Coverage thresholds are per group: P02a\x27s 100% applies to its own files; P02b/P02c add their own thresholds later; `examples.test.ts` claims only group (a) fixtures; `management.admin-exists` fires only when `users` is non-empty (an empty document is valid — the API seeds the first admin) | (a) global 100% + admin rule always (b) scoped | (a) turns sibling merges red | trivial | P02a/b/c, P06 |
| 2026-09-23 | D-049 | Input hardening in the schema: control characters (C0/C1, CR/LF) rejected in every free-text field that reaches a daemon config or CLI (AS-path regex, banners, descriptions); merge-patch rejects `__proto__`/`constructor`/`prototype` keys; prefix keys canonicalised (lowercase, compressed IPv6) before uniqueness checks | — | prevents FRR config-line injection and prototype tricks at the contract layer | trivial | P02a |
| 2026-09-23 | D-050 | NTP is modelled once, in `services.ntp` (chrony is a daemon rendered by RF-3); `system.ntp` is removed by P02a before the tag; DHCP client lives on the interface (`interfaces.<name>.dhcpClient`, D7.2) | (a) system.ntp (docs/04) (b) services.ntp | (b) matches the renderer ownership; docs/04 amended | trivial now | P02a, P02c, RF-3 |
| 2026-09-23 | D-051 | Secret references have the form `<kind>/<name>` with kinds psk, key, cert, password, token; the schema validates the format, the API validates existence (POST /secrets) — the schema scan is a guard, not the mechanism | (a) free string (b) enumerated kind prefix | (b) makes inline secrets structurally impossible | trivial | P02c, P02a, P06 |
| 2026-09-23 | D-052 | QoS (D7.8: policer, shaper, marking) gets a home at `services.qos`; HQoS stays excluded (V3) | (a) new top-level key (contract reshape) (b) under services | (b) additive, no new root key | trivial | P02c, F-qos-flat |
| 2026-09-23 | D-053 | `ha.vrrp` is a record keyed by name (pointer-addressable, merge-patchable) — same rule as D-045 for every keyed collection; P02c roots use `.prefault({})` like the other groups (D-017) | (a) arrays with itemKey (b) records | (b) consistent with D-045 | trivial | P02c |
| 2026-09-23 | D-054 | P02c helper primitives are not part of the public API: kept in a non-re-exported module (`domains/_shared/…`) so the later dedupe with P02a is not a breaking change | (a) export * (b) private module | (b) | trivial | P02c |
| 2026-09-23 | D-055 | Factories may use typed Go specs + structpb as a stand-in until P03b adds the domain leaf messages; P03b owns adding them (ACL, NAT, interface attrs first); each factory then adapts in a small follow-up commit on its next task | (a) block factories on proto (b) stand-in now, sync in P03b | (b) keeps 8 factories moving | low | DF-*, P03b |
| 2026-09-24 | D-056 | F-mpls-srmpls and F-igmp-mfib depend on RF-1 (FRR renderer): their LDP/PIM parts use the V5 agent-side sync, which reads FRR JSON — that needs FRR, not linux-cp | (a) no dep (tasks stall mid-way) (b) dep on P12 (parked on handover) (c) dep on RF-1 | (c) is the real prerequisite and is not blocked by handover | trivial | F-mpls-srmpls, F-igmp-mfib |
| 2026-09-24 | D-057 | The nftables host-policy renderer is owned by F-host-acl-nftables only; F-hardening-lite consumes it (new dep) | (a) both own it (b) single owner | (b) no two workers on one file set | trivial | F-host-acl-nftables, F-hardening-lite |
| 2026-09-24 | D-058 | Uncovered T1/T2 WBS items become tasks: F-startup-gen (D0.6, S2), F-vpp-debs (D0.2, S5, P10 depends on it), F-ha-state-sync (D5.6/D9.3/D9.4, S4 wave C, partial ok); all added to the freeze deps. `max_workers` set to 12 (the concurrency the manager actually runs; host: 32 cores, 94 GB) | (a) leave uncovered (b) add tasks | (b) PENDING-handover option 1 already relies on the generator; P10 assumed "our VPP debs" without a producer | low | P10, S6 freeze |
| 2026-09-24 | D-059 | Out of the 21-day scope and to be listed as explicit have-not in STATUS-FINAL: D10.1 VDOM, D12.3 pentest, D12.4 full docs, D12.5 QA/interop matrix, D12.6 72h soak/chaos, D12.7 certification | — | makes the omission visible instead of silent | trivial | STATUS-FINAL |
| 2026-09-24 | D-060 | PENDING-handover answered by the product owner: **option 2** — enable `linux_cp`, `linux_nl`, `npt66` in startup.conf and restart VPP once (done by the host manager under `flock -x` on the lab lock, backup kept); `handover` stays pending, so D-012 still applies to everyone else. P12 and the NPTv6 part of F-nat44-ei-64-66-nptv6 are un-parked (P12 becomes ready when its deps merge). NIC → port-group mapping still open | (1) handover: done (2) one-off plugin enable (3) keep parked | product owner's choice | trivial (restore backup + restart) | P12, F-nat44-ei-64-66-nptv6, DF-8 (lcp), F-startup-gen |
| 2026-09-24 | D-061 | Each schema group branch (P02a/b/c) syncs the proto messages of its own domains before merge (`contract(proto):` commit + `<id>-contract.md`, regenerate via `packages/proto/gen.sh`); renames allowed before `contracts-v1`. P02b's worker-prepared patch applied by the manager (nat: pools.interface, staticMappings.external.pool, map.interfaces, cnat addresses.interface, snat side→table). P03b keeps: domain leaf messages for the factories (D-055) + the final schema⊆proto cross-check | (a) sync on each P02x branch (b) P03b after all merge (c) drop new fields from examples | (b) impossible — P02x cannot merge red on P03's strict drift test; (c) hides drift | low | P02a, P02b, P02c, P03b |
| 2026-09-24 | D-062 | Copied from P02b (worker decisions D-P02b-9…12, details in the merged `docs/status/tasks/P02b-questions.md`): leaf UI hints merged privately in the nat/objects/acl files until `ui.ts` is fixed (P02a); `nat.enabled` optional — absent means on-if-configured (`isNat44Enabled()`), consumers must not read the raw field; object groups may be empty but a rule may not reference an empty group; one shared object namespace across addresses/services/groups | see P02b-questions.md | reviewer concurred on the namespace; the inference keeps a switched-off NAT config legal | trivial | F-nat44-*, F-object-model, F-acl, DF-3 |
| 2026-09-24 | D-063 | Descriptors whose VPP object has no dump return `ErrRetrieveUnsupported` (DF-2: adl ×2, classify interface-ip-table / l2-tables / output-acl). The P05 reconciler treats them as **write-only**: re-apply desired state on every resync (the Create must be idempotent), never delete on absence, skip them in post-apply verification, count them in a `retrieve_unsupported` metric; a descriptor may not fake Retrieve by echoing cached desired state | (a) echo desired (b) write-only rule in the reconciler (c) forbid such types | (a) hides drift and breaks restart-safety claims; (c) drops D2.x features | low | P05, DF-2, all DF-* |
| 2026-09-24 | D-064 | Shared-VPP crash rule: a host test that crashes VPP is disabled behind an opt-in env var at once (DF-2 proxy-ND `VRX_DF2_PROXY_ND`, DF-6 gtpu), the crashing message is recorded in docs/vpp-code-track.md, and workers check `systemctl show vpp -p NRestarts` before/after first runs of a new plugin. Observed 2026-09-24 00:25:16 and 00:26:04: SIGSEGV in gtpu_plugin.so via the API socket (DF-6 test); systemd auto-restarted VPP | — | protects 12 concurrent slots on one VPP | trivial | all DF-*, F-* |
| 2026-09-24 | D-065 | Interface references: every consumer depends on a generic alias key `interface/<name>` (descriptor owned by DF-1: Dependencies → creator key when we created it, none for physical/pre-existing NICs; Create verifies existence; Delete no-op; Retrieve from sw_interface_dump, never deletes foreign interfaces). Creator keys (`interface.loopback/…`, `tapv2.tap/…`) stay internal to DF-1 | (a) full creator keys in every consumer (b) alias `interface/<name>` (c) one `interface` descriptor with a type field | (b): config names interfaces by name; DPDK NICs have no creator; DF-2…6 need no change | low | DF-1…DF-8, P05, P08, all F-* |
| 2026-09-24 | D-066 | Copied from DF-4 (details in merged `docs/status/tasks/DF-4.md` → Decisions): ACL key `acl.acl/<name>`, VPP tag `<owner>:<name>`; duplicate tags → lowest index is canonical, extras surface as `acl.acl/<name>#<index>` and are deleted; `#` banned in names; foreign ACLs on an interface are preserved in order; acl-stats flag tied to the VPP main-thread PID (re-enabled after a VPP restart) | see DF-4.md | reviewer findings M1, L4, L6 | trivial | DF-2 (abf), F-acl, F-host-acl-nftables |
| 2026-09-24 | D-067 | P02c merged with: `ha.vrrp` as a map on new field 2 (field 1 reserved); two one-line test edits outside its file set accepted (contracttest desiredstate_test.go, proto desired-state.test.ts — forced by the vrrp reshape); task-branch history rewrite accepted (gitleaks on old commits; never on main); gitleaks hit `VRX_TEST_PSK_P02c_wg` is a test placeholder (false positive). Open for P02a (from P02c Q11): drop `system.ntp` (D-050), rename its DNS/NTP exports, `secretRef` in `<kind>/<name>` form (D-051). IKEv1 + GCM IKE proposals stay rejected (P02c Q12) | — | — | trivial | P02a, P11, F-vrrp-config-sync |
| 2026-09-24 | D-068 | VPP crash hazards found by factories are tracked as V8–V12 (gtpu, det44 disable, cnat NULL default, pnat lazy-init, ip6-nd proxy); every guard stays in the descriptor, crashing host tests are opt-in only (D-064). DF-3 policy: det44 is never disabled once enabled | (a) drop the objects (b) guard + track | (b) keeps the features; the guard is cheap | trivial | DF-2, DF-3, DF-6, F-det44-map-dslite-cnat |
| 2026-09-24 | D-069 | `interface/<name>` carries the LOGICAL interface name: the config/tag id for interfaces the agent creates, the VPP name for physical/pre-existing ones — and the startup.conf generator (F-startup-gen) names DPDK NICs by their logical name (`dpdk { dev <pci> { name lan } }`) so both coincide. One resolver (logical name → sw_if_index) exported by DF-1; DF-4 acl switched to it in DF-1's fix round | (a) VPP names (`tap240`) (b) logical names | config and UI only know logical names; VPP names of created interfaces are auto-generated and change across restarts | low | DF-1…DF-8, F-startup-gen, P05, P08 |
| 2026-09-24 | D-070 | P02a merged with: routing per P12 layout (D-045) except prefix lists / route maps are objects `{description?, family, rules[]}` / `{description?, entries[]}` (protobuf map values cannot be lists — P12 to confirm); OSPF area ids always strings; `macPattern` (all-zero / group-bit allowed) for MACIP match fields in acl.ts; `withUi` merges existing hints (P02b H1 root cause); `redactSecrets`/`secretPointers` helpers — P06 must use them for GET and audit, and map `MergePatchError` to HTTP 400 | see P02a-questions.md #10–#14 | — | trivial | P06, P12, F-acl |
| 2026-09-24 | D-071 | VPP-global singletons (plugin enable/disable such as nat44/nat64/nat66, NAT timeouts/forwarding, MAP params, acl-stats flag, similar) are managed only by the designated **globals owner** (agent config `globalsOwner: true`; the product agent on a real box, never a test slot on the shared host). Non-owners: may *require* a global (fail with a clear error if it is off) but never set, reset or disable it. Plugin disable additionally requires a complete emptiness check across all owners, else skip. Tests restore previous values, never VPP defaults. Ownership claim rule for every descriptor: own tag → ours; foreign tag → never touched; untagged → only via a ClaimStore record (DF-4 pattern). Deletes by index must re-verify identity (tag/tuple) in the same call sequence immediately before deleting | (a) every owner manages globals (b) single globals owner | (a) lets one slot break all others (DF-3 review H1/M4 reproduced it) | low | DF-1…DF-8, P05, F-nat*, F-acl |
| 2026-09-24 | D-072 | Static routes have exactly one programmer: by default the agent programs VPP directly (P05 static-route descriptor); FRR (staticd) gets a route only when the route carries an explicit flag (e.g. needed for redistribution into a routing protocol), and then the agent does NOT program it in VPP — linux-nl brings it in. Never both. FRR secrets (BGP/OSPF auth) go through a secret resolver in `Section.Render` and are redacted in Retrieve/DryRun/errors/logs (RF-1 review M2, before P12) | (a) both programmers (b) VPP only, FRR never (c) one owner per route, default VPP | (a) double FIB sources, deletes leave stale entries; (b) breaks redistribute-static | low | RF-1, P05, P12, F-bfd-redistribution |
| 2026-09-24 | D-073 | P05 open questions: (a) wiring interface references to `interface/<name>` + emitting alias objects from the desired-state builder is P08's job (first consumer of DF-1 + P05 together); (b) VPP does not store VRF/route descriptions → the agent keeps them in its state dir and returns them from Retrieve, so running-vs-actual diffs stay honest; (c) DF-2 aliases its `ErrRetrieveUnsupported` to the scheduler's copy on its next touch | (b): agent state vs API-side diff exclusion | the API should not special-case fields per domain | trivial | P05, P08, DF-2 |
| 2026-09-24 | D-074 | Copied from DF-6: every delete first checks that the object still exists (several VPP handlers crash or leak on a missing object — V8, V14); SRv6 encap policy must name its source address (VPP's global default cannot be read back); SR-MPLS segment lists and LISP locators kept in sorted order; srv6-mobile not built (no delete message, clashes with `sr.policy`); the SRv6 proxy binapi (`srv6_ad/am/as`) is generated by the manager on main when F-srv6 starts; Register signatures (scope/owner) are normalised when P08 wires the registry | see DF-6.md | crash avoidance | trivial | F-srv6, F-lisp, F-tunnels, P08 |
| 2026-09-24 | D-075 | Copied from DF-1 (details in merged DF-1.md → Decisions): MTU = per-protocol software MTU compared to the creation default (MTU equal to default rejected); rx-mode compared per device-type default; xconnect = one object per direction; sub-interfaces via `create_subif` only; memif sockets owned via a per-owner directory; physical/untagged NIC settings tracked in a per-owner ClaimStore (`iface.SetClaimStore` — P05/P08 must install a persisted one); resolver `iface.ResolveName` refuses foreign-tagged interfaces (`ErrForeignInterface`); ACL bindings may still name a foreign interface (shared ACL lists) | see DF-1.md | — | trivial | P05, P08, F-vlan-qinq, F-bonding, F-bridge-l2 |
| 2026-09-24 | D-076 | Tightens D-063: a write-only descriptor's Create must be idempotent under repeated resyncs. If VPP's add is not idempotent (fails on the second add, or stacks a feature twice), the descriptor keeps a persisted claim record keyed by the VPP boot identity (vpe pid / boot marker, as DF-2 does) and skips the re-add while that identity is unchanged; after a VPP restart it re-adds once. Test fakes must model duplicate-add behaviour (error or double-enable) so unit tests catch it | (a) re-apply blindly (D-063 as written) (b) boot-identity claim record | (a) breaks features (DF-6 review H3: toggles run twice per packet, 6rd/SR-MPLS/LISP-GPE fail on 2nd add) | low | all DF-*, P05 |
| 2026-09-24 | D-077 | DF-8 answers: Q1 tracedump/tracenode/prom/Trace Path object types dropped from DF-8 (no binapi / no API; V18) — F-capture-trace and F-dashboard-prom-alarms pick the path (enable the plugins in the F-vpp-debs build, prom via startup.conf generator); Q3 http_static / DUID host tests stay opt-in, run only in a manager VPP-restart window; Q5 shared helper package `descriptors/dfkit` (+ dfkittest, restarttest) accepted and becomes the recommended base for later descriptors; Q7 dhcp relay stays per-VRF (not a global) | Q1 (a) generate binapi on main (b) drop | no API exists for prom/Trace Path; tracedump needs a VPP rebuild | trivial | F-capture-trace, F-dashboard-prom-alarms, F-vpp-debs, F-kea-dhcp-relay |
| 2026-09-24 | D-078 | `contracts-v1` tagged at P03b's merge (cbfcdef); review request `docs/decisions/REVIEW-contracts-v1.md`. P03b answers: `vrx.model.{acl,nat,iface}.v1` accepted as agent-internal models (Go only, not imported by `vrx.v1`, `optional` rule does not apply); the shared `Redistribute` exception stays (4 documented keys); DF-3's model messages follow its spec at its merge; DF-1's remaining local protos (bond, memif, tap …) migrate when P08 touches them | (a) keep API-style presence for internal models (b) plain proto3 | internal models mirror Go specs 1:1 | trivial | P05, P06, P08, all F-* |
| 2026-09-24 | D-079 | Kea control path: the product talks to kea-dhcp4/6 over their own UNIX control sockets (root-only dir, 0600), no kea-ctrl-agent (deprecated in Kea 3.0; unauthenticated 127.0.0.1:8000 in RF-3 = review M3). Unbound listen/port/interface changes return a restart request with a convergence check (reload does not reopen sockets). Pending restart requests persist until acted on (chrony M2 pattern applies to every renderer) | (a) ctrl-agent + HTTP auth (b) direct UNIX sockets | (b) smaller attack surface, no deprecated component | low | RF-3, F-kea-dhcp-relay, F-unbound-chrony-syslog |
| 2026-09-24 | D-080 | VPP boot identity used by every persisted record (D-076 applied-once records, ClaimStore records, DF-2 classify store, DF-4 stats flag) = (kernel `/proc/sys/kernel/random/boot_id`, VPP main PID, VPP process start time from `/proc/<pid>/stat` field 22) — PID alone repeats across reboots. On identity change every record that refers to a VPP index or object id is invalidated (claims on untagged/unnamed objects expire; they are re-established only by our own successful Create). Per-interface records key on the interface's sw_if_index **and** logical name. One shared helper will live in the agent's vpp package (P08 consolidates; until then each factory implements the triple) | (a) PID only (b) PID + start time (c) boot_id + PID + start time | (c) is unique across reboots and restarts at no cost | low | DF-2, DF-3, DF-4, DF-6, DF-8, P05, P08 |
| 2026-09-24 | D-081 | F-startup-gen answers: Q5 isolcpus semantics = workers on isolated CPUs, main core off them (standard VPP practice; worker's choice confirmed); Q1 the four fields (`dataplane.managementPci`, `dataplane.devices{<pci>:{name,…}}`, `buffersPerNuma`, `plugins{<file>:bool}`) are added additively after contracts-v1 on a `contract/F-startup-gen` branch (schema + proto + drift guard), reviewed by the manager; Q3 the renderer is never part of commit apply (Apply refuses; applying = manager procedure); Q4 `tools/lab provision` switches to the generator → tech-debt | — | — | trivial | F-startup-gen, P04 tooling, P10 |
| 2026-09-24 | D-082 | Tests that read VPP-global settings hold `flock -s /run/lock/vrx-globals.lock`; opt-in tests that change them hold it exclusively and restore the previous value (shared-host-rules §7). DF-8 Q9 | (a) globals lock (b) lab lock exclusive | (b) would stall all 12 slots' VPP tests | trivial | all DF-*, F-* tests |
| 2026-09-24 | D-083 | RF-2 answers: strongSwan is NOT installed on the host (no package installs by the manager/workers); tests use stock debs unpacked under /run/vrx-test/<slot>/ in private namespaces (non-persistent — the harness re-unpacks), and the product strongSwan comes from P11's vrx build. govici (MIT) + x/sys accepted in apps/agent/go.mod. IPsec state/event messages for Retrieve/events are P11's contract addition | (a) apt install strongswan (disabled) (b) unpacked stock debs | (a) changes host packages owned by the bring-up agent | trivial | RF-2, P11, DF-5 |
| 2026-09-24 | D-084 | `dataplane.plugins` is a wrapped message (`optional PluginSet plugins { map<string,bool> switches }`) so presence is detectable: present → document authoritative; absent → overlay on the current startup.conf switches (with warnings). Decided before the field reaches main (contract/F-startup-gen) | (a) bare map + permanent overlay (b) wrapped message | (a) makes removing a plugin switch impossible without a hand edit | trivial (field not yet on main) | F-startup-gen, P06, P10 |
| 2026-09-24 | D-085 | prompts-s4 (42 feature prompts, files_owned, 0 overlaps) merged; dep suggestions accepted: F-loopback-bvi/F-tunnels +F-bridge-l2, F-vrrp-config-sync +P12, F-bfd-redistribution +F-ospf, NAT chain serialised (EI/DET44 after ED; HA-sync after EI), F-capture-trace +F-vpp-debs, F-ab-upgrade/F-images +P14, F-host-acl-nftables +F-object-model, S5 API/UI features (F-aaa, F-licensing, F-restconf-yang, F-backup-restore) +P08 (they share apps/ with P07b/P08). Rejected: F-ikev2-native +P11 (independent VPP-native path). F-vrrp-config-sync and F-det44-map-dslite-cnat re-estimated 16 h; LDP split from F-mpls-srmpls into F-mpls-ldp. F-host-stack scope cut to config exposure; NETCONF, SRv6-mobile, Ansible cut (see prompts) | — | parallel waves must not share domain files | low | S4/S5 |
| 2026-09-24 | D-086 | RF-4 answers: shared renderer helper package `internal/renderers/rfkit` accepted (consolidation with the RF-1 framework later, like dfkit/df2); gosnmp in go.mod accepted; stand-in fields for SNMP views, keepalived scripts/sync groups, syslog TLS files → additive contract branch when F-snmp / F-vrrp-config-sync / F-unbound-chrony-syslog start; rsyslog TLS stays built but untested until the TLS driver is packaged (P10 list); VRRP test keys `RF4tpsk*` accepted (8-char VRRPv2 limit); VRRP with auth renders VRRPv2 | — | — | trivial | RF-4, F-snmp, F-vrrp-config-sync, P10 |
| 2026-09-24 | D-087 | Incident 2026-09-24: two VPP crashes. 02:09:55 SIGSEGV in `fib_table_flush` (API) — manager error: `tools/ci.sh full` on main (CI slot 12) ran concurrently with worker TD-1 that the manager had also given slot 12 → same prefix/table range on one VPP, one run flushed tables the other used (V15 family). 02:23:03 SIGSEGV in `ip4_options_node_fn` (packet path) one second after DF-7 started igmp/vrrp/bfd/qos/span/mpls host tests in parallel. Rules: slot 12 reserved for CI (workers use 1–11); worker host tests one package at a time; DF-7 igmp/vrrp host tests opt-in until the trigger is identified (V22) | — | protects all slots on the shared VPP | trivial | manager, all workers, DF-7 |
| 2026-09-24 | D-088 | F-startup-gen split: generator + `contract/F-startup-gen` merge after fix round 2; `deploy/vpp/apply-startup.sh` (+ iface checker, fake-host tests) moves to new task F-startup-apply (re-review N1 hung-VPP/vppctl timeout, N2 ifupdown restore of mgmt IP/route, N3 vpp_papi missing → use the Go API client, N6 env through systemd-run, handover gate enforced). Applying startup.conf to vrx-a stays a manager step and cannot happen before the NIC mapping anyway | (a) third fix round on one branch (b) split | review rule max 2 rounds; the generator is correct and needed by P10/P14 | low | F-startup-gen, F-startup-apply |
| 2026-09-24 | D-089 | RF-2 answers: schema `vpn` tunnel name `max(60)` via the next additive contract branch (P11); stock strongSwan re-unpack after reboot is a documented manual step (no package downloads by the harness); owner-prefix scoping of shared daemons added to shared-host-rules; P11/DF-5 must flush orphaned SAs and call `AckRestart` after a charon restart. F-vpp-debs answers: patched builds get a local version suffix `+vrx<N>`; product ships neither vpp-dbg nor vpp-dev; tracedump/tracenode moved into vpp-plugin-core via the `--trace-plugins core` variant (V18) when F-capture-trace starts; build artefacts stay outside git under /srv/vrx-artifacts (P10 decides the repo) | — | — | trivial | P11, DF-5, P10, F-capture-trace |
| 2026-09-24 | D-090 | DF-7 answers: (1) LLDP index mismatch → loud failure `ErrIndexMismatch … NOT undone` is the product behaviour (the API cannot undo it; V20 lldp sw/hw index); (2) lb "removed" VIPs: the globals owner triggers VPP's lb cleanup after lb changes via the fixed-string `cli_inband` call (constant command, no user input — the only allowed CLI use, documented in ALLOWLIST); (3) vrrp and igmp host tests stay opt-in; one isolated run of each in a manager window (VPP otherwise idle) to confirm the V22b trigger | — | — | trivial | DF-7, F-lb, F-vrrp-config-sync, F-igmp-mfib |
| 2026-09-24 | D-091 | P06 merged (e20dd64) with its decisions D-P06-1…16 (Drizzle; password hashes only in app_user; RBAC checked on the resulting document and at commit — privileged changes need an admin at commit time, stale-lock takeover by a lower role discards privileged candidates; all secret writes admin-only, versioned with revisions and rollback; sync state in-sync/unknown/degraded with reconcile; commit result applied/partially-applied/not-applied; refresh-token chains in Valkey; AES-GCM with the secret name as AAD; WS auth before upgrade). F-startup-gen merged (9923526) incl. contract fields. Incident: an accidental merge commit on main (f8463a8) carried conflict markers because the manager ran a board commit in parallel with a merge — fixed forward in fdeda33; rule: the manager never runs git-mutating commands in parallel | — | — | trivial | P07b, P08, F-aaa, all S5 |
| 2026-09-24 | D-092 | VPP package versioning: only a byte-identical upstream tree is `26.06-release`; any patch — product, demo, optional OR build-infrastructure (e.g. hash-pinned DPDK python deps via dpdk.mk) — gets `+vrx<N>`. All pip installs in the VPP build are hash-locked offline (`pydeps.lock`) | (a) build patches exempt (b) any tree change suffixed | a package must never claim to be the host's exact build unless it is | trivial | F-vpp-debs, P10, P14 |
| 2026-09-24 | D-093 | F-sdk answers: Ansible stays cut (D-085, listed in STATUS-FINAL have-not); `sdk/test.sh` + `sdk/gen.sh --check` join tools/ci.sh (manager); Terraform = one confirmed commit per resource operation (simple, recoverable) — batching per run is a later enhancement; the candidate lock is per USER today, so two pipelines using keys of the same user share a candidate → documented limitation, recommended practice one service user per pipeline, per-API-key candidates → P06 tech-debt; `management.users` managed by automation allowed only for admin keys (P06 RBAC already enforces); MPL-2.0 HashiCorp modules acceptable in the separately distributed provider binary | — | — | trivial | F-sdk-terraform-ansible, P06 |
| 2026-09-24 | D-094 | Usage-limit interruption (~04:00): P07b, P13 (fix), F-sdk (fix) and the F-startup-apply review stopped mid-work; WIP salvaged (P07b 7cef6d4, P13 2b8147a, F-sdk 18c718f) and all four resumed with their context. P07b's orphaned API/agent/vite held the lab lock shared and made `ci.sh full` time out on its exclusive barrier → stopped by PID; rule: the lab lock is held only during actual test runs (shared-host-rules §10). DF-5's worker had been silent since 00:50 → CONTINUE respawn | — | — | trivial | all workers |
| 2026-09-24 | D-095 | Full integration on main: all 83 apps/agent packages green on real VPP; the smoke ping failed only because of polluted shared-VPP state; a manager debug ping then crashed VPP (04:50:27, `vnet_classify_find_entry` NULL on the packet path) — V19 is a crash vector: an interface re-using a sw_if_index inherits a classify binding to a deleted table. After the auto-restart the smoke test passes. New task TD-3: (a) DF-1 interface Create sanitizes inherited per-interface state (classify ip/l2 in+out tables, input/output ACL, ADL/feature arcs, vxlan bypass) before use — product-relevant after agent/VPP churn; (b) every descriptor that binds a classify table unbinds it before the table or the interface is deleted; (c) restart simulations that delete objects "behind the agent's back" delete dependents first; (d) a ci.sh full pre-flight that fails fast with a clear message if any interface carries a binding to a missing classify table | — | a shared-VPP crash on first packet | low | DF-1, DF-2, DF-7, DF-8, ci.sh |
| 2026-09-24 | D-096 | DF-5 answers: Q10 charon/kernel-vpp SPD+SA ids restart at 1 after a restart → P11 gives charon its own id range AND sweeps charon SPDs/SAs while charon is stopped (ordering: stop charon → sweep → start → AckRestart); Q11 key fingerprints stored for change detection use a keyed hash (HMAC-SHA256 with an agent-local 0600 key under the state dir), never a plain sha256 of the secret — a weak PSK must not be guessable offline from agent state; Q12 persisted record stores (`WithBootStore`, ClaimStore) are installed by P08's wiring (already on P08's notes) | Q11 (a) plain sha256 (b) keyed HMAC (c) reverse lookup by D-051 name | (b): no offline guessing, no dependency on secret store at Retrieve | low | DF-5, P11, P08 |
| 2026-09-24 | D-097 | Password lifecycle: password hashes live only in app_user; every path that writes a config snapshot back (reconcile, confirm revert, rollback, import) takes hashes from app_user, never from the snapshot. An admin password reset revokes all of the target's sessions, refresh chains AND API keys by default (explicit `keepApiKeys: true` for service users, audited); a self-service change with the current password keeps keys; wrong `current` counts toward lockout | (a) keep keys on reset (b) revoke by default | a reset is the response to suspected compromise; a 10-year key must not outlive it | low | TD-2, P06, P07b users UI, F-aaa |
| 2026-09-24 | D-098 | Manager outage 05:45→07:15 (previous manager session ended; no worker process alive): P08, TD-2, TD-3, F-startup-apply each had uncommitted work → salvaged as `wip(<id>): salvage …` commits on their branches (de60fa8, 8dde3e4, d5116b8, 028eda4) and respawned with CONTINUE envelopes (`/root/ngfw-wt/<id>.continue.md`, copied into each worktree) on the same slots; DF-5 board row corrected to merged (4b11eff) and its worktree removed; stale F-vpp-debs worktree removed (merged). Two untracked product-owner files in the main checkout (`gost.tar.gz`, `index.html`) are not project files → left untouched, only listed in `.git/info/exclude` so the merge gate (`status --porcelain` empty) holds | (a) discard dead workers' diffs and restart the rounds (b) salvage + CONTINUE (c) delete the stray files | (b) keeps ~1 h of fix-round work; deleting someone else's files is not the manager's call | low | P08, TD-2, TD-3, F-startup-apply |
| 2026-09-24 | D-099 | prompts-s4 suggested deps: every hard suggestion adopted (added now: F-rpf-adl-pbr +DF-4, F-ikev2-native +P11, F-host-stack +F-startup-gen, F-backup-restore +F-ab-upgrade, F-aaa/F-licensing/F-restconf-yang +P07b; the rest were already on the board); soft suggestions (F-acl→F-rpf-adl-pbr, F-ikev2-native→F-pki, F-loopback→F-tunnels, F-dashboard→F-startup-gen, F-ipfix-sflow→hsflowd) stay notes, not deps. Oversized rows (F-det44-map-dslite-cnat, F-vrrp-config-sync, F-host-stack, F-tunnels) are NOT split now: their prompts already order the work and fence the tail; the time box + "what is left" file decides, and a leftover becomes a follow-up row | (a) adopt all incl. soft (b) adopt hard only (c) also split 4 big rows now | soft deps serialise waves for little gain; splitting before a worker has touched the code guesses the seam | low | S4 waves |
| 2026-09-24 | D-100 | TD-2 questions: (1) the product nginx (P10) never proxies `/api` from plain :80 — redirect only — because the API trusts loopback peers as TLS-terminated; `POST /auth/login` gets the same transport check as password set; (2) creating an API key from a JWT session requires the current password (a stolen ≤15-min access token must not mint a long-lived key; keys created with an API key stay allowed for automation, audited); (3) disabling an account bumps the credential generation (access tokens die immediately, like a reset); (4) single-line string fields that allow LF → packages/schema patterns, tracked in docs/tech-debt.md for the next contract branch. (1)–(3) go to a new small row TD-4 (dep TD-2, 3 h) rather than a second TD-2 fix round | (a) fold into TD-2 round 2 (b) new row TD-4 (c) defer to F-aaa | TD-2's findings are fixed and under verify; a new round would delay the merge for items the review did not raise; F-aaa is S5 and too late for (2)/(3) | low | TD-2, TD-4, P10, packages/schema |
| 2026-09-24 | D-101 | VPP crash 07:27:32 (SIGSEGV PC 0x0, NRestarts 5→6, no core/backtrace): not V19 (live tables only, no packet sent by TD-3). Manager read of af_packet.c/file.h finds an fd-close-before-file-del ordering bug with a double close (V24); timing matches slot 1 (P08) deleting host-w1l0/host-w1w0 with their veths up 10 s earlier. Interim rule: every af_packet delete quiesces first — `tools/lab rig down/gc` now `ip link set <veth> down` before `delete host-interface` (manager edit, lab tooling); tests that delete af_packet interfaces through the agent must down the veth first; new row TD-5 makes the agent's af_packet Delete do it. Slots whose VPP objects vanished at 07:27:32 (all) re-create on their next run; P08 told. Also: `full-coredump` is set but no core was captured (coredumpctl empty) → product-owner item: enable systemd-coredump / LimitCORE for vpp.service (manager may not touch the unit before handover) | (a) treat as unexplained, keep going (b) interim rig/agent quiesce + V-item (c) stop all host tests until a VPP patch | (b): cheap, covers the only suspect path; (c) would halt P08, the critical path, on an unproven hypothesis | medium | P08, TD-3, TD-5, all host tests |
| 2026-09-24 | D-102 | TD-2 verify V2 — a password hash staged through the config API (the current Users page flow: stage hash → commit) bypassed D-097. Rule: when a commit/confirm promotes a snapshot in which an existing user's password hash changed, the API applies admin-reset semantics for that user in the same save: bump the credential generation, revoke sessions + refresh chains + API keys (no keepApiKeys on this path), audit row names the user and `via: config`. Staging stays allowed (the UI keeps working); P07b's Users page moves to `POST /users/{name}/password` in F-aaa (note on that row). V1 (keys created during a reset survive) → credential generation column in `app_user` bumped in the reset transaction; key creation locks the row and re-checks. V3–V5 fixed in the same round if cheap, else tech-debt | (a) refuse hash staging for existing users (b) revoke on promote (c) document only | (a) breaks the shipped UI flow; (c) leaves a compromise-response path that does nothing; (b) keeps both paths equivalent | low | TD-2, F-aaa, P07b |
| 2026-09-24 | D-103 | F-startup-apply: both fix rounds used; verify 0ff06fd APPROVE WITH CHANGES (N1–N7 fixed; new V1 stale dead-man can revert a newer committed apply, V2 rollback write failure leaves VPP stopped with locks held, V3 `systemctl show` failure → no rollback, V4 early crash+auto-restart accepted since NRestarts is not compared; V5–V9 low/nit). Merge now; V1–V4 (+V5–V9 if cheap, + harness/shellcheck in tools/ci.sh = L1) go to new row TD-6, which **must merge before any real `--apply`** — the handover gate already refuses every apply without a sha256-bound approval, so nothing can reach vrx-a meanwhile | (a) third fix round (b) merge + TD-6 gate before first apply (c) merge and put the lows in tech-debt | (a) breaks the 2-round rule for issues that cannot fire today; (c) would let a real apply run with a known VPP-down path | low | F-startup-apply, TD-6, handover |
| 2026-09-24 | D-104 | Wave-A readiness: F-rpf-adl-pbr gets dep TD-3 (both own descriptors/{classify,adl}); the two pre-factory example prompts (F-vlan-qinq, F-nat44-ed-sessions) are regenerated against the merged DF-1/DF-3 descriptors and P08 before spawning (manager-assistant, branch task/prompts-s4b) — they asked workers to rebuild descriptors that exist | (a) spawn with old prompts and rely on the fence (b) regenerate first | old prompts would cause duplicate descriptors and ownership collisions with TD-3/P08 | low | wave A |
| 2026-09-24 | D-105 | Manager handover ngfw-f3 → ngfw-46 (product owner's request, 13:51, main 2eb852b). Open answers: TD-2 Q6 — D-102 literal reading confirmed (an admin changing their own hash via the config API ends their own sessions and keys); P08 F1 — keep the old meaning of `/state/interfaces` `items[].config` (Retrieve view) and add a new field for the running config, so the change stays additive and the CLI keeps working; P08 F4 — af_packet may attach only to Linux netdevs of kind veth (agent-side check), the management NIC can never be taken by config; TD-3 M1 — placeholder cap = holes + 2×FreshRun, max 64, still fail-closed (done in TD-5, which now also owns ifsanitize's cap); TD-3 M2 — loop16000–loop16383 reservation is a semantic check on F-loopback-bvi's contract branch | F1 (a) CLI + contract doc follow the new meaning (b) keep old meaning, add field; F4 (a) name allow-list (b) veth kind only (c) lab-build gate; M1 (a) 16 fail-closed (b) holes+2×FreshRun ≤ 64 (c) conditional | F1 (b) avoids a reshape (always-PENDING #1) and a CLI change; F4 (b) is one netlink call and product NICs go through DPDK; M1 (a) lets one slot's classify churn fail every interface Create on the shared VPP | low | TD-2, P08, TD-3, TD-5, F-loopback-bvi-gso-lldp-span |
| 2026-09-24 | D-106 | CI/host fixes by the manager: (1) `golangci-lint run --allow-serial-runners` in apps/agent and apps/cli (parallel gates failed with "parallel golangci-lint is running" — TD-2/TD-3 false reds); manager gates on branches that predate this run with a per-run TMPDIR; (2) tools/ci.sh runs `make -C apps/cli lint test build` (stale operations_gen.go passed the gate, P08 F2); (3) tools/app set `umask 077` in its main shell, the product agent inherited it and created `/run/vrx-test` as 0700 at 13:50 → frr/_chrony test daemons of every slot got EACCES (P08 ci full 13:19 red in renderers/frr, chrony); umask moved into a subshell, `/run/vrx-test` and `w*` chmod 0755 | (a) serialize whole ci.sh runs with flock (b) golangci-lint serial runners + TMPDIR | (a) would queue 11 workers' gates behind each other | trivial | all gates, tools/app, P08 |
| 2026-09-24 | D-107 | P08 review BLOCK (6022f0d) → fix round 1. Manager items from the review: (1) N2 — `deploy/dev/pg-test.sh` re-moded every slot run dir to 0700 (`install -d -m 0700`), the same EACCES as D-106 → 0755 (pg.env stays 0600); P08's own run.sh/interfaces_test.go fixed in its round; (2) I3 — tools/app's product agent sets `VRX_GLOBALS_OWNER=0` (it must never own VPP globals on the shared host); (3) I1 — V24 double close happens with veths down too (quiesce ≠ fix): recorded in vpp-code-track V24; af_packet churn kept minimal; the VPP patch stays a V-item (no C code, product = DPDK); (4) I6 — VPP API stalls of seconds on af_packet create/delete: under investigation by the manager before the P08 re-review re-runs the topology test | I1 (a) patch VPP now (b) V-item + minimise churn | (a) is C code — forbidden in FAST MODE; af_packet is lab-only | low | P08, TD-5, all af_packet tests, tools/app |
| 2026-09-24 | D-108 | VPP audit (wf_7f25cdf3) results → PENDING-vpp-host-hardening.md for the product owner (VM memory reservation, hugepages on the kernel cmdline + netlink rmem, startup.conf headroom/diagnostics, optional +vrx1 patch build, 26.10 later). Source verdict: v26.06 == stable/2606 head (0 commits after the tag) — no rebuild. Done now without a restart: `tools/lab rig` creates af_packet with `tx-size 2048 tx-per-block 256` (measured: create 0.3 s / delete 0.7 s vs 40 s–5.5 min API stalls with the 66 MiB default TX ring, P08 I6); the agent's af_packet Create gets the same explicit ring in TD-5 | (a) wait for the owner's startup.conf/VM changes (b) shrink rings now in rig + agent (c) serialize af_packet creates with a lock | (a) keeps every af_packet test blocked; (c) still stalls every other client for the duration | low | all af_packet tests, TD-5, P08, tools/lab |
| 2026-09-24 | D-109 | Wave-A launch pack merged (f7e82f3): 17 feature envelopes + TD-4, hotspot map, launch plan, F-mpls-ldp prompt (LDP removed from F-mpls-srmpls; its RF-1 dep dropped). Decisions from the critic's list: (a) P12 gets a **hard** dep on F-vrf-static-ecmp (ListRoutes, /state/routes, viaFrr selector) — P12 is batch 2 anyway; (b) the API→agent secret channel is a security boundary → PENDING-secret-channel (only the E2E secret steps of P11/F-wireguard/P12/F-unbound wait; fixture resolver meanwhile); (c) F-bridge-l2 = per-member leaves + a named container, not a new root key `l2` (a new root would be a reshape, always-PENDING #1); (d) renderer stage = one singleton scheduler descriptor per renderer (no shared service.go stage now); (e) the proto/oneof numbers in wave-A-hotspots.md §2 are confirmed and binding; (f) envelope CI line = short TMPDIR, no whole-gate lock (D-106); (g) V23(a) belongs to F-rpf-adl-pbr. New row W-seed (anchor commit, 2 h, deps P08+TD-2); all 17 envelope rows depend on it; board files_owned synced from the envelopes | (a) hard / soft + merge order; (c) root key / container; (d) singleton descriptor / service.go stage; W-seed (i) seed first (ii) spawn without anchors and union-resolve each merge | (a) soft would let P12 duplicate F-vrf's route reader; W-seed (ii) costs ~10 min manager time × ~17 merges and risks bad unions, (i) costs ~2 h once | low | all S4 |
| 2026-09-24 | D-110 | TD-5 answers: D1 — the ifsanitize cap counts as holes both gaps and pops the pop order proves freed (option b; gaps-only failed closed on the first host run and would stay failed because each run replays the previous pop order); Q1 — a failed rollback quiesce fails closed and leaves the untagged `host-<dev>` named in the error (option a; deleting anyway risks V24, tagging needs a new orphan protocol); Q2 settle 200 ms kept; Q3 (over-cap pools fail every Create until the free list changes) → M3 exact readback stays tech-debt | D1 (a) gaps only (b) + proven-freed pops; Q1 (a) fail closed (b) delete anyway (c) orphan tag | (b)/(a) are the only options that are both live and crash-safe | low | TD-5, TD-3 |
| 2026-09-24 | D-111 | TD-2 verify round 2 = APPROVE WITH CHANGES (V1–V5 fixed, negative controls 7/8 fail on round-1 code). T1 (new e2e flaky under host load: V1 over the 30 s default, V4 hangs on the candidate lock) is fixed test-only by a short worker on the same branch; merge after the full api e2e suite is green 3× in a row + CI, without a third review round. syncUsers' unlocked "before" read is safe only with one API process — recorded as a known limit (HA is out of the 21-day plan) | (a) merge now, T1 as follow-up (b) test-only fix before merge (c) third review round | (a) would make main's `ci.sh full` flaky; (c) breaks the 2-round rule for test-only timing | trivial | TD-2, TD-4 |
| 2026-09-24 | D-112 | Merge procedure (product owner): from now on every task branch is rebased onto `main` and squashed into one commit before it is merged. The manager does it in the worktree after the review APPROVE: `refs/archive/<id>` keeps the reviewed history, `reset --soft` to the merge base + one Conventional-Commits commit (`contract(…)` if contract files changed), `rebase main`, then `tools/ci.sh --base main` and `merge --no-ff` as before so the pre-merge-commit hook still runs. Branches already in flight (P08, TD-2, TD-5, TD-6) follow it at their merge | (a) squash + rebase on the branch, `--no-ff` merge (b) `git merge --squash` (c) rebase + fast-forward | (b) and (c) create no merge commit, so the pre-merge-commit gate never runs; (a) keeps the hook and gives one commit per task | low | all tasks, prompts/MANAGER-PROMPT.md §2, docs/contributing.md |
| 2026-09-24 | D-113 | D-108 amended (TD-5 review H1): the af_packet TX **frame** must stay 67584 bytes (2048×33, VPP's default) — VPP's TX copy (`af_packet/device.c:561-573`) has no length check, so a GSO or jumbo frame larger than a 2048-byte slot overruns the ring (last slot past the mmap → shared-VPP crash). Rings are shrunk by frame COUNT instead: TX 16 × 67584 = 1 MiB, RX 2048 × 8 per block = 2.5 MiB. Rig fixed on main now (measured create 1.8 s / delete 1.7 s during an ESXi slow period, vs 40 s–5.5 min); TD-5 fix round applies the same to the agent + M1 (cap counts an unbroken freed run above an older hole) + L2 (rollback context) | (a) 2048-byte frame + MTU guard (b) default frame size, fewer frames (c) disable GSO/cksum offload + 2048 | (a)/(c) still leave jumbo MTU 9000 interfaces able to overrun; (b) is safe for any frame VPP can build | low | tools/lab, TD-5, all af_packet tests |
| 2026-09-24 | D-114 | Speculative starts (product owner: "reviews make everything wait — add agents"). A task whose only unmerged deps are branches already in fix/verify with frozen product code may start from that branch's tip: W-seed from task/P08 (fix round 1 running), TD-4 from task/TD-2 (T1 test-only fix running). Wave-A batch 1 starts from task/W-seed as soon as W-seed is done, before P08 merges. D-112's rebase + squash before every merge makes the later rebase a single step; the merger rebases in dependency order (P08 → W-seed → features; TD-2 → TD-4). If a base branch changes contract shape in its remaining round, dependents rebase and re-run CI; nothing merges out of order. Concurrency follows free slots (11), not the load-average cap, while load comes from short CI/test bursts | (a) wait for merges (b) speculative start on the base branch tip (c) speculative start on main with stubs | (a) leaves 7 of 11 slots idle for hours; (c) duplicates P08's code | low: at worst one extra rebase per task | W-seed, TD-4, wave A |
| 2026-09-24 | D-115 | TD-5 fix round 1 answers: F1 a failed Delete restores the netdev's link only when the interface is provably still in VPP; F2 an already-down netdev that is not a veth lets the delete proceed (nothing to quiesce, L3 refusal applies only to bringing an UP non-veth down); F3 Create-rollback bound 30 s on a WithoutCancel context. Re-review is a focused verify of the review findings only (speed, product-owner request), not a full second review | F1 (a) always restore (b) only if provably present; F3 (a) caller ctx (b) 30 s detached | (a)/(a) either reopen a downed link for a gone interface or reintroduce the orphan | trivial | TD-5 |
| 2026-09-24 | D-116 | TD-6 review APPROVE WITH CHANGES (621d1e2): V1–V9 verified fixed (main's script fails 13 checks, the branch passes 17/17). Merge now; F1 (two concurrent `--stage rollback` of one apply — per-apply lock + cancel the dead-man, or point the operator at the dead-man unit) and F2 (scenario 40's time bound is load-dependent → count the fake's `ip neigh` calls) go to new row TD-7, which, like TD-6 (D-103), must merge before any real `--apply`; F3–F9 → docs/tech-debt.md. The apply gate already refuses without a sha256-bound approval, and no real apply is possible before the NIC mapping + handover | (a) fix round on TD-6 (b) merge + TD-7 gate before the first real apply | (a) holds the S2 fix for a path that cannot run today; (b) keeps the D-103 guarantee | low | TD-6, TD-7, F-startup-apply |
| 2026-09-24 | D-117 | ui-nav-collapse (product-owner request made in session «وب») review APPROVE WITH CHANGES (3 lenses + adversarial verify, all 11 findings confirmed). Must fix before merge: flow.e2e.mjs `nav()` opens the owning group first (the P07b real-stack E2E cannot reach Users/Revisions otherwise); the group that holds the current page opens automatically on load/navigation (groups still start collapsed otherwise — a hidden aria-current page is an a11y regression vs main); per-instance ids via useId (duplicate ids with the phone drawer); open-state lifted to AppShell; the new test derives headers from buildNav (wave-A adds entries); docs/status/tasks/ui-nav-collapse.md with pasted checks + en/fa screenshots. Merge by the merger under D-112 after a focused verify | auto-open current group (a) yes (b) no, header aria-current only | (b) leaves the current page invisible on deep links; the product owner can overturn | trivial | ui-nav-collapse, wave-A nav hotspot |
| 2026-09-24 | D-118 | P08 focused re-review (3 lenses; 9 of its verifiers were cut by a usage-limit stop, the 5 finished agents agree) = APPROVE WITH CHANGES → fix round 2 (the last, 2-round rule): T1 the merged tree with TD-5 fails TD-5's V24 guard on coretest's type assertion → P08 amends TD-5's guard to skip `*ast.TypeAssertExpr` (allowed edit in a merged TD-5 file); R1 tolerant.go remembered-default Update → ErrRecreate; R1-cli the CLI skips rows whose `config` is null (D-105's "CLI untouched" is relaxed for this: without it `show interfaces <candidate-only>` exits 0); e2e must discriminate config vs running; tests for N4/N5; docs I4/Q3/Q4; R2-stores and R3-gauge → tech-debt. Topology test once after merging main (TD-5 rings). Usage-limit stop ~16:40: W-seed, TD-4, TD-7 salvaged as wip commits and resumed with CONTINUE envelopes (/root/ngfw-wt/CONTINUE-quota.md); tools/lab rig down/gc now tolerate the netns→veth teardown race (T6, 63178d2) | T1 (a) exempt coretest from the guard (b) skip type assertions (c) rename the fake handler; R1-cli (a) filter null-config rows (b) document the change | (b)/(a): keep the guard strict for real sends; keep the CLI's exit-code contract | low | P08, TD-5 guard, apps/cli |
| 2026-09-24 | D-119 | Wave-B/C/S5 launch pack (31 envelopes, prompt refresh, wave-BC-numbers.md, wave-BC-launch-queue.md) committed to main as files (not merged — merges belong to the merger). Critic actions: M1 new row W-seed-BC (anchor pass, 3 h) and every wave-B/C/S5 row depends on it; M2 new row TD-8 agent seams (Env.Publish/Resync, dynamic desired source, metrics hook; 4 h) — option (a) row; M3 daemons: one owner per slot INSTANCE (frrtest/swantest own pathspace/netns/sockets; units stay disabled), a root-netns zebra always one slot at a time — option (a); M4 P12 carries the S3 routing warning + S2 frr.RegisterInterfaceLines (a); M5 deps: F-igmp-mfib +P12; F-det44/F-ikev2-native/F-ra-vpn/F-capture-trace/F-backup-restore +F-vrf-static-ecmp; F-aaa +TD-4; F-dashboard +F-unbound; P10 +TD-7; F-mpls-ldp/F-igmp/F-dashboard +TD-8; M6 files_owned synced from all envelopes; M7 NAT44-ED IPFIX logging → F-ipfix-sflow gap descriptor (a); M8 PENDING-secret-channel parks the E2E secret steps of F-ikev2-native, F-pki, F-ra-vpn, F-snmp, F-ospf, F-isis-rip, F-bfd, F-mpls-ldp, F-host-stack, F-vrrp too; keepalived cluster auth sync → same PENDING (one secret channel decision, not two); M9 P10's agent unit grants AF_NETLINK + CAP_NET_ADMIN and the ReadWritePaths the agent already uses (implements TD-5/P05's documented root agent — not a new privilege decision); vrx-upgrade trigger = `vrx-upgrade@<op>.service`; BFD engine per box default VPP (F-bfd); priority bumps P12→3, P10/P14→5, F-vrrp→4 (critical paths 60–81 h); M10 network reachability checks before the S5/network rows (manager, when they become ready) | M2 (a) row (b) manager writes it (c) first consumer; M3 (a) per instance (b) one owner per daemon; M7 (a) F-ipfix (b) follow-up (c) have-not; M8 (a) extend the one PENDING (b) new PENDING-cluster-secret-sync | M3 (b) serialises FRR work into ~100 h; M8 (b) would ask the product owner the same boundary question twice | low | all S4/S5 rows |
| 2026-09-24 | D-125 | Plan changes from the product owner's REVIEW-2026-09-24 (66 items, each re-verified; verdicts in docs/status/review-2026-09-24-verdicts.md) and the architecture audit. **18 new rows:** TD-9 (agent: bounded VPP calls + txn semantics; also ARCH-01 agent half, a failed state save answers DEGRADED), TD-10a (API commit engine; also ARCH-01 API half, Health.last_txn_id vs running on boot/reconnect/RECONCILE_DONE), TD-10b (auth, session, audit), TD-11a (reachability check), TD-11b (claim safety), TD-11c (untagged NICs, alias delete order, claim-store scale), TD-13 (scheduler Validator + VPP→daemon stage, ARCH-02), TD-15 (API hygiene, ARCH-04/-11/-05), TD-16 (descriptors/kit + fsync, ARCH-06), TD-17/TD-18/TD-19 (the review plan's TD-12/13/14, renumbered because board TD-8 and TD-12 are taken: apply-startup product mode, repo/ops hygiene, install + lab provisioning), SEC-auth, UI-domain-editor, TEST-traffic-A/B/C, LAB-vpp-per-slot (parked on PENDING-vpp-host-hardening). No RF-1b, F-ansible or new API row: 4.1, 4.2 and 6.3a are wrong or by design. **Deps:** INTEGRATE-E2E += TD-9, TD-10a, TD-11a/b/c, TEST-traffic-A/B/C; P10 += TD-9, TD-10b (est 10 → 20); STATUS-FINAL += TD-11a; SECURITY-REVIEW += SEC-auth, TD-9; F-aaa += TD-10b, SEC-auth; F-restconf-yang += SEC-auth. No start gate is added to any wave-A row. **Merge order** goes in row notes as "merge after": wave A after TD-11a (2 h escape hatch); the NAT rows after TD-11b; F-bonding/F-bridge-l2 after TD-11c; TD-9 after TD-8/TD-12; P11, P12, F-kea, F-unbound, F-snmp and F-host-acl after TD-13. **DoD** is now "reachable from the desired state through the API and the UI", plus the per-wave TEST-traffic rule (docs/12; docs/11 §5, flagged for the product owner). docs/11: ✅ = in plan, not done; D8.9 → 🟡. Tech-debt gets owner/due-before columns. **PENDING:** 6 new (agent-privileges, vpp-c-track, tools-app-transport, session-revocation, system-identity, remote-mirror) and 2 refreshed (secret-channel, vpp-host-hardening) | gating: (a) hard deps from every wave-A row to TD-9/TD-11 (b) a new `merge_after` board field + a MANAGER-PROMPT merge step (c) "merge after" in row notes, deps only for start gates; rows: (a) one row per report section (b) split by file ownership (c) fold into the running rows | (a) would stall the 7 running speculative rows; (b) needs a board.py/prompt change the manager deferred; (c) leaves readiness untouched. Splitting by owned files keeps reconciler.go/service.go/pg-repo.ts hunks apart. Running envelopes get only amendments paid from their own budget | low (board rows and notes) | wave A, P10, P12, INTEGRATE-E2E, SECURITY-REVIEW, STATUS-FINAL, F-aaa, F-restconf-yang, new rows |
| 2026-09-24 | D-121 | Load-sensitive tests break merge gates (TD-6's pre-merge hook failed on TestGRPCRoundTrip's 50 ms sleep at load ~34; earlier TestPendingSurvives…, strongswan TestWatchResync, api vitest fetch timeouts at load 60–166): new row TD-12 (test code only, event waits with bounded deadlines, priority 1) runs now; the merger retries a gate once when the only failure is a listed flake | (a) de-flake now (b) lower concurrency during merge windows (c) retry blindly | (b) costs throughput the product owner asked for; (c) hides real regressions | trivial | all merge gates |
| 2026-09-24 | D-120 | The pre-merge-commit hook (shared by every worktree via .git/hooks) runs only when the toplevel is /root/ngfw: a worker merging main into its own task branch no longer triggers a 7+ min quick gate that could fail on unrelated reds (W-seed's main merge was blocked that way); gates stay at the merge into main | (a) hook for every merge (b) main only | (a) doubled worker merge time and blocked on others' reds | trivial | all workers |
| 2026-09-24 | D-122 | Architecture audit A1 (HIGH): W-seed and the 7 wave-A branches were based before TD-5/D-113/the rig fix → their worktree tools/lab had the 2048-byte TX frame (ring overrun) and their agent the 76 MiB default ring. Fixed by merging task/P08@abb6950 into task/W-seed (df67a8e, clean, af_packet+subsystems tests green); every F-* merges task/W-seed before any host run. A2: F-bridge-l2 took RoutingConfig.l2 = 20 → confirmed; next free RoutingConfig number = 21 | A1 (a) re-merge W-seed (b) restart the 7 workers on a new base; A2 (a) confirm 20 (b) renumber | (b) would throw away running work; renumbering after code exists costs a contract rework | low | W-seed, all wave-A, F-bridge-l2 |
| 2026-09-24 | D-123 | Web-ahead track (product owner: "push the web ahead"; architecture audit plan): WEB-1 (ui-kit SchemaForm gaps, base main) and WEB-2 (config screen kit + widgets + Secrets page, speculative on P08) start now; WEB-3 (committed browser harness) after ui-nav-collapse; WEB-4a/4b pre-built routing and HA screens merge UNROUTED (no router/nav/i18n lines) so no stubbed screen is ever reachable, and the feature adds routes + live status; TD-8 starts now (speculative on W-seed) with +fail-closed SlotIDRange and the ARCH-1 coverage table, and becomes a dep of F-neighbors-ra, F-acl, P11, P12, F-wireguard. Routed "backend pending" screens would relax the stub ban → not done (would need the product owner) | screens (a) unrouted merge (b) stacked branch per feature (c) routed with a banner | (c) violates the 00-CONTEXT stub ban; (b) delays merges | low | WEB-*, S4 web work |
| 2026-09-24 | D-124 | TD-4 review (APPROVE WITH CHANGES, d30d503) → fix round 1: H1 per-account Valkey rate limit before the key-creation `current` check (+ one `locked` wording, concurrency e2e with negative control); M1 failed key creations audited with the problem slug; L1 a `current` sent by an API-key caller is REJECTED with 400 `current-not-allowed-with-api-key` (a role-limited key must not be able to test its owner's password). TD-4 Q2 (tools/app plain HTTP) and Q3 (no revocation on demotion/deletion) → PENDING-tools-app-transport, PENDING-session-revocation | L1 (a) check it (worker's D-TD4-1) (b) reject 400 | (a) turns every role-limited key into a password oracle | trivial | TD-4, TD-10b |
| 2026-09-24 | D-126 | VPP crash #11 at 18:41:08 (SIGSEGV PC 0x0, NRestarts 0→1, first in instance 8760 after 5 h 38 min): during F-vlan-qinq's run-2 packet phase (dot1ad frames on af_packet) and 9 s after a slot ran a BLIND policer/flow classify unbind sweep (table_index 1..25 on sw_if_index 7). systemd-coredump captured a core (kept at /root/ngfw-wt/logs/crash-20260924-1841/ with the journal and the run logs), but the host has no gdb/elfutils, so the PC-0 frame cannot be unwound (the coredump stack shows only the signal handler) → product-owner item: allow installing gdb + the already-built vpp-dbg (PENDING-vpp-host-hardening option C). Rules: never sweep classify bindings by index (unbind only recorded bindings); no 802.1ad frames on the af_packet rig; F-vlan-qinq's packet phase is opt-in (VRX_QINQ_PACKETS=1) | (a) keep going with the rules (b) stop host packet tests until a backtrace | (b) stalls wave A on an unproven trigger; the same PC-0 signature as 07:27 (V24 suspect) | low | all slots, F-vlan-qinq, F-rpf-adl-pbr, TD-3 |
| 2026-09-24 | D-127 | tools/ci.sh contract guard: `git log … \| grep -qiE '^contract…'` under `set -o pipefail` failed ~55% of runs (grep -q exits on the first match, git gets SIGPIPE → 141 → the if sees failure → "CONTRACT FILES CHANGED WITHOUT A CONTRACT COMMIT" on branches that have one; F-vlan-qinq Q6, TD-4 Q10: 110/200). Fixed by capturing the subjects first and grepping a here-string; committed between merges so P08 (which carries contract commits) gates cleanly | (a) capture then grep (b) `grep -c` without -q (c) disable pipefail locally | (a) is the smallest and keeps pipefail everywhere | trivial | every merge gate |
| 2026-09-24 | D-128 | VPP crash root cause (supersedes D-126's guess): the 18:41:08 SIGSEGV PC 0x0 is `vppctl show trace` → `format_vlib_trace` (vlib/trace.c:159-162) calling a NULL formatter for a trace record whose node was a deleted and reused interface's tx node — read from the systemd core with the already-built vpp-dbg symbols (python core reader + addr2line; F-vlan-qinq review 674b03e H1). P08's topology test (interfaces_test.go:291/297, run by `ci.sh full`) and F-nat44-ed/F-vlan-qinq tests call `trace add`/`show trace`; the 07:27 crash (D-101, attributed to V24) happened during P08's topology run and is very likely the same bug. Rules: `show trace`, `trace add`, `clear trace` are BANNED on the shared VPP (tests and humans); `tools/ci.sh full` frozen until TD-20 (trace-free topology evidence + a static guard in the quick gate + shared-host-rules §11) merges; V-item for the one-line NULL guard upstream. D-126's classify-sweep and 802.1ad rules stay as hygiene. D-128b: the contract guard ignores `*.test.ts` / `*_test.go` under the contract paths (tests are not contract). CI slot 12 lent to F-vlan-qinq's one host run while `ci.sh full` is frozen | (a) ban trace (b) clear trace before each use (c) patch VPP now | (b) still races other slots' records; (c) is C code (PENDING-vpp-c-track) | low | all host tests, P08, F-nat44-ed, F-vlan-qinq, ci.sh |
| 2026-09-24 | D-129 | Review answers batch: F-object-model F7 — last-good FQDN answers kept at most 24 h after NXDOMAIN/failure, then dropped with a warning (env-configurable); F5 — the implemented-domains assertions in service_test.go: the first wave-A branch to merge defines them (registry-derived), later branches take main's version at rebase; F-nat44-ed Q2 — its `extensions` seam in coretest/fakevpp.go is the A6 seam (the merger keeps one copy; F-rpf-adl-pbr added the same), Q3 (c) + tech debt (d) "agent returns a canonical running form", Q4 → a later task, Q7 → tools/lab rig disables TX checksum offload on the netns veth end; TD-8 Q1–Q5 per its review (Q3 staged: refuse start-up without an ID range before the first id-allocating family merges — tools/app VRX_VPP_TABLE_BASE=13000, topology harness passthrough, P10 unit `all`); F-rpf-adl-pbr Q2 keep the pbr.acl-ref stand-in (F-acl removes it) | per item | the reviewers' recommendations; none reshapes a contract | trivial | F-object-model, F-nat44-ed-sessions, F-rpf-adl-pbr, TD-8, tools/lab |
| 2026-09-24 | D-130 | Merge gate: the separate branch gate is dropped. Under D-112 the branch is `main` + 1 commit, so the pre-merge-commit hook's quick gate (plus contract guard and gitleaks) runs on exactly the branch's tree; the bare gate on `main` after the merge stays. All git-mutating commands in /root/ngfw run under `flock /run/lock/vrx-main.lock env VRX_MAIN_LOCK_HELD=1`, one lock per merge sequence (merge → main gate → board), a red hook is aborted inside the lock, and a local pre-commit guard refuses commits in /root/ngfw without the variable (after a verifier committed to main mid-merge) | (a) hook as the branch gate (b) keep both gates | (b) runs the same tree twice (~11 min per merge) | trivial | all merges, prompts/MANAGER-PROMPT.md §2, docs/contributing.md |
| 2026-09-24 | D-131 | F-rpf-adl-pbr fix-round answers: Q5 ADL `defaultAllow` = "non-IP frames pass", default true (a firewall that silently drops ARP/LLDP on an ADL-enabled port is the worse surprise; reversible per interface); the `pbr.acl-ref` stand-in and TestRpfAdlPbrWithoutFAcl are removed by F-acl (F-acl envelope addendum; merge order F-rpf-adl-pbr → F-acl); F-loopback reuses the `Services` constant/map entry and `desired.ServicesMembers` instead of declaring its own; WEB-1 H1 fix removes P08's `dropPhantomOptionals` (allowed edit in a merged P08 file: the presence toggle made it delete enabled-with-defaults objects) | Q5 (a) pass non-IP (b) drop non-IP | (b) breaks ARP on every ADL port unless the user knows to allow it | trivial | F-rpf-adl-pbr, F-acl, F-loopback, WEB-1 |
| 2026-09-24 | D-132 | Numbers and merge notes from the F-vrf-static-ecmp review: NextHop field 4 `vrf` is allocated to F-vrf-static-ecmp (no collision across all 25 branches; added to wave-A-hotspots §2); the viaFrr selector is registered once at init by F-vrf-static-ecmp and P12 must not call frr.RegisterStaticSelector; FIB/route and NAT session views never auto-poll full VPP dumps (a walk holds the worker barrier) — manual refresh or ≥ 30 s, one walk at a time in the agent (same rule for every future *list* RPC that walks a VPP table) | (a) poll freely (b) bounded walks + no fast polling | (a) stalls packet processing on every poll | low | F-vrf-static-ecmp, F-nat44-ed-sessions, P12, all list RPCs |
| 2026-09-25 | D-133 | Claim durability (TD-11b × TD-11c, TD-11c review F1): claim-first ordering stays — a keyed claim is appended to an unsynced journal BEFORE the VPP write, and the per-transaction batching compacts the journal into the snapshot with one atomic write+fsync at transaction end; start-up replays the journal. A failed claim flush answers DEGRADED, never APPLIED. Interface creators must RegisterKind or provide a KeyProvider for delete order (guard test). Also: F-kea-dhcp-relay fixes dhcp.client's write-before-claim (it owns dhcp/**); F-wireguard's peer.go uses scheduler.PartialCreate; new rows TD-8b, TD-21 (scheduler O(n²) in dependents/topo), TD-22 (small review follow-ups) | (a) accept N3 (claims lost on agent crash mid-txn) (b) fsync per claim (c) unsynced journal + batched compaction | (a) leaves live invisible VPP objects that block re-commit until VPP restarts; (b) loses the 600× speed-up; (c) keeps both (claims are boot-scoped, so kernel-crash durability is not needed) | low | TD-11b, TD-11c, TD-9, F-kea-dhcp-relay, F-wireguard |
| 2026-09-25 | D-134 | Merger request: squashes stay mechanical (conflicts, regeneration, renumbering) — no new unreviewed code at merge. Shared test seams that several branches extend (fake-agent `action` dispatch table; one coretest/fakevpp.go extension registry = F-nat44-ed's `extensions` seam hoisted, D-129 A6) land first as row TD-23 (test-only, reviewed); each feature adds one registration line at rebase. Until TD-23 merges, the affected branches (F-rpf-adl-pbr, F-neighbors-ra, F-bridge-l2, F-bonding, F-nat44-ed-sessions, F-vrf-static-ecmp, F-nat44-ei) are held and unaffected ones merge first | (a) merger folds handlers inside a squash (b) seams row first | (a) puts unreviewed test infrastructure into a merge; a silently-winning handler would hide missing tests | trivial | TD-23 + 7 wave-A branches |
| 2026-09-25 | D-135 | TD-10a decisions (review 3abfd0a): D-TD10a-1 a commit/rollback/confirm finding the Postgres advisory lock busy waits 1 s then answers 409 commit-busy (also secret delete and password set); D-TD10a-2 the advisory-lock client listens for 'error' (a dropped PG connection marks sync unknown, never crashes the API); D-TD10a-3/4 per the review; D-TD10a-5 accepted with a tombstone as tech debt; clients (web 130 s, CLI 150 s) look up the outcome before reporting "not applied"; P10's nginx proxy_read_timeout ≥ 130 s | wait (a) 0 ms 409 (b) 1 s then 409 (c) queue | (a) spurious 409s on back-to-back commits; (c) hides a stuck commit behind a spinner | low | TD-10a, TD-10b, P10 |
| 2026-09-25 | D-136 | Manager: F-kea-dhcp-relay and F-host-acl-nftables are released from the D-125 "merge after TD-13" gate (reviewer recommendation, review bfedc2b8 §6): the transaction rollback already covers a Kea render failure after the relay change; the Kea descriptors become TD-13's first Validator adopter at TD-13's rebase. Also: tools/app runs VRX_KEA_MODE=off and VRX_HOST_ACL_MODE=check and VRX_TRUST_PROXY=none on the shared host (status quo, until P12 / PENDING-tools-app-transport) | (a) keep the gate (b) lift it for F-kea only | (a) holds an approved branch ~5 h behind a not-yet-reviewed TD-13 | trivial | F-kea-dhcp-relay, F-host-acl-nftables, TD-13, tools/app |
| 2026-09-25 | D-137 | VPP 26.06 dns plugin: dns_resolve_name/ip with the plugin disabled or without a name server dereferences NULL in ip4_sas (dns.c:780→234) — crashed the shared VPP 2026-09-25 04:27 (NRestarts 1→2). Rule: dns_resolve_* only after dns_name_server_add_del + dns_enable_disable(1) succeeded; the agent's dns_lookup action answers 409 otherwise (F-unbound 5c80aba0); host tests that reach dns.api are opt-in (VRX_DNS_VPP_HOST=1, D-064) | — | a single API call crashes VPP | low | F-unbound-chrony-syslog, DF-8 helpers, docs/vpp-code-track.md |
| 2026-09-25 | D-138 | Descriptor pattern sanctioned (F-loopback review Q4): for a VPP setting that has a read-back API but no full dump (e.g. gso via feature_is_enabled), Retrieve = the read-back for keys in a per-boot agent record (the mactime pattern) — not write-only — so a lost enable is detected as drift. Write-only (D-076 record) stays for settings with no read-back at all (e.g. npt66) | (a) write-only everywhere (b) read-back + per-boot record where a read-back exists | (a) misses a lost enable after a VPP restart until the next full resync | trivial | F-loopback-bvi-gso-lldp-span, feature template |
| 2026-09-25 | D-139 | Correction to D-137 (F-unbound review b665904f): the VPP 26.06 dns-plugin NULL dereference is triggered when NO IPv4 name server has been added since VPP started (is_enabled plays no part): dns_resolve_name, dns_resolve_ip, any client's UDP 53 query to a VPP address with the cache on, and `vppctl show dns servers` with IPv6-only servers all crash; add/del and enable/disable do not. Rules: the VPP DNS cache requires at least one IPv4 upstream (schema + builder + lookup guard); lookup readiness comes from live applied state on the current VPP connection, never from the saved config | (a) guard the lookup only (b) refuse IPv6-only caches everywhere + live readiness | (a) leaves the UDP 53 client path and a post-restart window as crash vectors on a product box | low | F-unbound-chrony-syslog, DF-8 dns.*, docs/vpp-code-track.md |
| 2026-09-25 | D-140 | Addendum to D-137/D-139 (F-unbound verify 4507e5ea): the ikev2 plugin also calls the dns plugin's resolver — `ikev2_initiate_sa_init` on a profile with a responder HOSTNAME calls dns_resolve_name (ikev2.c:4870-4897, 5826). Rule for P11 / F-ikev2-native: a responder hostname is accepted only when this agent's dns.Readiness fact holds (IPv4 server added + enable on the current VPP boot); otherwise refuse at build time with a pointer, or resolve the name in the agent and send an address. DF-5's responder_hostname.go and InitiateSAInit are never combined until then | (a) trust users not to set hostnames (b) gate on dns.Readiness / resolve in the agent | (a) one IKE initiate crashes VPP | low | P11, F-ikev2-native, DF-5 |
| 2026-09-25 | D-142 | F-licensing stores the installed licence as a file (`VRX_LICENSE_FILE`, default /var/lib/vrx/license.vrxlic, atomic write 0640), not in PostgreSQL | (a) file (b) PostgreSQL table + migration | a single signed blob; no migration race with F-dashboard/F-aaa tables; the file is self-verifying | low | F-licensing, F-backup-restore, deploy |
| 2026-09-25 | D-143 | A commit using an unlicensed feature is rejected by the `license` validation stage with 403 problem+json (`license-required`, `tier: license`, `errors[].pointer` = first offending node); tampered/invalid upload = 400 | (a) 403 (b) 422 (c) 400 validation | authorisation-like refusal; 422 already means apply failure in P06 | low | F-licensing, P07b UI, P13 CLI |
| 2026-09-25 | D-144 | Licence enforcement grandfathers the running configuration: a node already present in the latest revision never offends, so an expired licence only rejects NEW unlicensed use and never forces removal (D-040: data plane never degraded) | (a) grandfather against running (b) reject any unlicensed node | (b) would block every commit on an expired box until config is deleted | low | F-licensing |
| 2026-09-25 | D-145 | An invalid (bad signature, wrong host, not yet valid, unreadable) or expired-past-grace licence falls back to the community entitlement set (warning logged for an unreadable file), never "reject everything"; community is permissive until PENDING-licensing-matrix is answered | (a) community fallback (b) reject all commits | (b) turns a licence glitch into a management outage | low | F-licensing |
