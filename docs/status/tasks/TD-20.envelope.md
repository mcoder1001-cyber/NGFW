# TASK ENVELOPE — TD-20 ban `show trace` / `trace add` on the shared VPP (D-128, crash root cause)
id: TD-20   branch: task/TD-20   worktree: /root/ngfw-wt/TD-20   base: task/P08@998e391 (P08 merging now; rebase at merge)   started: 2026-09-24T19:39
why: the 18:41 VPP crash (and very likely 07:27) = `vppctl show trace` → format_vlib_trace (vlib/trace.c:159-162) calls a NULL formatter for a trace record whose node was a deleted/reused interface's tx node (root-caused from the core with vpp-dbg symbols: /root/ngfw-wt/F-vlan-qinq/docs/status/tasks/F-vlan-qinq-review.md H1). Any slot running it can crash VPP for everyone. `tools/ci.sh full` is FROZEN until this merges.
slot: 8 → eval "$(tools/lab env 8)" (w8). One topology run at the end (no trace), NRestarts before/after.
scope:
  1. test/topology/interfaces/interfaces_test.go:291-302: replace `trace add` + `show trace` with trace-free evidence of the same path: rx counter of host-<p>l0 and tx counter of host-<p>w0 increase by the number of echo requests/replies sent (read via binapi stats or `vppctl show interface` counters), plus the FIB entry (binapi ip_route_dump or `show ip fib`) — assertions as strict as before about which interfaces carried the packets.
  2. A static guard in tools/ci.sh quick (new small step, manager allows this hunk): fail if any tracked file outside apps/agent/binapi and docs/ calls vppctl with `trace add` / `show trace` / `clear trace` (Go vppctl(t, "trace"…), shell, TS). Message names D-128.
  3. docs/lab/shared-host-rules.md: new §11 "Packet trace is banned on the shared VPP (D-128)" with the reason and the alternatives.
  4. docs/vpp-code-track.md: V-new (manager numbers it) — format_vlib_trace NULL guard for records of deleted/reused nodes; one-line upstream fix; add the crash evidence paths (/root/ngfw-wt/logs/crash-20260924-1841).
files you own: test/topology/interfaces/interfaces_test.go (trace block only) tools/ci.sh (the new guard step only) docs/lab/shared-host-rules.md (§11 only) docs/vpp-code-track.md (V-new append only) docs/status/tasks/TD-20*
CI: `TMPDIR=/tmp/g-w8 tools/ci.sh --base main` (your new guard must pass on your branch and FAIL on the pre-fix test — paste both).
GIT RULE: git only as `git -C /root/ngfw-wt/TD-20 …`; NEVER in /root/ngfw.
time box: 2 h. finish: TD-20.md with pasted evidence; commit; final message = 6 lines.
never: run show trace / trace add yourself · merge · restart/kill VPP · pkill
