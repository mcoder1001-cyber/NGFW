# Status 2026-09-25 11:35 (+0330) — manager cycle 23 (ngfw-46)

## خلاصه (فارسی)
- نشست مرج‌کننده («بررسی پروژه») نزدیک ۷ ساعت است در حالت waiting مانده و در این مدت هیچ مرجی انجام نشده. ۲۱ شاخهٔ تأییدشده در صف‌اند و TD-25 اول صف است.
- در این چرخه تأیید شدند: UI-domain-editor (که حالا مسیرهای با هر عمقی را پوشش می‌دهد) و F-unbound. F-unbound هنوز یک دور rebase لازم دارد.
- یک مسیر دیگر به کرش VPP از راه IKEv2 پیدا شد و به‌عنوان D-140 به LOG و به promptهای P11 و F-ikev2-native اضافه شد.
- P10 (بسته‌بندی) پیش‌دستانه شروع شد، چون سر طولانی‌ترین زنجیرهٔ برنامه است. چهار ویژگی wave C هم در جریان‌اند.
- هیچ ایجنتی به لیمیت نخورد. VPP پایدار است (NRestarts=2) و بار سرور حدود ۱۹ است.

## Detail
Merged since cycle 22: none (merger waiting).
New approvals: UI-domain-editor c95bafe0 (after WEB-1), F-unbound-chrony-syslog 4507e5ea (rebase round after F-kea + TD-13 + TD-23; L3 = fail closed for non-default syslog VRF).
Running: P10 s6 (speculative on main 16467e5e; nft base vs host-ACL rule in the addendum), F-qos-flat s1, F-lb s2, F-srv6 s4 (CI), F-mpls-srmpls s5.
Decisions: D-140 (ikev2 responder hostname → dns resolver crash path; P11/F-ikev2-native gate on dns.Readiness).
Risks: merge backlog 21; rebase debt; P10 may hit permission limits (network/nspawn) — told to stop and report.
Next: TD-25 merge → host runs released; then TD-23 → D-134 branches; agent core chain TD-10a → TD-11c → TD-8b → TD-13 → TD-9.
