.venv/
__pycache__/
*.egg-info/
.pytest_cache/
build/
# OpenAPI snapshot written by gen.sh (the API build is the source; never committed twice)
openapi.json
# terraform working files of manual runs (state may hold sensitive values — never commit)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform/terraform-provider-vrx
