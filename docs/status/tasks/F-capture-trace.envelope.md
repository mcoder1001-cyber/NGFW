# TASK ENVELOPE — F-capture-trace
id: F-capture-trace   branch: task/F-capture-trace   worktree: /root/ngfw-wt/F-capture-trace   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave C (day 13-15): pcap capture, BPF trace filter, trace node, Trace Path, PG
prompt: prompts/features/F-capture-trace.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main + P08 in wave-B/C prep)   wbs: D8.2, D7.11
scope: the `capture` Action end to end on DF-8's pcap/trace descriptors (file moved to an agent dir 0600, list/download/delete, retention), PG interface create/delete + `pg_capture` (stream definition documented as blocked), trace = skip-unless-loaded (no tracedump on vrx-a, V18), API, Tools → Capture UI, docs; no `cli_inband`/vppctl
merged deps you can rely on: P08 (subsystems/`Wiring.BootStore()` + interface ClaimStore, `interface/<name>` resolver, the Action RPC stub), DF-8 (descriptors/pcap: `pcap.NewCapture(c, owner, boot)`, `ErrCaptureBusy`, `pcap.filter-function` (globals); descriptors/trace: `trace.bpf-filter` (globals, pcap-filter alphabet validator)), F-vpp-debs (`deploy/vpp/build.sh --trace-plugins core`, D-089 — builds, never installs) (+ on main by then: TD-2, TD-3, W-seed with the `Action` type switch; F-vrf-static-ecmp's Action bridge and generic Action stream method in agent.client.ts only if merged when you start — check `git log main`)
read first: prompts/features/F-capture-trace.md · docs/status/wave-A-hotspots.md (§0 rules; A1 A4 C5–C7 P1 P3 P4 P5 W1–W3) · docs/status/wave-BC-numbers.md § F-capture-trace · docs/agent/descriptors/{pcap,trace}.md · deploy/vpp/README.md (V18 variant) · docs/vpp-code-track.md V18 · docs/decisions/LOG.md D-049, D-063, D-064, D-071, D-076, D-077, D-080, D-082, D-087, D-089, D-104
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; VPP's capture file name must start with `w<SLOT>-` (VPP writes `/tmp/w<SLOT>-…`), moved at once into /run/vrx-test/w<SLOT>/captures/ (dir 0700, files 0600); PG interface ids only <SLOT>000–<SLOT>999, tagged `w<SLOT>:…`)
daemon-owner: none (`tcpdump`/`tshark` only read finished capture files)
files you own exclusively: apps/agent/internal/descriptors/pcap/** apps/agent/internal/descriptors/trace/** (both gap-only: a proven defect or a missing helper, name the test) apps/agent/internal/descriptors/pg/** (new, on descriptors/dfkit) docs/agent/descriptors/pcap.md docs/agent/descriptors/trace.md docs/agent/descriptors/pg.md apps/agent/internal/actions/capture-trace/** apps/agent/internal/subsystems/capture_trace*.go apps/agent/internal/agent/rpc_capture_trace*.go apps/api/src/features/capture-trace/** apps/api/test/e2e/capture-trace* apps/web/src/domains/tools/capture-trace/** apps/web/src/locales/{en,fa}/capture-trace.json docs/user/tools/capture-trace.md test/topology/capture-trace/** docs/status/tasks/F-capture-trace*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-capture-trace` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-capture-trace.md and say so in the questions file); never reorder or reformat other lines): A1 apps/agent/internal/subsystems/subsystems.go (no `Domains` entry — capture and PG are actions, not config; at most one line wiring your handler, whose `Wiring` accessor (BootStore, claims, globals flag) lives in your own subsystems/capture_trace.go) · A4 apps/agent/internal/agent/server.go (one `case` for `ActionRequest_Capture` under your anchor in W-seed's type switch → your handler in internal/actions/capture-trace; `Capture*`/`Pg*` RPC methods live in your rpc_capture_trace.go) · C5 packages/proto/vrx/v1/dataplane.proto (`CaptureAction` 7 `drop`, 8 `error_filter` as one-line insertions in the P03 message; `Capture*`/`Pg*` RPCs under the service anchor only if the agent keeps the files; messages in a `// ----- F-capture-trace -----` section) · C6 docs/contracts/proto.md (`### F-capture-trace: …`) · C7 generated files (regen, never hand-merged: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`) · P1 apps/api/src/app.module.ts · P3 `POST /api/v1/actions/capture` as a static route in your own controller (beats `:action`; `capture` stays in P06's ACTIONS list — never edit apps/api/src/actions/**) · P4 apps/api/src/agent/agent.client.ts (reuse F-vrf-static-ecmp's Action stream method if it is on main, else one `captureAction` method; + `Capture*`/`Pg*` methods) · P5 apps/api/src/testing/fake-agent.ts (stubs on the contract commit; real fake in features/capture-trace/fake.ts) · W1 apps/web/src/router.tsx (replace the `tools` NotAvailable route with your Tools → Capture route — no other wave-A–C task touches that line) · W2 apps/web/src/nav/nav.ts + nav.test.ts (the `tools` NavItem: path/available — same) · W3 apps/web/src/i18n.ts · A7 docs/vpp-code-track.md (V18 follow-up only)
contract numbers: docs/status/wave-BC-numbers.md § F-capture-trace — **CaptureAction 7 `drop`, 8 `error_filter`**; RPCs `CaptureList`, `CaptureRead`, `CaptureDelete`, `PgInterfaceCreate`, `PgInterfaceDelete`, `PgCapture` only if the agent keeps the files / runs PG; no new ActionRequest member (capture = existing 3), no CaptureDirection value, no EventKind; commit them first as a separate `contract(proto): …` commit on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-capture-trace-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, VPP packages (installing vpp-plugin-devtools or a `--trace-plugins core` build is a manager/handover step), apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned; tracedump binapi = manager regen once the plugin is on the host), apps/agent/internal/descriptors/{ipfix,flowprobe,sflow}/** (F-ipfix-sflow), apps/agent/internal/descriptors/{dfkit,interface,af_packet}/** (shared / DF-1 / TD-5 — read-only), apps/agent/internal/actions/vrf-static-ecmp/** (F-vrf-static-ecmp), apps/agent/internal/agent/{agent,service,state,ifstate,metrics,telemetry,events}.go + apps/agent/cmd/** (A5 core), apps/api/src/actions/** (F-vrf-static-ecmp's generic bridge), apps/api/src/{auth,users,secrets,commit,datastore,audit}/** (use the services — the download is audited through the existing interceptor/service), deploy/vpp/** (F-vpp-debs/TD-6), tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
host facts: vrx-a runs the stock host build: no `tracedump`/`tracenode` plugin (they ship in `vpp-plugin-devtools`, not installed) and no tracedump binapi (`apps/agent/binapi/trace` is the unrelated `trace_profile_*` API); Trace Path has no API; classic `trace add`/`show trace` and PG stream definitions exist only as CLI (`cli_inband` not allowed here — open question) · binapi `interface` (`pcap_trace_on/off`, `pcap_set_filter_function`), `bpf_trace_filter` (`bpf_trace_filter_set_v2`), `pg` (`pg_create_interface_v3`, `pg_capture`, `pg_enable_disable`, `pg_delete_interface`) verified · `tcpdump` 4.99.6 and `tshark` present · VPP writes the capture world-readable (0664) under /tmp · no data NICs bound (D-026): af_packet rig only (D-010)
obligations accumulated from decisions (all must be honoured and listed in F-capture-trace.md with evidence): D-104 use DF-8's pcap/trace descriptors, do not rebuild · D-076/D-080 the running capture is recorded in the persisted `Wiring.BootStore()`: a restarted agent recognises and stops only its **own** capture on the same VPP boot identity; never stops another owner's capture (`ErrCaptureBusy` → 409 problem+json `busy`) · one capture per VPP is shared by all 12 slots: keep test captures ≤ 30 s / ≤ 1000 packets and always stop them in `t.Cleanup` · D-071/D-082 the BPF filter and the filter function are VPP globals: only the globals owner sets them; a host test with a BPF expression only behind `VRX_DF8_GLOBALS=1` (or your own opt-in var) holding `flock -x /run/lock/vrx-globals.lock` and restoring the previous filter · 00-CONTEXT rule 9 / D-049 the BPF expression is validated to the pcap-filter alphabet (DF-8's validator; `;`, quotes → 400 with a `pointer`), never a shell, no `cli_inband`/vppctl · the capture file is moved out of /tmp at once (0600, agent dir, sha256 + size + owner recorded), retention by count and bytes caps; download admin-only and audited · D-063 the capture is write-only (no pcap status message); PG interfaces are read back from `sw_interface_dump` (`pg<id>` + your tag), `pg_capture`/`pg_enable_disable` are write-only · D-064 crash → opt-in at once + V-item
coordination: (1) F-vrf-static-ecmp (wave A) owns apps/api/src/actions/**, the generic Action stream method in agent.client.ts and the ping/traceroute cases; W-seed seeded the `Action` type switch — you add only your case · (2) F-ipfix-sflow owns IPFIX; F-dashboard-prom-alarms the dashboards; F-backup-restore the support bundle (may reference captures later — no API for it now) · (3) tracedump/tracenode: when the manager installs a V18 build on vrx-a and regenerates binapi, a follow-up row wires trace — your UI already shows "not available on this build"
V19/V24 SAFETY (D-095, D-101): before ANY ping/traffic through the rig for the capture evidence, run TD-3's preflight on the rig interfaces' sw_if_index and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots; paste `tcpdump -r` of the downloaded file and `ls -l` showing 0600
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-capture-trace.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-capture-trace-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-capture-trace.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite), by PID · no capture of yours running (pcap_trace_off only for your own) · no /tmp/w<SLOT>-* capture files left · no w<SLOT> PG interfaces left · lab lock released · vrx_w<SLOT> dropped · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-capture-trace-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
