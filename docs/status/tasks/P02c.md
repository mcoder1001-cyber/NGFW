# P02c — schema group (c): vpn, tunnels, services, ha — done 2026-09-23, review fixes 2026-09-24

> **Fix round 2026-09-24:** the sections below describe the first round (base `main@2de6c2f`, default-free roots, `vrrp[]`, free-form
> secret refs); they are superseded where the "Review fixes" section at the end says so. The branch was rebuilt on top of main as two
> contract commits (see Review fixes → F1/history).

Branch `task/P02c`, worktree `/root/ngfw-wt/P02c`, base `main@2de6c2f`, slot 6 (`w6`). CONTINUE respawn after the usage-limit
interruption: continued from `b815d15` (recovered draft of `domains/vpn.ts`). Files touched: `packages/schema/src/domains/{vpn,tunnels,services,ha}.ts`
(+ `.test.ts`), `packages/schema/src/semantic/{vpn,tunnels,services,ha}.ts` (+ `.test.ts`), `packages/schema/src/semantic/tunnels-common.ts`
(+ test), `packages/schema/src/semantic/vpn.fixtures.ts`, `packages/schema/examples/{vpn,tunnels,services,ha}-*.json` + `invalid-*.json`,
`docs/contracts/schema-vpn-tunnels-services-ha.md`, `docs/status/tasks/P02c*.md`. Nothing else (no `index.ts`, `primitives.ts`, `gen.ts`,
other domain files, `package.json`, lockfile). Contract commit: `a306c2b contract(schema): model vpn, tunnels, services and ha domains …`.

## What was built

Full models for the four group-(c) root keys (docs/04 shapes, WBS D6.2/D6.5/D6.6/D7.1/D7.3/D7.5/D9.1 plus the docs/04 keys lldp/ipfix and
D6.4/D6.9/D7.4/D7.6/D9.2 kept minimal), documented field-by-field in `docs/contracts/schema-vpn-tunnels-services-ha.md`:

- **vpn** — `ipsec { settings, proposals{ ike, esp }, tunnels{ … } }` in the P11 §2 shape (`localAddr, remoteAddr|%any, localId, remoteId,
  auth{psk(secretRef)|cert}, proposal, localTs[], remoteTs[], ikeVersion 1|2, dpd, natT, mobike, mode, rekey, startAction, vrf,
  routeBased{ipipInterface}`), AEAD/integrity consistency, `wireguard.interfaces{ peers{} }` (public keys validated as exactly 32 base64 bytes,
  private/preshared keys as references), `pki { cas, certificates, hsm }` (references only, ACME), `remoteAccess{}` (IKEv2 + EAP, pools).
- **tunnels** — `gre{}` (l3/teb/erspan), `vxlan{}` (vni, ports, multicast + mcastInterface, decap), `ipip{}` (p2p/p2mp, dscp); every tunnel has
  `underlayVrf` + `vrf`, optional `instance` (fixes the VPP name so other domains can reference it), interface addresses.
- **services** — `dhcp { servers{} (Kea instances: vrf, interfaces, subnets{pools, reservations, options}), relays{} }`, `dns { resolvers{}
  (Unbound instances: listen, access control, forwarders/DoT, forward zones, local zones with typed records, DNSSEC, cache), vppCache }`,
  `snmp` (communities and USM keys as references, v3 users, trap receivers), `lldp`, `ipfix { exporters{}, flowprobe, sflow }`, `ntp` (chrony).
  "One Kea/Unbound instance per X" is the record key + `vrf` + bind set (vdom.md #5).
- **ha** — `vrrp[]` (VRRPv3: interface, vrId, family, priority, interval in ms, preempt/accept, unicast peers, addresses, tracking, engine
  vpp|keepalived), `cluster` (membership, config sync, state-sync flags, key as reference).
- **26 semantic validators** (`vpn.*` 9, `tunnels.*` 7, `services.*` 6, `ha.*` 4) — proposals/PKI/IPIP references exist, VRFs exist, local /
  source / bind addresses configured on an interface in the right VRF, route-based IPsec ↔ IPIP consistency, peer/instance/socket/VRID
  uniqueness, DHCP subnet within an interface prefix and non-overlapping, DNS listen sockets unique per VRF, VRRP virtual addresses claimed once
  and inside the interface prefix, and a group-wide inline-secret scan (PEM banners, 32-byte base64 blobs).
- `semantic/tunnels-common.ts`: pure IPv4/IPv6 math + duck-typed lookups into `interfaces`/`vrfs` (work with the P02s placeholders and P02a's
  future models alike).
- 9 test files / 319 new tests (483 total in the package) with hostile inputs: inline `psk`/`password`/`privateKey`/`community`/`key`, PEM
  in refs, 43/45-char and wrong-padding WireGuard keys, `%any` + start, MOBIKE on IKEv1, mixed families, multicast/unspecified endpoints,
  VNI 2^24, VRID 0/256, priority 256, 15 ms interval, pools outside subnets, duplicate MAC with different case/separator, DUID on v4,
  forwarder == listen (loop), Kea/Unbound passthrough keys, control characters, etc.
- 19 example fixtures: 7 valid, 5 `invalid-*` (schema-rejected), 4 `<domain>-semantic-<rule>.json` (schema-valid, one finding each).

## How it was verified (real output, host `ngfw`, worktree `/root/ngfw-wt/P02c`)

Package gate (`cd packages/schema && pnpm typecheck && pnpm exec eslint src && pnpm exec prettier --check … && pnpm test`):

```
$ tsc -p tsconfig.json --noEmit
(eslint: no findings)
Checking formatting...
All matched files use Prettier code style!
 ✓ src/json.test.ts (9 tests) 25ms
 ✓ src/diff.test.ts (6 tests) 13ms
 ✓ src/pointer.test.ts (5 tests) 34ms
 ✓ src/merge-patch.test.ts (17 tests) 27ms
 ✓ src/semantic/tunnels-common.test.ts (10 tests) 66ms
 ✓ src/ui.test.ts (2 tests) 45ms
 ✓ src/primitives.test.ts (65 tests) 54ms
 ✓ src/domains/vpn.test.ts (72 tests) 136ms
 ✓ src/domains/ha.test.ts (31 tests) 171ms
 ✓ src/domains/tunnels.test.ts (34 tests) 141ms
 ✓ src/domains/services.test.ts (107 tests) 224ms
 ✓ src/index.test.ts (5 tests) 51ms
 ✓ src/semantic/index.test.ts (6 tests) 119ms
 ✓ src/semantic/ha.test.ts (7 tests) 157ms
 ✓ src/semantic/tunnels.test.ts (10 tests) 184ms
 ✓ src/examples.test.ts (20 tests) 218ms
 ✓ src/semantic/vpn.test.ts (22 tests) 260ms
 ✓ src/semantic/services.test.ts (10 tests) 284ms
 ✓ src/domains/domains.test.ts (41 tests) 308ms
 ✓ src/generate.test.ts (4 tests) 210ms
 Test Files  20 passed (20)
      Tests  483 passed (483)
   Duration  3.36s (transform 5.87s, setup 0ms, collect 22.40s, tests 2.73s, environment 16ms, prepare 6.99s)
```

`pnpm gen` determinism (run twice, sha256 of the group-(c) outputs + OpenAPI components):

```
schema: wrote root + 13 domain schemas + openapi components
gen deterministic: 5 files identical across two runs
942b750bb0bd3379  dist/json-schema/vpn.json
2cd8ae2c76c041d8  dist/json-schema/tunnels.json
066055ceaca42cf0  dist/json-schema/services.json
51d40ba93ad81c7c  dist/json-schema/ha.json
878b728cb9befac7  dist/openapi-components.json
vpn.json 66450 bytes · tunnels.json 22072 bytes · services.json 91014 bytes · ha.json 12970 bytes
```

Generated output sample (secrets are references in the JSON Schema the UI consumes; tunnels are strict objects):

```
vpn.json title: VPN | sub-trees: ipsec,wireguard,pki,remoteAccess | required at root: undefined
tunnel.auth: {"oneOf":[{"type":"object","properties":{"method":{"type":"string","const":"psk"},"secretRef":{"type":"string",
  "minLength":1,"maxLength":127,"pattern":"^[A-Za-z0-9][A-Za-z0-9_.-]{0,62}(?:\\/[A-Za-z0-9][A-Za-z0-9_.-]{0,62})?$",
  "title":"Pre-shared key (reference)","x-vrx-ui":{}}},"required":["method","secretRef"],"additionalProperties":false},{"type":"object",
  "properties":{"method":{"type":"string","const":"cert"},"cer…
tunnel.additionalProperties: false | localAddr: {"anyOf":[{"type":"string","format":"ipv4",…},{"type":"string","format":"ipv6",…}],
  "title":"Local address","x-vrx-ui":{"help":"Must be configured on an interface in the tunnel VRF"}}
ha.json vrId: {"type":"integer","minimum":1,"maximum":255,"title":"VRID","x-vrx-ui":{"widget":"number","help":"Virtual router id, unique per
  interface and address family"}} x-vrx-ui: {"order":120}
```

Registered validators (`SEMANTIC_VALIDATORS` filtered to the group):

```
26 group-(c) validators: vpn.proposal-exists vpn.pki-reference-exists vpn.vrf-exists vpn.local-address-configured vpn.route-based-ipip
vpn.ipsec-peer-unique vpn.wireguard-unique vpn.remote-access-pools-no-overlap vpn.no-inline-secret-material tunnels.name-unique
tunnels.vrf-exists tunnels.source-address-configured tunnels.endpoints-unique tunnels.instance-unique tunnels.mcast-interface-exists
tunnels.address-overlap services.vrf-exists services.interface-references services.dhcp-subnet-within-interface-prefix
services.dhcp-subnets-unique services.bind-address-configured services.dns-listen-unique ha.vrrp-vrid-unique ha.interface-exists
ha.vrrp-virtual-addresses ha.vrf-exists
```

CI gate, run 1 on the contract commit `a306c2b` (`tools/ci.sh --base main`, `/root/ngfw-wt/logs/P02c-ci.log`):

```
== install (frozen lockfile) ==
== generated output must be clean ==
== contract guard vs main ==
== forbidden patterns ==
== typecheck ==
== lint ==
== test ==
== build ==
== agent ==
CI GATE PASSED
```

CI gate, run 2 on the docs commit `816291c` (`/root/ngfw-wt/logs/P02c-ci2.log`):

```
== install (frozen lockfile) ==
== generated output must be clean ==
$ turbo run gen
• turbo 2.11.2
== contract guard vs main ==
== forbidden patterns ==
== typecheck ==
$ turbo run typecheck
• turbo 2.11.2
== lint ==
$ turbo run lint
• turbo 2.11.2
== test ==
$ turbo run test
• turbo 2.11.2
== build ==
$ turbo run build
• turbo 2.11.2
== agent ==
CI GATE PASSED
```

## Out of scope / left undone

- Branch-coverage measurement (`@vitest/coverage-v8` is on `task/P02a`, not on main — D-024); every validator branch has a dedicated test.
- Re-pointing the local primitives (`secretReference`, `transportPort`, `hostOrIpAddress`, `ipv4OrIpv6Cidr`, `mtuField`, …) and the IP math to
  P02a's `primitives.ts`/`ip.ts` once merged — follow-up `contract(schema)`; field names and JSON Schema do not change.
- VXLAN-GPE, GTP-U, L2TPv3, PPPoE, SRv6, LISP (D6.6 "schedule last", D6.7/D6.8 T3) — additive later keys of `tunnels`.
- DHCP DDNS, Unbound views, SNMP views/private MIB, QoS (D7.8), keepalived-specific knobs beyond the VRRPv3 model.
- API endpoints, UI, renderers (P06/P07/P11 consume this contract).

## Open questions

See `docs/status/tasks/P02c-questions.md` (8 items, none blocking): index.test.ts vs D-017 nested defaults; primitive dedupe after P02a/P02b
merge; `services.ntp` vs docs/04 `system.ntp`; interface-reference convention; scope of pki/remoteAccess/ipfix/cluster; coverage tooling;
VRID uniqueness per (interface, family); Zod 4 `unrecognized_keys` pointer mapping for P06.

## Decisions taken (to be copied to docs/decisions/LOG.md)

| id | decision | options | why |
|---|---|---|---|
| P02c-1 | Group-(c) domain roots are default-free (sub-trees `.optional()`, defaults inside) | (a) `.prefault({})` on sub-trees (b) default-free roots (c) edit P02a's `index.test.ts` | (b) passes `RootConfig.parse({})[key] == {}` standalone; (a) fails CI until P02a changes the test; (c) not my file |
| P02c-2 | Local primitives exported under non-colliding names (`secretReference`, `transportPort`, …) | (a) same names as P02a/P02b and let the merge resolve (b) distinct names + later dedupe | (b) merges stay clean (`export *` in index.ts errors on duplicates) |
| P02c-3 | IP math + duck-typed lookups in `semantic/tunnels-common.ts`, imported by `domains/services.ts` for intra-object rules | (a) inline per validator (b) shared helper under `semantic/` (c) wait for P02a `ip.ts` | (b) one implementation, not re-exported by `index.ts`, replaceable by P02a's later |
| P02c-4 | VRRP VRID unique per `(interface, addressFamily)` | (a) per interface (b) per interface + family | (b) RFC 5798 / VPP `vrrp_vr_add_del` semantics; (a) rejects valid dual-stack setups |
| P02c-5 | chrony modelled as `services.ntp` (client + server) | (a) omit NTP (b) `services.ntp` (c) wait for P02a `system.ntp` | D7.4 is a group-(c) row; overlap raised as question 3 |
| P02c-6 | Semantic-invalid fixtures named `<domain>-semantic-<rule>.json` | (a) `invalid-*` (b) `<domain>-semantic-*` | `examples.test.ts` treats `invalid-*` as schema-invalid; these are schema-valid |
| P02c-7 | Interface references are VPP names; tunnels are referenceable only with an explicit `instance` (`gre<n>`, `ipip<n>`, `vxlan_tunnel<n>`), WireGuard as `wg<instance>`; IPsec `routeBased.ipipInterface` is the `tunnels.ipip` object name (P11) | (a) object names everywhere (b) VPP names for interfaces, object name for the IPsec↔IPIP link | (b) matches `interfaces` keys and P11 §2 |
| P02c-8 | All secret material through `*Ref` fields holding a `secretReference`; strict objects reject inline fields; group-wide string scan for PEM / base64 keys | (a) schema-only (b) schema + semantic scan | (b) defence in depth for free-text fields |
