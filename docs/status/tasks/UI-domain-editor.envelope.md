# TASK ENVELOPE — UI-domain-editor: advanced configuration editor (web-ahead track, review 6.2, D-125)
id: UI-domain-editor   branch: task/UI-domain-editor   worktree: /root/ngfw-wt/UI-domain-editor
base: task/WEB-1@c2e3eabb (SPECULATIVE, D-114: WEB-1 is approved and in the merge queue; its SchemaForm presence toggle is what this page renders). When WEB-1 lands on main, `git -C /root/ngfw-wt/UI-domain-editor merge main`.
slot: 11 → eval "$(tools/lab env 11)" (ports 4100/6100, DB vrx_w11) — ONLY for the final real-stack screenshot run; before starting it check `ss -ltn 'sport = :4100'` is empty and /run/vrx-test/w11 is absent (a reviewer may be using it); full teardown after.
scope: route /config/* renders a SchemaForm for the JSON-pointer subtree of the candidate config, through the GENERIC candidate routes (GET/PATCH/PUT/DELETE /api/v1/config/{path}) and the existing pending-change bar; shows the commit's notApplied items and agent.unsupported-field warnings for the subtree; a tree/breadcrumb of domain paths derived from the generated schema (never a hand-written list); read-only view of running vs candidate for the subtree; DomainPlaceholderPage links "Open in the advanced editor" to /config/<domain>.
architecture rules: one schema for three consumers — the form and the path tree come from the generated @ngfw/schema / api-client only, no hand-written API types; the UI never talks to the agent or VPP (API only); no timer below 30 s on anything that reaches VPP (D-132), Refresh button instead; secrets render as write-only fields (never echo); do not call dropPhantomOptionals (deprecated no-op after WEB-1).
files you own: apps/web/src/domains/advanced/** apps/web/src/pages/DomainPlaceholderPage.tsx apps/web/src/locales/{en,fa}/advanced.json + the hunks under the existing `// wave-A: UI-domain-editor` anchors in apps/web/src/router.tsx and apps/web/src/i18n.ts; docs/status/tasks/UI-domain-editor*
must not touch: BUILT_DOMAINS, NavItem.available, nav.test.ts, packages/ui-kit (report kit gaps in UI-domain-editor-questions.md instead), product API/agent code.
proof: unit tests (vitest) for path parsing, subtree PATCH/DELETE as merge patches, unsupported-field warnings, en/fa key parity; `pnpm --filter @ngfw/web test`, lint, typecheck, logical-CSS check; screenshots with the WEB-3 harness if it has merged (else apps/web/test/e2e/flow pattern): /config/interfaces and /config/system in en + fa/RTL, light + dark, pageErrors 0.
CI: `TMPDIR=/tmp/g-ude tools/ci.sh --base main`.
GIT RULE: git only as `git -C /root/ngfw-wt/UI-domain-editor …`; NEVER in /root/ngfw.
time box: 5 h. finish: docs/status/tasks/UI-domain-editor.md with evidence + screenshot list; commit; final message = 6 lines.
never: merge · new dependencies · ports 3000/8080/9101 · tools/app · pkill · show trace
