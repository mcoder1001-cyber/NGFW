# TASK ENVELOPE — TD-8 agent seams (D-119 M2 + architecture audit A3/A4, D-123)
id: TD-8   branch: task/TD-8   worktree: /root/ngfw-wt/TD-8   base: task/W-seed@df67a8e (SPECULATIVE D-114; contains P08 round 2 + TD-5 + seams)   started: 2026-09-24T18:08
slot: none — unit tests only (no host/VPP runs, no dev servers)
scope: wire W-seed's inert seams for real — (1) subsystems.Env event-publish reaches the agent's event bus (agent/events.go) and Resync reaches service.Resync, so EventKind features (F-neighbors-ra, F-object-model, P11, P12, F-wireguard, F-acl) can publish; (2) S1 dynamic desired source hook (a feature-owned source merged into the projection under the Service txn lock, for FRR→VPP sync like F-mpls-ldp/F-igmp-mfib); (3) metrics collector hook for feature metrics; (4) SlotIDRange goes through Env and FAILS CLOSED when VRX_VPP_TABLE_BASE is unset unless VRX_VPP_ID_RANGE=all is set explicitly (the product agent / tools/app get an explicit reserved range — document it in docs/lab/shared-host-rules.md under a new §11 in your status file for the manager to copy); (5) an ARCH-1 coverage table in docs/agent/README.md: which domain/descriptor families are wired end to end vs registered-only. Unit tests for each seam (default inert, wired path works, fail-closed range).
files you own: apps/agent/internal/agent/{agent,service,metrics,events}.go (seam hunks only) apps/agent/internal/subsystems/seams*.go docs/agent/README.md (coverage table section) docs/status/tasks/TD-8*
est/time box: 6 h / 9 h.
GIT RULE: run git ONLY inside your own worktree (`git -C <worktree> …`); NEVER in /root/ngfw (main — the merger works there).
CI: `TMPDIR=/tmp/g-<id> tools/ci.sh --base main` (short TMPDIR). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to tools/app — never touch them. Stagger heavy test runs (host load; D-121).
Architecture (00-CONTEXT) is non-negotiable: one schema → three consumers (never hand-duplicate a type; UI derives from packages/schema); every UI string through t() with identical en/fa keys; logical CSS only; no UI screen with a stubbed backend is routed; secrets never enter form state, query cache or logs.
WIP commits every 45 min; finish with docs/status/tasks/<id>.md (what / how verified with pasted output / out of scope / questions), all committed, final message = 8-line summary.
never: merge · edit files you do not own · restart/kill VPP · pkill
