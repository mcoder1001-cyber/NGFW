# TASK ENVELOPE — F-kea-dhcp-relay
id: F-kea-dhcp-relay   branch: task/F-kea-dhcp-relay   worktree: /root/ngfw-wt/F-kea-dhcp-relay   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave B (day 10-12): Kea DHCPv4/v6 server + VPP DHCP relay/client
prompt: prompts/features/F-kea-dhcp-relay.md   (template: prompts/FEATURE-TEMPLATE.md)   wbs: D7.1, D7.2
scope: Kea DHCPv4/v6 server + VPP DHCP relay/client — the RF-3 Kea renderer, DF-8 dhcp descriptors and the services.dhcp schema/rules exist, and P08 already projects the DHCPv4 client: server + relay projection, lease/relay state, API, UI, docs
merged deps you can rely on: P08 (vertical slice: `interfaces.<if>.dhcpClient` → `dhcp.client` is projected, registered and reconnected; desired/subsystems/projection patterns), RF-3 (renderers/kea, D-079 control sockets), DF-8 (descriptors/dhcp) (+ pre-wave merges TD-2, TD-3)
slot: 2 → VRX_SLOT=2 VRX_TEST_PREFIX=w2 VRX_HTTP_PORT=3000+100×2 VRX_WEB_PORT=5000+100×2 VRX_METRICS_PORT=9100+10×2+1 VRX_AGENT_SOCKET=/run/vrx-test/w2/agent.sock VRX_PG_DATABASE=vrx_w2 VRX_VALKEY_DB=2 VRX_VPP_TABLE_BASE=2000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env 2)"`; rig prefix w2 → 10.2.{1,2}.0/24; slots 1–11 only, 12 = CI)
daemon-owner: kea — slot test instances only (RF-3 `kea.TestPaths(w2)` under /run/vrx-test/w2/, slot netns, own unix control sockets); `kea-dhcp4-server`/`kea-dhcp6-server` units stay disabled and stopped; never kea-ctrl-agent (D-079)
files you own exclusively: apps/agent/internal/renderers/kea/** apps/agent/internal/descriptors/dhcp/** (gap-only: keep `NewClient`/`WithInterfaceKey`/`Reconnected` as P08 calls them) docs/agent/renderers/kea.md docs/agent/descriptors/dhcp.md apps/agent/internal/desired/{kea,dhcp_relay}*.go apps/agent/internal/subsystems/kea*.go apps/agent/internal/agent/rpc_kea*.go packages/schema/src/semantic/kea-dhcp-relay*.ts apps/api/src/features/kea-dhcp-relay/** apps/api/test/e2e/kea-dhcp-relay* apps/web/src/domains/services/kea-dhcp-relay/** apps/web/src/locales/{en,fa}/kea-dhcp-relay.json docs/user/services/kea-dhcp-relay.md test/topology/kea-dhcp-relay/** docs/status/tasks/F-kea-dhcp-relay*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["services"]` — shared with F-rpf-adl-pbr (wave A) and F-unbound-chrony-syslog: whoever lands first adds the key, the others append; register `dhcp.NewProxy`/`NewProxyVSS` (+ dhcp6 client objects if you project them) one per line — NOT `dhcp.Register`, which would register `dhcp.client` a second time (Register panics on duplicates, D-030); a lease accessor in your subsystems/kea.go) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (lease/relay state RPC in your rpc_kea.go) · C1 packages/schema/src/domains/services.ts (only if relay option-82 / client classes are needed — ext/ file + one key line) · C2 packages/schema/src/semantic/index.ts (one spread) · C5 packages/proto/vrx/v1/dataplane.proto (DhcpLeases rpc + messages in a `// ----- F-kea-dhcp-relay -----` section) · C6 docs/contracts/proto.md · C7 generated files · P1 apps/api/src/app.module.ts · P4 agent.client.ts · P5 fake-agent.ts · W1 router.tsx · W2 nav.ts (+nav.test.ts: BUILT_DOMAINS 'services') · W3 i18n.ts · services domain page + tab registry apps/web/src/domains/services/{ServicesPage.tsx,tabs.ts} (shared with F-unbound-chrony-syslog; the manager seeds it before spawn — if it is not on main, do NOT create it (two parallel creators = add/add conflict): register your screen as its own route + NavItem under W1/W2 and the manager folds it into the shell at merge) · A7 docs/vpp-code-track.md
contract numbers (docs/status/wave-BC-numbers.md "Batch-2 follow-ons"): rpc `DhcpLeases` (+ messages; relay state may come from Retrieve); **DhcpRelay 9–10** reserved for option-82/remote-id only if you build them (DhcpRelay max is 8 on P08) — nothing else, no ActionRequest, no EventKind, never "next free"; commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-kea-dhcp-relay-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi (P04/manager-owned), /etc/kea and the kea units, apps/agent/internal/desired/interfaces.go (P08 — the client projection is done), apps/web/src/domains/interfaces/** (P08; the client toggle stays in its drawer — link only), apps/agent/internal/renderers/{unbound,chrony,rsyslog,rfkit}/** and apps/agent/internal/descriptors/dns/** (F-unbound-chrony-syslog / shared), apps/agent/internal/agent/{agent,service,state,ifstate}.go (A5), tools/ci.sh, tools/lab
host facts: Kea 3.0.3 (`kea-dhcp4`, `kea-dhcp6`), units installed + disabled; `perfdhcp` not installed; `dhclient`/`dhcpcd` present — run `dhclient -sf /bin/true -lf/-pf` under /run/vrx-test/w2/ (dhclient-script under `ip netns exec` rewrites the host's /etc/resolv.conf) · the relay test path goes client netns → VPP (`dhcp.proxy` in a slot table 2000–2999) → Kea in the other slot netns over the af_packet rig (D-010) · no data NICs bound (D-026)
obligations accumulated from decisions (all must be honoured and listed in F-kea-dhcp-relay.md with evidence): D-079 Kea over its own unix control sockets (root-only dir), no kea-ctrl-agent, restart/start requests persist until acted on · D-077 Q7 relay is per VRF, not a global · D-063/D-076 dhcp6 client objects are write-only / applied-once · D-065/D-069 interface refs are `interface/<name>` · D-071 `dhcp.dhcp6-duid` is VPP-global → only via `dhcp.RegisterGlobals` in the globals owner (slot agents VRX_GLOBALS_OWNER=0) · RF-3 review L3: leases are never in Retrieve — `Leases(ctx, fam, limit)` pages
coordination: (1) renderer stage — no renderer is called by the agent today (`service.go` runs only the scheduler); default = wrap each Kea family in one singleton scheduler descriptor under `Domains["services"]` (the F-host-acl-nftables default (a)) unless the manager has put a shared renderer stage on main; log the choice with options · (2) F-unbound-chrony-syslog (parallel) shares `Domains["services"]` and the services page · (3) P08 owns the interface drawer and the client projection
V19/V24 SAFETY (D-095, D-101): before ANY DHCP traffic through the rig, run TD-3's preflight on the rig interfaces' sw_if_index (no classify/ACL/SPD binding) and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b (kept outside the repo) for screenshots
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-kea-dhcp-relay.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-kea-dhcp-relay-wip.md current
CI: `TMPDIR=/tmp/g-w2 tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-kea-dhcp-relay.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
questions: docs/status/tasks/F-kea-dhcp-relay-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own

MANAGER ADDENDA: insert only under your `wave-BC: F-kea-dhcp-relay` anchors (seeded by W-seed-BC). D-128 — never run `show trace` / `trace add` / `clear trace` on the shared VPP. D-126 — no classify sweeps by index. kea runs only as your slot-local instance in your netns (daemon-owner: kea); units stay disabled; never touch /etc/kea. Secrets (TSIG/DDNS keys if any) wait for PENDING-secret-channel — use the fixture resolver. GIT RULE: git only inside your own worktree, NEVER in /root/ngfw. CI: main's tools/ci.sh copy if your branch copy fails in the contract guard (D-127).
