# P02b — schema group (b): `nat`, `objects`, `acl` — done 2026-09-23

Branch `task/P02b`, worktree `/root/ngfw-wt/P02b`, base `main@2de6c2f`, slot 5 (`w5`). CONTINUE respawn after the
usage-limit interruption; continued from `ec0ccda` (manager-recovered drafts of the three domain files). Files
touched: `packages/schema/src/domains/{nat,objects,acl}.ts` (+ `.test.ts`), `packages/schema/src/semantic/{nat,objects,acl}.ts`
(+ `.test.ts`, `nat-objects-acl-examples.test.ts`), `packages/schema/examples/{nat,objects,acl}-*.json` +
`invalid-{nat,objects,acl}-*.json`, `docs/contracts/schema-nat-objects-acl.md`, `docs/status/tasks/P02b*.md`. Nothing else.

## What was built

- **`domains/nat.ts`** — NAT44 at the top level exactly as docs/04 and `F-nat44-ed-sessions §Contract` expect
  (`mode ed|ei, inside[], outside[], pools[{name, range, vrf?}], staticMappings[{name, local{ip,port?},
  external{ip|pool|interface, port?}, protocol?, vrf?, twiceNat…}], timeouts{udp,tcpEstablished,tcpTransitory,icmp},
  sessionLimit ≥ 1024`), plus `identityMappings`, `loadBalancedMappings`, `outputFeature`, plugin flags, and the
  sibling translators `ipfix, nat64, nat66, nptv6, det44, dslite, map (MAP-E/MAP-T/lw4o6), cnat` (WBS D4.1, D4.3–D4.6).
- **`domains/objects.ts`** — `addresses` (host/network/range/fqdn), `addressGroups`, `services` (tcp/tcp-udp/udp/sctp
  ports + TCP flags, icmp/icmp6 type/code, any, other), `serviceGroups`, `schedules` (recurring HH:MM / once RFC 3339),
  `zones`, `tags` — records keyed by `objectName` (WBS D5.1).
- **`domains/acl.ts`** — `lists` (L3/L4, permit/deny/reflect, object/prefix/inline matches, schedule), `macip`
  (source MAC+mask+prefix), `host` (nftables accept/drop/reject), `attachments` (interface or zone target, direction,
  sequence, **`vrf` first-class**), `macipAttachments`, `hostAttachments` (WBS D5.2, D5.3).
- **Semantic validators** (30 rules, all `{ pointer, message }[]` via `jsonPointer()`): `nat.*` ×14 (interfaces exist,
  inside/outside disjoint, pools ordered + non-overlapping, static-mapping external exactly-one/pool exists/**ports
  require protocol**, identity, load-balanced, ED-only features, VRFs exist, nat64/nptv6/det44/map/cnat/dslite
  consistency), `objects.*` ×8 (names disjoint, tags exist, ranges ordered, group members exist/no duplicates/no
  cycles, service specs, schedules valid, zones reference existing disjoint interfaces), `acl.*` ×8 (sequences unique,
  object/service/schedule references exist, family consistency, tags, MACIP masks, attachments incl. VRF match,
  one MACIP list per interface, host chains). Prototype-safe lookups (`constructor` is a legal name).
- **Tests**: 133 new (domains 49 hostile-input cases incl. `24:00`, `0x50`, `10.0.0.01`, `a'.repeat(64)`, mixed
  families; semantic 53 + 12 example documents). **Examples**: 4 valid documents, 8 schema-invalid, 7 semantic-invalid
  with their expected pointers asserted. **Docs**: `docs/contracts/schema-nat-objects-acl.md` (one table per key,
  one per rule set, examples table, F-nat44 mapping, contract-change rule).
- Fixed two cross-group hazards found by reading the other branches' committed files (read-only `git show`): the
  `export *` collisions `ipv4Address`/`ipv6Address` (P02a) and `l4Port` (P02c) — mine are now private / `l4PortNumber`.

## How it was verified (real output, host `ngfw`, worktree `/root/ngfw-wt/P02b`, HEAD `41689e8`)

`tools/ci.sh --base main` (log `/root/ngfw-wt/logs/P02b-ci.log`):

```
== install (frozen lockfile) ==
== generated output must be clean ==
$ turbo run gen
• turbo 2.11.2
== contract guard vs main ==
== forbidden patterns ==
== typecheck ==
$ turbo run typecheck
• turbo 2.11.2
== lint ==
$ turbo run lint
• turbo 2.11.2
== test ==
$ turbo run test
• turbo 2.11.2
@ngfw/schema#test:  ERROR  command (/root/ngfw-wt/P02b/packages/schema) /usr/bin/pnpm run test exited (1)
 ERROR  run failed: command  exited (1)
```

**CI GATE NOT PASSED — one assertion, not mine.** The failing test is `packages/schema/src/index.test.ts:26`
(P02a-owned): `for (const key of ROOT_KEYS) expect(r[key]).toEqual({})`, written for the P02s placeholders. Any domain
with field defaults breaks it — P02a's `system`/`dataplane` and P02c's `vpn` use `.default(...)` too — so it must be
relaxed by its owner (P02b-questions #1 has the one-line fix). Every other gate step is green; the steps the gate
skipped after `set -e` were run by hand:

```
== pnpm test (whole package) ==
 ✓ src/pointer.test.ts (5 tests) 40ms
 ✓ src/diff.test.ts (6 tests) 24ms
 ✓ src/json.test.ts (9 tests) 31ms
 ✓ src/merge-patch.test.ts (17 tests) 37ms
 ✓ src/ui.test.ts (2 tests) 34ms
 ✓ src/primitives.test.ts (65 tests) 71ms
 ✓ src/domains/objects.test.ts (10 tests) 184ms
 ✓ src/domains/acl.test.ts (10 tests) 189ms
 ✓ src/domains/nat.test.ts (29 tests) 265ms
 ✓ src/semantic/index.test.ts (6 tests) 120ms
 ❯ src/index.test.ts (5 tests | 1 failed) 138ms
   × RootConfig > accepts an empty document and fills every domain with its default 109ms
     → expected { enabled: false, mode: 'ed', …(19) } to deeply equal {}
 ✓ src/semantic/nat-objects-acl-examples.test.ts (12 tests) 197ms
 ✓ src/domains/domains.test.ts (41 tests) 202ms
 ✓ src/semantic/acl.test.ts (11 tests) 249ms
 ✓ src/examples.test.ts (23 tests) 252ms
 ✓ src/semantic/objects.test.ts (20 tests) 219ms
 ✓ src/generate.test.ts (4 tests) 171ms
 ✓ src/semantic/nat.test.ts (22 tests) 292ms
 ❯ src/index.test.ts:26:49
 Test Files  1 failed | 17 passed (18)
      Tests  1 failed | 296 passed (297)

== vitest run --exclude src/index.test.ts ==
 Test Files  17 passed (17)
      Tests  292 passed (292)

== gen determinism (sha256 over dist/json-schema/*.json + dist/openapi-components.json, two runs) ==
ab8a3a9858c10e46779c790690d7a54e5b01df0fa6207affe91f6238ada18825  -
ab8a3a9858c10e46779c790690d7a54e5b01df0fa6207affe91f6238ada18825  -
acl.json dataplane.json ha.json interfaces.json management.json nat.json objects.json root.json routing.json services.json system.json tunnels.json vpn.json vrfs.json

== dist/json-schema/nat.json ==
{"title":"NAT","ui":{"order":60},"keys":["enabled","mode","inside","outside","outputFeature","insideVrf","outsideVrf",
 "forwarding","staticMappingOnly","connectionTracking","sessionLimit","pools","staticMappings","identityMappings",
 "loadBalancedMappings","timeouts","ipfix","nat64","nat66","nptv6","det44","dslite","map","cnat"],
 "sessionLimit":{"type":"integer","minimum":1024,"maximum":2147483647,"title":"Session limit",
 "x-vrx-ui":{"group":"General","help":"Max sessions per worker thread; omit = VPP default"}},"pools_ui":{"group":"Pools","order":10}}
== dist/openapi-components.json ==
RootConfig SystemConfig DataplaneConfig InterfacesConfig VrfsConfig RoutingConfig NatConfig ObjectsConfig AclConfig VpnConfig TunnelsConfig ServicesConfig HaConfig ManagementConfig
AclConfig.attachments.items.required= ["list","target","sequence"]
ObjectsConfig.addresses.propertyNames.pattern= ^[A-Za-z0-9][A-Za-z0-9_.-]*$

== pnpm build (root, turbo) ==
 Tasks:    12 successful, 12 total
Cached:    5 cached, 12 total
  Time:    20.641s

== apps/agent: make lint && make test && make build (log /root/ngfw-wt/logs/P02b-agent.log) ==
go vet ./...
0 issues.
go test -race -count=1 ./...
?   	ngfw/agent/cmd/vrx-agent	[no test files]
?   	ngfw/agent/gen/vrx/v1	[no test files]
ok  	ngfw/agent/internal/agent	1.131s
go build -trimpath -ldflags "-s -w -X main.version=41689e8" -o bin/vrx-agent ./cmd/vrx-agent
AGENT-STEPS-OK
```

Package-level `pnpm typecheck` (`tsc --noEmit`) and `pnpm lint` (`eslint src`) print no findings; Prettier was run on
every file I touched. `git status --short` is empty after gen/build (dist is ignored).

## Out of scope / left undone

- The `index.test.ts:26` relaxation (P02a-owned; P02b-questions #1). Until it lands, `tools/ci.sh` on this branch
  stops at `test` with that single assertion.
- Branch-coverage measurement: `@vitest/coverage-v8` exists only on P02a's branch (D-024); every validator branch has
  a dedicated test but the number was not measured (P02b-questions #7).
- Moving the local primitives / lookup helpers into P02a-owned files (P02b-questions #3) — mechanical follow-up.
- API endpoints, UI, renderers, proto messages (F-nat44 adds the proto), VPP verification — not part of P02.

## Open questions

See `docs/status/tasks/P02b-questions.md` (8 items): the stale root test, the remaining P02a↔P02c `secretRef`
export clash, primitive relocation, implied `default` VRF, sub-interface key shape, `external.pool` semantics,
coverage tooling, contract-guard subject on a `task/` branch.

## Decisions taken

D-P02b-1 … D-P02b-8 with options in `docs/status/tasks/P02b-questions.md` (NAT44 at the top level with sibling
translators and one `staticMappings` list; `external` = exactly one of ip/pool/interface; per-family shared object
namespace; implied `default` VRF; global pool-overlap check; helpers inside owned files; hard VRF-match on
attachments; same-day recurring schedules).
