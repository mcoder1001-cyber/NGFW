# TASK ENVELOPE — P12
id: P12   branch: task/P12   worktree: /root/ngfw-wt/P12   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave B (day 10-12): FRR + linux-cp framework, BGP
prompt: prompts/P12-frr-linuxcp.md   (template: none)   wbs: D3.1, D3.2, D3.3
scope: FRR + linux-cp framework, BGP — the RF-1 FRR framework, DF-8 lcp descriptors and the P02a routing schema exist: BGP + policy sections, the linux-cp pair model/wiring, FRR→VPP FIB proof
merged deps you can rely on: P08 (vertical slice: desired/subsystems/projection patterns; `routing.static` via core + `frr.StaticOwnedByFRR`), RF-1 (renderers/frr framework + frrtest), DF-8 (descriptors/lcp: `lcp.itf-pair`, `lcp.default-netns`), F-vrf-static-ecmp (board dep proposed in docs/status/wave-A-launch-plan.md — spawn only after it merges: `ListRoutes`, the `/state/routes` controller in apps/api/src/features/vrf-static-ecmp/, `viaFrr` + its selector registration) (+ pre-wave merges TD-2, TD-3: TD-3's interface sanitizer is in the lcp Create)
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI)
daemon-owner: frr — slot test instances only through `frrtest` (pathspace w<SLOT>, /run/vrx-test/w<SLOT>/frr, symlink /run/frr/w<SLOT>, lock /run/vrx-test/w<SLOT>/frr.lock); `frr.service` stays disabled and stopped; nothing under /etc/frr is written
files you own exclusively: apps/agent/internal/renderers/frr/bgp/** apps/agent/internal/renderers/frr/policy/** (prefix lists / route maps, reused by F-ospf …) apps/agent/internal/renderers/frr/*.go apps/agent/internal/renderers/frr/templates/** apps/agent/internal/renderers/frr/frrtest/** (framework: gap-only — a proven defect or a missing hook; F-ospf/F-isis-rip/F-bfd-redistribution depend on you) apps/agent/internal/descriptors/lcp/** (gap-only; keep TD-3's sanitizer hunk) apps/agent/internal/lcpmap/** (linux-cp interface mapper for `frr.WithInterfaceMapper`) apps/agent/internal/desired/{bgp,lcp}*.go apps/agent/internal/subsystems/{frr,lcp}*.go apps/agent/internal/agent/rpc_{bgp,routing}*.go packages/schema/src/semantic/bgp*.ts apps/api/src/features/bgp/** apps/api/test/e2e/bgp* apps/web/src/domains/routing/bgp/** apps/web/src/locales/{en,fa}/bgp.json docs/user/routing/bgp.md docs/agent/renderers/{frr,frr-bgp}.md docs/agent/descriptors/lcp.md test/topology/bgp/** docs/status/tasks/P12*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["routing"]` append + `Domains["interfaces"]` for `lcp.itf-pair`; Register lines; new `*Wiring` methods in your subsystems/{frr,lcp}.go) · A2 apps/agent/internal/agent/projection.go (builder hook; replace the one "routing protocols … RF-1" warning hunk for the leaves you implement) · A4 apps/agent/internal/agent/server.go (RPC methods in your rpc_bgp.go) · C1 packages/schema/src/domains/{routing,interfaces}.ts via an ext/ file + one key line (LCP pair leaf, `static[].tag`) · C2 packages/schema/src/semantic/index.ts · C5 packages/proto/vrx/v1/dataplane.proto (routing/BGP state rpc + messages in a `// ----- P12 -----` section, EventKinds, `StaticRoute.tag`, the LCP leaf) · C6 docs/contracts/proto.md · C7 generated files · P1 apps/api/src/app.module.ts · P2 apps/api/src/state/state.controller.ts / F-vrf-static-ecmp's `/state/routes` controller (FRR-source annotation + `proto` filter hunk only) · P4 agent.client.ts · P5 fake-agent.ts · P6 apps/api/src/infra/bus.ts (routing/neighbour topic) · W1 router.tsx · W3 i18n.ts · W2 apps/web/src/nav/nav.ts + nav.test.ts (a BGP NavItem, labelKey in your namespace; F-vrf-static-ecmp creates no routing tab registry — use one only if the manager has seeded it) · apps/agent/internal/renderers/ALLOWLIST.md (rows only for a new exec — vtysh/frr-reload rows exist) · subsystems.Env event sink — seeded by the manager in the anchor commit (the agent bus is unexported; shared with F-neighbors-ra, F-object-model, P11, P12, F-wireguard); never add it yourself in agent.go (A5) — if it is absent, write the question and keep the publisher behind an interface in your own files · A7 docs/vpp-code-track.md
contract numbers: needs the LCP pair leaf (`Interface` 20–29 range for waves B+, or a routing-level leaf), `StaticRoute.tag` (RF-1 Q1), a routing/BGP state rpc (RF-1 Q2), EventKind ×2 (routing change, BGP neighbour state — RF-1 Q3) — from the manager's wave-B allocation table, never "next free"; `static[].viaFrr` and its selector are F-vrf-static-ecmp's — consume them; commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/P12-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi (P04/manager-owned), /etc/frr, frr.service, apps/agent/internal/renderers/frr/{ospf,isis,rip,bfd,redistribute,ldp,pim}/** (later F-tasks), apps/agent/internal/descriptors/core/** (the static-route skip is already wired via `frr.StaticOwnedByFRR`), apps/agent/internal/desired/interfaces.go (P08; your LCP builder is its own file), apps/agent/internal/agent/{agent,service,state,ifstate}.go (A5), tools/ci.sh, tools/lab
host facts: linux_cp/linux_nl loaded since D-060 — not parked; no `linux-cp { default netns }` section → linux_nl listens in the netns that was the lcp default when its socket opened: the ROOT netns today (lcp_nl.c); `lcp.default-netns` is VPP-global (D-071: globals owner only; DF-8's host test skips when it is set) · FRR 10.7.1 installed, frr.service disabled · `frrtest` refuses the root namespace and anything with ens192 · no data NICs bound (D-026): LCP pairs only on slot loopbacks / rig af_packet interfaces, never ens*; host tap names `w<SLOT>-…` (≤ 15 chars) · `lcp_itf_pair_replace_begin/end` act on every owner's pairs — never on the shared host
obligations accumulated from decisions (all must be honoured and listed in P12.md with evidence): D-072 one programmer per static route (default VPP); F-vrf-static-ecmp registers `frr.RegisterStaticSelector` for `viaFrr` — never call it again (it panics on a second call); FRR secrets only through `rc.Secret`, redacted in Retrieve/DryRun/errors/logs · D-045/D-070 routing layout (prefix lists / route maps are objects — confirm with P02a's shape) · D-056 LDP/PIM stay with F-mpls-srmpls/F-igmp-mfib · D-071 no `lcp default netns` change from a slot · DF-8 L3: the VPP-side host tap stays untagged · RF-1 review M3: RIB-size-safe readers only (scoped `show … json`, streamed) · D-060 linux-nl facts above
coordination: (1) renderer stage — no renderer is called by the agent today; default = one singleton scheduler descriptor (e.g. `frr.config/vrx`) that renders every registered RF-1 section into one frr.conf (the F-host-acl-nftables default (a)); later protocol tasks only add sections; log the choice with options · (2) secret channel for `passwordRef` does not exist (P11 default owner): until it is on main, a neighbour with a password is refused with a clear DryRun error — say so · (3) F-vrf-static-ecmp (wave A) owns `/state/routes` + `ListRoutes` + `viaFrr` — extend, do not duplicate · (4) linux-nl test plan: BEFORE the first host run write it with options in P12-questions.md — the VRX-side FRR's routes reach VPP only from the netns linux_nl listens in (root today): a slot kernel VRF/table in <SLOT>000–<SLOT>999 bound to your LCP taps, or a manager globals window that sets `lcp default netns`; BGP routes never in the root netns main table (management path on ens192) · (5) P11 (parallel) uses a test-fixture LCP pair for IKE; F-vrrp-config-sync, F-ospf, F-isis-rip, F-bfd-redistribution depend on you
V19/V24 SAFETY (D-095, D-101): before ANY traffic through the rig, run TD-3's preflight on the rig interfaces' sw_if_index and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b (kept outside the repo) for screenshots; paste `vppctl show ip fib table <id>` counts and the harness's `vtysh --vty_socket <run dir> -N w<SLOT> -c "show bgp summary"`
time box: 24 h — when exceeded: stop, commit WIP, write docs/status/tasks/P12.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/P12-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/P12.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
questions: docs/status/tasks/P12-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
