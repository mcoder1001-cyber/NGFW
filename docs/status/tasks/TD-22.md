# TD-22 — small follow-ups from reviews

## What
1. **dhcp.client claim-before-write** (TD-11b Q3, D-133) — `apps/agent/internal/descriptors/dhcp/client.go` Create now
   calls `tg.ClaimFirst(ctx)` before `dhcp_client_config`; the "already exists" path uses `Claim.Adopt()` (a claim
   made by this Create proves nothing), a failed add returns `Claim.Undo(err)`. ownership.go comment updated.
   Tests: `dhcp/client_claimfirst_test.go` — `TestClientClaimsBeforeWrite` (refused claim → 0 VPP writes),
   `TestClientClaimOrderAndUndo` (claim recorded before the first `dhcp_client_config`; failed add leaves no claim).
   The existing H1 tests (`TestClientForeignOnUntagged`, `TestClientOnUntaggedInterface`) still pass.
2. **InterfaceDrawer saveSub stale dialog** (F-bridge-l2 review #3) — the sub-interface patch is now diffed against
   the value the dialog opened with (`subDialog.value`), like `saveInterface` (N4), not against the fresh candidate:
   a field another session set meanwhile (`l2` once F-bridge-l2 lands) is no longer sent as `null`. New test in
   `InterfacesPage.test.tsx` uses `mtu` as the stand-in (the `l2` field is not on this base).
3. **examples.test.ts SIBLING** — widened once: group prefixes + every `F-*` board slug + the first word of each slug
   (so `loopback-*.json`, `vlan-qinq-*.json`, `bridge-l2-*.json`, `neighbors-ra-*.json`, `vrf-static-ecmp-*.json` …),
   still anchored so unknown prefixes fail. New `SIBLING (TD-22)` test with positive and negative names.
4. **CommitDialog reuses applyOutcome() on a lost commit answer — NOT DONE.** `applyOutcome()` does not exist on
   this base (be53867): it comes from TD-10a (state: review, unmerged; `git log --all -S applyOutcome` finds only the
   board row). The row's own note says "merge after … TD-10a". Also the file is `apps/web/src/config/CommitDialog.tsx`,
   not `apps/web/src/components/…` as files_owned says. Needs a rebase onto TD-10a first.

## Verification (cloud container; fake VPP; no VRX_INTEGRATION, real-VPP integration tests skipped)
```
$ go vet ./internal/descriptors/dhcp/ && go test -count=1 ./internal/descriptors/dhcp/ ./internal/descriptors/dfkit/...
ok  	ngfw/agent/internal/descriptors/dhcp	0.011s
ok  	ngfw/agent/internal/descriptors/dfkit	0.052s
$ go test ./internal/descriptors/...   → all ok ; gofmt -l → empty
# with client.go reverted (old claim-after-write):
--- FAIL: TestClientClaimOrderAndUndo: claim made after [1] dhcp_client_config call(s); want before the first
# with InterfaceDrawer.tsx reverted:
× a sub-interface edit diffs against the value its dialog opened with …   + "mtu": null,
$ turbo run lint typecheck test --filter=@ngfw/web --filter=@ngfw/schema
@ngfw/schema:test:  Test Files 37 passed (37)  Tests 1215 passed (1215)
@ngfw/web:test:     Test Files 14 passed (14)  Tests 99 passed (99)
Tasks: 17 successful, 17 total
$ tools/ci.sh check
CI GATE FAILED — gitleaks found secrets in the commit history
```
The gitleaks failure is pre-existing: the 3 findings (generic-api-key in packages/schema/src/domains/vpn.test.ts:252,
apps/agent/internal/renderers/rsyslog/rsyslog_test.go:31, strongswan/apply_test.go:375) are in commits c2a8ab7e6 and
7b2f4381c, ancestors of be53867, and `tools/ci.sh check` fails identically on be53867. (gitleaks appeared in
/usr/local/bin during this session; TD-18's earlier check ran without it.) All other check sections passed.

## Out of scope
Item 4 (above). Other TD-22-owned tech-debt lines (F-bonding member classes, DNS resolve guard, SessionsTab timer
test, key-file O_NONBLOCK, TD-24 Q1/Q2, F-kea V1/V2, F-object-model metrics, F-acl flag save/restore) are outside
this envelope's four items and mostly outside files_owned.

## Open questions
- Item 4: start a TD-22b after TD-10a merges, or fold it into TD-10a's fix round?
- gitleaks findings on main history: allowlist (test fixtures) or rewrite — manager call.
