# TASK ENVELOPE — WEB-3 committed browser harness (web-ahead track, D-123)
id: WEB-3   branch: task/WEB-3   worktree: /root/ngfw-wt/WEB-3   base: main@f13b744   started: 2026-09-24T23:47
slot: 11 → eval "$(tools/lab env 11)" (ports 4100/6100, DB vrx_w11) — only for the one real-stack demo run
why: every feature worker hand-rolls its screenshots/E2E (P08's script was lost — review F6; F-vlan-qinq and F-nat44-ed-sessions each committed a private shots.mjs; others used ad-hoc headless Chrome from a scratchpad). One committed harness makes UI evidence reproducible and cheap for ~40 remaining feature screens.
scope: apps/web/test/e2e/lib/** — a small library over the already-used headless Chrome + playwright-core (NO new dependency; find what the repo already uses: `git -C /root/ngfw grep -n "playwright" -- package.json '*/package.json'`, and read test/topology/vlan-qinq/shots.mjs on task/F-vlan-qinq and test/topology/nat44-ed-sessions/shots.mjs on task/F-nat44-ed-sessions via `git show`): login helper, nav helper that opens collapsed groups (ui-nav-collapse), wait-for-current-page, en/fa + light/dark toggles, screenshot naming `<slug>-<lang>-<theme>-<step>.png`, pageError collection that fails the run. apps/web/test/e2e/shots.mjs — CLI entry: `node apps/web/test/e2e/shots.mjs --base http://127.0.0.1:<port> --screens <slug> --out <dir> --langs en,fa`. apps/web/test/e2e/screens/_example.mjs — the pattern each feature copies (one exported async function per screen). apps/web/test/e2e/README.md — how a worker uses it with the slot stack (tools/app is NOT to be used; slot ports only). Migrate flow.e2e.mjs to the lib only where it removes duplication, keeping its 43 checks identical.
proof: one real-stack run on slot 11 (API + agent owner w11 + vite preview of this worktree's build, same procedure «وب» used for ui-nav-collapse; no af_packet) producing screenshots of 2 existing screens (Interfaces, System › Users) in en and fa, pageErrors 0; flow.e2e.mjs still 43/43; then full teardown (pg-test.sh drop w11, flushdb 11, stop by PID, remove /run/vrx-test/w11/…).
files you own: apps/web/test/e2e/{lib/**,shots.mjs,README.md,screens/_example.mjs,flow.e2e.mjs} docs/status/tasks/WEB-3*
must not touch: product code, tools/app, other features' private shots.mjs (they migrate later).
CI: `TMPDIR=/tmp/g-web3 tools/ci.sh --base main`.
GIT RULE: git only as `git -C /root/ngfw-wt/WEB-3 …`; NEVER in /root/ngfw.
time box: 4 h. finish: WEB-3.md with pasted evidence + the screenshot list; commit; final message = 6 lines.
never: merge · new dependencies · ports 3000/8080/9101 · pkill · show trace
