# TASK ENVELOPE — TD-10b API auth, session, audit (REVIEW-2026-09-24 §2.3, verified; D-125)
id: TD-10b   branch: task/TD-10b   worktree: /root/ngfw-wt/TD-10b   base: task/TD-4@c05183a (SPECULATIVE D-114: TD-4 approved, queued for merge)   started: 2026-09-24T23:41
slot: 5 → eval "$(tools/lab env 5)" (vrx_w5, Valkey db 5, ports 3500/5500) — api e2e under `tools/lab lock shared`
read first: /root/ngfw-wt/REVIEW-2026-09-24-verdicts.md, /root/ngfw-wt/REVIEW-2026-09-24-verified-plan.md (section A "TD-10b"), /root/ngfw-wt/REVIEW-2026-09-24-verify-results.json (section s2), /root/ngfw/docs/decisions/PENDING-session-revocation.md and PENDING-tools-app-transport.md, LOG D-091, D-097, D-100, D-102, D-124; TD-4's review/verify files in your base.
scope (exactly, from the board row): 2.3a lockout keyed by (user, IP); the last enabled admin is only throttled, never locked; a root-only break-glass unlock tool (script under deploy/ or tools/, packaged later by P10); 2.3b trustProxy via VRX_TRUST_PROXY (default loopback only) and the peer address used consistently in secureTransport(); vite dev proxy xfwd; 2.3c logout revokes per session id; demotion/deletion revocation: build the RECOMMENDED option of PENDING-session-revocation in one isolated hunk (it follows the product owner's answer); 2.3d VRX_SESSION_MAX_SEC absolute refresh lifetime next to the sliding TTL; 2.3e audit logout/refresh and 401 mutations; an audit write failure → system_event + counter (fail closed on privileged routes if the LOG decision says so); 2.3g /api/docs usable in a browser: option (a) refresh-cookie auth or (b) loopback-only — pick, test, log the options; tech-debt P06: JWT key rotation, key-file owner check. Every behaviour change has a test that FAILS on the base first (paste). Record each decision (lockout key, per-sid logout, VRX_SESSION_MAX_SEC, audit fail-closed, /api/docs) in TD-10b.md with options for the manager's LOG.
files you own: apps/api/src/{auth,users,audit}/** apps/api/src/common/principal.ts apps/api/src/app.ts (trustProxy + /api/docs hook) apps/api/src/datastore/pg-repo.ts (syncUsers hunk only) tools/app (vite xfwd + banner only) the new break-glass tool docs/status/tasks/TD-10b*
must not touch: apps/api/src/commit/** (TD-10a), secrets service, agent code.
CI: `TMPDIR=/tmp/g-w5 tools/ci.sh --base main`. Ports 3000/8080/9101 belong to the running tools/app — never restart it; edit its script only.
GIT RULE: git only as `git -C /root/ngfw-wt/TD-10b …`; NEVER in /root/ngfw.
time box: 12 h (WIP every 45 min). finish: TD-10b.md with pasted evidence; commit; final message = 10-line summary.
never: merge · secrets in files/logs · pkill
