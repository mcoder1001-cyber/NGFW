# TASK ENVELOPE — F-qos-flat
id: F-qos-flat   branch: task/F-qos-flat   worktree: /root/ngfw-wt/F-qos-flat   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave C (day 13-15): policer, marking, QoS record/map, DSCP/dot1p (HQoS = V3, excluded)
prompt: prompts/features/F-qos-flat.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main + P08 in wave-B/C prep)   wbs: D7.8
scope: flat QoS — DF-7's policer/qos descriptors and the `services.qos` schema (D-052, with `qos-references`/`qos-consistency` rules) exist: two new semantic rules, projection (policers, "shaper" = egress policer, record/store/egress-map/mark), policer counters + reset, API, UI, docs; HQoS/queues fenced (V3)
merged deps you can rely on: P08 (desired/subsystems/projection patterns; `df7.SetBootStore` installs the persisted DF-7 BootStore; `Wiring.ClassifyStore()`), DF-7 (descriptors/policer: `policer.Register` = policer.policer/interface/bind/classify; descriptors/qos: `qos.Register` = qos.record/store/egress-map/mark; shared helper descriptors/df7) (+ on main by then: TD-2, TD-3, W-seed; wave-A features only if merged when you start — check `git log main`)
read first: prompts/features/F-qos-flat.md · docs/status/wave-A-hotspots.md (§0 rules; A1 A2 C2 C4–C7 P1 P3 P4 P5 W1–W3; C1 only with a gap) · docs/status/wave-BC-numbers.md § F-qos-flat · docs/agent/descriptors/policer.md + qos.md · packages/schema/src/semantic/services.ts (`services.qos-references`, `services.qos-consistency`) · docs/vpp-code-track.md V3, V20 · docs/decisions/LOG.md D-052, D-063, D-064, D-071, D-076, D-080, D-082, D-087, D-104, D-109
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; policer names start with `w<SLOT>`, egress-map ids only <SLOT>000–<SLOT>999 via `df7.WithIDRange`, attachments only on your slot loopbacks / rig interfaces)
daemon-owner: none
files you own exclusively: apps/agent/internal/descriptors/policer/** apps/agent/internal/descriptors/qos/** (both gap-only: a proven defect or a missing helper, name the test) docs/agent/descriptors/policer.md docs/agent/descriptors/qos.md apps/agent/internal/desired/qos*.go apps/agent/internal/subsystems/qos*.go apps/agent/internal/agent/rpc_qos*.go packages/schema/src/domains/ext/qos-flat*.ts (only with a gap) packages/schema/src/semantic/qos-flat*.ts packages/schema/examples/qos-flat-*.json packages/proto/test/fixtures/qos-flat-*.json apps/api/src/features/qos-flat/** apps/api/test/e2e/qos-flat* apps/web/src/domains/services/qos-flat/** apps/web/src/locales/{en,fa}/qos-flat.json docs/user/services/qos-flat.md test/topology/qos-flat/** docs/status/tasks/F-qos-flat*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-qos-flat` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-qos-flat.md and say so in the questions file); never reorder or reformat other lines): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["services"]` — exists by wave C; if not, whoever lands first adds the key — policer.*/qos.* names one per line; one `policer.Register` and one `qos.Register` line — **never `df7/registry.Register`** (it registers lb/mpls/span/lldp/bfd/vrrp/igmp too; duplicates panic, D-030); `df7.WithIDRange`/`df7.WithClassifyTables` options from your own subsystems/qos.go) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (no Action case; `QosPolicerState`/`QosPolicerReset` live in your rpc_qos.go) · C1 packages/schema/src/domains/services.ts (only with a proven gap — ext file + one key line) · C2 packages/schema/src/semantic/index.ts (one spread for `qosFlatValidators`) · C3 packages/schema/src/index.ts (only with an ext file) · C4 new `qos-flat-*.json` fixtures only · C5 packages/proto/vrx/v1/dataplane.proto (`QosPolicerState`, `QosPolicerReset` under the service anchor; messages in a `// ----- F-qos-flat -----` section) · C6 docs/contracts/proto.md (`### F-qos-flat: QosPolicerState, QosPolicerReset`) · C7 generated files (regen, never hand-merged: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`) · P1 apps/api/src/app.module.ts · P3 `POST /api/v1/actions/qos/policers/{name}/reset` as a static route in your own controller (never edit apps/api/src/actions/**) · P4 apps/api/src/agent/agent.client.ts (`qosPolicerState`, `qosPolicerReset`) · P5 apps/api/src/testing/fake-agent.ts (UNIMPLEMENTED stubs on the contract commit; real fake in features/qos-flat/fake.ts) · W1 apps/web/src/router.tsx · W2 apps/web/src/nav/nav.ts (+nav.test.ts: `services` into BUILT_DOMAINS only if not there yet) · W3 apps/web/src/i18n.ts · services domain page + tab registry apps/web/src/domains/services/{ServicesPage.tsx,tabs.ts} (W-seed shell; one line — if the shell is not on main, do NOT create it: own route + NavItem under W1/W2, the manager folds it in) · A7 docs/vpp-code-track.md (V3 note only if something new)
contract numbers: docs/status/wave-BC-numbers.md § F-qos-flat — RPCs `QosPolicerState`, `QosPolicerReset` (+ their messages); `QosService` 5 / `QosPolicer` 13 / `QosInterface` 7 are reserved **only for a proven gap**; nothing else (no ActionRequest member, no EventKind); commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-qos-flat-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned), apps/agent/internal/descriptors/df7/** (DF-7 shared helper + registry — read-only), apps/agent/internal/descriptors/{lb,mpls,span,lldp,bfd,vrrp,igmp}/** (F-lb and later feature tasks), apps/agent/internal/descriptors/{classify,df2}/** (F-rpf-adl-pbr / TD-3 — you only reference classify tables by name), apps/agent/internal/agent/{agent,service,state,ifstate,metrics,telemetry,events}.go + apps/agent/cmd/** (A5 core), apps/agent/internal/desired/interfaces.go (A3), apps/api/src/actions/** (generic bridge, F-vrf-static-ecmp), apps/api/src/{auth,users,secrets,commit,datastore}/**, tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
host facts: `policer_plugin.so` present, qos is vnet core; binapi `policer`/`policer_types`/`qos` verified (`policer_add/update/del`, `policer_dump_v2`, `policer_reset`, `policer_input/output`, `policer_bind`, `policer_classify_set_interface`, `qos_record_enable_disable`, `qos_store_enable_disable`, `qos_egress_map_update/delete/dump`, `qos_mark_enable_disable`) · vrx-a has **no VPP worker threads** → `policer.bind` answers INVALID_WORKER (skip it, it is fake-tested only) · `qos.store` supports source `ip` only on 26.06 · no data NICs bound (D-026): af_packet rig only (D-010)
obligations accumulated from decisions (all must be honoured and listed in F-qos-flat.md with evidence): D-104 use DF-7's descriptors, do not rebuild · D-063/D-076/D-080 `policer.interface` and `policer.classify` are write-only: applied once per VPP boot identity (persisted DF-7 BootStore); **never un-apply `policer_input/output` on an interface that had no policer applied in this VPP lifetime** (out-of-bounds write in VPP, policer.md) — the fake must model duplicate-apply · V3: the "shaper" is an egress policer (1r2c, cir = rateKbps, burst = burstBytes or ≈10 ms, exceed → drop) named `shaper:<name>`, documented as drop-based rate limiting, not queueing · D-071 egress-map ids by id range (`df7.WithIDRange`, slot range in tests); no QoS globals · semantic rules exist (`services.qos-references`, `services.qos-consistency`) — add only `store.source` ip-only, `mark` requires `map`, shaper vs `policer.output` exclusive, in your own `semantic/qos-flat.ts` (rule ids `services.qos-flat-…`) · D-064 crash → opt-in at once + V-item
coordination: (1) F-lb (parallel) also builds on DF-7 — `descriptors/df7/**` read-only for both · (2) `policer.classify` references DF-2 classify tables by name (F-rpf-adl-pbr owns classify; no classify UI here) · (3) F-acl owns ACL-matched marking, F-mpls-srmpls MPLS EXP on label routes · (4) F-kea-dhcp-relay, F-unbound-chrony-syslog, F-host-stack, F-snmp, F-lb, F-ipfix-sflow share `Domains["services"]` and the services page
V19/V24 SAFETY (D-095, D-101): counters evidence with traffic is optional — if you send packets through the rig, run TD-3's preflight on the rig interfaces first and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots; paste `vppctl show policer`, `show qos egress map`, `show qos mark`
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-qos-flat.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-qos-flat-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-qos-flat.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite), by PID · lab lock released · vrx_w<SLOT> dropped · no w<SLOT> policers, egress maps or marks left in VPP (Retrieve + `vppctl show policer` / `show qos egress map` pasted; write-only attachments listed) · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-qos-flat-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
