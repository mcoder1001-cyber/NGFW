# TASK ENVELOPE — F-bfd-redistribution
id: F-bfd-redistribution   branch: task/F-bfd-redistribution   worktree: /root/ngfw-wt/F-bfd-redistribution   base: main@<BASE>   started: <STARTED>
title: Wave B (day 10-12): BFD (VPP+FRR), redistribution matrix, route-policy UX
prompt: prompts/features/F-bfd-redistribution.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed on task/prep-rest against main, task/P08, task/W-seed and the P12/F-vrf-static-ecmp envelopes)   wbs: D3.8, D3.10
scope: VPP BFD end to end on DF-7's merged descriptors (projection of `routing.bfd.sessions` + auth/multihop, BFD events), FRR `bfd` section for protocol-attached BFD (bfdd, profiles), the redistribution matrix (state reader over the protocols' own `redistribute` lines), route-policy UX on top of P12's prefix-list/route-map editors (reorder, where-used), API, UI, docs. NOT the `viaFrr` flag or its selector (F-vrf-static-ecmp owns both, D-109a)
merged deps you can rely on: P08, P12, DF-7, F-ospf, W-seed (+ the wave-B/C anchor pass, docs/status/wave-BC-numbers.md "Pack rules")
  - DF-7: apps/agent/internal/descriptors/bfd/ + docs/agent/descriptors/bfd.md — `bfd.auth-key/<id>`, `bfd.udp-session/<if>/<local>/<peer>`, global `bfd.echo-source` (`bfd.RegisterGlobals`, globals owner only), `bfd.WatchEvents` + `bfd.Sessions`, the `bfd.Secrets` resolver interface; `df7.SetBootStore` is installed by P08's wiring
  - P08: desired/ + subsystems/ + projection patterns, `Wiring.Publish` event seam (W-seed), the RF-1 routing warning hunk (seam S3)
  - P12: FRR singleton descriptor, linux-cp mapper, LCP pairs, routing state RPC, `renderers/frr/policy` + the prefix-list/route-map editors in apps/web/src/domains/routing/bgp/, BGP `redistribute` lines
  - F-ospf: `ospf`/`ospf6` sections with their own `redistribute` lines and `ip ospf bfd` for `bfd: true`; OspfInterface 9 `auth`
  - F-vrf-static-ecmp (through P12): `routing.static[].viaFrr` + `frr.RegisterStaticSelector` registered once — consume, never register again (it panics)
  - also on main: TD-2, TD-3 (V19 sanitizer + preflight), TD-5 if merged
read first: prompts/features/F-bfd-redistribution.md · docs/status/wave-BC-numbers.md (Pack rules, section F-bfd-redistribution) · docs/status/wave-A-hotspots.md (§0 rules; ids A1 A2 A6 C1–C7 P1 P4 P5 P6 W1–W3 A7) · docs/agent/descriptors/bfd.md · apps/agent/internal/renderers/frr/README.md · docs/status/tasks/{P12,F-ospf,F-vrf-static-ecmp,DF-7}.md · docs/decisions/LOG.md D-051, D-063, D-071, D-072, D-076, D-080, D-082, D-087, D-094, D-095, D-101, D-109 · docs/decisions/PENDING-secret-channel.md
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100·<SLOT> VRX_WEB_PORT=5000+100·<SLOT> VRX_METRICS_PORT=9100+10·<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock
  - source of truth: `eval "$(tools/lab env <SLOT>)"`
  - rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; BFD auth-key conf ids and any table id in <SLOT>000–<SLOT>999 (`df7.WithIDRange` from `subsystems.SlotIDRange()`)
  - test agents run with VRX_GLOBALS_OWNER=0 (D-071)
  - slots 1–11 only; 12 is CI
daemon-owner: frr — slot test instances only through `frrtest` (pathspace w<SLOT>, `Daemons: bfdd, ospfd`), for the netns peer and the VRX side; `frr.service` stays disabled and stopped; nothing under /etc/frr; other FRR tasks may run in their own slots only after the manager has logged per-slot-instance FRR ownership (wave-BC-launch-queue.md M3; until then one FRR task at a time). A zebra in the ROOT netns (linux-nl FIB proof) is host-wide in any case: one slot at a time, manager window (P12 coordination 4)
obligations:
  - D-104: use, do not rebuild — DF-7's bfd descriptors (gap-only), RF-1's framework, P12's editors (import and extend; never a second prefix-list/route-map editor), F-ospf's sections. Existing rules `routing.bfd-session-unique`, `routing.interface-exists`, `routing.route-map-exists` stay — extend in YOUR semantic file
  - D-072 / D-109a: `viaFrr` and its selector are F-vrf-static-ecmp's: do not add a schema field, do not call `frr.RegisterStaticSelector`; your acceptance only *uses* a `viaFrr: true` route
  - D-071: `bfd.echo-source` only through `bfd.RegisterGlobals` on the globals owner; slot agents never set it
  - D-051: BFD keys are `key/<name>` refs (VPP side through DF-7's `bfd.Secrets` resolver, FRR side through `rc.Secret`); secret ≤ 20 bytes; never in a Value, error, log, fixture or status file; PENDING-secret-channel: the end-to-end key step waits, tests use a slot-local fixture resolver with `VRXTPSKbfd<n>` literals (D-086 precedent)
  - source fact (VPP 26.06 bfd_udp.c): VPP registers UDP 3784/3785 (single-hop) and 4784 (multi-hop) for its own BFD node when the first VPP session of that kind exists and releases them at zero — while ANY VPP BFD session of that address family and hop type exists on a VPP (on the shared host: any slot's), BFD packets to VPP's addresses never reach FRR bfdd behind linux-cp. Consequences you implement and document: (a) the semantic rule is per box, not per peer — VPP sessions and FRR protocol-attached BFD of the same AF/hop type cannot coexist (error with `pointer`; see the prompt's open question); (b) the FRR-bfdd-over-LCP host step runs only in a manager window with no VPP BFD session on the host, otherwise netns↔netns bfdd + the reason; (c) `### V-new (F-bfd-redistribution)`
  - D-063/D-076/D-080: DF-7's rules hold (auth-key rotation = new id; write-only parts applied once per boot identity); delete-before-table ordering is not relevant (no FIB entries)
  - RF-1 review M3: matrix route counts from scoped per-protocol summaries (`show ip route summary json` / per-protocol counts), never a full-RIB dump
files you own exclusively:
  - apps/agent/internal/descriptors/bfd/** and docs/agent/descriptors/bfd.md (gap-only: DF-7 built them; edit only for a proven defect, name the test)
  - apps/agent/internal/descriptors/core/coretest/bfd_redistribution*.go (A6: new file only)
  - apps/agent/internal/renderers/frr/{bfd,redistribute}/** (section `bfd` order 700; `redistribute` = matrix state reader, no section), docs/agent/renderers/frr-{bfd,redistribute}.md
  - apps/agent/internal/desired/bfd*.go (sessions + auth keys builder/assembler), apps/agent/internal/subsystems/bfd*.go (bfd.Register with the secrets resolver, RegisterGlobals on the globals owner, WatchEvents → `Wiring.Publish`, blank imports), apps/agent/internal/agent/rpc_bfd*.go (`BfdState`, `RedistributionMatrix`)
  - packages/schema/src/domains/ext/bfd-redistribution*.ts, packages/schema/src/semantic/bfd-redistribution*.ts (rule ids `routing.bfd-redistribution-…`), packages/schema/examples/bfd-redistribution-*.json, packages/proto/test/fixtures/bfd-redistribution-*.json
  - apps/api/src/features/bfd-redistribution/** (index.ts exports {controllers, providers}; `BfdRedistributionController`; real fake behaviour in fake.ts), apps/api/test/e2e/bfd-redistribution*
  - apps/web/src/domains/routing/bfd-redistribution/**, apps/web/src/locales/{en,fa}/bfd-redistribution.json
  - docs/user/routing/bfd-redistribution.md, test/topology/bfd-redistribution/**, docs/status/tasks/F-bfd-redistribution*
shared hotspots (append-only, conflicts resolved by the manager at merge):
  - protocol: docs/status/wave-A-hotspots.md §0. Insert only directly below your own anchor `// wave-BC: F-bfd-redistribution` (manager's wave-B/C anchor pass); if missing, say so in the questions file and insert at the end of that anchor block. dataplane.proto: keep the one-blank-line framing. Never reorder or reformat other lines. List every hunk under "Shared hunks" in docs/status/tasks/F-bfd-redistribution.md
  - A1 apps/agent/internal/subsystems/subsystems.go: `bfd.NameAuthKey`/`bfd.NameSession` (the package's name constants) into `Domains[Routing]`; one call into your subsystems/bfd.go at the end of `Register()`
  - A2 apps/agent/internal/agent/projection.go: one builder call in project(), one assembler call in assemble(); drop `bfd` from the RF-1 routing warning per seam S3
  - C1 packages/schema/src/domains/routing.ts: `BfdSessionSchema.{auth, multihop}`, `BfdSchema.profiles` (+ `OspfInterfaceSchema.bfdProfile` / `IsisInterfaceSchema.bfdProfile` only if per-interface profiles are built) key lines · C2 semantic/index.ts · C3 schema/src/index.ts · C4 new fixture files only
  - C5 packages/proto/vrx/v1/dataplane.proto: `BfdSession` 8–9, `BfdConfig` 2, (`OspfInterface` 10, `IsisInterface` 8), `EventKind` 22, `BfdState` + `RedistributionMatrix` rpcs under the service anchor; new messages in `// ----- F-bfd-redistribution -----`
  - C6 docs/contracts/proto.md · C7 generated, never hand-edited (apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md)
  - P1 apps/api/src/app.module.ts · P4 apps/api/src/agent/agent.client.ts · P5 apps/api/src/testing/fake-agent.ts (UNIMPLEMENTED stubs on the contract commit; real fake in features/bfd-redistribution/fake.ts) · P6 apps/api/src/infra/bus.ts (`bfd-redistribution.events`) + telemetry/relay.service.ts (EventKind 22 case)
  - W1 apps/web/src/router.tsx (BFD, redistribution matrix, policy UX routes) · W2 apps/web/src/nav/nav.ts + nav.test.ts (NavItems in the routing group, labelKeys in `bfd-redistribution:`) · W3 apps/web/src/i18n.ts
  - A7 docs/vpp-code-track.md: `### V-new (F-bfd-redistribution)` (VPP BFD port claim vs FRR bfdd); the manager numbers it
  - allocated numbers (docs/status/wave-BC-numbers.md, a merge blocker if reused): **BfdSession 8 `auth` / 9 `multihop`**, **BfdConfig 2 `profiles`**, **OspfInterface 10 `bfd_profile`** and **IsisInterface 8 `bfd_profile`** (only if built; else reserved), **EventKind 22** — nothing else; `StaticRoute.via_frr` (7) is F-vrf-static-ecmp's
contract: commit `contract(schema): bfd auth, multihop, profiles` and `contract(proto): bfd auth/multihop/profiles, BfdState, RedistributionMatrix, bfd event` as separate commits on YOUR branch first + docs/status/tasks/F-bfd-redistribution-contract.md; tell the manager in the questions file and keep building. Never a `contract/` branch
files you must not touch:
  - everything else
  - never: /root/ngfw (main), other worktrees, /etc (incl. /etc/vpp and /etc/frr), /root/vpp, the frr/vpp units
  - apps/agent/binapi + tools/binapi-gen.sh, tools/lab, tools/ci.sh, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
  - apps/agent/internal/descriptors/{df7,dfkit}/** (shared helpers), every other descriptor package (vrrp is F-vrrp-config-sync's; mpls/igmp are F-mpls-srmpls'/F-igmp-mfib's)
  - apps/agent/internal/renderers/frr/*.go, frr/frrtest/**, frr/templates/**, frr/{bgp,policy}/** (RF-1/P12), frr/{ospf,isis,rip,ldp,pim}/** (the protocols render their own `redistribute` and `… bfd` lines)
  - P12's files (lcp, lcpmap, desired/{bgp,lcp}*, subsystems/{frr,lcp}*, rpc_{bgp,routing}*, apps/api/src/features/bgp/**, apps/web/src/domains/routing/bgp/** — import its components, never edit them); F-ospf's and F-vrf-static-ecmp's files
  - agent core (A5): apps/agent/internal/agent/{agent,service,state,ifstate}.go, apps/agent/cmd/**; apps/agent/internal/desired/interfaces.go (A3); packages/schema/src/domains/routing.ts and semantic/routing.ts beyond the anchored key lines; apps/api/src/state/**
host facts:
  - FRR 10.7.1 installed (`bfdd`, `ospfd`), `frr.service` disabled; frrtest refuses the root netns and ens192
  - VPP bfd is core (no plugin switch); binapi `bfd` has `bfd_udp_add/mod/del`, `bfd_udp_session_set_flags`, `bfd_udp_auth_activate/deactivate`, `bfd_udp_enable_multihop`, `bfd_udp_session_dump`, `want_bfd_events` — check every name in apps/agent/binapi/bfd/
  - the UDP port claim above (bfd_udp.c:596–651 register, :787–824 release); linux-cp punts unknown UDP to the tap (lcp_interface.c `udp_punt_unknown`) only while VPP holds no BFD port
  - DF-7's bfd host test ran without incident (only igmp/vrrp are opt-in after D-087); still one host package at a time
  - no data NICs bound (D-026): af_packet rig only (D-010)
coordination: (1) P12 owns the prefix-list/route-map editors and BGP `redistribute` toggles — you add reorder + where-used by importing them into your policy page; if they are not reusable, questions file · (2) F-ospf / F-isis-rip render `redistribute` and `… bfd` lines; you read them for the matrix and only link to their editors · (3) F-vrrp-config-sync: BFD for VRRP is theirs · (4) the redistribution of `ospf6`/`ripng` into other protocols is an open question, not built
V19/V24 SAFETY (D-095, D-101): before ANY BFD traffic through the rig, run TD-3's preflight on the rig interfaces' sw_if_index and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots (en + fa/RTL: BFD sessions grid, redistribution matrix, policy editor with reorder/where-used); paste `vppctl show bfd sessions` (keys redacted), the harness's `show bfd peers`, and the event timestamps for the 2 s check
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-bfd-redistribution.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-bfd-redistribution-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-bfd-redistribution.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite; FRR daemons through the harness Stop) by PID · lab lock released · vrx_w<SLOT> dropped · no w<SLOT> BFD sessions / auth keys left (Retrieve + `show bfd sessions` pasted) · no ns-w<SLOT>-* netns left · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-bfd-redistribution-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
