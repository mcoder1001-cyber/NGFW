# TASK ENVELOPE — TD-24 interface-ip reconcile must not delete a DHCP-leased address (manager ngfw-46; F-kea review Q5, rated H)
id: TD-24   branch: task/TD-24   worktree: /root/ngfw-wt/TD-24   base: task/TD-11c@e83c6318 (SPECULATIVE, D-114: TD-11c approved, in the merge queue; it edits the same core files) — when TD-11c lands on main, `git -C /root/ngfw-wt/TD-24 merge main`
slot: 7 → eval "$(tools/lab env 7)" — ONLY for the one host proof run (rig prefix w7), under `tools/lab lock`, VPP NRestarts before/after (now 2)
bug (confirmed by the F-kea reviewer, bfedc2b8 §4, on main): core/ifaddr.go InterfaceAddrDescriptor.Retrieve (main :209-246) reports EVERY address on a tagged interface (ip_address_dump), so an address that VPP's DHCP client assigned (a lease, never in desired state) is seen as undesired and the next reconcile/resync DELETES it — on any sub-interface or af_packet interface with `dhcp: client`. Same class: IPv6 addresses VPP autoconfigures (ip6 nd address-autoconfig, if enabled by F-neighbors-ra) — check and cover if present.
fix (declarative, no imperative workaround):
  1. Retrieve skips addresses VPP owns: for each tagged interface, read dhcp_client_dump (binapi) once per Retrieve and exclude the lease prefix (lease host address + mask) on that sw_if_index; same for autoconf'd IPv6 if the API exposes it (else document the gap in TD-24-questions.md with the V-number).
  2. Bounded and cheap (D-132): one dhcp_client_dump per Retrieve, not per interface; no new polling.
  3. A desired static address that EQUALS the lease is a config error — add/keep the schema rule (DHCP client excludes static IPv4 is F-kea's rule — reuse it, don't duplicate).
  4. Tests that fail on the base: fake VPP with a dhcp client lease on a tagged sub-interface → Retrieve omits it → Plan has no Delete for it; a static address next to it is still reconciled; restart/resync keeps the lease.
  5. Host proof (slot 7, one run): af_packet rig + sub-interface with `dhcp: client` against a slot-local dnsmasq/kea in the w7 netns (copy F-kea's topology rig pattern: `git show task/F-kea-dhcp-relay:test/topology/kea-dhcp-relay/`), agent resync twice → the lease address stays (show int addr / ip_address_dump pasted); NRestarts unchanged. Never run show trace / trace add / clear trace; no classify sweeps.
files you own: apps/agent/internal/descriptors/core/ifaddr.go (Retrieve hunk) + core/*_test.go (new tests) + test/topology/interfaces or a new test/topology/dhcp-client-lease (host proof) + docs/agent/descriptors/core*.md (one paragraph) + docs/status/tasks/TD-24*
CI: `TMPDIR=/tmp/g-td24 tools/ci.sh --base main`.
GIT RULE: git only as `git -C /root/ngfw-wt/TD-24 …`; NEVER in /root/ngfw.
time box: 3 h. finish: TD-24.md with evidence; commit; final message = 5 lines.
never: merge · show trace · classify sweeps · VPP restart · pkill · tools/app · ports 3000/8080/9101 · host kea/dnsmasq units (slot-local instances only)
