# Status 2026-09-25 13:30 — cloud manager session (claude/zealous-lovelace-1q9hzg)

## خلاصه (فارسی)
- در این نشست ابری ۶ کار مرج شد: TD-16، TD-11a، TD-18، F-licensing، F-lisp، F-ipfix-sflow. پیشرفت: ۳۸٫۶٪ بر حسب ساعت، ۵۲ از ۱۳۰ کار.
- سه شاخهٔ تأییدشده منتظر وابستگی‌های سرورند: TD-15 (بعد از TD-10a/b)، TD-22 (بعد از WEB-1/TD-10a)، F-snmp (بعد از TD-13).
- F-host-stack در بازبینی BLOCK شد (حذف بی‌صدای بقیهٔ services) و در دور اصلاح است.
- تصمیم در انتظار شما: PENDING-licensing-matrix (فهرست قابلیت‌های لایسنسی و کلید امضا)؛ فعلاً همه‌چیز آزاد است.
- gitleaks سه یافتهٔ قدیمی در تاریخچه (c2a8ab7، 7b2f438) دارد که `tools/ci.sh check` را برای همه قرمز می‌کند — تصمیم شما: allowlist یا بازنویسی تاریخچه.

## Detail
Merged: TD-16 144b458 · TD-11a 7f0ba7d (+bd88518 kit in table) · TD-18 a9ab2d6 · F-licensing f3cd427 · F-lisp 23818d6 · F-ipfix-sflow b24e3c0.
Approved, held (board order): TD-15 (branch worktree-agent-a52e6f5b9e6b01a70) · TD-22 (task/TD-18-22 1def4e6; CommitDialog item needs TD-10a) · F-snmp (task/F-snmp d4ba33a).
Running: F-host-stack fix round r1. Not started here: P11 (needs VPP host/strongSwan build), TD-17.
Decisions: D-141 memif/tapv2 library-only · D-142..D-145 licensing · D-146 ipfix ip4+ip6 default → ip4 + warning.
PENDING new: PENDING-licensing-matrix.
Follow-ups: TD-16 remainder (df2/df6 strict host bits, per-family ErrRetrieveUnsupported, Register(r,Env) adoption); df6 singletons declare ownership mode (drop lisp wrapper); SnmpStage.Close on shutdown; hsflowd packaging (P10); schema default ip6=false for flowprobe.
Host evidence owed (no VPP/PostgreSQL in cloud container): F-lisp, F-ipfix-sflow, F-licensing, F-snmp, TD-11a live reachability; `tools/ci.sh --base main` on host.
Env notes: task branches are local to this container (only the integration branch is pushed); turbo needs npm_config_manage_package_manager_versions=false here.
