# TD-16 — descriptors/kit (audit ARCH-06)

## What
- New `apps/agent/internal/descriptors/kit`:
  - `WriteFileAtomic(path, data, perm)`: temp `path.tmp` → write → **fsync file** → close → rename → **fsync dir** (`SyncDir`). Fixes the crash-loss in `df2.WriteFileAtomic` (no fsync at all) and the df6 `FileClaimStore.save` (no fsync).
  - `ParsePrefix`: the one policy — trim space, reject zones, reject IPv4-mapped IPv6, **reject host bits**; all errors wrap `kit.ErrBadPrefix`.
  - `MaskPrefix`: same checks but masks host bits (transitional, see open questions).
  - `ErrRetrieveUnsupported` (= `scheduler.ErrRetrieveUnsupported`) + `RetrieveUnsupported(name)`.
  - `Env{Client, Owner}`, `Ctor`, `Register(r, env, ctors...)`.
- Consolidation hunks (files_owned only): `dfkit/addr.go`, `df7/addr.go`, `vpn/addr.go` ParsePrefix → `kit.ParsePrefix` (they were already strict; error wrapping kept: Specf / `vpn:` prefix); `df2/addr.go`, `df6/addr.go` → `kit.MaskPrefix` (df6 still wraps ErrBadValue); `df2/claims.go` WriteFileAtomic and `df6/claims.go` save → `kit.WriteFileAtomic(…, 0o600)`.

## Verification
```
$ gofmt -l internal/descriptors            # (empty)
$ go vet ./internal/descriptors/...         # clean
$ go test ./...                             # all ok (apps/agent)
ok  ngfw/agent/internal/descriptors/kit
ok  ngfw/agent/internal/descriptors/dfkit / df2 / df6 / df7 / vpn (+subpackages)
```
One run of `go test ./...` showed `renderers/strongswan TestWatchResync` failing (45 s timing test, untouched package); it passed on rerun and on the base — load flake.

`tools/ci.sh check`: **FAILED at gitleaks on pre-existing history**, not on this branch's changes: 3 `generic-api-key` hits in base commits 7b2f438 (`renderers/rsyslog/rsyslog_test.go`, `renderers/strongswan/apply_test.go`) and c2a8ab7 (`packages/schema/src/domains/vpn.test.ts`). Later stages not reached. Manager needs to allowlist those fixtures or rewrite base history.

No VPP needed (pure Go/filesystem); no integration tests involved.

## Out of scope
- Flipping df2/df6 to reject host bits: `ip6_nd`, `lisp`, `sr`, `sr_mpls` KeyOf and `df2_test.go TestAddresses` rely on masking messy input (5 tests fail with the strict policy). Those files are not owned by TD-16.
- Replacing the per-family `ErrRetrieveUnsupported` vars in `df2/errors.go`, `df6/errors.go`, `dfkit/errors.go` (not owned; df7/vpn already alias the scheduler sentinel; the texts match so `scheduler.IsRetrieveUnsupported` accepts all).
- Moving families' `Register` functions onto `kit.Register`/`Env` (register files not owned).
- `dfkit/boot.go` (fsyncs file, no dir fsync) and `vpn/secret.go` (already fsyncs file + dir) — not owned.
- Reusing TD-9's helper: not merged; kit owns `SyncDir`, TD-9 can rebase onto it.

## Open questions
1. Strict host-bit policy for df2/df6: needs a follow-up row owning df2/df6 errors.go + the dependent descriptor tests/KeyOf (decide: reject in Validate, keep masking only in KeyOf?).
2. gitleaks hits in base history (above).
