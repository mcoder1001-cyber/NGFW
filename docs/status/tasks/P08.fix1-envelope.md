# TASK ENVELOPE — P08 fix round 1 (manager ngfw-46, 2026-09-24)
id: P08   branch: task/P08 (has commits — continue, do not restart)   worktree: /root/ngfw-wt/P08   base: main (now 4fcb624+)
read first: docs/status/tasks/P08-review.md (verdict BLOCK, 6022f0d) — it is your work list; then P08.md, P08-contract.md, P08-wip.md
slot: 1 → eval "$(tools/lab env 1)"  (w1, ports 3100/5100/9111, DB vrx_w1, tables 1000-1999); daemon-owner: none
step 0: `git merge main` into task/P08 (main has TD-3: ifsanitize + af_packet/interface descriptor changes, tools/app, ci.sh apps/cli step). Resolve conflicts
  keeping TD-3's sanitizer wiring intact; run `cd apps/agent && go build ./... && go test ./internal/...` before anything else.
fix (all, in this order):
  N1 (high) agent_integration_test.go hostDoc: Retrieve now always reports enabled/promiscuous → update the expected doc; TestAgentOnHost + TestAgentProcessOnHost green on slot 1.
  F1 (D-105) keep the old meaning of /state/interfaces items[].config (Retrieve view); running config goes in a new additive field; update the web UI (item.config users) and the e2e test; drop `actual` (reviewer: redundant); correct P08-contract.md (additive only). CLI untouched.
  F2 regenerate apps/cli/internal/api/operations_gen.go after F1 (`pnpm gen` then `make -C apps/cli gen`); `make -C apps/cli lint test` green.
  N2 test/topology/interfaces/run.sh:13 and interfaces_test.go:134 → slot run dir 0755 (never re-mode an existing dir to 0700; D-106/D-107).
  F5 tolerant.go Update: on inEffect(new) failure return ErrRecreate (or check before Delete) so no partial change escapes the journal; unit test.
  F4 (D-105) af_packet attaches only to Linux netdevs of kind veth — a semantic/validation-time check with a clear problem+json pointer, plus the agent-side guard.
  F3, I4 list neighbor + description tag as out of scope/deferred and the coretest fake edits in P08.md. I5 rename the column "Errors / drops".
  lows N3–N7: fix if each is < 20 min, else list them in P08.md "left for later" with file:line.
V24/I6 WARNING (D-107): af_packet deletes double-close fds even with veths down, and VPP's API currently stalls for seconds around af_packet create/delete (manager investigating).
  Minimise af_packet churn: run the topology test (test/topology/interfaces) ONCE at the end; if it fails with 502/504 during af_packet create/delete, paste the timings and the agent log excerpt and stop re-running — the manager decides.
  NRestarts before/after every host run pasted; if it rises, stop host runs and write it down.
CI: `TMPDIR=/tmp/g-p08 tools/ci.sh --base main` green (short TMPDIR). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to tools/app — never touch them.
time box: 4 h. WIP commits every 45 min; keep P08-wip.md current.
finish: P08.md updated (fix round 1 section: each finding → fix → pasted evidence), all committed on task/P08, worktree clean, final message = 10-line summary (last commit, CI result, topology result, NRestarts, what is left).
never: merge to main · restart/kill VPP · pkill · edit /etc/vpp · touch main or other worktrees · secrets in files
