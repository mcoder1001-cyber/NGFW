# TASK ENVELOPE — F-lb
id: F-lb   branch: task/F-lb   worktree: /root/ngfw-wt/F-lb   base: main@1d3ccf31   started: 2026-09-25T10:30
title: Wave C (day 13-15): Load Balancer plugin (GRE/NAT/L3DSR/maglev)
prompt: prompts/features/F-lb.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main + P08 in wave-B/C prep)   wbs: D7.9
scope: T3, minimal and honest — DF-7's lb descriptors exist: the `services.lb` contract, projection (conf/vip/as/intf-nat), read-only VIP state, flush action, the D-090 lb GC in the globals owner, API, UI with the write-only/GC notice, docs
merged deps you can rely on: P08 (desired/subsystems/projection patterns; `df7.SetBootStore` already installs the persisted DF-7 BootStore), DF-7 (descriptors/lb: `lb.Register`, `lb.RegisterGlobals`, `lb.DumpVIPs`, `lb.FlushVIP`; shared helper descriptors/df7) (+ on main by then: TD-2, TD-3, W-seed; wave-A features only if merged when you start — check `git log main`)
read first: prompts/features/F-lb.md · docs/status/wave-A-hotspots.md (§0 rules; A1 A2 A7 C1–C7 P1 P3 P4 P5 W1–W3) · docs/status/wave-BC-numbers.md § F-lb · docs/agent/descriptors/lb.md + policer.md (DF-7 conventions) · docs/status/tasks/DF-7-questions.md Q1 (lb GC options) · docs/vpp-code-track.md V15, V20 · docs/decisions/LOG.md D-052, D-063, D-064, D-071, D-076, D-080, D-082, D-087, D-090, D-104, D-109
slot: 2 → VRX_SLOT=2 VRX_TEST_PREFIX=w2 VRX_HTTP_PORT=3000+100×2 VRX_WEB_PORT=5000+100×2 VRX_METRICS_PORT=9100+10×2+1 VRX_AGENT_SOCKET=/run/vrx-test/w2/agent.sock VRX_PG_DATABASE=vrx_w2 VRX_VALKEY_DB=2 VRX_VPP_TABLE_BASE=2000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env 2)"`; rig prefix w2 → 10.2.{1,2}.0/24; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; VIPs only in 10.2.250.0/24 (+ a slot-unique IPv6 /64), application servers only inside 10.2.0.0/16 — lb installs both into table 0, the shared FIB)
daemon-owner: none
files you own exclusively: apps/agent/internal/descriptors/lb/** (gap-only: a proven defect or a missing helper, name the test) docs/agent/descriptors/lb.md apps/agent/internal/desired/lb*.go apps/agent/internal/subsystems/lb*.go apps/agent/internal/agent/rpc_lb*.go packages/schema/src/domains/ext/lb*.ts packages/schema/src/semantic/lb*.ts packages/schema/examples/lb-*.json packages/proto/test/fixtures/lb-*.json apps/api/src/features/lb/** apps/api/test/e2e/lb* apps/web/src/domains/services/lb/** apps/web/src/locales/{en,fa}/lb.json docs/user/services/lb.md test/topology/lb/** docs/status/tasks/F-lb*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-lb` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-lb.md and say so in the questions file); never reorder or reformat other lines): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["services"]` — exists by wave C; if not, whoever lands first adds the key — `lb.vip`/`lb.as`/`lb.intf-nat` (+ `lb.conf` in the globals owner) one per line; one `lb.Register` line and `lb.RegisterGlobals` only when `env.GlobalsOwner` — **never `df7/registry.Register`** (it registers every DF-7 family; duplicates panic, D-030); the GC hook in your own subsystems/lb.go) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (no Action case; `LbState`/`LbFlushVip` live in your rpc_lb.go) · C1 packages/schema/src/domains/services.ts (one key line `lb`; sub-schema in your ext file) · C2 packages/schema/src/semantic/index.ts (one spread) · C3 packages/schema/src/index.ts (export of your ext file) · C4 new `lb-*.json` fixtures only · C5 packages/proto/vrx/v1/dataplane.proto (`LbState`, `LbFlushVip` under the service anchor; `lb = 11` one line in `ServicesConfig`; messages in a `// ----- F-lb -----` section) · C6 docs/contracts/proto.md (`### F-lb: LbState, LbFlushVip`) · C7 generated files (regen, never hand-merged: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`) · P1 apps/api/src/app.module.ts · P3 `POST /api/v1/actions/lb/vips/{name}/flush` as a static route in your own controller (never edit apps/api/src/actions/**) · P4 apps/api/src/agent/agent.client.ts (`lbState`, `lbFlushVip`) · P5 apps/api/src/testing/fake-agent.ts (UNIMPLEMENTED stubs on the contract commit; real fake in features/lb/fake.ts) · W1 apps/web/src/router.tsx · W2 apps/web/src/nav/nav.ts (+nav.test.ts: `services` into BUILT_DOMAINS only if not there yet) · W3 apps/web/src/i18n.ts · services domain page + tab registry apps/web/src/domains/services/{ServicesPage.tsx,tabs.ts} (W-seed shell; one line — if the shell is not on main, do NOT create it: own route + NavItem under W1/W2, the manager folds it in) · apps/agent/internal/renderers/ALLOWLIST.md (one row: the constant `cli_inband` lb GC command, D-090 — not a binary, documented there by decision) · A7 docs/vpp-code-track.md (a V20 follow-up note only if you learn something new)
contract numbers: docs/status/wave-BC-numbers.md § F-lb — **ServicesConfig 11 `lb`**, new `Lb*` messages, RPCs `LbState`, `LbFlushVip`; nothing else (no ActionRequest member, no EventKind); commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-lb-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned), apps/agent/internal/descriptors/df7/** (DF-7 shared helper + registry — read-only), every other DF-7 family (policer/qos = F-qos-flat; mpls, span, lldp, bfd, vrrp, igmp = their feature tasks), apps/agent/internal/descriptors/{nat44ed,cnat,natcommon}/** (NAT tasks — NAT44-ED load-balanced static mappings and CNAT VIPs are theirs), apps/agent/internal/agent/{agent,service,state,ifstate,metrics,telemetry,events}.go + apps/agent/cmd/** (A5 core), apps/agent/internal/desired/interfaces.go (A3), apps/api/src/actions/** (generic bridge, F-vrf-static-ecmp), apps/api/src/{auth,users,secrets,commit,datastore}/**, tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
host facts: `lb_plugin.so` present; binapi `lb` (`lb_conf`, `lb_add_del_vip_v2`, `lb_add_del_as`, `lb_add_del_intf_nat4/6`, `lb_vip_dump`, `lb_as_dump`, `lb_flush_vip`) · V20: VPP frees deleted VIPs/ASes only in a CLI-triggered GC, lb enum fields not byte-swapped (the descriptor swaps + runtime enum check), `lb_vip_details` partly garbage → all lb objects are write-only; every VIP delete/recreate leaves a "removed" VIP and each AS's recursive `/32` in **table 0** until a GC or a VPP restart · no data NICs bound (D-026): af_packet rig only (D-010)
obligations accumulated from decisions (all must be honoured and listed in F-lb.md with evidence): D-104 use DF-7's descriptors, do not rebuild · D-063/D-076/D-080 lb objects are write-only: the VIP/AS boot records and the `intf-nat` applied-once record live in the persisted DF-7 BootStore; `lb.DumpVIPs` is state for the API, never Retrieve · D-071 `lb.conf` is a VPP global: only the globals owner registers it; slot agents require it · D-090 (2) after a transaction that deleted or recreated lb objects the **globals owner** runs VPP's lb GC once through a constant `cli_inband` command (no user input; DF-7-questions Q1 option a) + its ALLOWLIST row; unit-test that a slot agent never sends it · D-082 a test that runs the GC or changes `lb.conf` is VPP-global: own opt-in var (`VRX_LB_GLOBALS=1`), `flock -x /run/lock/vrx-globals.lock`, previous `lb.conf` values restored (not VPP defaults), manager window only · D-064 crash → opt-in at once + V-item · DF-7 precedent: the lb host test is opt-in (`VRX_LB_HOST=1`, like `VRX_DF7_LB=1`) and run once for the evidence; list the leftovers it creates in F-lb.md
coordination: (1) F-qos-flat (parallel) also builds on DF-7 — `descriptors/df7/**` is read-only for both · (2) F-kea-dhcp-relay, F-unbound-chrony-syslog, F-host-stack, F-snmp, F-qos-flat, F-ipfix-sflow share `Domains["services"]` and the services page · (3) F-nat44-ed-sessions owns NAT44-ED load-balanced static mappings (`nat.loadBalancedMappings` — not this plugin); F-det44-map-dslite-cnat owns CNAT VIPs; F-vrrp-config-sync owns VIP HA
V19/V24 SAFETY (D-095, D-101): the GRE evidence is optional — if you send a flow from ns-w2-lan to the VIP, run TD-3's preflight on the rig interfaces first and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots; paste `vppctl show lb vips verbose`
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-lb.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-lb-wip.md current
CI: `TMPDIR=/tmp/g-w2 tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-lb.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite), by PID · lab lock released · vrx_w2 dropped · your VIPs/ASes deleted through the agent (`vppctl show lb vips verbose` pasted; "removed" leftovers listed, V20) · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-lb-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own

## Manager addendum (2026-09-25 10:30, ngfw-46) — rules that landed after this envelope was written
- **TD-11b (on main):** every descriptor you register declares `RecordsNoOwnership()` (finds its objects by key/tag) or `CheckPersistent() error` (records claims over the persisted Wiring store); the agent refuses to start otherwise. Creates are claim-first (dfkit ClaimFirst).
- **TD-11c (approved, merging):** every interface creator registers `iface.RegisterKind` or provides the `interface/<name>` KeyProvider and removes itself from the guard allowlist (mpls-tunnel → F-mpls-srmpls; gre/ipip/vxlan… → F-tunnels).
- **TD-8 seams (on main):** use Env.Publish / RequestResync / dynamic desired sources / metrics collector / Wiring.IDRange — never fork them; the agent refuses to start without an ID range (TD-8b).
- **TD-23 (approved, merging):** fake-agent actions via `registerActionHandler(kind, fn)`; coretest hooks via `RegisterExtension(name, fn)` (and `RegisterFeatureIsEnabled` for feature_is_enabled) — until it is on main, keep your hook in your own file so it becomes one registration line.
- **D-132:** no UI timer < 30 s on anything that walks VPP; Refresh button; the agent serialises full walks (one in flight, UNAVAILABLE after 3 s).
- **D-137/D-139:** never call VPP dns.api; **D-128:** never `show trace`/`trace add`/`clear trace`; **D-126:** no classify sweeps by index.
- **WEB-1 (approved, merging):** SchemaForm presence toggle — never call `dropPhantomOptionals`.
- **Host runs:** af_packet creates on the shared VPP fail until TD-25 merges (approved, first in the merge queue). Do code, unit tests, fake-VPP tests and CI first; the manager tells you when host runs are open. VPP NRestarts is 2 (baseline).
- **Usage limit:** if you are stopped by it, the manager resumes you; commit early and often.
