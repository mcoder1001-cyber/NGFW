# TASK ENVELOPE — F-ipfix-sflow
id: F-ipfix-sflow   branch: task/F-ipfix-sflow   worktree: /root/ngfw-wt/F-ipfix-sflow   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave C (day 13-15): IPFIX/flowprobe + sFlow exporters
prompt: prompts/features/F-ipfix-sflow.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main + P08 in wave-B/C prep)   wbs: D7.6
scope: DF-8's ipfix/flowprobe/sflow descriptors and the `services.ipfix` schema exist: projection (exporter 0 + further exporters, flowprobe params/interfaces, sflow global/interfaces), one semantic rule, `/state/ipfix`, UI with the hsflowd banner, docs; hsflowd packaging and NAT logging enables fenced
merged deps you can rely on: P08 (desired/subsystems/projection patterns, `interface/<name>` aliases, `Wiring.ClassifyStore()`), DF-8 (descriptors/ipfix: `ipfix.Register(r, c, store, …)` + `RegisterGlobals` (exporter 0); descriptors/flowprobe: `Register` + `RegisterGlobals` (params); descriptors/sflow: `Register` + `RegisterGlobals` (global)) (+ on main by then: TD-2, TD-3, W-seed; wave-A features only if merged when you start — check `git log main`)
read first: prompts/features/F-ipfix-sflow.md · docs/status/wave-A-hotspots.md (§0 rules; A1 A2 C2 C4–C7 P1 P4 P5 W1–W3; C1 only with a gap) · docs/status/wave-BC-numbers.md § F-ipfix-sflow · docs/agent/descriptors/{ipfix,flowprobe,sflow}.md · docs/vpp-code-track.md V16, V17 · docs/decisions/LOG.md D-063, D-064, D-065, D-069, D-071, D-076, D-077, D-080, D-082, D-087, D-104
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; test collectors = Go UDP listeners on 127.0.0.1 / the rig address, IPFIX 3<SLOT>71, sFlow 3<SLOT>72; flowprobe/sflow only on your slot interfaces)
daemon-owner: none (hsflowd is not installed and is not part of this task)
files you own exclusively: apps/agent/internal/descriptors/ipfix/** apps/agent/internal/descriptors/flowprobe/** apps/agent/internal/descriptors/sflow/** (all gap-only: a proven defect or a missing helper, name the test) docs/agent/descriptors/ipfix.md docs/agent/descriptors/flowprobe.md docs/agent/descriptors/sflow.md apps/agent/internal/desired/ipfix_sflow*.go apps/agent/internal/subsystems/ipfix_sflow*.go apps/agent/internal/agent/rpc_ipfix_sflow*.go packages/schema/src/domains/ext/ipfix-sflow*.ts (only with a gap) packages/schema/src/semantic/ipfix-sflow*.ts packages/schema/examples/ipfix-sflow-*.json packages/proto/test/fixtures/ipfix-sflow-*.json apps/api/src/features/ipfix-sflow/** apps/api/test/e2e/ipfix-sflow* apps/web/src/domains/services/ipfix-sflow/** apps/web/src/locales/{en,fa}/ipfix-sflow.json docs/user/services/ipfix-sflow.md test/topology/ipfix-sflow/** docs/status/tasks/F-ipfix-sflow*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-ipfix-sflow` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-ipfix-sflow.md and say so in the questions file); never reorder or reformat other lines): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["services"]` — exists by wave C; if not, whoever lands first adds the key — ipfix.*/flowprobe.*/sflow.* names one per line; `ipfix.Register` (with `Wiring.ClassifyStore()`), `flowprobe.Register`, `sflow.Register` one line each; the three `RegisterGlobals` only when `env.GlobalsOwner`) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (no Action case; `IpfixState` lives in your rpc_ipfix_sflow.go) · C1 packages/schema/src/domains/services.ts (only with a gap — ext file + one key line) · C2 packages/schema/src/semantic/index.ts (one spread) · C3 packages/schema/src/index.ts (only with an ext file) · C4 new `ipfix-sflow-*.json` fixtures only · C5 packages/proto/vrx/v1/dataplane.proto (`IpfixState` under the service anchor; messages in a `// ----- F-ipfix-sflow -----` section) · C6 docs/contracts/proto.md (`### F-ipfix-sflow: IpfixState`) · C7 generated files (regen, never hand-merged: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`) · P1 apps/api/src/app.module.ts · P4 apps/api/src/agent/agent.client.ts (`ipfixState`) · P5 apps/api/src/testing/fake-agent.ts (UNIMPLEMENTED stub on the contract commit; real fake in features/ipfix-sflow/fake.ts) · W1 apps/web/src/router.tsx · W2 apps/web/src/nav/nav.ts (+nav.test.ts: `services` into BUILT_DOMAINS only if not there yet) · W3 apps/web/src/i18n.ts · services domain page + tab registry apps/web/src/domains/services/{ServicesPage.tsx,tabs.ts} (W-seed shell; one line — if the shell is not on main, do NOT create it: own route + NavItem under W1/W2, the manager folds it in) · A7 docs/vpp-code-track.md (V16/V17 follow-ups only)
contract numbers: docs/status/wave-BC-numbers.md § F-ipfix-sflow — RPC `IpfixState` (+ its messages); `IpfixService` 4 / `.Exporter` 9 / `.Flowprobe` 7 / `.Sflow` 9 are reserved **only for a gap**; nothing else (no ActionRequest member, no EventKind); commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-ipfix-sflow-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned), apps/agent/internal/descriptors/{classify,df2,dfkit}/** (F-rpf-adl-pbr / shared helpers — read-only), apps/agent/internal/descriptors/{nat44ed,nat44ei,natcommon,det44}/** and apps/agent/internal/desired/nat*.go (NAT tasks — their IPFIX logging enables are theirs), apps/agent/internal/descriptors/{pcap,trace}/** (F-capture-trace), apps/agent/internal/agent/{agent,service,state,ifstate,metrics,telemetry,events}.go + apps/agent/cmd/** (A5 core), apps/agent/internal/desired/interfaces.go (A3), apps/api/src/{auth,users,secrets,commit,datastore}/**, tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
host facts: `flowprobe_plugin.so`, `sflow_plugin.so` present; ipfix export is vnet core; binapi `ipfix_export`, `flowprobe`, `sflow` · **`hsflowd`, `nc`, `socat` are NOT installed** (no installs): VPP's sflow plugin samples but exports nothing without hsflowd; collectors in tests are Go UDP listeners · `tcpdump`/`tshark` present for optional evidence · no data NICs bound (D-026): af_packet rig only (D-010)
obligations accumulated from decisions (all must be honoured and listed in F-ipfix-sflow.md with evidence): D-104 use DF-8's descriptors, do not rebuild · D-071 exporter 0 (`ipfix.default-exporter`, IPv4 collector only — the only one flowprobe uses), `flowprobe.params` and `sflow.global` are VPP globals: only the globals owner registers them; slot agents require them · D-082 the collector evidence changes exporter 0 = a VPP-global change: own opt-in var `VRX_IPFIX_GLOBALS=1`, `flock -x /run/lock/vrx-globals.lock`, save and restore exactly the previous exporter-0/flowprobe/sflow values (never VPP defaults), manager window; otherwise skip with the reason · D-063/V16 `ipfix.classify-*` are write-only and get no UI · V17 `sflow.interface` learns hw→sw in Create; after an agent restart the first resync re-creates once — show that one re-create in the log excerpt · DF-8: `flowprobe.params` is refused by VPP while **any** owner has a flowprobe interface — a blocked change is a documented skip, not a failure · D-065/D-069 interface refs `interface/<name>` (logical names) · D-064 crash → opt-in at once + V-item
coordination: (1) NAT IPFIX logging: EI's `nat44-ei.ipfix` is F-nat44-ei-64-66-nptv6's; the NAT44-ED enable (`nat_ipfix_enable_disable`) has **no owner** (DF-3 → "DF-8", DF-8 did not build it; ED envelope → "DF-8 / F-ipfix-sflow"; F-det44 → you) — do not build it unless the manager assigns it; write it in the questions file. Exporter 0 is yours; NAT loggers send through it · (2) hsflowd packaging → P10 follow-up (record it) · (3) F-kea-dhcp-relay, F-unbound-chrony-syslog, F-host-stack, F-snmp, F-lb, F-qos-flat share `Domains["services"]` and the services page
V19/V24 SAFETY (D-095, D-101): before ANY traffic through the rig (flow records need packets), run TD-3's preflight on the rig interfaces' sw_if_index and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots; paste `vppctl show flowprobe interface`, `show flowprobe params`, `show sflow`
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-ipfix-sflow.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-ipfix-sflow-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-ipfix-sflow.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite/collectors), by PID · lab lock released · vrx_w<SLOT> dropped · no w<SLOT> flowprobe/sflow interfaces or exporters left (Retrieve + `vppctl show flowprobe interface` / `show sflow` pasted); globals restored if you changed them · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-ipfix-sflow-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
