# TASK ENVELOPE — TD-8b agent seams follow-up (manager ngfw-46, D-129 Q3 + TD-8 verify V1–V5)
id: TD-8b   branch: task/TD-8b   worktree: /root/ngfw-wt/TD-8b   base: main@d3a7c266 (TD-8, TD-7, TD-11b merged)   started: 2026-09-25T04:10
slot: 5 → eval "$(tools/lab env 5)" — ONLY for the one topology/interfaces run (rig prefix w5, VPP table base 5000); TD-10b shares slot 5 but uses only the API/DB side — never touch vrx_w5 / Valkey 5 / port 3500
why: TD-8's verify (docs/status/tasks/TD-8-verify.md "Findings") — V1 must land before F-mpls-ldp and F-igmp-mfib; Q3 (D-129) flips the ID range from fail-open to refuse-start-up before the first id-allocating family merges.
scope:
  V1 (MEDIUM) per-key quarantine in apps/agent/internal/agent/dynsource.go (fallback :262-285, syncLocked :396-420): on a culprit key, rerun with the source's KVs minus the culprit's change (Create → drop the key; Update → desire the planned op's Old; Delete → keep Old), keep the source's descriptors in scope, quarantine only that key, report it as today, retry only it with backoff. Still declarative — no imperative VPP calls. Tests: the verify's probes G (after a VPP restart one rejected + one creatable → the creatable one IS restored) and H (commit deletes loop702 + adds a rejected dynamic object → APPLIED with both deletions), each failing on the base. Fix the headline text in seams.go:225 / README §S1 to match.
  V3 one sentence in the SyncFunc doc: "… or from any goroutine they start".
  V4 culprit matching: blame a source only when every key the verify error names belongs to that source (test).
  Q3 flip + V2: start-up is refused without an ID range (ErrNoIDRange) — the message must cite shared-host-rules.md §12 (V5: §11 is the trace ban, D-128); add `df7.WithIDs(*IDRange)`; tools/app gets `VRX_VPP_TABLE_BASE=13000` in its env block (EDIT THE FILE ONLY — never run tools/app, never restart the product agent, never touch ports 3000/8080/9101 or /run/vrx/agent.sock); test/topology/interfaces passes VRX_VPP_TABLE_BASE through; a P10 note in docs/tech-debt.md for the packaged unit (`all`); add the feature-prompt rule to prompts/FEATURE-TEMPLATE.md: "an id-allocating family takes its range only from w.IDRange(), never nil or a missing option; its test asserts NoIDs() owns nothing".
  Add the §12 text to docs/lab/shared-host-rules.md (the ID-range rule, from TD-8.md "§11").
files you own: apps/agent/internal/agent/dynsource.go apps/agent/internal/subsystems/seams*.go apps/agent/internal/descriptors/df7/options*.go (WithIDs only) tools/app (env line only) test/topology/interfaces (env passthrough only) docs/lab/shared-host-rules.md (§12 only) prompts/FEATURE-TEMPLATE.md (one rule) docs/tech-debt.md (one P10 line) docs/status/tasks/TD-8b*
proof: unit tests with base-first failures (pre-fix run pasted); `cd apps/agent && go test -race -count=1 ./internal/agent/... ./internal/subsystems/... ./internal/descriptors/df7/...`; ONE topology/interfaces run on slot 5 under `tools/lab lock` with VRX_VPP_TABLE_BASE passed through, VPP NRestarts before/after unchanged.
CI: `TMPDIR=/tmp/g-td8b tools/ci.sh --base main`.
GIT RULE: git only as `git -C /root/ngfw-wt/TD-8b …`; NEVER in /root/ngfw.
time box: 4 h. finish: docs/status/tasks/TD-8b.md (each item: commit, test, pre-fix failure, evidence), commit; final message = 6 lines.
never: merge · show trace / trace add / clear trace · classify sweeps · VPP restart · pkill · run tools/app · real --apply
addendum (04:20): (a) from TD-9's worker — give dynamic-source syncs a deadline (bounded like TD-9's transaction clock; TD-9 is in review, not merged: VRX_AGENT_VPP_REPLY_TIMEOUT 30 s per reply, 5 min per transaction — use a local constant and note it for the rebase). (b) TD-11c (in verify) changes syncLocked by 7 lines to use its claim-flush batch helper: read `git -C /root/ngfw-wt/TD-8b diff main task/TD-11c -- apps/agent/internal/agent/dynsource.go` and keep your change compatible; whichever of TD-11c/TD-8b merges second rebases.
addendum (04:30, TD-11c verify): keep TD-11c's claim-flush batch bracket around the source apply in syncLocked (dynsource.go:410-428 on task/TD-11c) — the per-key quarantine rerun must run inside the same bracket.
