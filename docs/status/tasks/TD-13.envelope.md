# TASK ENVELOPE — TD-13 scheduler tier-3 Validator + VPP→daemon stage (manager ngfw-46, audit ARCH-02, D-125)
id: TD-13   branch: task/TD-13   worktree: /root/ngfw-wt/TD-13   base: main@4f472cc7   started: 2026-09-25T04:35
slot: none needed (unit tests with fake descriptors). No host runs.
why: CRITICAL PATH — P11, P12, F-kea-dhcp-relay, F-unbound-chrony-syslog, F-snmp and F-host-acl-nftables merge after TD-13. Today a daemon's config is checked only inside its Create (D-109(d)), i.e. AFTER the VPP objects of the same transaction changed; DryRun calls no renderer (service.go:599-622); the scheduler has no validation hook (reconciler.go:35-73).
scope:
  1. scheduler: an OPTIONAL `Validator` interface a descriptor may implement — `Validate(ctx, key string, value proto.Message, view ReadOnlyView) error` (name the view after what the scheduler already passes to Create; keep it read-only). Called (a) in Plan/DryRun for every Create/Update op of that descriptor, (b) once more before the first Create of the transaction. A validator error fails the transaction BEFORE any VPP write, with a problem that names the key (and a JSON pointer when the value carries one). Validators are bounded (ctx deadline; reuse TD-9's constants if merged, else a local 30 s constant noted for rebase) and must be side-effect free (no files outside a private temp dir, no daemon reloads) — document this contract in scheduler/descriptor.go and docs/agent/*.
  2. stage ordering: a descriptor declares its stage — `StageVPP` (default) or `StageDaemon` — and the executor orders all VPP-stage creates/updates before daemon-stage ones and deletes in reverse, INSIDE the existing dependency order (a real dependency always wins; the stage only breaks ties). Rollback stays the exact reverse.
  3. DryRun: runs the validators and reports their errors in the existing DryRun answer. If that needs a new proto field or outcome detail, it is a contract change: one `contract(proto)` commit, additive only, and draft the D-entry text in TD-13.md (the manager writes it into LOG).
  4. An example adopter in tests only: a fake daemon descriptor with `Validate` that shells nothing, plus a test that a failing validator leaves the fake VPP untouched (zero API calls), and that stage ordering puts daemon creates after VPP creates when they are independent.
  5. docs: the adoption recipe for F-kea (kea-dhcp4 -t), F-unbound (unbound-checkconf), F-host-acl (nft -c), P12 (vtysh -C / frr-reload --test), P11 (swanctl --load-all --noprompt dry equivalents) — one line each in TD-13.md and prompts/FEATURE-TEMPLATE.md ("a daemon descriptor implements Validator and declares StageDaemon"). Do NOT edit those feature branches.
coordination: TD-9 (in fix round) and TD-11c (approved, merging soon) also touch reconciler.go / service.go — keep your hunks small and separate (new file scheduler/validator.go + stage.go where possible); whichever merges second rebases. Read `git -C /root/ngfw-wt/TD-13 diff main task/TD-9 -- apps/agent/internal/scheduler apps/agent/internal/agent/service.go` and the same for task/TD-11c before you start, and don't fight their structure.
architecture: the agent stays declarative (validators only read); binapi-only VPP names; no secrets in problem text or logs (validator errors from daemons may echo config — redact secret leaves).
files you own: apps/agent/internal/scheduler/{validator.go,stage.go,*_test.go (new)} + small hunks in scheduler/{descriptor,reconciler}.go and agent/service.go (DryRun) + packages/proto (additive, only if needed) + docs/agent/scheduler*.md + prompts/FEATURE-TEMPLATE.md (one rule) + docs/status/tasks/TD-13*
proof: every new test fails on the base (paste); `cd apps/agent && go test -race -count=1 ./internal/scheduler/... ./internal/agent/... ./internal/subsystems/...`; `TMPDIR=/tmp/g-td13 tools/ci.sh --base main`.
GIT RULE: git only as `git -C /root/ngfw-wt/TD-13 …`; NEVER in /root/ngfw.
time box: 5 h. finish: docs/status/tasks/TD-13.md (+ D-entry draft) and TD-13-questions.md; commit; final message = 6 lines.
never: merge · host/VPP runs · show trace · pkill · tools/app · ports 3000/8080/9101
