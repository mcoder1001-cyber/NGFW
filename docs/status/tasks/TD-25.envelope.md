# TASK ENVELOPE — TD-25 ifsanitize: stop the classify-pool hole ratchet (manager ngfw-46; URGENT — blocks every af_packet create on the shared VPP)
id: TD-25   branch: task/TD-25   worktree: /root/ngfw-wt/TD-25   base: main   started: 2026-09-25T05:15
slot: 1 → eval "$(tools/lab env 1)" — ONLY for the host proof (rig prefix w1), under `tools/lab lock`, NRestarts before/after (now 2)
incident (F-acl worker, 05:05): since ~05:05 every af_packet create on the shared VPP fails: `no clean sw_if_index obtained (VPP V19 quarantine): placeholder cap reached before every freed classify table index was resurrected (64 placeholders, cap 64 for 122 freed indices seen)`. `show classify tables` = none configured → the classify pool has ~122 FREE indices since the 04:27 VPP restart.
root cause (manager's reading — verify it): Sanitize (apps/agent/internal/vpp/ifsanitize/sanitize.go:30-50, 92-103, FreshRun=8 at :275) resurrects EVERY free index with placeholder tables, proves the free list empty by popping FreshRun fresh indices, then deletes all placeholders. vppinfra pools never shrink, so every fresh index it popped becomes a permanent hole when deleted → the free list grows by up to 2×FreshRun per create-phase run → after a few dozen creates it exceeds MaxPlaceholders (64) and every create fails closed. A ratchet: raising the cap only postpones it, and each create gets slower.
fix (this is tech-debt "TD-3 re-review M3", now due):
  1. Resurrect ON DEMAND only: read the new/deleting interface's bindings exactly where VPP offers a readback (classify_table_by_interface: ip4/ip6/l2 input-ACL tables; the in/out ACL, policer-classify and flow-classify dumps; l2 feature bits via the L3-mode reset already in place; IPsec SPD via ipsec_spd_interface_dump) — find what each binding names; only if a binding names a FREED index, pop the LIFO free list until that index comes back (placeholders only for the indices freed after it), unbind through it, delete the placeholders. NEVER pop fresh indices "to prove the free list empty" — if the target index is not in the free list it exists and needs no resurrection.
  2. Kinds with no readback: keep a bounded probe, but it must never create a fresh index (stop at the first fresh pop and delete it LAST so it goes back on top of the free list → net pool growth 0); document each such kind.
  3. Keep the quarantine fallback (Acquire) for Unclearable; keep fail-closed semantics; keep the metrics (update names/help if semantics change).
  4. A RATCHET TEST on a fake that models vppinfra pools exactly (LIFO free_indices, vector never shrinks): 200 create/delete cycles of interfaces with and without stale bindings → the classify pool's vector length and free-list length stay bounded (assert equal before/after), and every stale binding is still cleared. Plus the existing V19/V21/V23 tests still pass. The test must fail on the base (paste).
  5. Host proof on slot 1: with the shared VPP's current ~122+ free indices, 20 af_packet create/delete cycles through the agent succeed; a probe before/after shows the classify free list did NOT grow (read it via the placeholder-free method you implement, or `vppctl show classify tables verbose` — read-only, never `show trace`); NRestarts unchanged.
  6. docs: update the sanitize.go header, docs/vpp-code-track.md V19/V23 "fix" column, and D-095/D-105 references; draft the D-entry text in TD-25.md.
constraints: binapi only; touches only the given sw_if_index + its own placeholders; D-126 (no classify sweeps by index), D-128 (no trace), D-132 (no fast polling); never restart VPP; never edit /etc/vpp.
files you own: apps/agent/internal/vpp/ifsanitize/** + sanitizetest/** + docs/vpp-code-track.md (V19/V23 rows) + docs/status/tasks/TD-25*
CI: `TMPDIR=/tmp/g-td25 tools/ci.sh --base main`.
GIT RULE: git only as `git -C /root/ngfw-wt/TD-25 …`; NEVER in /root/ngfw.
time box: 4 h (urgent: commit a working fix as early as possible; polish after). finish: TD-25.md with evidence + D-entry draft; commit; final message = 6 lines.
never: merge · show trace / trace add / clear trace · classify sweeps by index · VPP restart · pkill · tools/app · ports 3000/8080/9101
