# TASK ENVELOPE — F-isis-rip
id: F-isis-rip   branch: task/F-isis-rip   worktree: /root/ngfw-wt/F-isis-rip   base: main@<BASE>   started: <STARTED>
title: Wave B (day 10-12): IS-IS, RIPv2/RIPng via FRR
prompt: prompts/features/F-isis-rip.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed on task/prep-rest against main, task/P08, task/W-seed and the P12 envelope)   wbs: D3.6, D3.7
scope: IS-IS (v4+v6) and RIPv2/RIPng via FRR on the existing `routing.isis` / `routing.rip` model + contract (IS-IS per-interface families, area/domain password refs, `rip.version`, a new `routing.ripng` key) — FRR `isis`/`rip`/`ripng` sections in P12's FRR singleton, adjacency/peer state + IS-IS adjacency events, API, UI (IS-IS page, RIP page), docs; IS-IS over linux-cp needs VPP's OSI punt (`lcp_osi_proto_enable`, VPP-global) → one small globals-only descriptor; learned routes reach VPP through linux-nl (P12's pattern)
merged deps you can rely on: P08, P12, W-seed (+ the wave-B/C anchor pass, docs/status/wave-BC-numbers.md "Pack rules")
  - P08: desired/ + subsystems/ + projection patterns; the "routing protocols … RF-1" warning hunk in projection.go (seam S3); `subsystems.SlotIDRange()` (W-seed)
  - P12: the FRR singleton descriptor rendering every registered RF-1 section (name in P12.md), the linux-cp mapper `internal/lcpmap`, the LCP pair model, the routing/BGP state RPC, the linux-nl test plan (P12-questions.md), `renderers/frr/policy`, the `proto` filter on F-vrf-static-ecmp's `/state/routes`
  - through P12: RF-1 (sections, state readers, pollers, `rc.Secret` + redaction incl. `isis area-password/domain-password` and `ip rip authentication string`, `rc.MapInterface`, `frrtest`), DF-8 (`lcp.itf-pair`), F-vrf-static-ecmp
  - also on main: TD-2, TD-3 (V19 sanitizer + preflight), TD-5 if merged
read first: prompts/features/F-isis-rip.md · docs/status/wave-BC-numbers.md (Pack rules, section F-isis-rip) · docs/status/wave-A-hotspots.md (§0 rules; ids A1 A2 C1–C7 P1 P4 P5 P6 W1–W3 A7) · apps/agent/internal/renderers/frr/README.md · docs/agent/renderers/frr.md · docs/status/tasks/P12.md + P12-questions.md · docs/agent/descriptors/lcp.md · docs/vpp-code-track.md V1 · docs/decisions/LOG.md D-045, D-051, D-063, D-071, D-072, D-076, D-080, D-082, D-087, D-089, D-094, D-095, D-101, D-109 · docs/decisions/PENDING-secret-channel.md
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100·<SLOT> VRX_WEB_PORT=5000+100·<SLOT> VRX_METRICS_PORT=9100+10·<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock
  - source of truth: `eval "$(tools/lab env <SLOT>)"`
  - rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; announced test prefixes inside 10.<SLOT>.0.0/16 (+ one slot-unique IPv6 /48 for RIPng/IS-IS v6); any kernel VRF/table id in <SLOT>000–<SLOT>999; IS-IS NET system ids derived from the slot (e.g. 0000.0000.0<SLOT>xx)
  - test agents run with VRX_GLOBALS_OWNER=0 (D-071)
  - slots 1–11 only; 12 is CI
daemon-owner: frr — slot test instances only through `frrtest` (pathspace w<SLOT>, /run/vrx-test/w<SLOT>/frr, symlink /run/frr/w<SLOT>, lock /run/vrx-test/w<SLOT>/frr.lock; `Daemons: isisd, ripd, ripngd` on top of mgmtd/zebra/staticd); `frr.service` stays disabled and stopped; nothing under /etc/frr is written; other FRR tasks may run in their own slots at the same time only after the manager has logged per-slot-instance FRR ownership (wave-BC-launch-queue.md M3; until then one FRR task at a time). A zebra in the ROOT netns (linux-nl FIB proof) is host-wide in any case: one slot at a time, manager window (P12 coordination 4)
obligations:
  - D-104: use, do not rebuild — RF-1's framework and `frrtest`, P12's singleton/mapper/LCP pairs/state RPC. The rules `routing.interface-exists` (IS-IS/RIP interfaces) and `routing.route-map-exists` (redistribute) and RIP's network uniqueness already exist: test them, extend to `ripng` in YOUR semantic file, never edit semantic/routing.ts
  - D-051 + D-072 (second half): IS-IS area/domain passwords (and the optional RIP key) are `password/<name>` refs resolved only through `rc.Secret`; unit test for `<redacted>` in DryRun, errors and Retrieve; PENDING-secret-channel: the end-to-end secret step waits, tests use a slot-local fixture resolver with `VRX_TEST_PSK_F-isis-rip_<n>` literals
  - `lcp.osi-proto` (new, package `descriptors/lcp_osi`): binapi `lcp_osi_proto_enable` / `lcp_osi_proto_get` (VPP-global, enable-only — VPP has no disable) → registered only on the globals owner (D-071), Retrieve from `lcp_osi_proto_get`, Delete = documented no-op (the `sr-mpls.endpoint-color` precedent), write the "no disable" gap as `### V-new (F-isis-rip)`; slot agents only *require* it (clear error if IS-IS is configured and the OSI punt is off)
  - host IS-IS FIB proof needs that global: check `lcp_osi_proto_get` first; enable it only behind an opt-in (`VRX_FISIS_GLOBALS=1`) under `flock -x /run/lock/vrx-globals.lock` (D-082) in a manager window — it cannot be undone before a VPP restart, so ask in the questions file first; without the window: IS-IS renderer + state with the FIB step skipped with the reason, plus the fake-client test
  - RF-1 review M3: scoped readers only (`show isis neighbor json`, `show isis database json` paged, `show ip rip status` / `show ipv6 ripng status` parsed); never a full-RIB dump, never the LSDB in Retrieve
  - canonical `show running-config` form; Linux names from `rc.MapInterface`; unmapped interface = error; seam S2 for the per-interface `ip router isis` / `ipv6 router isis` lines (P12's hook, else own `interface` block proven converged, else questions file)
  - D-072: IS-IS/RIP never program VPP; routes arrive through linux-nl
files you own exclusively:
  - apps/agent/internal/renderers/frr/{isis,rip}/** (sections `isis` 470, `rip` 420, `ripng` 430 — RIPng lives in the rip package), docs/agent/renderers/frr-{isis,rip}.md
  - apps/agent/internal/descriptors/lcp_osi/**, docs/agent/descriptors/lcp_osi.md, apps/agent/internal/descriptors/core/coretest/isis_rip*.go (A6: new file only)
  - apps/agent/internal/subsystems/isis_rip*.go (blank imports + `lcp_osi` registration helper), apps/agent/internal/desired/isis_rip*.go (the `lcp.osi-proto` requirement object, if projected), apps/agent/internal/agent/rpc_isis_rip*.go (only with the `IsisRipState` RPC)
  - packages/schema/src/domains/ext/isis-rip*.ts, packages/schema/src/semantic/isis-rip*.ts (rule ids `routing.isis-rip-…`), packages/schema/examples/isis-rip-*.json, packages/proto/test/fixtures/isis-rip-*.json
  - apps/api/src/features/isis-rip/** (index.ts exports {controllers, providers}; `IsisRipController`; real fake behaviour in fake.ts), apps/api/test/e2e/isis-rip*
  - apps/web/src/domains/routing/isis-rip/**, apps/web/src/locales/{en,fa}/isis-rip.json
  - docs/user/routing/isis-rip.md, test/topology/isis-rip/**, docs/status/tasks/F-isis-rip*
shared hotspots (append-only, conflicts resolved by the manager at merge):
  - protocol: docs/status/wave-A-hotspots.md §0. Insert only directly below your own anchor `// wave-BC: F-isis-rip` (manager's wave-B/C anchor pass); if missing, say so in the questions file and insert at the end of that anchor block. dataplane.proto: keep the one-blank-line framing. Never reorder or reformat other lines. List every hunk under "Shared hunks" in docs/status/tasks/F-isis-rip.md
  - A1 apps/agent/internal/subsystems/subsystems.go: one line at the end of `Register()` (your subsystems/isis_rip.go registers `lcp.osi-proto` on the globals owner only)
  - A2 apps/agent/internal/agent/projection.go: drop `isis`/`rip` from the RF-1 routing warning per seam S3 (not in-place if P12 left one `if` — questions file)
  - C1 packages/schema/src/domains/routing.ts: `RoutingSchema.ripng`, `IsisSchema.{areaPasswordRef, domainPasswordRef}`, `IsisInterfaceSchema.{ipv4, ipv6}`, `RipSchema.version`, (`RipInterfaceSchema.auth`) key lines under your anchors (sub-schemas in ext/isis-rip.ts) · C2 packages/schema/src/semantic/index.ts · C3 packages/schema/src/index.ts · C4 new fixture files only
  - C5 packages/proto/vrx/v1/dataplane.proto: `RoutingConfig` 14, `IsisConfig` 6–7, `IsisInterface` 6–7, `RipConfig` 6, (`RipInterface` 2), `EventKind` 21, the `IsisRipState` rpc only if needed; new messages in `// ----- F-isis-rip -----`
  - C6 docs/contracts/proto.md · C7 generated, never hand-edited (apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md)
  - P1 apps/api/src/app.module.ts · P4 agent.client.ts + P5 fake-agent.ts (only with a new RPC) · P6 apps/api/src/infra/bus.ts (`isis-rip.events`) + telemetry/relay.service.ts (EventKind 21 case)
  - W1 apps/web/src/router.tsx (IS-IS and RIP routes) · W2 apps/web/src/nav/nav.ts + nav.test.ts (two NavItems in the routing group, labelKeys in `isis-rip:`) · W3 apps/web/src/i18n.ts
  - A7 docs/vpp-code-track.md: `### V-new (F-isis-rip)` (OSI punt enable-only; any linux-cp CLNS gap); the manager numbers it
  - allocated numbers (docs/status/wave-BC-numbers.md, a merge blocker if reused): **RoutingConfig 14 `ripng`**, **IsisConfig 6 `area_password_ref` / 7 `domain_password_ref`**, **IsisInterface 6 `ipv4` / 7 `ipv6`**, **RipConfig 6 `version`**, **RipInterface 2 `auth`** (only if built), **EventKind 21** — nothing else (IsisInterface 8 is F-bfd-redistribution's)
contract: commit `contract(schema): routing ripng, isis families + passwords, rip version` and `contract(proto): …` as separate commits on YOUR branch first + docs/status/tasks/F-isis-rip-contract.md; tell the manager in the questions file and keep building. Never a `contract/` branch
files you must not touch:
  - everything else
  - never: /root/ngfw (main), other worktrees, /etc (incl. /etc/vpp and /etc/frr), /root/vpp, the frr/vpp units
  - apps/agent/binapi + tools/binapi-gen.sh, tools/lab, tools/ci.sh, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
  - apps/agent/internal/renderers/frr/*.go, frr/frrtest/**, frr/templates/**, frr/{bgp,policy}/** (RF-1/P12), frr/{ospf,bfd,redistribute,ldp,pim}/** (sibling tasks)
  - P12's files: apps/agent/internal/descriptors/lcp/** (your OSI descriptor is its own package), apps/agent/internal/lcpmap/**, desired/{bgp,lcp}*.go, subsystems/{frr,lcp}*.go, agent/rpc_{bgp,routing}*.go
  - agent core (A5): apps/agent/internal/agent/{agent,service,state,ifstate}.go, apps/agent/cmd/**; apps/agent/internal/desired/interfaces.go (A3); packages/schema/src/domains/routing.ts and semantic/routing.ts beyond the anchored key lines; apps/api/src/state/**
host facts:
  - FRR 10.7.1 installed (`isisd`, `ripd`, `ripngd`, also `fabricd`), `frr.service` disabled; frrtest refuses the root netns and ens192
  - source (VPP 26.06 lcp.c/lcp.api): IS-IS frames reach the LCP tap only after `lcp_osi_proto_enable(<proto>)`, which registers the OSI protocol on `osi_plugin.so` (on disk and loaded by default) towards `linux-cp-punt-xc`; it is VPP-wide and has no disable; binapi `lcp.LcpOsiProtoEnable` / `LcpOsiProtoGet` exist on main
  - RIP (UDP 520) and RIPng (UDP 521) are not claimed by VPP; linux-cp punts unknown UDP (lcp_interface.c `udp_punt_unknown`) and installs (*,224.0.0.0/24)/(*,ff00::/8) accept mfib entries — confirm 224.0.0.9 / ff02::9 reach the tap
  - linux_cp/linux_nl loaded (D-060), linux_nl in the ROOT netns → P12's linux-nl test plan (slot kernel VRF); routes never in the root netns main table
  - no data NICs bound (D-026): af_packet rig only (D-010); LCP pairs only on slot interfaces, taps `w<SLOT>-…` ≤ 15 chars
coordination: (1) F-ospf (parallel) — sibling sections, same S2/S3 hunks · (2) F-bfd-redistribution renders the BFD side of `bfd: true` (IS-IS `isis bfd` line is yours, profiles are theirs; IsisInterface 8 theirs) · (3) redistributing `ripng` into other protocols is out of scope · (4) P12 owns the LCP pair model: your interfaces get pairs from it, never create pairs yourself
V19/V24 SAFETY (D-095, D-101): before ANY traffic through the rig, run TD-3's preflight on the rig interfaces' sw_if_index and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots (en + fa/RTL: IS-IS page, RIP page with the RIPng tab); paste `show isis neighbor`, `show ip rip status`, `vppctl show ip fib table <id>` / `show ip6 fib table <id>` counts and `lcp_osi_proto_get` output
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-isis-rip.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-isis-rip-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-isis-rip.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite; FRR daemons through the harness Stop) by PID · lab lock released · vrx_w<SLOT> dropped · no w<SLOT> LCP pairs, tables or routes left (Retrieve + `show ip fib table <id>` pasted) · no ns-w<SLOT>-* netns left · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-isis-rip-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
