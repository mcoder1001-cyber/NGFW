# P05 — vrx-agent core: govpp, reconciler, ownership scoping, gRPC server, resync, confirm timer

Branch `task/P05` (worktree `/root/ngfw-wt/P05`), slot 7 (`w7`, tables 7000–7999, loop700–799, 10.7.0.0/16,
socket `/run/vrx-test/w7/agent.sock`, metrics 9171). main merged twice (last: `76b017c`, P02a proto sync).
Full transcripts: `docs/status/tasks/P05-evidence.txt` (manual run with the real binary),
`docs/status/tasks/P05-integration.txt` (host integration tests). Manager notes answered: D-063 (write-only),
Normalizer, D-065 (alias / observe-only) — see "Decisions".

## What was built (apps/agent)

| area | files | notes |
|---|---|---|
| VPP connection | `internal/vpp/conn.go` | govpp `socketclient` + `core.AsyncConnect`, health probe (govpp control-ping), reconnect rounds with exponential backoff 1 s → 30 s, `States()` notifications, fail-fast `ErrDisconnected`; implements the frozen `vpp.Client` |
| reconciler | `internal/scheduler/reconciler.go` | validate → plan (deletes reverse-topological first, then creates/updates topological; ties = registration order, key) → apply with journal → verify by re-Retrieve (2 retries) → rollback in reverse (ROLLED_BACK / DEGRADED). Scope (D-041), `ErrRecreate` with dependent re-creation, **live dependents are removed and re-created around any create/delete/recreate** (VPP refuses e.g. an interface table rebind while addresses exist), write-only descriptors (D-063), `Normalizer`, observe-only `DeleteOnAbsence()` (D-065), `Reapplier` (resync re-asserts locks), `KeyProvider` aliases |
| core descriptors | `internal/descriptors/core/` + `README.md` | `vrf`, `interface.loopback`, `interface-ip.table` (VRF binding), `interface-ip`, `ip.route`; values in `core_model.proto`; stateful fake VPP model `coretest/` |
| ownership | tags `<owner>:<name>` (loopbacks), table names `<owner>:<vrf>`, `internal/ownertable` (`owned-<owner>.json`) for routes | Retrieve filters by owner; two agents with different owners are tested on the fake and on the host |
| agent service | `internal/agent/{service,projection,state,events,telemetry,metrics,server,agent}.go` | Apply (apply / confirm / confirm-and-apply, txn_id retry idempotency, ABORTED on reuse, FAILED_PRECONDITION while pending, UNAVAILABLE without VPP), DryRun (no mutation, no events), Retrieve (owned, canonical DesiredState), Health, StreamEvents (bounded, drop-oldest + "dropped N events"), StreamStats (stats segment, per-stream interval 200–60000 ms, drop-oldest), Action → UNIMPLEMENTED; persistence `desired.pb` + `confirmed.pb` + `agent-state.json`; resync on start and on every VPP (re)connect; confirm timer that survives restarts (expired → revert right after the startup resync); link events from `want_interface_events`; Prometheus text on `VRX_METRICS_ADDR`/`VRX_METRICS_PORT` (default 127.0.0.1:9101) |
| binaries | `cmd/vrx-agent` (env config documented in the file), `cmd/vrx-agentctl` (dev client: health/apply/confirm/dryrun/retrieve/events/stats) | socket 0660, group `VRX_SOCKET_GROUP` or primary group + warning (never groupadd); refuses to steal a live socket, replaces a stale one |

## How it was verified

### Unit tests (`make test` = `go test -race -count=1 ./...`, fake VPP only)
```
ok  	ngfw/agent/internal/agent	3.044s
ok  	ngfw/agent/internal/contracttest	0.196s
ok  	ngfw/agent/internal/descriptors/acl	0.055s
ok  	ngfw/agent/internal/descriptors/core	0.042s
ok  	ngfw/agent/internal/ownertable	0.018s
ok  	ngfw/agent/internal/renderers	0.326s
ok  	ngfw/agent/internal/scheduler	0.024s
ok  	ngfw/agent/internal/vpp	0.017s
ok  	ngfw/agent/internal/vpp/fake	0.019s
ok  	ngfw/agent/internal/vpp/vpptest	0.023s
```
Scheduler: ordering through aliases, idempotency (empty plan, no mutation), in-place update, recreate cascade, rollback
of delete/update/create in reverse (exact op sequence asserted), SKIPPED after failure, DEGRADED on failed rollback,
missing mandatory dependency (nothing applied), dependency satisfied by out-of-scope actual state, scope never touches
other descriptors, "cannot delete what out-of-scope objects need", cycles, invalid desired, verify mismatch → rollback,
Retrieve errors, write-only (resync re-apply, no delete on absence after restart, delete after removal, cache restored on
rollback, foreign sentinel), Normalizer, observe-only, re-creation of live optional dependents, Reapplier only on resync.
Core: keys/deps, exact binapi requests, Retrieve == desired, idempotency (only dumps sent), other owner invisible and
untouched, route update, VRF rename recreate cascade, rebind with existing addresses, repair after loss, delete all,
VPP errors (instance in use, not owned, missing table, mixed family, table 0), disconnected. Agent: gRPC round trip
over a unix socket (Health/Apply/Retrieve/StreamEvents/StreamStats/Action/owner mismatch), request validation codes,
txn retry idempotency, D-041 presence table, validation pointers/rules, rollback results, confirm / confirm-and-apply /
timeout revert with event order and `seq`, pending confirm across restart (expired → reverted), resync after loss,
converged resync sends no mutation, owned leftovers deleted / foreign kept, DryRun, Retrieve subsystems, event overflow,
metrics exposition, **VPP reconnect → VPP_DISCONNECTED/VPP_CONNECTED + resync** (fake connection; VPP restarts are
forbidden, D-012), DEGRADED + recovery, blackhole routes, every valid `packages/schema/examples/*.json` projects without
error and round-trips.

### Host integration (VRX_INTEGRATION=1, `flock -s /run/lock/vrx-lab.lock`, VPP 26.06 on this host)
```
$ VRX_INTEGRATION=1 VRX_TEST_PREFIX=w7 VRX_VPP_TABLE_BASE=7000 flock -s /run/lock/vrx-lab.lock go test -count=1 -v -run OnHost ./internal/agent/ ./internal/descriptors/core/
=== RUN   TestAgentOnHost
    agent_integration_test.go:272: restart after loss: converged in 212.184162ms
--- PASS: TestAgentOnHost (4.85s)
=== RUN   TestAgentProcessOnHost
    agent_integration_test.go:397: kill -9 1081664
    agent_integration_test.go:404: restart after kill -9 + loss: converged in 1.21870104s
    agent_integration_test.go:407: kill -9 1081947
--- PASS: TestAgentProcessOnHost (7.12s)
ok  	ngfw/agent/internal/agent	12.007s
=== RUN   TestCoreOnHost
    core_integration_test.go:101: apply: {Created:10 Updated:0 Deleted:0 Unchanged:0 Failed:0 Reverted:0} in 27.287475ms
    core_integration_test.go:149: rollback case 0: err=create interface-ip/loop712/10.7.11.2/24: sw_interface_add_del_address loop712 10.7.11.2/24 add=true: VPPApiError: Address in use (-105) results=[{Key:interface-ip/loop711/10.7.13.1/24 Op:create Code:REVERTED Err:<nil>} {Key:interface-ip/loop712/10.7.11.2/24 Op:create Code:FAILED ...}]
    core_integration_test.go:149: rollback case 1: err=create ip.route/7011/10.7.111.0/24: core: egress interface "w7c-nosuch" not found results=[{Key:interface-ip/loop711/10.7.13.1/24 Op:create Code:REVERTED Err:<nil>} {Key:ip.route/7011/10.7.111.0/24 Op:create Code:FAILED ...}]
--- PASS: TestCoreOnHost (0.17s)
ok  	ngfw/agent/internal/descriptors/core	0.195s
NRestarts=2          (systemctl show vpp -p NRestarts before and after: 2 → 2, D-064)
```
`TestAgentOnHost`: apply two prefixed loopbacks with IPv4/IPv6 addresses in VRF 7001 + two static routes → Retrieve ==
desired → re-apply is empty (`unchanged:10`) → a second agent with owner `w7b` on the same VPP sees nothing and its
authoritative-empty apply deletes nothing → stop, delete our objects via binapi, start → recreated → restart without
loss keeps every sw_if_index → `confirm_timeout_sec=2` apply adds loop703, the agent reverts on its own
(`RECONCILE_DONE:w7-it-3, CONFIRM_REVERTED:w7-it-3, RECONCILE_DONE:`) → delete all → nothing of ours left.
`TestAgentProcessOnHost` runs the built binary as a child process and kills it with SIGKILL **by PID**.

### Agent-restart simulation with the real binary (FAST MODE DoD 3; from `P05-evidence.txt`)
```
$ vrx-agentctl apply doc.json -txn w7-e1          → "status": "APPLY_STATUS_APPLIED", "summary": {"created": 10}
$ vppctl show interface address
loop701 (dn):
  L3 10.7.1.1/24 ip4 table-id 7001 fib-idx 2
  L3 2001:db8:7::1/64 ip6 table-id 7001 fib-idx 2
loop702 (dn):
  L3 10.7.2.1/24 ip4 table-id 7001 fib-idx 2
$ vppctl show ip fib table 7001
w7:w7-red, fib_index:2, flow hash:[src dst sport dport proto flowlabel ] epoch:0 flags:none locks:[interface:2, API:1, recursive-resolution:1, ]
...
10.7.100.0/24
  unicast-ip4-chain
$ vrx-agentctl apply doc.json -txn w7-e2          → APPLIED, no results, "unchanged": 10   (idempotent)

$ kill -9 992220                                   (the agent PID we started)
$ python3 delete_owned.py w7                       (vpp_papi = binary API; only tag/name "w7:*")
delete_loopback loop702 2 0
delete_loopback loop701 5 0
ip_table_add_del del 7001 ip4 0
ip_table_add_del del 7001 ip6 0
$ vppctl show interface address
local0 (dn):                                       (loop701/loop702 gone)
$ (start vrx-agent again)  grep resync agent-2.log
{"msg":"reconcile done","owner":"w7","txn_id":"","mode":"resync","domains":["interfaces","vrfs","routing"],"status":"APPLY_STATUS_APPLIED","summary":"created:7 updated:2 unchanged:1",...}
$ vrx-agentctl retrieve                            → the same document as applied (interfaces, vrfs, routing)
$ vppctl show interface address
loop701 (dn):
  L3 10.7.1.1/24 ip4 table-id 7001 fib-idx 2
  L3 2001:db8:7::1/64 ip6 table-id 7001 fib-idx 2
loop702 (dn):
  L3 10.7.2.1/24 ip4 table-id 7001 fib-idx 2

=== kill -9 of a converged agent, restart: no VPP change ===
$ vppctl show interface loop701   → loop701  5  down
$ kill -9 1015409
{"msg":"reconcile done","owner":"w7","txn_id":"","mode":"resync",...,"summary":"unchanged:10","reapplied":1,...}
$ vppctl show interface loop701   → loop701  5  down        (same sw_if_index; "reapplied":1 = idempotent VRF lock re-assert)

=== confirmed commit: apply with confirm_timeout_sec=5, never confirmed ===
$ vrx-agentctl apply doc-pending.json -txn w7-pending -confirm 5   → APPLIED, created 3 (loop703, its address, a table-0 route), "confirmDeadline": "...21:22:55Z"
$ vrx-agentctl health   → "pendingConfirmTxnId": "w7-pending", "confirmDeadline": ...
events: RECONCILE_START w7-pending · LINK_DOWN loop703 · RECONCILE_DONE w7-pending · CONFIRM_REVERTED w7-pending · RECONCILE_START (revert) · LINK_DOWN loop703 deleted=true
$ vrx-agentctl confirm w7-pending
vrx-agentctl: rpc error: code = FailedPrecondition desc = transaction "w7-pending" is not pending confirmation
$ curl -s 127.0.0.1:9171/metrics | grep -v '^#'   (excerpt)
vrx_agent_vpp_connected 1
vrx_agent_confirm_reverts_total 1
vrx_agent_reconcile_total{status="APPLY_STATUS_APPLIED"} 3
vrx_agent_reconcile_duration_seconds_count 3
vrx_agent_retrieve_unsupported_objects 0

=== cleanup through the agent ===
$ vrx-agentctl apply empty.json -txn w7-cleanup -subsystems interfaces,vrfs,routing   → "summary": {"deleted": 10}
$ kill -TERM <pid>  → {"msg":"vrx-agent stopped","owner":"w7"}
$ vppctl show interface address → local0 only · vppctl show ip table → no table 7001
```
**Bug found by this simulation and fixed:** after the loss VPP kept table 7001 alive through route/interface locks
while its API lock was gone (`locks:[interface:2, recursive-resolution:1]`), so Retrieve saw a healthy VRF. Removing
the routes later would have silently dropped the VRF. Fix: `scheduler.Reapplier` (resync re-asserts the idempotent
`ip_table_add_del` add) and VRF `Update` re-adds both families. Re-run: both families show `locks:[interface:2, API:1, ...]`
after the loss restart (end of `P05-evidence.txt`).

### Acceptance
- [x] `go test ./...` green; integration suite green against the host VPP (above).
- [x] `grep -r "vl_api_\|_reply" internal/ | grep -v binapi` — no hand-typed message in P05 code; the only hits are
  comments/docs and the frozen P05a stand-in test types (`scheduler/example_descriptor_test.go`, `vpp/fake/fake_test.go`)
  and DF-4 comments. `grep -rn "vppctl\|exec.Command"` over P05 production code is empty (tests build/run the binary).
- [x] Applying the same desired state twice produces an empty plan (`unchanged:10`, no results, only dumps sent).
- [x] `kill -9 <agent PID>` → restart → no VPP changes (`unchanged:10`, same sw_if_index; the only message sent is the
  idempotent VRF lock re-assert).

### CI gate
```
$ tools/ci.sh --base main
no contract files changed in the 18 commit(s) of HEAD since main (76b017c)
...
== summary (quick) ==
  contract guard: HEAD vs main                       0m00s
  tools (golangci-lint, gitleaks)                    0m02s
  install (pnpm --frozen-lockfile --prefer-offline)   0m02s
  generate + generated-output gate                   0m20s
  forbidden patterns (+ gitleaks)                    0m04s
  lint · typecheck · unit tests · build (turbo)   0m21s
  apps/agent: make lint test build                   0m31s
  test/ Go modules, unit mode (test/integration/smoke)   0m01s
  mode quick · wall time 1m22s · logs /root/ngfw-wt/logs/ci/P05-20260924-005912-1083476

CI GATE PASSED
```

## Review fixes (review `docs/status/tasks/P05-review.md`, verdict BLOCK → fix round)

main merged first (`d8cdc0b`). Claim rule D-071 applied to every core descriptor: our own tag or name means ours;
a foreign one is never touched; anything else is ours only through a claim record made by our own successful Create.

| finding | fix | commit | regression test |
|---|---|---|---|
| **H1** foreign VRF table deleted by our rollback | `vrf.go`: Create dumps the id first; a family under another name makes it fail with `ErrTableConflict` **before any message**; cleanup removes only families this call created; Delete, Update and Reapply re-verify `<owner>:<vrf>` right before acting | `1b3c874` | host `TestClaimRulesOnHost` (id collision between owners `w7rb`/`w7ra`, owner B's VRF and routes unchanged) + fake `TestVRFTableIDOfAnotherOwner` (no `ip_table_add_del` sent; a table renamed by someone else survives Delete) |
| **H2** foreign route overwritten, then deleted | `route.go`: claim only when the FIB has no entry for that prefix/table (VPP's default-drop `/0` excepted), otherwise `ErrRouteConflict` with nothing sent and nothing recorded; Delete acts only on claimed routes and re-checks the FIB entry first | `1b3c874` | host `TestClaimRulesOnHost` (table-0 prefix collision; a default route over the real default-drop entry is added and removed) + fake `TestRouteOfAnotherOwner` |
| **H3** failed revert lost, pending config becomes permanent | the revert is made durable **before** the attempt: `desired := confirmed`, the transaction stays pending with `reverting: true`, persisted; failure → DEGRADED, and every resync (reconnect/restart) retries the revert and never re-applies the unconfirmed document; new applies/confirms are refused while the revert is owed | `636981c` | `TestRevertOwedWhenVPPDownAtDeadline` (VPP disconnected at the deadline → still pending across a restart → reconnect resync converges to the baseline → a second resync changes nothing) |
| **M1** confirm accepted after the deadline | confirm → `FAILED_PRECONDITION` when `now ≥ deadline` or the revert is owed; resync with an expired pending txn goes straight to the revert | `636981c` | `TestLateConfirmRejectedAfterRestart` (restart past the deadline, confirm before the first resync is rejected) + host re-run below |
| **M2** map write under the read lock | `plan()` returns the write-only set; `ApplyWith` records it under the write lock | `c2f54e1` | `TestConcurrentPlansWithWriteOnly` under `-race`: 4× `WARNING: DATA RACE` on the old code, clean now |
| **M3** three state files not atomic together | one authoritative `agent-state.json` (metadata + both documents as protobuf JSON, one atomic rename); `desired.pb` is a mirror written afterwards and never read (old layout migrates) | `636981c` | `TestStateCrashInjection` (a crash injected before the state write gives the old consistent state; one before the mirror gives the pending txn, still unconfirmed) + `TestStateMigratesOldLayout` |
| **M4** `vrf` unset for default-table interfaces | `assemble` reports `vrf: "default"` for owned interfaces without a binding | `7a607c3` | `TestDefaultVRFNoDrift` (Retrieve == canonical; re-applying the retrieved document changes nothing) |
| **M5** addresses on non-loopback interfaces | handed to P08, documented in `descriptors/core/README.md` ("Handoff to P08") | `7a607c3` | — |
| L1 stale Meta index in undo/delete | loopback/address/binding deletes re-resolve the sw_if_index from the tag right before deleting | `1b3c874` | covered by the rollback/recreate tests |
| L2 owner table lost/corrupt | recovery procedure in the core README | `7a607c3` | — |
| L3 unimplemented domains silently applied | DryRun warns `agent.unimplemented-domain` for non-empty domains this build does not implement | `7a607c3` | `TestUnimplementedDomainWarning` |
| L4 scope creep (`vrx-agentctl`) | kept (reviewer: acceptable) | — | — |
| D-073b (LOG) descriptions | VRF and static-route descriptions are returned from the agent's stored state for existing objects; the `unsupported-field` warnings for them are gone | `7a607c3` | `TestDescriptionsRoundTrip` |



**Found during the fix round (VPP 26.06 quirk, shared host):** deleting a table with `ip_table_add_del` (is_add=0)
while it still holds API **drop** routes leaks those entries into the next table that reuses the FIB index. A fresh
table 7059 started with `10.7.200.0/24`/`10.7.250.0/24` (earlier blackhole tests: mine and the reviewer's papi
deletion) and with `10.10.82.1/32`, which is **slot 10's**. I removed only my `10.7.*` leftovers, plus the
`10.10.82.1/32` copy inside my own probe table. Our reconciler always deletes routes before their table. The
loss-simulation helpers now flush the table (`ip_table_flush`) before deleting it. Suggest a `vpp-code-track` item
and a note to slot 10.

### Host run after the fixes (`VRX_INTEGRATION=1`, lab lock, then `go test -race ./internal/...`)
```
NRestarts=2
=== RUN   TestCoreOnHost
--- PASS: TestCoreOnHost (0.18s)
=== RUN   TestClaimRulesOnHost
    core_integration_test.go:249: id collision: ROLLED_BACK: create vrf/7050: core: VRF table id is in use by another owner or by VPP: table 7050 ipv6=false is named "w7rb:blue", we are "w7ra:red"
    core_integration_test.go:255: prefix collision: ROLLED_BACK: create ip.route/0/10.7.99.0/24: core: route prefix already present in the FIB and not owned by this agent: table 0 10.7.99.0/24
--- PASS: TestClaimRulesOnHost (0.10s)
ok  	ngfw/agent/internal/descriptors/core	0.314s
=== RUN   TestAgentOnHost
    agent_integration_test.go:275: restart after loss: converged in 216.60886ms
--- PASS: TestAgentOnHost (5.02s)
=== RUN   TestAgentProcessOnHost
    agent_integration_test.go:400: kill -9 1311334
    agent_integration_test.go:407: restart after kill -9 + loss: converged in 1.020763052s
    agent_integration_test.go:410: kill -9 1311775
--- PASS: TestAgentProcessOnHost (7.40s)
ok  	ngfw/agent/internal/agent	12.500s
NRestarts=2
ok  	ngfw/agent/internal/agent	7.295s
ok  	ngfw/agent/internal/contracttest	1.934s
ok  	ngfw/agent/internal/descriptors/acl	1.214s
ok  	ngfw/agent/internal/descriptors/core	1.222s
ok  	ngfw/agent/internal/ownertable	1.090s
ok  	ngfw/agent/internal/renderers	1.435s
ok  	ngfw/agent/internal/scheduler	1.263s
ok  	ngfw/agent/internal/vpp	1.084s
ok  	ngfw/agent/internal/vpp/fake	1.101s
ok  	ngfw/agent/internal/vpp/vpptest	1.098s
```

### Agent-restart simulation re-run with the real binary (`docs/status/tasks/P05-review-evidence.txt`)
```
$ systemctl show vpp -p NRestarts                        → NRestarts=2
apply doc.json -txn w7-r1                                → APPLIED, created 10; Retrieve shows "blackhole": false, "vrf": "w7-red"
kill -9 1270301 ; python3 delete_owned.py w7             → delete_loopback loop702 16 0 / loop701 18 0 / ip_table_add_del del 7001 ip4 0 / ip6 0
restart → {"msg":"reconcile done","mode":"resync","status":"APPLY_STATUS_APPLIED","summary":"created:10",...}
vppctl show ip fib table 7001 10.7.100.0/24              → w7:w7-red … locks:[interface:2, API:1, recursive-resolution:1, ]
kill -9 1270668 ; restart → {"msg":"reconcile done","mode":"resync","summary":"unchanged:10","reapplied":1,...}
vppctl show interface loop701                            → loop701  18  down   (same index)
apply doc-pending.json -confirm 3 ; kill -9 1270833 ; restart 4 s later
  → {"level":"WARN","msg":"confirm timeout: reverting to the last confirmed state","txn_id":"w7-r-pending"}
  → {"msg":"reconcile done","mode":"revert","status":"APPLY_STATUS_APPLIED","summary":"deleted:3 unchanged:10",...}
vrx-agentctl confirm w7-r-pending                        → FailedPrecondition … not pending confirmation
cleanup apply (subsystems interfaces,vrfs,routing)       → "deleted": 10 ; vppctl: no loop7xx, no table 7001
$ systemctl show vpp -p NRestarts                        → NRestarts=2
```

### CI gate after the fixes
```
$ tools/ci.sh --base main
== summary (quick) ==
  contract guard: HEAD vs main                       0m00s
  tools (golangci-lint, gitleaks)                    0m01s
  install (pnpm --frozen-lockfile --prefer-offline)   0m01s
  generate + generated-output gate                   0m20s
  forbidden patterns (+ gitleaks)                    0m03s
  lint · typecheck · unit tests · build (turbo)   0m19s
  apps/agent: make lint test build                   0m34s
  test/ Go modules, unit mode (test/integration/smoke)   0m02s
  warnings:
    - commit subject(s) not in Conventional Commits form (type(scope): subject):
      review(P05): findings
  mode quick · wall time 1m21s · logs /root/ngfw-wt/logs/ci/P05-20260924-012100-1277337

CI GATE PASSED
```
(The Conventional-Commits warning refers to main's `review(P05): findings` commit, not to this branch's commits.)

## Fix round 2 (re-review `docs/status/tasks/P05-rereview.md`: N1/N2 High → BLOCK)

main merged first (`f996d69`: contracts-v1, DF-2, RF-1, P03b).

| finding | fix | commit | tests |
|---|---|---|---|
| **N1** VPP-generated FIB entries treated as a foreign owner | Route ownership is decided by the entry's **best source** (`ip_route_v2_dump` `src`, verified on the host: API=8, recursive-resolution=18, default-route=20, special=1). Only client sources block a claim: API, CLI, DHCP, SR, 6RD, LISP, MAP and plugin-allocated sources (lcp-rt, nat, lb). Retrieve reports a claimed prefix only while its best source is API. Delete sends a delete only when an API source can exist, so VPP-generated entries are never removed | `21fb537` | host `TestRouteOverVPPEntriesOnHost` (the reviewer's sequence: /32 to an existing route's next hop, remove, re-add, both in one fresh transaction: all APPLIED). The fake models RR /32s, connected and adjacency entries (`TestRouteOverVPPGeneratedEntries`). The foreign-API collisions still fail (`TestClaimRulesOnHost`, `TestRouteOfAnotherOwner`) |
| **N2** an unrevertable owed revert wedges Apply | A new non-confirm Apply supersedes an owed revert: it is logged, pending/`reverting` are cleared, and the new document becomes the target. The contract §4.4 FAILED_PRECONDITION still applies to a pending txn *before* its deadline. A failed revert retries on a backoff timer (5 s → 60 s) independent of VPP reconnects and still on every resync. The degraded message states both | `7135f5c` | `TestNewApplySupersedesOwedRevert` (the revert fails deterministically on a foreign route → a new Apply succeeds, pending cleared, not degraded; a later Apply converges). `TestOwedRevertRetriedByTimer` (obstacle removed → the timer retry restores the baseline, pending cleared) |
| L-a `vrf: "default"` literal | Table 0 is named by id: the desired VRF with id 0 if one exists, else `default` (interfaces and routes) | `7135f5c` | `TestTableZeroNamedByID` |
| L-b D-072 hook | `frr.StaticOwnedByFRR(i, route, nil)` is wired in the projection (skip). Schema flag + selector → P08/P12, noted in the core README, with the `ErrRouteConflict` behaviour on a linux-nl duplicate | `7135f5c` | existing frr tests; the default selects none |
| L-c loopback Delete | `errors.Is(err, ErrNotOwned)` → nil, other errors are returned | `7135f5c` | core tests |
| N3 stale claim | documented as a residual in the core README (plus the connected-prefix hiding case) | `7135f5c` | — |

### Host (`VRX_INTEGRATION=1`, lab lock, slot 7; includes the real-binary `kill -9` restart simulation)
```
NRestarts=2
=== RUN   TestCoreOnHost
--- PASS: TestCoreOnHost (0.20s)
=== RUN   TestClaimRulesOnHost
    core_integration_test.go:249: id collision: ROLLED_BACK: create vrf/7050: core: VRF table id is in use by another owner or by VPP: table 7050 ipv6=false is named "w7rb:blue", we are "w7ra:red"
    core_integration_test.go:255: prefix collision: ROLLED_BACK: create ip.route/0/10.7.99.0/24: core: route prefix already present in the FIB and not owned by this agent: table 0 10.7.99.0/24 (fib source 8)
--- PASS: TestClaimRulesOnHost (0.10s)
=== RUN   TestRouteOverVPPEntriesOnHost
    core_integration_test.go:311: aggregate via next hop: APPLIED {Created:2 Updated:0 Deleted:0 Unchanged:0 Failed:0 Reverted:0}
    core_integration_test.go:311: + /32 for the next hop: APPLIED {Created:1 Updated:0 Deleted:0 Unchanged:2 Failed:0 Reverted:0}
    core_integration_test.go:311: remove the /32: APPLIED {Created:0 Updated:0 Deleted:1 Unchanged:2 Failed:0 Reverted:0}
    core_integration_test.go:311: re-add the /32: APPLIED {Created:1 Updated:0 Deleted:0 Unchanged:2 Failed:0 Reverted:0}
    core_integration_test.go:311: remove both routes: APPLIED {Created:0 Updated:0 Deleted:2 Unchanged:1 Failed:0 Reverted:0}
    core_integration_test.go:311: both in one fresh transaction: APPLIED {Created:2 Updated:0 Deleted:0 Unchanged:1 Failed:0 Reverted:0}
--- PASS: TestRouteOverVPPEntriesOnHost (0.11s)
ok  	ngfw/agent/internal/descriptors/core	0.446s
=== RUN   TestAgentOnHost
    agent_integration_test.go:275: restart after loss: converged in 220.062419ms
--- PASS: TestAgentOnHost (4.87s)
=== RUN   TestAgentProcessOnHost
    agent_integration_test.go:400: kill -9 1562842
    agent_integration_test.go:407: restart after kill -9 + loss: converged in 1.02125652s
    agent_integration_test.go:410: kill -9 1562931
--- PASS: TestAgentProcessOnHost (6.67s)
ok  	ngfw/agent/internal/agent	11.594s
NRestarts=2
```

### Fake (the regression tests of both rounds)
```
--- PASS: TestVRFTableIDOfAnotherOwner (0.00s)
--- PASS: TestRouteOfAnotherOwner (0.00s)
--- PASS: TestRouteOverVPPGeneratedEntries (0.00s)
ok  	ngfw/agent/internal/descriptors/core	0.027s
--- PASS: TestDefaultVRFNoDrift (0.03s)
--- PASS: TestRevertOwedWhenVPPDownAtDeadline (1.03s)
--- PASS: TestLateConfirmRejectedAfterRestart (1.12s)
--- PASS: TestStateCrashInjection (0.02s)
--- PASS: TestNewApplySupersedesOwedRevert (1.02s)
--- PASS: TestOwedRevertRetriedByTimer (1.08s)
--- PASS: TestTableZeroNamedByID (0.00s)
ok  	ngfw/agent/internal/agent	4.362s
```
Full unit suite: `go test -race ./internal/...` all `ok` (incl. the merged DF-*/RF-1 packages); golangci-lint `0 issues`.

### CI gate
```
$ tools/ci.sh --base main
== summary (quick) ==
  contract guard: HEAD vs main                       0m00s
  tools (golangci-lint, gitleaks)                    0m02s
  install (pnpm --frozen-lockfile --prefer-offline)   0m02s
  generate + generated-output gate                   0m32s
  forbidden patterns (+ gitleaks)                    0m03s
  lint · typecheck · unit tests · build (turbo)   0m26s
  apps/agent: make lint test build                   0m30s
  test/ Go modules, unit mode (test/integration/smoke)   0m02s
  warnings:
    - commit subject(s) not in Conventional Commits form (type(scope): subject):
      review(P05): re-review after fix round
      review(P05): findings
  mode quick · wall time 1m39s · logs /root/ngfw-wt/logs/ci/P05-20260924-014727-1565474

CI GATE PASSED
```
(The two warnings are main's own `review(P05)` commits.)

## Out of scope / left undone
- **Interface attributes** (enabled/MTU/MAC/rx-mode/promiscuous/unnumbered/subinterfaces/dhcpClient/description) are
  DF-1/P08: the projection ignores them, DryRun reports `agent.unsupported-field` warnings, Retrieve leaves them unset —
  the API's running-vs-actual diff will show those leaves until P08 wires DF-1. VRF and route `description` likewise.
- **DF-1 alias wiring (D-065):** when DF-1 merges, the wiring must register its `interface` alias descriptor, set
  `core.Env.IfRef = core.AliasInterfaceRef`, and have the projection emit an `interface/<name>` object per document
  interface (creator key for loopbacks). Until then core objects depend on `interface.loopback/<n>` directly.
- `StreamStats.worker_cpu` is not filled (the host has no workers; counters per interface are).
- VPP reconnect resync is proven with a fake connection only (no VPP restart allowed, D-012).
- Routes in the shared table 0 are attributed by the owner table; VPP's API route source is shared, so another
  owner adding the same prefix in table 0 would replace our path set (slot ranges avoid it; production has one agent).
- No descriptor doc under `docs/agent/descriptors/` (outside my file set): the object ↔ message table is
  `apps/agent/internal/descriptors/core/README.md`.

## Open questions (also in `P05-questions.md`)
1. D-063 sentinel: `scheduler.ErrRetrieveUnsupported` (same text as `df2.ErrRetrieveUnsupported`, both recognised);
   DF-2 should alias to it after merge.
2. D-065 wiring owner: who switches `IfRef` + emits alias objects — P08 or the manager's integration commit?
3. (answered by D-073: P08 owns the alias wiring; descriptions are kept by the agent, implemented in the fix round)

## Decisions (options considered → chosen, why)
| id | decision | options | why |
|---|---|---|---|
| D-P05-1 | Core descriptor values are agent-internal protos (`core_model.proto`) + one projection `DesiredState ⇄ KVs` | (a) vrx.v1 messages directly (b) DesiredState fragments (c) internal messages + projection | (a) loses keys (VRF name, table id) and the diff granularity; (c) is what DF-* do (D-055) |
| D-P05-2 | VRF key `vrf/<table id>`, VPP table name `<owner>:<vrf>`, table 0 never an object | key by name / by id | DF-2…6 depend on `vrf/<id>`; the name is recoverable from VPP |
| D-P05-3 | Route ownership = owner table `owned-<owner>.json` (added before create, removed after delete) | FIB-source filter / "all routes in owned tables" / owner table | routes have no tag; table 0 is shared; one mechanism for both |
| D-P05-4 | Execution order: deletes (reverse topological) first, then creates/updates | creates first / deletes first | frees addresses/instances before re-use; matches Ligato |
| D-P05-5 | Live dependents (incl. optional deps) are deleted before and re-created after any create/delete/recreate of their dependency | recreate only on ErrRecreate / always | VPP refuses a table rebind with addresses; stale sw_if_index in routes after a loopback re-create |
| D-P05-6 | D-063 implementation: in-process cache is the write-only descriptors' actual state; resync re-applies; delete only when removed from desired after this process applied it | persist the cache / don't delete at all | never delete on absence (D-063) while still honouring explicit removals |
| D-P05-7 | D-065: loopback no longer provides `interface/<name>`; `Env.IfRef` (direct creator key until DF-1's alias is wired); `DeleteOnAbsence()` capability; undesired observe-only objects satisfy dependencies but never constrain or get deleted/verified | keep KeyProvider alias / creator keys only / capability | manager instruction; DF-1 owns the alias |
| D-P05-8 | `scheduler.Reapplier`: resync calls idempotent `Reapply` for unchanged objects (VRF lock); VRF `Update` re-adds both families | treat as unchanged / always recreate VRF | found in the loss simulation; recreate would flap all dependents |
| D-P05-9 | Persistence = `desired.pb` + `confirmed.pb` (binary DesiredState) + `agent-state.json` (managed domains, txn history as protojson, pending deadline), atomic rename | one new internal proto / sqlite | no extra codegen, human-readable metadata, pb stays the contract type |
| D-P05-10 | Domains present in the document but not implemented are skipped (logged); only naming them in `subsystems` fails with UNIMPLEMENTED | reject any present unimplemented domain | the API sends all 13 domains (D-041); rejecting would make every full-document apply fail |
| D-P05-11 | Revert applies the confirmed baseline with subsystems = managed(pending) ∪ managed(confirmed) | confirmed domains only | a domain first introduced by the pending txn must be emptied too |
| D-P05-12 | Interface VRF binding lives in core as `interface-ip.table` | leave to DF-1 | not in DF-1's list; required for "loopbacks with IPs in your VRF range" |
| D-P05-13 | Retrieve always sets `blackhole` (derivable from the drop path), `distance` only when VPP preference ≠ 0 | omit false | Zod defaults `blackhole: false`; explicit presence keeps the diff honest (D-039) |
| D-P05-14 | Prometheus text exposition hand-written (no client_golang) | prometheus/client_golang | 7 series; avoids a dependency tree in the privileged process |
| D-P05-15 | StreamStats samples the stats segment per stream at its interval (default 1 Hz), 4-batch drop-oldest buffer | one global 1 Hz sampler | streams may ask 200 ms–60 s; segment reads are cheap (AD-5) |
| D-P05-16 | Dev client `cmd/vrx-agentctl` | grpcurl (not installed) | evidence + manager diagnostics; P13 remains the product CLI |
