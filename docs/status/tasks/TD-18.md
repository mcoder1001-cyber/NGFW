# TD-18 — repo & ops tooling hygiene

## What
- **5.4a** `tools/manager/merge.sh` **deleted** (decision: delete, not rewrite). Nothing references it
  (`grep -rn merge.sh` over prompts/, docs/contributing.md, tools/ finds only plan/tasks.yaml's own row), and it
  contradicts D-112/D-130: no rebase+squash, a separate branch gate D-130 dropped, no `flock /run/lock/vrx-main.lock`,
  `git add -A` on main. The merge procedure lives in prompts/MANAGER-PROMPT.md §2 + docs/contributing.md; a second,
  diverging copy in a script is the risk the review flagged.
- **5.4b** `tools/operator/wt.sh`: the id is validated (`^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`) before any use; every
  path/branch/base that reaches the remote shell goes through `printf %q`; `commit` sends the message on stdin
  (`git commit -F -`) instead of interpolating it into a quoted remote string. `run` still passes its command verbatim (that is its purpose).
- **5.7c** `deploy/dev/pg-test.sh`: all admin SQL goes to psql on stdin (`sql() { adm <<<"$1"; }`), so the role
  password in `create/alter role … password` no longer appears in argv (`ps`, /proc/*/cmdline).
- **5.1b** `.gitignore` += `.venv/ *.tfstate *.tfstate.* .terraform/ /.scratch/`.
- **5.3** skipped (per envelope; waits on PENDING-remote-mirror).

## Verification (cloud container, fake ssh/psql/runuser on PATH; no real host or PostgreSQL)
```
$ tools/operator/wt.sh log 'x;rm -rf /' ; echo rc=$?
invalid id 'x;rm -rf /' (allowed: [A-Za-z0-9._-], max 64)
rc=1
$ tools/operator/wt.sh commit TD-18 'fix: a "quoted" $(id) msg'
ARGV[cd /root/ngfw-wt/TD-18 && git add -A && git commit -q -F - && git log --oneline | head -1]
STDIN[fix: a "quoted" $(id) msg]
$ tools/operator/wt.sh new TD-18 'main; id'
ARGV[git -C /root/ngfw worktree add /root/ngfw-wt/TD-18 -b task/TD-18 main\;\ id && echo created /root/ngfw-wt/TD-18]
$ VRX_PG_PASSWORD=Secret123 deploy/dev/pg-test.sh create w9      # fake psql logs argv + stdin
ok     env …/run/w9/pg.env (0600) · DSN postgres://vrx_w9:<redacted>@127.0.0.1:5432/vrx_w9
argv-with-pw: 0
psql argv: -X -v ON_ERROR_STOP=1 -qAt
stdin: alter role vrx_w9 with login password '<the password>'
$ bash -n deploy/dev/pg-test.sh && bash -n tools/operator/wt.sh   → syntax ok
$ tools/ci.sh check → check PASSED (gitleaks not installed — built-in grep only)
```
Not run: shellcheck (not installed here), a real PostgreSQL create/drop (no postgres in the container) — run
`deploy/dev/pg-test.sh create tdx && … drop tdx` once on the host.

## Out of scope
5.3 (.gitlab-ci.yml). tech-debt line 94 (test/e2e lint/forbidden-pattern coverage, owner TD-18) touches
apps/web lint config and tools/ci.sh, which are not in files_owned — left for the manager to re-home.

## Open questions
- The last check's TCP login still passes the password via `PGPASSWORD` env (readable only by the same uid via /proc/<pid>/environ) — acceptable?
