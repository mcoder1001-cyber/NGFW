# TASK ENVELOPE — F-host-stack
id: F-host-stack   branch: task/F-host-stack   worktree: /root/ngfw-wt/F-host-stack   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave C (day 13-15): expose host-stack config — session layer, app namespaces, session rules, TCP source addresses, http_static (opt-in)
prompt: prompts/features/F-host-stack.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main + P08 in wave-B/C prep)   wbs: D7.10
scope: scoped down (D-085): new `descriptors/hoststack` (session global, app namespaces, session rules, TCP source addresses, opt-in http_static) + the `services.hostStack` contract, projection, `/state/host-stack`, one Services tab, docs with the explicit have-not list. VCL/LD_PRELOAD, TLS engine tuning, QUIC, HTTP/3, SRTP, HSI and every startup.conf `session`/`tcp` knob are fenced
merged deps you can rely on: P08 (desired/subsystems/projection patterns, `Wiring.BootStore()`, `interface/<name>` aliases), DF-1 (`iface.ResolveName`, D-069 logical names), F-startup-gen (the start-up generator `renderers/vppstartup` owns every startup.conf stanza — read-only for you) (+ on main by then: TD-2, TD-3, W-seed; wave-A features only if merged when you start — check `git log main`)
read first: prompts/features/F-host-stack.md · docs/status/wave-A-hotspots.md (§0 rules; A1 A2 A4 C1–C7 P1 P4 P5 W1–W3) · docs/status/wave-BC-numbers.md § F-host-stack · apps/agent/internal/descriptors/dfkit/ (README, globals.go, restarttest) · docs/decisions/PENDING-secret-channel.md · prompts/features/F-rpf-adl-pbr.md (Auto-SDL = the other session-layer user) · docs/decisions/LOG.md D-049, D-051, D-063, D-064, D-071, D-076, D-077 (Q3, Q5), D-080, D-082, D-085, D-087, D-109
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; session-rule prefixes inside 10.<SLOT>.0.0/16 (+ one slot-unique IPv6 /48), rule ports 3<SLOT>90–3<SLOT>99, rule tags and namespace ids start with `w<SLOT>`, fib ids only <SLOT>000–<SLOT>999)
daemon-owner: none (the host stack runs inside VPP; http_static is a VPP plugin, not a daemon)
files you own exclusively: apps/agent/internal/descriptors/hoststack/** docs/agent/descriptors/hoststack.md apps/agent/internal/desired/host_stack*.go apps/agent/internal/subsystems/host_stack*.go apps/agent/internal/agent/rpc_host_stack*.go packages/schema/src/domains/ext/host-stack*.ts packages/schema/src/semantic/host-stack*.ts packages/schema/examples/host-stack-*.json packages/proto/test/fixtures/host-stack-*.json apps/api/src/features/host-stack/** apps/api/test/e2e/host-stack* apps/web/src/domains/services/host-stack/** apps/web/src/locales/{en,fa}/host-stack.json docs/user/services/host-stack.md test/topology/host-stack/** docs/status/tasks/F-host-stack*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-host-stack` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-host-stack.md and say so in the questions file); never reorder or reformat other lines): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["services"]` exists by wave C — F-rpf-adl-pbr/F-kea-dhcp-relay/F-unbound-chrony-syslog add it; if not, whoever lands first adds the key, the others append — your hoststack.* names one per line; one `hoststack.Register` line; `hoststack.RegisterGlobals` (session enable/engine, http_static) only when `env.GlobalsOwner`) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (no Action case; `HostStackState` lives in your rpc_host_stack.go) · C1 packages/schema/src/domains/services.ts (one key line `hostStack`; sub-schema in your ext file) · C2 packages/schema/src/semantic/index.ts (one spread) · C3 packages/schema/src/index.ts (export of your ext file) · C4 new `host-stack-*.json` fixtures only · C5 packages/proto/vrx/v1/dataplane.proto (`HostStackState` under the service anchor; `host_stack = 10` one line in `ServicesConfig`; messages in a `// ----- F-host-stack -----` section) · C6 docs/contracts/proto.md (`### F-host-stack: HostStackState`) · C7 generated files (regen, never hand-merged: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`) · P1 apps/api/src/app.module.ts · P4 apps/api/src/agent/agent.client.ts (`hostStackState`) · P5 apps/api/src/testing/fake-agent.ts (UNIMPLEMENTED stub on the contract commit; real fake in features/host-stack/fake.ts) · W1 apps/web/src/router.tsx · W2 apps/web/src/nav/nav.ts (+nav.test.ts: `services` into BUILT_DOMAINS only if not there yet) · W3 apps/web/src/i18n.ts · services domain page + tab registry apps/web/src/domains/services/{ServicesPage.tsx,tabs.ts} (W-seed shell, shared with every services feature; one line — if the shell is not on main, do NOT create it (add/add conflict): own route + NavItem under W1/W2, the manager folds it in at merge) · A7 docs/vpp-code-track.md (`### V-new (F-host-stack)` only if needed)
contract numbers: docs/status/wave-BC-numbers.md § F-host-stack — **ServicesConfig 10 `host_stack`**, new `HostStack*` messages, RPC `HostStackState`; nothing else (no ActionRequest member, no EventKind); commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-host-stack-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned), apps/agent/internal/renderers/** (vppstartup = start-up generator, manager-owned; proposals for `session`/`tcp` stanzas go to the questions file), apps/agent/internal/descriptors/{auto_sdl,dfkit,df2,classify}/** (F-rpf-adl-pbr / shared helpers — read-only), apps/agent/internal/agent/{agent,service,state,ifstate,metrics,telemetry,events}.go + apps/agent/cmd/** (A5 core), apps/agent/internal/desired/interfaces.go (A3), existing apps/agent/internal/descriptors/core/coretest/* files (A6), apps/api/src/{auth,users,secrets,commit,datastore}/**, tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
host facts: session/tcp/http_static/tls_openssl binapi present (checked 2026-09-24) — no app-namespace dump, no session-layer getter, `tcp_configure_src_addresses` has no delete flag, `http_static_enable_v5` no disable/dump; `session_enable_disable_v2` carries only `rt_engine_type` (DISABLE | RULE_TABLE | NONE | SDL) · the host VPP's session layer is whatever startup.conf set (handover-gated, D-012): read it once with `vppctl show session verbose` (read-only) and paste it; **never enable, disable or re-engine it** (same rule as F-rpf-adl-pbr) · http_static cannot be undone: never enabled on the shared VPP outside a manager VPP-restart window (D-077 Q3), opt-in `VRX_HOSTSTACK_HTTP_STATIC=1` · no data NICs bound (D-026)
obligations accumulated from decisions (all must be honoured and listed in F-host-stack.md with evidence): D-104/D-077 Q5 build on `descriptors/dfkit`, do not fork its helpers · D-063/D-076/D-080 `hoststack.session`, `hoststack.namespace`, `hoststack.tcp-src`, `hoststack.http-static` are write-only: idempotent Create or an applied-once record in `Wiring.BootStore()` keyed by the boot identity; never echo desired state from Retrieve; `hoststack.session-rule` Retrieve from `session_rules_v2_dump` filtered to your tag prefix · D-071 session enable/engine and http_static are VPP globals → only the globals owner registers them (slot agents require, never set); `tcp-src` has no delete → irreversible per fib: tests only on a slot fib behind `VRX_HOSTSTACK_TCPSRC=1`, documented · D-082 tests reading a VPP-wide setting hold `flock -s /run/lock/vrx-globals.lock`, tests changing one `-x` behind their opt-in var and restore the previous value · D-064 anything that crashes VPP → opt-in at once + V-item · D-049 `wwwRootPath` under /var/lib/vrx/www/, tags and namespace ids: no control chars, no `..` · D-051 namespace secret only as a `<kind>/<name>` ref · one VPP-wide rt engine: session rules (rule-table) + `services.autoSdl.enabled` (sdl, F-rpf-adl-pbr) in one document → 400 with a pointer (your semantic file, rule ids `services.host-stack-…`)
coordination: (1) secret channel — nothing carries secret material API→agent (PENDING-secret-channel, D-109 b); a namespace with a `secretRef` is refused by DryRun with a clear message until it lands; tests use namespaces without a secret · (2) F-rpf-adl-pbr owns `services.autoSdl` + `descriptors/auto_sdl` (SDL engine) — you add only the cross-check rule · (3) F-kea-dhcp-relay, F-unbound-chrony-syslog, F-snmp, F-lb, F-qos-flat, F-ipfix-sflow share `Domains["services"]` and the services page · (4) buffer/congestion-control tuning = start-up generator proposals in your questions file, not fields you add
SAFETY: no data-plane packets are needed; the ONE host check (namespaces + session rules, prefixed tags) runs only if the session layer is already on with the rule-table engine — otherwise it skips with the pasted reason and the checks run on the fake client · one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-host-stack.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-host-stack-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-host-stack.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite), by PID · lab lock released · vrx_w<SLOT> dropped · no w<SLOT> session rules or namespaces left in VPP (Retrieve + `vppctl show session rules` pasted) · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-host-stack-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
