# TASK ENVELOPE — P10
id: P10   branch: task/P10   worktree: /root/ngfw-wt/P10   base: main@<BASE>   started: <STARTED>
title: S5 system (day 16-18): Debian packaging + systemd + install (Ubuntu 26.04, our VPP debs)
prompt: prompts/P10-packaging-deb.md   (template: none; refreshed on task/prep-rest against main@11a175b: F-vpp-debs/F-startup-gen/F-startup-apply merged, D-057/D-089/D-092/D-100)   wbs: D0.4
scope: one source package `deploy/debian/vrx/` → vrx-agent, vrx-api, vrx-web, vrx-meta; hardened base units; nginx site (TLS, :80 redirect only); firstboot (DB, migrations, secrets, admin seed, startup.conf via `vrx-startupgen`); the static nftables base table `inet vrx_base`; a signed local APT repo (reprepro) with our VPP debs; install tests in a debootstrap chroot / systemd-nspawn container. Nothing is installed on this host
merged deps you can rely on: P08, F-vpp-debs (+ F-startup-gen, F-startup-apply; TD-6 before you package apply-startup.sh — D-103)
  - F-vpp-debs: `deploy/vpp/build.sh` + `verify.sh` + `manifest.json` (`ship: true` = the 7 runtime packages, D-089; version `26.06-release+vrx<N>`, D-092). Its build output was lost with its worktree (D-098); `/srv/vrx-artifacts/` does not exist yet
  - F-startup-gen: `apps/agent/cmd/vrx-startupgen` (document → startup.conf; `--check`, `--diff`), docs/agent/renderers/vppstartup.md · F-startup-apply + TD-6: `deploy/vpp/apply-startup.sh` (manager procedure, handover gate)
  - P06/TD-2: the API's environment (`apps/api/src/config.ts`: `VRX_DATABASE_URL`, `VRX_JWT_SECRET`, `VRX_SECRET_KEY_FILE`, `VRX_BOOTSTRAP_ADMIN_USER/PASSWORD`, `VRX_COOKIE_SECURE`, …), Drizzle migrations in apps/api/migrations · P08: the agent's environment (`VRX_OWNER`, `VRX_GLOBALS_OWNER`, `VRX_AGENT_STATE_DIR`, `VRX_SOCKET_GROUP`, …), the web build
read first: prompts/P10-packaging-deb.md · docs/status/wave-BC-numbers.md (section "S5 system": pack rules SY6, SY7, SY9 + "P10") · deploy/vpp/README.md (P10 bullets) · docs/09-os-packages.md · docs/01-architecture.md AD-6 · scripts/10-install-runtime.sh · docs/agent/renderers/vppstartup.md · docs/lab/shared-host-rules.md · docs/decisions/LOG.md D-001, D-028, D-057, D-071, D-079, D-081, D-084, D-086, D-089, D-092, D-098, D-100, D-103
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100·<SLOT> VRX_WEB_PORT=5000+100·<SLOT> VRX_METRICS_PORT=9100+10·<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock
  - source of truth: `eval "$(tools/lab env <SLOT>)"`
  - chroots, container roots, APT repo output and VPP build output live under /root/ngfw-wt/P10/.scratch/ (and the git-ignored deploy/vpp/.build/); nspawn machine name `w<SLOT>-p10`, always `--private-network`
  - slots 1–11 only; 12 is CI
daemon-owner: none on the host. The host's nginx (:80 default site), PostgreSQL 18, Valkey and sshd serve other users: never reload, restart, reconfigure or enable anything on the host. Inside your own container every unit is yours
HOST FIREWALL SAFETY: every worker and the manager reach this host over SSH on ens192.
  - in the host's root netns only `nft -c -f <file>` (check) and `nft list` may run: never load, flush or delete a ruleset, never `systemctl … nftables`
  - `inet vrx_base` is loaded only inside your container
obligations:
  - D-001/D-089/D-092: ship only `ship: true` packages from a `deploy/vpp/build.sh` output that passes `deploy/vpp/verify.sh --require-files <out>`; `vrx-meta` `Depends: vpp (= <manifest.version>)`. Rebuild in your worktree (≈45 min, `nohup … &`, polled); it needs PyPI + the DPDK tarball once (the wheelhouse was lost). If unreachable → PENDING-network in the questions file; package against a placeholder and finish the rest. Hand the output dir to the manager for `/srv/vrx-artifacts/vpp/<version>/`; never commit a `.deb`
  - F-startup-gen (D-081/D-084): no hand-written startup.conf template. Firstboot renders `/etc/vpp/startup.conf` with `vrx-startupgen` from the default dataplane document (no devices → `dpdk { no-pci }`); vrx-agent ships `vrx-startupgen` and `deploy/vpp/apply-startup.sh` (from main after TD-6)
  - D-071: the product agent unit sets `VRX_GLOBALS_OWNER=1` (the shared host's tools/app uses 0 — never start your unit here)
  - privilege split (AD-6): agent root with `CapabilityBoundingSet=CAP_NET_ADMIN CAP_SYS_ADMIN CAP_IPC_LOCK`, `RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6 AF_NETLINK` (rtnetlink: TD-5 af_packet quiesce, nft, linux-cp — the prompt's list without AF_NETLINK breaks the agent; prep-rest correction, the manager logs it), `ReadWritePaths` = exactly the daemon config dirs its renderers write + `/var/lib/vrx/agent` + `/run/vrx`; api user `vrx`, no capabilities, `ProtectSystem=strict`, `NoNewPrivileges=yes`, `ReadWritePaths=/var/lib/vrx /data`. `systemd-analyze security --offline=yes`: api ≤ 3.0, agent ≤ 5.0
  - D-100: nginx redirects :80 → :443 and never proxies `/api` on :80; 443 proxies `/api` (incl. WebSocket `/api/v1/stream`), `/restconf`, `/.well-known/host-meta` to 127.0.0.1:3000; `VRX_COOKIE_SECURE=true`
  - D-057: the static base policy is its own table `inet vrx_base`; the host-policy renderer's `inet vrx` is F-host-acl-nftables' — never write into it
  - D-079: kea-dhcp4/6-server without kea-ctrl-agent · D-086: rsyslog's TLS driver in the Depends · D-028: `postgresql (>= 16)`
  - secrets: firstboot generates `VRX_JWT_SECRET` and the `VRX_SECRET_KEY_FILE` (0600, owner vrx) on the box, reads `/etc/vrx/bootstrap.env` once and deletes it; the APT signing key lives in `~/.config/ngfw/apt-signing/` (0700): never committed, never printed, never copied into the worktree. Test keys are throwaway keys under `.scratch/`
  - seed the SY6 anchors for later touchers in your install lists: `# wave-BC: F-ab-upgrade`, `# wave-BC: F-hardening-lite`, `# wave-BC: F-backup-restore`
  - create `/data/{backups,updates,support}` (F-backup-restore, F-ab-upgrade) with the owners documented in docs/install/bare-metal.md
files you own exclusively:
  - deploy/debian/vrx/** (the source package: control, rules, *.install, postinst/prerm/postrm, units wiring), deploy/debian/README.md
  - deploy/systemd/vrx-* (unit files; never `deploy/systemd/ngfw-manager.service`, the manager's supervisor)
  - deploy/apt/** (reprepro conf, publish helper), scripts/publish-apt.sh
  - deploy/nginx/** (site config, TLS bootstrap), deploy/nftables/** (`inet vrx_base`), deploy/firstboot/** (firstboot script + unit)
  - docs/install/bare-metal.md, docs/install/upgrade.md
  - test/topology/packaging/** (chroot/nspawn install test; host-root parts behind `VRX_INTEGRATION=1`, skipped in unit mode)
  - docs/status/tasks/P10*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-BC-numbers.md "S5 system"):
  - protocol: docs/status/wave-A-hotspots.md §0. List every hunk under "Shared hunks" in docs/status/tasks/P10.md
  - SY7 root .gitignore `/.scratch/`: seeded by the manager; if absent, never `git add -A`, add paths explicitly
  - SY9 tools/ci.sh: ask (questions file) for a `deploy/debian`/`deploy/apt` shellcheck + packaging-test step; never edit it
  - D4 apps/agent/go.{mod,sum}, pnpm-lock.yaml: no dependency changes expected; a need → questions file
  - D4/SY8 apps/api/package.json: one minimal named hunk only if `pnpm deploy` needs it (`files`/`bin` only, never `dependencies`). Not owned: F-aaa, F-pki and F-restconf-yang may add dependencies to the same file at the same time (prep-rest critic), so the manager unions it and re-resolves pnpm-lock.yaml at merge
contract numbers: none (docs/status/wave-BC-numbers.md "P10": names and paths only — package, unit, user, directory, nft table, APT suite names are allocated there)
files you must not touch:
  - everything else
  - never: /root/ngfw (main), other worktrees, /etc, /boot, /data on this host, /root/vpp (read-only), host services
  - deploy/debian/vrx-strongswan/** and deploy/strongswan/** (P11 — disjoint by construction; if P11 runs in parallel, never look to "fix" its dir), deploy/vpp/** tracked files (F-vpp-debs/F-startup-apply/TD-6; running build.sh writes only the git-ignored .build/), deploy/dev/**, deploy/systemd/ngfw-manager.service
  - apps/**, packages/** source (beyond the package.json hunk), apps/agent/binapi, tools/*, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md
host facts (verified 2026-09-24; no host package installs):
  - Ubuntu 26.04.1 `resolute`, kernel 7.0; mirror `http://repo.amnafzar.ir/ubuntu/` (+ deb.frrouting.org frr-stable, deb.nodesource.com node_22.x — both signed-by keyrings in /usr/share/keyrings); debootstrap has a `resolute` script; systemd-nspawn (systemd 259), reprepro 5.3, dpkg-buildpackage/debuild/dh, fakeroot, gpg, dpkg-scanpackages present; lintian, dpkg-sig, apt-ftparchive, mmdebstrap absent → lintian inside the chroot
  - host versions to pin against: frr 10.7.1, nodejs 22.23.2, postgresql-18 18.6, valkey-server 9.0.4, nginx 1.28.3, kea 3.0.3, unbound 1.24.2, chrony 4.8, rsyslog 8.2512 (no TLS driver installed), snmpd 5.9.4, keepalived 2.3.4, nftables 1.1.6; hsflowd is not in the archive → out, listed
  - no KVM, no VM (D-002): the VM reboot test is deferred; the container boot (vpp masked) is the evidence
  - disk: check `df -BG /` ≥ 40 G free before the VPP build and each chroot; remove chroots you no longer need
coordination:
  - P11 (parallel or later): `vrx-strongswan` is P11's package; vrx-meta `Recommends:` it until P11 merges
  - F-restconf-yang: nginx routes `/restconf` + `/.well-known/host-meta` · F-backup-restore / F-ab-upgrade: `/data/*` dirs, the api unit's `ReadWritePaths`, how the agent triggers `vrx-upgrade` (recommended: fixed-name `vrx-upgrade@<op>.service` units outside the agent sandbox — leave a hook, F-ab-upgrade ships the unit) · F-hardening-lite: adds drop-ins on top of your units, keep them minimal · P14/F-images/F-hardening-lite consume the repo the manager publishes to `/srv/vrx-artifacts/apt/`
evidence: `dpkg-deb -I/-c` per package, lintian (chroot) or the documented gate, `systemd-analyze security --offline=yes` scores, install/remove/purge/reinstall logs, container boot (`systemctl --failed` empty apart from masked vpp), `nft -c` of `inet vrx_base`, the rendered startup.conf, `verify.sh --require-files` on the VPP output, host-unchanged listings (root-netns `nft list tables`, `/etc/nginx`, `/etc/systemd/system`, `dpkg -l | wc -l` before/after)
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/P10.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/P10-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/P10.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: every container stopped (`machinectl list` shows no `w<SLOT>-p10`), every mount under `.scratch/` unmounted (`findmnt | grep /root/ngfw-wt/P10` empty), processes you started stopped by PID, chroots removed or listed with their size, the VPP output handed over (path in P10.md), no key material outside `~/.config/ngfw/apt-signing/`, dist/ and apps/agent/bin removed
questions: docs/status/tasks/P10-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own · install, remove or upgrade a package on this host
