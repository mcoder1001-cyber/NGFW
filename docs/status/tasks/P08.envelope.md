# TASK ENVELOPE — P08
id: P08   branch: task/P08   worktree: /root/ngfw-wt/P08   base: main@587fa74   started: 2026-09-24
title: Vertical slice: interfaces end to end (af_packet rig) — CRITICAL PATH, gates all S4 waves
prompt: prompts/P08-vertical-slice-interfaces.md
merged deps you can rely on: P05 (agent core), P06 (API), P07a+P07b (UI), DF-1 (interfaces + interface/<name> alias + iface.ResolveName), DF-2 (neighbors etc.), DF-3/4/6/7/8 (other factories), TD-1 (vpp/bootid), RF-1/2/3/4 (renderers), P13 (CLI), F-sdk
slot: 1 → VRX_SLOT=1 VRX_TEST_PREFIX=w1 VRX_HTTP_PORT=3100 VRX_WEB_PORT=5100 VRX_METRICS_PORT=9111 VRX_AGENT_SOCKET=/run/vrx-test/w1/agent.sock VRX_PG_DATABASE=vrx_w1 VRX_VALKEY_DB=1 VRX_VPP_TABLE_BASE=1000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env 1)"`; rig prefix w1 → 10.1.{1,2}.0/24)
obligations accumulated from decisions (all must be honoured and listed in P08.md with evidence):
  - D-065/D-069/D-073a: the desired-state builder emits `interface/<name>` alias objects and wires every interface reference to them; logical names everywhere
  - D-063/D-076 write-only + observe-only (DeleteOnAbsence false) semantics active in the reconciler for all registered descriptors
  - persisted stores in the agent state dir: DF-1 iface.SetClaimStore, DF-6/DF-8/DF-3 claim + applied-once records, DF-2 classify store, DF-5 WithBootStore (when merged) — all keyed with bootid (D-080); in-memory defaults are forbidden in the product agent
  - DF-8: call the DHCP client's Reconnected() from P05's reconnect hook
  - D-071: the product agent runs with globalsOwner=true on a real box; on this shared host test agents run with globalsOwner=false
  - D-072: static routes default to VPP; the FRR selector hook stays inactive until P12
  - D-073b: VRF/route descriptions kept in agent state
  - Normalizer hook applied before diffing
V19 SAFETY (D-095, crash on first packet): before ANY ping/traffic through the rig, verify there is no classify/ACL/SPD binding on the rig interfaces' sw_if_index (dump + assert); if TD-3 (sanitizer) merges while you work, merge main and use it. Never send packets through an unchecked interface. Check `systemctl show vpp -p NRestarts` before/after every host run — stop if it rises.
coordination: TD-2 (apps/api follow-ups, in review) and TD-3 (descriptor V19 guard, running) are open — do not edit their files (apps/api/src/{auth,users,candidate,diff}*, apps/agent/internal/descriptors/{interface,classify,adl,policer,ipfix}/*) beyond wiring calls; merge main when they land.
files you own exclusively: apps/agent/cmd/vrx-agent/** apps/agent/internal/{agent,subsystems,desired}/** apps/api/src/state/** apps/web/src/domains/interfaces/** test/topology/interfaces/** docs/user/interfaces/** docs/status/vertical-slice.md docs/status/tasks/P08*
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b (kept outside the repo) for screenshots; a video is optional; say so.
time box: 16 h — commit WIP at least every 45 min; keep docs/status/tasks/P08-wip.md current
finish: `tools/ci.sh --base main` green · topology test + restart-safety test green on slot 1 (pasted) · docs/status/tasks/P08.md + docs/status/vertical-slice.md with pasted real output · everything committed · stack stopped, lab lock only during runs (D-094), vrx_w1 dropped · final message = 10-line summary
never: merge · restart/kill VPP · Docker · pkill · secrets in files/logs · install packages
