# TASK ENVELOPE — F-snmp
id: F-snmp   branch: task/F-snmp   worktree: /root/ngfw-wt/F-snmp   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave C (day 13-15): SNMP v2c/v3 via snmpd renderer + private MIB
prompt: prompts/features/F-snmp.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main + P08 in wave-B/C prep)   wbs: D7.5
scope: RF-4's snmpd renderer and the `services.snmp` schema exist: the D-086 stand-in fields into the contract, a singleton scheduler descriptor wrapping the renderer, the AgentX subagent with VRX-MIB (VPP interface counters, agent health, revision), `/state/snmp`, UI, docs
merged deps you can rely on: P08 (desired/subsystems/projection patterns), RF-4 (renderers/snmpd: `TestPaths`, parse-run Validate, `WithSecretResolver`, persisted restart requests, gosnmp client, poller; shared helpers renderers/rfkit) (+ on main by then: TD-2, TD-3, W-seed; wave-A features only if merged when you start — check `git log main`)
read first: prompts/features/F-snmp.md · docs/status/wave-A-hotspots.md (§0 rules; A1 A2 C1–C7 P1 P4 P5 W1–W3 D4) · docs/status/wave-BC-numbers.md § F-snmp · apps/agent/internal/renderers/snmpd/README.md + docs/agent/renderers/snmpd.md · apps/agent/internal/renderers/{README.md,ALLOWLIST.md} · docs/decisions/PENDING-secret-channel.md · docs/decisions/LOG.md D-040, D-051, D-055, D-079, D-086, D-089, D-091, D-109
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; SNMP test ports per RF-4: agent udp 127.0.0.1:3<SLOT>61, trap listener / alternate listen 3<SLOT>62–3<SLOT>63; AgentX socket under /run/vrx-test/w<SLOT>/snmpd/)
daemon-owner: snmpd — slot test instances only: RF-4's harness (`snmpd.TestPaths("w<SLOT>")` under /run/vrx-test/w<SLOT>/snmpd/, child `snmpd -f … -C -c <dir>/snmpd.conf` bound only to loopback slot ports, exclusive /run/vrx-test/w<SLOT>/snmpd.lock); the system `snmpd` unit is installed, **disabled and inactive** — never start, enable, reload or reconfigure it, never touch /etc/snmp; stop every snmpd you started by its PID
files you own exclusively: apps/agent/internal/renderers/snmpd/** (gap-only: keep RF-4's redaction, parse run, restart-request persistence and goldens; name the test for every change) docs/agent/renderers/snmpd.md apps/agent/internal/snmpagent/** deploy/snmp/** apps/agent/internal/desired/snmp*.go apps/agent/internal/subsystems/snmp*.go apps/agent/internal/agent/rpc_snmp*.go packages/schema/src/domains/ext/snmp*.ts packages/schema/src/semantic/snmp*.ts packages/schema/examples/snmp-*.json packages/proto/test/fixtures/snmp-*.json apps/api/src/features/snmp/** apps/api/test/e2e/snmp* apps/web/src/domains/services/snmp/** apps/web/src/locales/{en,fa}/snmp.json docs/user/services/snmp.md test/topology/snmp/** docs/status/tasks/F-snmp*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-snmp` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-snmp.md and say so in the questions file); never reorder or reformat other lines): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["services"]` — exists by wave C; if not, whoever lands first adds the key — your singleton descriptor name, one Register line; the AgentX subagent is background work started from your own subsystems/snmp.go, never from agent.go) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (no Action case; `SnmpState`, if needed, lives in your rpc_snmp.go) · C1 packages/schema/src/domains/services.ts (the D-086 stand-ins: one key line each inside `SnmpSchema` (`sysServices`, `views`, `monitors`, `subagent`), the community and v3-user schemas (`view`); sub-schemas in your ext/snmp.ts — single toucher of those blocks) · C2 packages/schema/src/semantic/index.ts (one spread) · C3 packages/schema/src/index.ts (export of your ext file) · C4 new `snmp-*.json` fixtures only · C5 packages/proto/vrx/v1/dataplane.proto (`SnmpService` 12–15, `Community` 4, `V3User` 7 as one-line insertions inside P02c's `SnmpService` block; new messages in a `// ----- F-snmp -----` section; `SnmpState` under the service anchor only if needed) · C6 docs/contracts/proto.md (`### F-snmp: …`) · C7 generated files (regen, never hand-merged: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`) · P1 apps/api/src/app.module.ts · P4 apps/api/src/agent/agent.client.ts (only with a new RPC) · P5 apps/api/src/testing/fake-agent.ts (only with a new RPC: UNIMPLEMENTED stub on the contract commit; real fake in features/snmp/fake.ts) · W1 apps/web/src/router.tsx · W2 apps/web/src/nav/nav.ts (+nav.test.ts: `services` into BUILT_DOMAINS only if not there yet) · W3 apps/web/src/i18n.ts · services domain page + tab registry apps/web/src/domains/services/{ServicesPage.tsx,tabs.ts} (W-seed shell; one line — if the shell is not on main, do NOT create it: own route + NavItem under W1/W2, the manager folds it in) · apps/agent/internal/renderers/ALLOWLIST.md (rows only if you execute a new binary — none expected, the snmpd row exists) · D4 apps/agent/go.{mod,sum} (an AgentX library is a new dependency: questions file first, the manager runs `go mod tidy` on main)
contract numbers: docs/status/wave-BC-numbers.md § F-snmp — **SnmpService 12 `sys_services`, 13 `views`, 14 `monitors`, 15 `subagent` (only if the UI needs the switch); SnmpService.Community 4 `view`; SnmpService.V3User 7 `view`**; new `Snmp*` messages; RPC `SnmpState` only if renderer Retrieve cannot carry the state; nothing else (no ActionRequest member, no EventKind); commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-snmp-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, /etc/snmp and the snmpd unit, apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned), apps/agent/internal/renderers/{rfkit,unbound,chrony,rsyslog,kea,frr,strongswan,keepalived,vppstartup}/** and renderers/{renderer.go,helpers_*} (shared / other tasks — a need goes to the questions file), apps/agent/internal/agent/{agent,service,state,ifstate,metrics,telemetry,events}.go + apps/agent/cmd/** (A5 core — the stats reader there is unexported: open your own stats-segment connection), apps/agent/internal/desired/interfaces.go (A3), apps/api/src/{auth,users,secrets,commit,datastore}/** (use the secrets service, do not edit it), tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
host facts: net-snmp `snmpd` 5.9.4 installed (unit disabled, inactive); **`snmpwalk`, `snmpget`, `snmptrapd` are NOT installed** and nothing gets installed — walks and GETs run in-process through gosnmp (BSD-2, already in apps/agent/go.mod, D-086), the trap receiver is a gosnmp `TrapListener` on 3<SLOT>62; a community or passphrase never goes into a process argv (RF-4 README) · aes256 privacy is not in this net-snmp build (RF-4 rejects it) · stats segment `/run/vpp/stats.sock` (root:vpp) for the VRX-MIB interface table · no data NICs bound (D-026): VPP interfaces = slot loopbacks / rig host-interfaces
obligations accumulated from decisions (all must be honoured and listed in F-snmp.md with evidence): D-086 RF-4's stand-ins (views, community/user view, sysServices, monitors) move into the contract now; RF-4 then reads the typed fields, not structpb · D-109 (d) renderer stage = one singleton scheduler descriptor wrapping the snmpd renderer (Create/Update = Render → Validate → Apply, Delete = RF-4's disabled rendering, Retrieve = renderer state) — log the choice with options · D-051/D-040 communities and USM passphrases are `password/<name>` refs; values never in DesiredState, GET, logs, fixtures or status files (redact with `rfkit.Redactor`; test literals `VRX_TEST_PSK_F-snmp_*`) · D-079 (RF-4 H1) startup-only directive changes (listen, AgentX socket, engine id) → persisted restart request until acted on, never a silent SIGHUP · RF-4 review M1: default listen stays loopback-only with the `# WARNING:` line · RF-4: `vrf` ≠ `default` stays rejected unless binding snmpd to a VRF is cheap (it needs a Linux VRF = linux-cp) — say which in F-snmp.md · VRX-MIB under a documented placeholder enterprise OID (PEN question)
coordination: (1) secret channel — nothing carries secret material API→agent (PENDING-secret-channel, D-109 b; it lists P11/F-wireguard/P12/F-unbound — F-snmp is the same case: tell the manager in the questions file); until the answer the renderer resolves through a slot-local fixture resolver and only the end-to-end secret step waits · (2) renderer stage — if the manager has put a shared renderer stage on main when you start, use it instead of the singleton descriptor · (3) F-unbound-chrony-syslog (wave B) shares `renderers/rfkit` (read-only for both) and `Domains["services"]`; F-kea-dhcp-relay, F-host-stack, F-lb, F-qos-flat, F-ipfix-sflow share the services page · (4) F-dashboard-prom-alarms may later send traps through your renderer — no trap-target API for it now
SAFETY: no data-plane packets are needed; the subagent reads counters only (stats segment, read-only) · one host test package at a time (D-087); if any host run touches VPP, paste `systemctl show vpp -p NRestarts` before/after and stop if it rises; never touch the host's own daemons (kill only PIDs you started)
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots; gosnmp walk output pasted with communities/passphrases redacted
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-snmp.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-snmp-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-snmp.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite/snmpd/trap listener), by PID · lab lock released · vrx_w<SLOT> dropped · /run/vrx-test/w<SLOT>/snmpd removed · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-snmp-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
