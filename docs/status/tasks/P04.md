# P04 — Lab tooling for the shared host, veth/netns packet rig, binapi for all plugins, local PostgreSQL/Valkey

branch `task/P04` · worktree `/root/ngfw-wt/P04` · base `main@c8867a5` · slot 3 (`w3`) · 2026-09-23 13:13 → 14:05 (+0330)
Everything below ran on the host `vrx-a` against the **real VPP v26.06-release** (`/run/vpp/api.sock`). Path recorded: **af_packet** (D-010).

## What was built

| item | files | notes |
|---|---|---|
| `tools/lab` (bash, python3-yaml for inventory) | `tools/lab` | `status`, `vppctl <vm>`, `provision <vm> [--apply]` (vrx-a = verify-only), `restart-vpp`/`kill-vpp` (refuse while `handover: pending`, else `flock -x /run/lock/vrx-vpp.lock`), `lock shared\|exclusive <cmd>` / `lock status` (`/run/lock/vrx-lab.lock`), `env <slot>`, `rig up\|down\|gc\|show <prefix>`, `inventory`. Local mode = `mgmt: local` (no SSH); remote mode over SSH for future VMs. Never edits `/etc/vpp`, never installs packages, never restarts VPP on this host. |
| inventory | `test/topology/{vrx-a,vrx-b,vrx-c,host-lan,host-wan,peer-frr,peer-sswan}.yml`, `README.md` | vrx-a with real facts (7× vmxnet3 incl. 6 unassigned — see questions #1); the others `state: planned`, `mgmt: tbd` |
| packet rig | `tools/lab rig` | `ns-<p>-lan`/`ns-<p>-wan`, veths `<p>l0↔<p>l1`, `<p>w0↔<p>w1`, VPP `create host-interface name <p>l0/<p>w0` → `host-<p>l0`/`host-<p>w0` with `10.<slot>.1.1/24`, `10.<slot>.2.1/24`; namespaces `.2` + default route via VPP. Idempotent `up`, exact `down` (VPP first, then netns/veth) with leftover check, `gc` by prefix, `show`. Prefix must match `^[a-z][a-z0-9]{1,5}$`; slot from `w<N>`, `VRX_SLOT`, or a hash into 100–199 for other prefixes. Output always has `path: af_packet`. |
| binapi | `tools/binapi-gen.sh`, `apps/agent/binapi/**` (143 packages + `MANIFEST` + `README.md`) | reads `/usr/share/vpp/api/{core,plugins}` (`VPP_API_DIR` overridable), `binapi-generator` govpp v0.13.0, `--import-prefix ngfw/agent/binapi --no-source-path-info`, generates to a temp dir, `gofmt` check, `rsync --delete` into place, MANIFEST of `vl_api_version` CRCs, warns on go.mod/generator version mismatch, verifies `go build ./binapi/...` (non-zero exit on failure). Idempotent, commits nothing. `apps/agent/go.mod` now requires `go.fd.io/govpp v0.13.0`. |
| local services | `deploy/dev/README.md`, `deploy/dev/pg-test.sh` | PostgreSQL **18.6** and Valkey **9.0.4** were already installed/enabled/active and localhost-bound → nothing installed. `pg-test.sh create\|drop\|dsn\|list <name>`: role+db `vrx_<name>`, random password only in `/run/vrx-test/<name>/pg.env` (0600), TCP login check, idempotent create, verified drop. |
| smoke test | `test/integration/smoke/{go.mod,go.sum,smoke_test.go,run.sh,README.md}` | own module (`replace ngfw/agent => ../../../apps/agent`); `VRX_INTEGRATION=1`, root, `flock -s` lab lock; govpp `show_version` ⊇ 26.06 via generated `vpe`; `rig up`; `sw_interface_dump` shows both host-interfaces; ping LAN→WAN; stats-segment rx counters increase on both; `rig down` → no prefixed netns/veth/VPP object. |
| docs | `docs/lab/vmware.md` | VM shapes (vrx-b/c 4 vCPU/6 GB/4× vmxnet3 mgmt/lan/wan/p2p; host-lan/wan, peer-frr, peer-sswan 1 vCPU/1 GB), port-group requirements, addressing, how `provision` treats each, checklist. |

## Acceptance evidence (pasted, real output)

### 1. `tools/lab status` — VPP active, 26.06, plugin count, `handover: pending`
```
$ tools/lab status
== vrx-a  (mgmt: local, role: vrx, state: active) ==
host:        ubuntu-26.04 · Ubuntu 26.04.1 LTS · 7.0.0-31-generic · load 3.61 2.37 1.62
vpp.service: active (enabled) pid 1109 since Wed 2026-09-23 12:42:53 +0330
vpp version: vpp v26.06-release built by root on ubuntu-26.04 at 2026-09-23T08:06:28
plugins:     84 loaded · af_packet: yes · linux_cp: no · linux_nl: no · npt66: no · dpdk: yes
interfaces:  local0
sockets:     api.sock (root:vpp 775) cli.sock (root:vpp 775) stats.sock (root:vpp 775)
handover:    pending  (docs/lab/host-vrx-a.md) → restart-vpp/kill-vpp REFUSED
startup.conf: /etc/vpp/startup.conf (read-only) · hugepages 1024 total, 963 free
data NICs:   6 non-mgmt of 7 in inventory (kernel driver, not in startup.conf) · data_path: af_packet
path:        af_packet
rig:         prefixes with rig objects:
netns:
postgresql:  active · 18 main port 5432 online · listen 127.0.0.1:5432
valkey:      active · v=9.0.4 · listen 127.0.0.1:6379 [::1]:6379  · ping PONG
locks:       /run/lock/vrx-lab.lock: free · /run/lock/vrx-vpp.lock: free (no file)
```

### 2. `rig up` + ping through VPP + `show interface` rx counters + clean `rig down` (prefix w3 = my slot; w9 identical by construction)
```
$ tools/lab rig up w3
rig up w3 (slot 3, path af_packet)
  create netns ns-w3-lan
  create veth w3l0 <-> ns-w3-lan:w3l1
  create vpp host-w3l0 (af_packet on w3l0)
  create netns ns-w3-wan
  create veth w3w0 <-> ns-w3-wan:w3w1
  create vpp host-w3w0 (af_packet on w3w0)
rig: up
prefix: w3
slot: 3
path: af_packet
lan_ns: ns-w3-lan / lan_veth: w3l1 / lan_ip: 10.3.1.2/24 / lan_gw: 10.3.1.1 / vpp_lan_if: host-w3l0
wan_ns: ns-w3-wan / wan_veth: w3w1 / wan_ip: 10.3.2.2/24 / wan_gw: 10.3.2.1 / vpp_wan_if: host-w3w0
ping: ip netns exec ns-w3-lan ping -c 3 10.3.2.2

$ ip netns exec ns-w3-lan ping -c 3 -i 0.3 10.3.2.2
PING 10.3.2.2 (10.3.2.2) 56(84) bytes of data.
64 bytes from 10.3.2.2: icmp_seq=2 ttl=63 time=1.94 ms
64 bytes from 10.3.2.2: icmp_seq=3 ttl=63 time=1.31 ms
--- 10.3.2.2 ping statistics ---
3 packets transmitted, 2 received, 33.3333% packet loss, time 602ms      (seq 1 lost to ARP resolution; ttl=63 = one hop through VPP)

$ vppctl show interface
              Name               Idx    State  MTU (L3/IP4/IP6/MPLS)     Counter          Count
host-w3l0                         1      up          9000/0/0/0     rx packets                     8
                                                                    rx bytes                     672
                                                                    tx packets                     3
                                                                    tx bytes                     238
                                                                    drops                          5
                                                                    ip4                            3
                                                                    ip6                            4
host-w3w0                         2      up          9000/0/0/0     rx packets                     5
                                                                    rx bytes                     414
                                                                    tx packets                     3
                                                                    tx bytes                     238
                                                                    drops                          3
                                                                    ip4                            2
                                                                    ip6                            2
local0                            0     down          0/0/0/0
$ vppctl show interface addr
host-w3l0 (up):
  L3 10.3.1.1/24
host-w3w0 (up):
  L3 10.3.2.1/24
local0 (dn):

$ tools/lab rig up w3        # idempotent
  reuse  netns ns-w3-lan / reuse  veth w3l0 / reuse  vpp host-w3l0 / reuse  netns ns-w3-wan / reuse  veth w3w0 / reuse  vpp host-w3w0
$ tools/lab rig down w3
rig down w3
  delete vpp host-w3l0
  delete netns ns-w3-lan (and its veth peer)
  delete vpp host-w3w0
  delete netns ns-w3-wan (and its veth peer)
rig: down … path: af_packet
rc=0
$ ip netns list; ip -o link | grep -E "w3[lw]" || echo "no w3 links"; vppctl show interface | grep w3 || echo "no w3 vpp ifs"
no w3 links
no w3 vpp ifs
```
`rig gc` on a half-built rig (netns + veth + VPP if created by hand): `rig show w3` → `rig: partial (3/6 objects)`; `rig gc w3` → `delete vpp host-w3l0`, `delete netns ns-w3-lan`, `rig: down`, rc=0, no leftovers. Bad prefixes `W3`, `toolong7`, `w` are refused (rc=1).

### 3. `kill-vpp` / `restart-vpp` refuse while handover is pending
```
$ tools/lab kill-vpp vrx-a
REFUSED: 'kill-vpp vrx-a' is not allowed while docs/lab/host-vrx-a.md says 'handover: pending' (D-012).
  Nobody restarts or kills the VPP process before the VPP bring-up agent hands the host over.
  Restart-safety is proven by the agent-restart simulation (FAST MODE DoD 3): stop YOUR agent, delete YOUR prefixed
  objects via binapi, start YOUR agent → it recreates them. After handover only the manager runs this, under
  flock -x /run/lock/vrx-vpp.lock.
rc=2
$ tools/lab restart-vpp vrx-a
REFUSED: 'restart-vpp vrx-a' is not allowed while … 'handover: pending' (D-012).   rc=2
$ systemctl is-active vpp
active                                   (VPP pid 1109 untouched, up since 12:42:53)
```

### 4. `apps/agent/binapi/` committed; rerunning the generator yields an empty diff
```
$ tools/binapi-gen.sh
binapi-gen: govpp v0.13.0 · input /usr/share/vpp/api (54 core + 89 plugin .api.json) · output apps/agent/binapi
binapi-gen: go build ./binapi/... ok
binapi-gen: 143 packages in apps/agent/binapi (import ngfw/agent/binapi/<api>) · review: git status --short apps/agent/binapi
$ git status --short apps/agent/binapi | wc -l
0
$ ls apps/agent/binapi | wc -l; du -sh apps/agent/binapi        # 143 packages + MANIFEST + README.md
145
5.3M	apps/agent/binapi
$ head -4 apps/agent/binapi/interface/interface.ba.go
// Code generated by GoVPP's binapi-generator. DO NOT EDIT.
// versions:
//  binapi-generator: v0.13.0
//  VPP:              26.06-release
$ cd apps/agent && go build ./... && go vet ./... && echo ok
ok
$ VPP_API_DIR=/nonexistent tools/binapi-gen.sh
binapi-gen: /nonexistent has no core/ and plugins/ — is vpp installed here? (VPP_API_DIR overrides)     rc=1
```

### 5. `pg-test.sh create w3 && drop w3`; `psql` / `valkey-cli ping` on localhost
```
$ deploy/dev/pg-test.sh create w3
create role vrx_w3
create database vrx_w3 (owner vrx_w3)
check  vrx_w3 as vrx_w3 · PostgreSQL 18.6 (Ubuntu 18.6-0ubuntu0.26.04.1) on x86_64-pc-linux-gnu
ok     env /run/vrx-test/w3/pg.env (0600) · DSN postgres://vrx_w3:<redacted>@127.0.0.1:5432/vrx_w3
$ ls -l /run/vrx-test/w3/pg.env
-rw------- 1 root root 214 Sep 23 13:28 /run/vrx-test/w3/pg.env
$ deploy/dev/pg-test.sh list
vrx_w3  owner vrx_w3
$ deploy/dev/pg-test.sh create w3            # idempotent
reuse  role vrx_w3 (password refreshed) / reuse  database vrx_w3 / check … / ok
$ psql "$(deploy/dev/pg-test.sh dsn w3)" -Atc "create table t(x int); insert into t values (1); select count(*) from t"
CREATE TABLE
INSERT 0 1
1
$ deploy/dev/pg-test.sh drop w3
drop   database vrx_w3
drop   role vrx_w3
ok     nothing named vrx_w3 / vrx_w3 remains
$ valkey-cli -h 127.0.0.1 -n 3 ping
PONG
$ deploy/dev/pg-test.sh create "W3;drop"
pg-test: name 'W3;drop' must match ^[a-z][a-z0-9_]{0,15}$ (e.g. w3)     rc=1
```

### 6. Smoke test (Go, `VRX_INTEGRATION=1`, shared lock) — `path: af_packet`
```
$ VRX_TEST_PREFIX=w3 test/integration/smoke/run.sh        # = tools/lab lock shared go test -count=1 -v ./...
=== RUN   TestPingThroughVPPOverAfPacketRig
    smoke_test.go:72: vpp: program=vpe version=26.06-release built=2026-09-23T08:06:28
    smoke_test.go:99: vpp interfaces: host-w3l0=2 host-w3w0=1
    smoke_test.go:105: ping ns-w3-lan → 10.3.2.2 through VPP:
        4 packets transmitted, 3 received, 25% packet loss, time 905ms
        rtt min/avg/max/mdev = 1.277/1.309/1.345/0.027 ms
    smoke_test.go:114: rx packets host-w3l0: 1 → 9
    smoke_test.go:114: rx packets host-w3w0: 0 → 6
    smoke_test.go:122: path: af_packet · prefix: w3 · rig down clean
--- PASS: TestPingThroughVPPOverAfPacketRig (3.19s)
ok  	ngfw/test/integration/smoke	3.218s
$ go test -count=1 -v ./...                                # without VRX_INTEGRATION
    smoke_test.go:44: integration test: set VRX_INTEGRATION=1 to run against the host VPP (path af_packet)
--- SKIP: TestPingThroughVPPOverAfPacketRig (0.00s)
```

### 7. `provision vrx-a` (verify-only) — all PASS, nothing changed
```
$ tools/lab provision vrx-a
provision vrx-a — LOCAL MODE = VERIFY ONLY (handover: pending). Nothing is changed on this host.
  PASS  OS ubuntu-26.04 · vpp package 26.06-release (from /root/vpp/build-root, D-001) · packages vpp-plugin-core/-dpdk/vpp-drivers/
        vpp-crypto-engines/libvppinfra/python3-vpp-api · vpp.service active+enabled · 3 sockets root:vpp 775 · show version v26.06-release ·
        84 plugins · af_packet loaded · hugepages 1024 × 2 MB · 143 .api.json · binapi-generator govpp v0.13.0 · go1.26.0 ·
        tools ip/ping/flock/python3/jq/psql/valkey-cli · python3-yaml · /run/lock writable · ip_forward=1 · postgresql 18 · valkey PONG
  INFO  linux_cp / linux_nl / npt66 plugin not loaded (needs startup.conf → after handover; tests t.Skip)
verify: all checks passed — vrx-a is ready for tools/lab rig, binapi-gen.sh and the integration suite.      rc=0
$ tools/lab provision vrx-b            # planned VM: prints the plan + rendered startup.conf, refuses (rc=3)
```

### 8. CI gate
```
$ tools/ci.sh --base main          (log: /root/ngfw-wt/logs/P04-ci.log)
== install (frozen lockfile) == / == generated output must be clean == / == contract guard vs main == / == forbidden patterns ==
== typecheck == / == lint == / == test == / == build == / == agent ==
CI GATE PASSED
```
`shellcheck -S warning tools/lab tools/binapi-gen.sh deploy/dev/pg-test.sh` → clean. `gofmt -l`/`go vet` on the smoke module → clean.

## Decisions taken (for docs/decisions/LOG.md — options considered)
- **D-P04-1** Rig slot/addressing: `w<N>` prefixes → `10.<N>.{1,2}.0/24`; other prefixes hash into `10.100–199.x.x` (or `VRX_SLOT`). Options: (a) require `--slot` (b) derive from prefix + hash fallback. (b) keeps `rig up <prefix>` one-argument. Reversal: trivial.
- **D-P04-2** Inventory = YAML parsed by python3 + PyYAML (both on the host). Options: (a) flat key=value in bash (b) yq (not installed) (c) python3-yaml. (c) matches the `.yml` requirement with no new install. Reversal: low.
- **D-P04-3** `binapi-gen.sh` uses `--no-source-path-info` (output independent of `VPP_API_DIR`) but keeps the version header (`VPP: 26.06-release`, generator v0.13.0) as provenance, plus a `MANIFEST` of `vl_api_version` CRCs. Options: (a) strip all headers (b) keep both. Reversal: trivial.
- **D-P04-4** `pg-test.sh` stores the generated password only in `/run/vrx-test/<name>/pg.env` (0600, tmpfs) and prints redacted DSNs. Options: (a) fixed well-known dev password in the repo (forbidden by 00-CONTEXT) (b) random per create in /run. Reversal: trivial.
- **D-P04-5** Smoke test is its own Go module (`test/integration/smoke`, replace → apps/agent) rather than a package inside `apps/agent`. Options: (a) inside the agent module (b) own module. (b) keeps `make test` in apps/agent unit-only and lets `tools/ci.sh full` (P09) call `run.sh`. Reversal: low.
- **D-P04-6** `kill-vpp` = `systemctl kill -s SIGKILL vpp` under `flock -x /run/lock/vrx-vpp.lock` + `flock -x` lab lock (after handover only); `restart-vpp` = `systemctl restart vpp` under the same locks. Both wait for `api.sock` + `show version`. Reversal: trivial.
- Not a decision but a fact: PostgreSQL/Valkey were already present → nothing installed (README records versions).

## Out of scope / left undone
- Remote `provision --apply` for vrx-b/c is implemented but **untested** (no VM exists). DPDK on vrx-a (needs startup.conf → handover) untouched.
- `tools/ci.sh full` integration wiring is P09's; the entry point is `VRX_TEST_PREFIX=<p> test/integration/smoke/run.sh`.
- No golangci-lint on the host (P09); `go vet` only. Generated binapi is `DO NOT EDIT`-headed so linters skip it.
- host-lan/wan, peer-frr/sswan software install (iperf3/FRR/strongSwan) belongs to the tasks that need them.

## Open questions
See `docs/status/tasks/P04-questions.md` (8 items: 6 unassigned vmxnet3 NICs on vrx-a; `apps/agent/go.mod` change; metrics port formula for slots 10–12; rig naming wording in shared-host-rules §2; PostgreSQL 18 vs "16"; plugin count 84 vs 94; remote provision untested; wt.sh pull/push ordering hazard) + items 9–12 added after the review (below).

---

## Review fixes (2026-09-23 14:50 → 15:25 +0330 — after `P04-review.md` returned BLOCK on `6f9f330`)

All seven findings addressed; F1–F4 (required) re-tested on `vrx-a` against the real VPP v26.06-release, **pid 1109 untouched throughout**
(`ActiveEnterTimestamp 12:42:53` before and after; `/etc/vpp/startup.conf` mtime 11:55 unchanged). Nothing outside the P04 file list was
edited. Logs: `/root/ngfw-wt/logs/P04-fix-{F1-F2,F3-F5,smoke,ci}.log`.

| finding | fix | files |
|---|---|---|
| **F1** HIGH — `rig down`/`gc` matched other slots' VPP interfaces by unanchored prefix (`w1` → `host-w11l0`) | `rig_own_netns/links/vpp` match exactly `^ns-<p>-(lan\|wan)$`, `^<p>[lw][0-9]+$`, `^host-<p>[lw][0-9]+$`; `rig_leftovers` and `gc` use only these. Go `leftovers()` uses the same three anchored regexps (`rigObjectsFor`, prefix quoted) + pure unit test `TestRigObjectMatchIsAnchored` (runs without `VRX_INTEGRATION`, covers w1 vs w10/w11/w12 and foreign names) | `tools/lab`, `test/integration/smoke/smoke_test.go` |
| **F2** MED — `rig up` printed `up` after VPP refused the address; `VRX_SLOT` silently overrode the prefix | `rig_side_up`: if VPP already has `<gw>/24` → `reuse addr`; else `set interface ip address` via `vpp_cli_ok` (error regex now also catches `conflict(s)`/`already`) and **die with VPP's message** on any refusal, then read the address back (`vppctl` emits CRLF — `tr -d '\r'` added to `vpp_ifnames`/`vpp_if_addrs`, the first fix attempt failed on exactly that). `rig_print` adds `vpp_lan_addr`/`vpp_wan_addr` read from VPP. `rig_resolve_slot`: `w<N>` → slot N always, a different `VRX_SLOT` only warns; non-`w` prefixes **require** `VRX_SLOT` (hash fallback removed, F7); `VRX_SLOT ≤ 12` warns that it is a worker range. `down`/`gc`/`show` need no slot | `tools/lab` |
| **F3** MED — guard trusted `vpp.handover_doc`, `VRX_TOPOLOGY_DIR` and the worktree's own copy | `handover_sources`: fixed path `docs/lab/host-<vm>.md`, read from the canonical main checkout `/root/ngfw` (fallback `git -C $ROOT show main:…`) **and** from the calling worktree; `done` only when every source says done; absent/unparseable → pending. Inventory key deleted from `vrx-a.yml`; `status` and the REFUSED message print both sources | `tools/lab`, `test/topology/vrx-a.yml` |
| **F4** MED — remote `--apply` could ssh to this host and ignored `role` | `remote_ip()` (every remote command goes through it via `run_on`) refuses `127.*`, `::1`, `0.0.0.0` and every address in `ip -o addr`/`hostname -I`; `cmd_provision`: `role ≠ vrx` → one-line plan, `--apply` refused; `--apply` additionally needs `VRX_LAB_REMOTE_APPLY=1`; all checks run before any ssh/scp; dry run unchanged (rc 0) | `tools/lab`, `docs/lab/vmware.md` |
| **F5** LOW — locks released before VPP was back; bare `rig` never took the shared lock | `vpp_cycle_locked`: `systemctl restart\|kill` **and** the 30 s wait for `api.sock` + `show version` inside one `flock -x vpp.lock` + `flock -x lab.lock`; `rig up\|down\|gc` hold `flock -s` on the lab lock (fd 9, wait ≤ 120 s, then a clear message pointing at `lock status`) | `tools/lab` |
| **F6** LOW — smoke module invisible to the quick gate | `tools/ci.sh` is P09-owned → asked in `P04-questions.md` #9 (exact step to add, or a root `go.work`); the new unit test makes that step meaningful without VPP | `P04-questions.md` |
| **F7** LOW — smaller items | `fmt` import + `var _ = fmt.Sprintf` removed; `ip netns list`/`ip -o link` failures are `t.Fatalf` (no vacuous "clean"); `set interface mtu packet 1500` on both host-interfaces (was 9000 vs veth 1500); hash-to-slot removed (F2); `pg-test.sh` reuse says "password unchanged, re-applied from pg.env" vs "refreshed: VRX_PG_PASSWORD\|new"; `epoll_ctl` quirk documented below and in questions #10 (`host-vrx-a.md` is not P04's) | `smoke_test.go`, `tools/lab`, `deploy/dev/pg-test.sh` |

### F1 re-test — the reviewer's three commands, pasted (`/root/ngfw-wt/logs/P04-fix-F1-F2.log`)
```
### F1 re-test 2026-09-23T15:01:01+0330 — a w11 rig up, then rig down w1 / rig gc w1 must touch nothing
$ tools/lab rig up w11
rig up w11 (slot 11, path af_packet)
  create netns ns-w11-lan
  create veth w11l0 <-> ns-w11-lan:w11l1
  create vpp host-w11l0 (af_packet on w11l0)
  set    addr host-w11l0 10.11.1.1/24
  create netns ns-w11-wan
  create veth w11w0 <-> ns-w11-wan:w11w1
  create vpp host-w11w0 (af_packet on w11w0)
  set    addr host-w11w0 10.11.2.1/24
rig: up
prefix: w11
slot: 11
path: af_packet
… vpp_lan_if: host-w11l0 / vpp_lan_addr: 10.11.1.1/24 / vpp_wan_if: host-w11w0 / vpp_wan_addr: 10.11.2.1/24
rc=0

$ tools/lab rig down w1
rig down w1
rig: down                                   ← nothing deleted (no "delete …" line), rc=0
rc=0

$ tools/lab rig gc w1
rig gc w1 (exactly: ns-w1-lan|wan, w1[lw]<n>, host-w1[lw]<n> — nothing of any other prefix)
rig: down                                   ← nothing deleted, rc=0
rc=0

$ tools/lab rig show w11 | grep -E "^(rig|vpp_)"; ip netns list
rig: up
vpp_lan_if: host-w11l0
vpp_lan_addr: 10.11.1.1/24
vpp_wan_if: host-w11w0
vpp_wan_addr: 10.11.2.1/24
ns-w11-wan (id: 1)
ns-w11-lan (id: 0)

$ tools/lab rig down w11
rig down w11
  delete vpp host-w11l0
  delete netns ns-w11-lan (and its veth peer)
  delete vpp host-w11w0
  delete netns ns-w11-wan (and its veth peer)
rig: down
rc=0

$ ip netns list; vppctl show interface | awk "NR>1 && /^[^ ]/{print $1}"
local0
```

### F2 — pasted
```
$ VRX_SLOT=7 tools/lab rig up w3
warning: rig: prefix w3 means slot 3 — ignoring VRX_SLOT=7 (the prefix wins)
rig up w3 (slot 3, path af_packet)
  … set    addr host-w3l0 10.3.1.1/24 … set    addr host-w3w0 10.3.2.1/24
rig: up / slot: 3 / vpp_lan_addr: 10.3.1.1/24 / vpp_wan_addr: 10.3.2.1/24          rc=0

$ VRX_SLOT=3 tools/lab rig up w3x        # same 10.3.x.x as the live w3 rig → VPP refuses → up FAILS
warning: rig: VRX_SLOT=3 is worker slot w3's range (10.3.x.x) — its rig and yours cannot coexist
rig up w3x (slot 3, path af_packet)
  create netns ns-w3x-lan
  create veth w3xl0 <-> ns-w3x-lan:w3xl1
  create vpp host-w3xl0 (af_packet on w3xl0)
error: rig: VPP refused 'set interface ip address host-w3xl0 10.3.1.1/24': set interface ip address: failed to add 10.3.1.1/24 on host-w3xl0 which conflicts with 10.3.1.1/24 for interface host-w3l0 — is 10.3.x.x already used by another rig? (vppctl show interface addr)
rc=1
$ tools/lab rig show w3x | head -1; tools/lab rig down w3x
rig: partial (3/6 objects)
rig down w3x
  delete vpp host-w3xl0
  delete netns ns-w3x-lan (and its veth peer)
rig: down                                                                              rc=0

$ tools/lab rig up abc
error: rig: prefix 'abc' is not w<N>, so its addresses cannot be derived — export VRX_SLOT=<13..254> (10.<slot>.{1,2}.0/24); no hash fallback
rc=1
$ tools/lab rig up w3        # idempotent reuse incl. address
  reuse  netns ns-w3-lan / reuse  veth w3l0 / reuse  vpp host-w3l0 / reuse  addr host-w3l0 10.3.1.1/24 / … / reuse  addr host-w3w0 10.3.2.1/24
rig: up                                                                                rc=0
$ tools/lab rig down w3 → delete vpp host-w3l0 / delete netns ns-w3-lan / delete vpp host-w3w0 / delete netns ns-w3-wan / rig: down   rc=0
$ ip netns list; vppctl show interface | awk … → local0
```

### F3 — pasted (nothing restarted: pid 1109, ActiveEnterTimestamp 12:42:53 unchanged)
```
# a) an inventory in VRX_TOPOLOGY_DIR whose vrx-a.yml points handover_doc at a file saying `handover: done`
$ VRX_TOPOLOGY_DIR=$tmp tools/lab status | grep handover
handover:    pending  (canonical:/root/ngfw/docs/lab/host-vrx-a.md=pending · worktree:docs/lab/host-vrx-a.md=pending) → restart-vpp/kill-vpp REFUSED
$ VRX_TOPOLOGY_DIR=$tmp tools/lab kill-vpp vrx-a
REFUSED: 'kill-vpp vrx-a' is not allowed while docs/lab/host-vrx-a.md says 'handover: pending' (D-012).
  sources: canonical:/root/ngfw/docs/lab/host-vrx-a.md=pending · worktree:docs/lab/host-vrx-a.md=pending
  (read from the canonical main checkout AND this worktree — both must say done; inventory/env cannot override this)
  …                                                                                    rc=2
# b) this worktree's copy edited to say done (reverted right after with git checkout); canonical main still pending
$ tools/lab status | grep handover
handover:    pending  (canonical:/root/ngfw/docs/lab/host-vrx-a.md=pending · worktree:docs/lab/host-vrx-a.md=done) → restart-vpp/kill-vpp REFUSED
$ tools/lab restart-vpp vrx-a
REFUSED: 'restart-vpp vrx-a' is not allowed while docs/lab/host-vrx-a.md says 'handover: pending' (D-012).
  sources: canonical:/root/ngfw/docs/lab/host-vrx-a.md=pending · worktree:docs/lab/host-vrx-a.md=done          rc=2
$ git status --short docs/lab/host-vrx-a.md →            (reverted, clean)
$ systemctl show vpp -p MainPID -p ActiveEnterTimestamp
ActiveEnterTimestamp=Wed 2026-09-23 12:42:53 +0330
MainPID=1109
```

### F4 — pasted (temporary inventory in a mktemp dir, removed afterwards; no ssh was attempted)
```
$ VRX_TOPOLOGY_DIR=$t2 tools/lab provision selfvm --apply     # role vrx, mgmt 172.30.126.195 = this host
provision selfvm — REMOTE MODE (…)
error: REFUSED: selfvm has mgmt 172.30.126.195, which is THIS host (::1 127.0.0.1 172.30.126.195 192.168.122.1 fe80::… ) — remote commands never target the machine they run on (ssh-to-self would write /etc/vpp here). Use 'mgmt: local' for this host.   rc=1
$ VRX_TOPOLOGY_DIR=$t2 tools/lab vppctl lo show version           # mgmt 127.0.0.1
error: REFUSED: lo has mgmt 127.0.0.1, which is THIS host (…)                                                          rc=1
$ VRX_TOPOLOGY_DIR=$t2 tools/lab provision hostx --apply      # role host
provision hostx — role 'host' (no VPP): plan only
  plain Linux networking; its software (iperf3 / FRR / strongSwan) is installed by the task that needs it (docs/lab/vmware.md).
error: REFUSED: provision --apply on hostx (role 'host') — tools/lab provisions VPP only on 'role: vrx' machines             rc=1
$ VRX_TOPOLOGY_DIR=$t2 tools/lab provision farvm --apply      # role vrx, remote 10.255.255.250, no confirmation
error: REFUSED: provision --apply writes /etc/vpp/startup.conf + /etc/sysctl.d/80-vpp.conf and installs packages on root@10.255.255.250 — rerun with VRX_LAB_REMOTE_APPLY=1 to confirm   rc=1
$ VRX_TOPOLOGY_DIR=$t2 tools/lab provision farvm              # dry run
dry run — add --apply (and VRX_LAB_REMOTE_APPLY=1) to execute; never runs against this host.                            rc=0
$ ls -la /etc/vpp/startup.conf → -rw-r--r-- 1 root root 8211 Sep 23 11:55 /etc/vpp/startup.conf   (unchanged)
```

### F5 — pasted
```
$ (tools/lab lock exclusive sleep 3 &); sleep 0.3; tools/lab lock status | head -1; time (tools/lab rig down w1 | head -1)
/run/lock/vrx-lab.lock: exclusively-held
rig down w1
real	0m2.967s            ← rig waited for the exclusive holder, then ran
```
The locked restart/kill body (`vpp_cycle_locked`) cannot be executed while `handover: pending`; it is `bash -n`/shellcheck-clean and `kill-vpp vrx-a` still refuses (rc=2, above).

### Smoke test, MTU, pg-test after the fixes — pasted (`/root/ngfw-wt/logs/P04-fix-smoke.log`)
```
$ VRX_TEST_PREFIX=w3 test/integration/smoke/run.sh
=== RUN   TestPingThroughVPPOverAfPacketRig
    smoke_test.go:72: vpp: program=vpe version=26.06-release built=2026-09-23T08:06:28
    smoke_test.go:99: vpp interfaces: host-w3l0=2 host-w3w0=1
    smoke_test.go:105: ping ns-w3-lan → 10.3.2.2 through VPP:
        4 packets transmitted, 3 received, 25% packet loss, time 904ms
    smoke_test.go:114: rx packets host-w3l0: 1 → 9
    smoke_test.go:114: rx packets host-w3w0: 1 → 8
    smoke_test.go:122: path: af_packet · prefix: w3 · rig down clean
--- PASS: TestPingThroughVPPOverAfPacketRig (3.58s)
=== RUN   TestRigObjectMatchIsAnchored
--- PASS: TestRigObjectMatchIsAnchored (0.00s)
ok  	ngfw/test/integration/smoke	3.603s
$ tools/lab rig up w3 >/dev/null; vppctl show interface | grep -E "^(host-w3| +Name)"; tools/lab rig down w3 >/dev/null
              Name               Idx    State  MTU (L3/IP4/IP6/MPLS)     Counter          Count
host-w3l0                         1      up          1500/0/0/0     rx packets                     2
host-w3w0                         2      up          1500/0/0/0
$ deploy/dev/pg-test.sh create w3 >/dev/null; deploy/dev/pg-test.sh create w3; deploy/dev/pg-test.sh drop w3 | tail -1
reuse  role vrx_w3 (password unchanged, re-applied from /run/vrx-test/w3/pg.env)
reuse  database vrx_w3
check  vrx_w3 as vrx_w3 · PostgreSQL 18.6 (Ubuntu 18.6-0ubuntu0.26.04.1) on x86_64-pc-linux-gnu
ok     env /run/vrx-test/w3/pg.env (0600) · DSN postgres://vrx_w3:<redacted>@127.0.0.1:5432/vrx_w3
ok     nothing named vrx_w3 / vrx_w3 remains
$ shellcheck -S warning tools/lab tools/binapi-gen.sh deploy/dev/pg-test.sh test/integration/smoke/run.sh → clean · gofmt -l → none · go vet ./... → ok
```

### VPP log quirk (F7, not P04's doing — `docs/lab/host-vrx-a.md` is not P04-owned, see questions #10)
```
$ journalctl -u vpp --since "3 hours ago" --no-pager | grep -c epoll_ctl
48
Sep 23 15:02:13 ubuntu-26.04 vpp[1109]: vlib/file: vlib_file_update: epoll_ctl() failed on epfd (7), file 'host-w3w0 queue 0' (fd 24), errno 9
```
One such line per `delete host-interface` (errno 9 = EBADF: the af_packet plugin closes the queue fd before vlib updates its epoll set).
Harmless — the interface is gone and the next `create` works; the unit logs to the journal, `/var/log/vpp/vpp.log` does not exist here.

### Decisions amended / added (for docs/decisions/LOG.md)
- **D-P04-1 (amended)** Non-`w<N>` prefixes no longer hash into `10.100–199.x.x`; they **require** an explicit `VRX_SLOT` (13–254 recommended). `w<N>` → slot N is fixed and `VRX_SLOT` cannot move it (warning). Reason: review F2/F7 — silent bucket/slot collisions produced an addressless rig that still reported `up`. Reversal: trivial.
- **D-P04-7** Handover guard = two-source AND on the fixed path `docs/lab/host-<vm>.md`: canonical main checkout (`/root/ngfw`, fallback `git show main:`) **and** the calling worktree; no inventory/env override. Options: (a) canonical only (b) both must agree. (b) chosen: a stale canonical checkout or a stale branch each alone cannot unlock a restart. Reversal: trivial.
- **D-P04-8** Remote `provision --apply` needs `role: vrx` + a non-self mgmt IP + `VRX_LAB_REMOTE_APPLY=1`; dry run stays the default. Options: (a) interactive prompt (b) env confirmation. (b): works from scripts and `wt.sh run`. Reversal: trivial.

### Left undone after the review
- F5's locked restart window (`vpp_cycle_locked`) is not executable until `handover: done` — syntax/shellcheck only.
- F6 gate wiring → P09 (questions #9). `docs/lab/host-vrx-a.md` epoll_ctl line → manager (questions #10).

### CI gate after the fixes — pasted (`/root/ngfw-wt/logs/P04-fix-ci.log`, run on commit `6601efd`, docs-only commits follow)
```
$ tools/ci.sh --base main
== install (frozen lockfile) == / == generated output must be clean == / == contract guard vs main == / == forbidden patterns ==
== typecheck == / == lint == / == test == / == build == / == agent ==
CI GATE PASSED
```
`git status --short` after the review-fix commits: empty (verified with `wt.sh log P04` before the final commit; the branch is not merged).
