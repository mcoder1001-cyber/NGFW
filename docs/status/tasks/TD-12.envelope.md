# TASK ENVELOPE — TD-12 (de-flake load-sensitive tests that break merge gates)
id: TD-12   branch: task/TD-12   worktree: /root/ngfw-wt/TD-12   base: main@16b622a   started: 2026-09-24T17:58
why: merge gates (pre-merge hook quick) fail on sleep/timing-based tests whenever host load is 30+ (12 agents run CI in parallel). Each false red costs the merger a full re-run.
scope — TEST CODE ONLY, make each wait on the event/condition with a bounded deadline instead of fixed sleeps / tight timeouts:
  1. apps/agent/internal/agent/service_test.go TestGRPCRoundTrip (~:671, a 50 ms sleep) — failed the TD-6 merge gate at load ~34
  2. apps/agent/internal/agent TestPendingSurvivesRestartAndRevertsAfterDeadline ("pending txn not persisted" at load 166)
  3. apps/agent/internal/renderers/strongswan TestWatchResync (load flake at load 140)
  4. apps/api vitest "Timeout calling fetch" at high load: raise the vitest worker/fetch timeouts in the api (and web if the same) vitest config to load-tolerant values (config only)
  5. grep apps/agent and apps/api tests for other time.Sleep-then-assert / fixed short timeouts on the scheduler/service/stream paths; fix the ones on the quick-gate path, list the rest in TD-12.md
proof: for each, show it failing (or flaky) under load — e.g. run with `stress-ng --cpu 32 --timeout 120s` if installed, else in parallel with a `go test -count=20` of another package — and passing after, with -count=20 under the same load. Paste output.
slot: 6 → eval "$(tools/lab env 6)"; no VPP host tests needed (unit tests only).
files you own: the test files named above (+ the vitest configs of apps/api, apps/web), docs/status/tasks/TD-12*. Never product code.
CI: `TMPDIR=/tmp/g-td12 tools/ci.sh --base main`. Ports 3000/8080/9101 belong to tools/app.
GIT RULE: run git ONLY inside /root/ngfw-wt/TD-12 (`git -C /root/ngfw-wt/TD-12 …`); NEVER in /root/ngfw (main).
time box: 2 h. finish: docs/status/tasks/TD-12.md with evidence; commit; final message = 6 lines.
never: merge · product code changes · restart/kill VPP · pkill
