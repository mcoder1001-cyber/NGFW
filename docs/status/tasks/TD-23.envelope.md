# TASK ENVELOPE — TD-23 shared test seams: fake-agent action dispatch + coretest fakevpp extension registry (merger request, D-134)
id: TD-23   branch: task/TD-23   worktree: /root/ngfw-wt/TD-23   base: main@228a7c57   started: 2026-09-24T23:56
why: four wave-A branches each add their own `action` handler to apps/api/src/testing/fake-agent.ts (the last merge would silently win) and three add their own hook line to apps/agent/internal/descriptors/core/coretest/fakevpp.go. The merger keeps squashes mechanical, so the seams land on main first; each feature adds ONE registration line at rebase.
scope — TEST INFRASTRUCTURE ONLY, no feature handlers:
  1. apps/api/src/testing/fake-agent.ts: `actionHandlers: Record<ActionKind, handler>` + `registerActionHandler(kind, fn)`; the generic `action` RPC dispatches on the request's oneof kind; an unregistered kind answers UNIMPLEMENTED properly (call ends with a status — never `call.destroy()` without a status, the bug F-nat44-ed found); a unit test for dispatch, unknown kind, and handler error.
  2. apps/agent/internal/descriptors/core/coretest/fakevpp.go: one extension-hook registry (`RegisterExtension(name, func(*FakeVPP))` or equivalent), modelled on F-nat44-ed-sessions' `extensions` seam — read it with `git -C /root/ngfw show task/F-nat44-ed-sessions:apps/agent/internal/descriptors/core/coretest/fakevpp.go` and the one-liners F-rpf-adl-pbr / F-bridge-l2 / F-bonding added (`git -C /root/ngfw diff df67a8e task/<branch> -- apps/agent/internal/descriptors/core/coretest/`) so every one of them can become a single RegisterExtension call; unit test that two extensions compose and neither silently replaces the other (the F-rpf-adl-pbr × F-bridge-l2 mactime case).
  3. A short README comment in each file: "features register here; never edit the dispatcher".
files you own: apps/api/src/testing/fake-agent.ts (dispatcher only) apps/api/src/testing/*.test.ts (new) apps/agent/internal/descriptors/core/coretest/{fakevpp.go (registry only),extensions_test.go} docs/status/tasks/TD-23*
CI: `TMPDIR=/tmp/g-td23 tools/ci.sh --base main`.
GIT RULE: git only as `git -C /root/ngfw-wt/TD-23 …`; NEVER in /root/ngfw.
time box: 1.5 h. finish: TD-23.md with the registration line each of F-rpf-adl-pbr, F-bridge-l2, F-bonding, F-nat44-ed-sessions, F-neighbors-ra, F-vrf-static-ecmp, F-nat44-ei will need at rebase; commit; final message = 6 lines.
never: feature handlers · product code · merge
