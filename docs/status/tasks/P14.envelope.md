# TASK ENVELOPE — P14
id: P14   branch: task/P14   worktree: /root/ngfw-wt/P14   base: main@<BASE>   started: <STARTED>
title: S5 system (day 16-18): unattended installer ISO (Ubuntu 26.04 autoinstall + offline pool)
prompt: prompts/P14-iso-installer.md   (template: none; refreshed on task/prep-rest against main@11a175b: host facts, `dpdk { no-pci }` via vrx-startupgen, tools/ci.sh is manager-only)   wbs: D0.3
scope: `deploy/image/iso/` autoinstall (user-data/meta-data, §7 partition layout, no default user, bootstrap password once), an offline pool (P10's repo + dependency closure), late-commands, first-boot banner, `deploy/image/build-iso.sh` → `vrx-<version>.iso` + SHA256 + GPG signature, shared pieces in `deploy/image/common/`, static validation; the QEMU/VM install is deferred (no KVM)
merged deps you can rely on: P10 (+ F-vpp-debs, F-startup-gen)
  - P10: `deploy/debian/vrx/` packages, `deploy/apt/` + the repo the manager published to `/srv/vrx-artifacts/apt/`, firstboot (`/etc/vrx/bootstrap.env` = `VRX_BOOTSTRAP_ADMIN_USER/PASSWORD`, startup.conf via `vrx-startupgen` → `dpdk { no-pci }` with no devices), docs/install/bare-metal.md
  - F-vpp-debs: `/srv/vrx-artifacts/vpp/<version>/manifest.json` (record `upstream.commit`, `patches[].sha256`, `build.inputs` in the image build info — deploy/vpp/README.md P14 bullet)
read first: prompts/P14-iso-installer.md · docs/status/wave-BC-numbers.md (section "S5 system": SY7, SY9 + "P14": partition labels, appliance marker) · docs/09-os-packages.md §3/§4/§7 · docs/install/bare-metal.md · deploy/vpp/README.md · scripts/30-tune-dataplane.sh · docs/decisions/PENDING-vpp-host-hardening.md (option B) · docs/decisions/LOG.md D-001, D-002, D-058, D-059, D-081, D-089, D-092
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100·<SLOT> VRX_WEB_PORT=5000+100·<SLOT> VRX_METRICS_PORT=9100+10·<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock
  - source of truth: `eval "$(tools/lab env <SLOT>)"`
  - base ISO, build chroot, pool, ISO tree and outputs live under /root/ngfw-wt/P14/.scratch/ (never in git); loop devices only on your own `.scratch/` files; nspawn machine name `w<SLOT>-p14`, always `--private-network`
  - slots 1–11 only; 12 is CI
daemon-owner: none (no daemon runs on the host; nothing is installed on the host)
obligations:
  - offline install: every dependency of `vrx-meta` resolves from the embedded pool (`apt-get install --simulate` against the pool only, pasted); autoinstall has no interactive section
  - VPP stays off the NICs until the UI wizard binds them: P10's firstboot renders `dpdk { no-pci }` (there is no `dpdk { disable }` stanza). Kernel cmdline from scripts/30-tune-dataplane.sh / docs/09 §4; write 1G-vs-2M hugepages and `net.core.rmem_max` for linux_nl (PENDING-vpp-host-hardening option B) as a question with your default, do not decide it silently
  - partition labels and the appliance marker exactly as allocated in wave-BC-numbers "P14" (`VRX-EFI`, `vrx-rootA`, `vrx-rootB` reserved for F-ab-upgrade, `vrx-log`, `vrx-pg`, `vrx-data`; `/etc/vrx/appliance`), because F-ab-upgrade and F-images depend on them
  - the bootstrap admin password is random per install, shown once on the console, written only to `/etc/vrx/bootstrap.env` (0600), removed after first login; never in the ISO, logs or status files
  - signing: GPG key outside the repo (P10's `~/.config/ngfw/apt-signing/` or a dedicated key — questions file with your default); never committed, never printed
  - reproducibility: same inputs → identical package manifest (pasted twice); record the base ISO's sha256 and the VPP manifest version
  - D-059: secure boot signing is out; D-002: no QEMU/KVM/libvirt (libvirt is installed on the host by mistake — never use it)
files you own exclusively:
  - deploy/image/iso/** (autoinstall user-data/meta-data, late-commands, pool builder)
  - deploy/image/common/** (package list, first-boot banner unit + `/etc/issue` writer, bootstrap-password writer: F-images reuses them read-only)
  - deploy/image/build-iso.sh
  - docs/install/iso.md, test/topology/iso/** (static validation; root/loop/network parts behind `VRX_INTEGRATION=1`, skipped in unit mode), docs/status/tasks/P14*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-BC-numbers.md "S5 system"):
  - protocol: docs/status/wave-A-hotspots.md §0. List every hunk under "Shared hunks" in docs/status/tasks/P14.md
  - SY7 root .gitignore `/.scratch/`: seeded by the manager; if absent, never `git add -A`
  - SY9 tools/ci.sh: the `iso` long target and a `deploy/image` shellcheck step are the manager's; ask in the questions file
  - P10's `deploy/debian/vrx/**`, `deploy/apt/**`, `deploy/firstboot/**` are read-only: a needed change → questions file
contract numbers: none (docs/status/wave-BC-numbers.md "P14": names only)
files you must not touch:
  - everything else
  - never: /root/ngfw (main), other worktrees, /etc, /boot, /root/vpp, host packages or services
  - deploy/debian/**, deploy/apt/**, deploy/firstboot/**, deploy/nginx/**, deploy/systemd/** (P10), deploy/image/{vm,cloud}/** (F-images), deploy/upgrade/** (F-ab-upgrade), deploy/vpp/** (F-vpp-debs)
  - apps/**, packages/**, tools/*, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md
host facts (verified 2026-09-24; no host package installs):
  - no Ubuntu ISO on the host: download the 26.04 live-server ISO (~3 GB) once and verify Ubuntu's `SHA256SUMS` + GPG signature. If it is unreachable → PENDING-network in the questions file; build and validate everything else (autoinstall, pool, scripts)
  - present: xorriso, grub-mkrescue/grub-mkstandalone (**x86_64-efi only**), genisoimage, gpg, zstd, debootstrap (`resolute`), systemd-nspawn, losetup, sfdisk, mkfs.ext4; absent: mtools/dosfstools (EFI image), GRUB i386-pc (BIOS), isolinux, cloud-init, squashfs-tools → inside a build chroot under `.scratch/` (packages from the mirror `repo.amnafzar.ir`) or `apt-get download` + `dpkg -x` under `.scratch/`
  - no KVM (D-002): the install/boot test is deferred with an exact VM test plan in P14.md
  - disk: ISO + pool + chroot ≈ 8–10 GB; check `df -BG /` ≥ 40 G free first; delete the chroot at the end
HOST BOOT SAFETY: every GRUB tool call takes explicit paths into your ISO tree or image (`--boot-directory`, `--efi-directory`, `--no-nvram`, `grub-editenv <file>`); a bare call acts on this host's `/boot`. Paste `sha256sum /boot/grub/grubenv` before/after
coordination:
  - F-ab-upgrade (next): rootB reserved and labelled; the appliance marker; health endpoints; update bundles on `/data`
  - F-images (next): reuses `deploy/image/common/` and your package list, read-only; keep them stable and documented
  - P10: if firstboot or the repo lacks something the installer needs, questions file (never patch P10's files)
evidence: `xorriso -indev … -ls` of pool + autoinstall files, `cloud-init schema --config-file user-data` (from the build chroot), `apt-get install --simulate` against the pool only, the manifest from two builds (identical), sha256 + `gpg --verify` of the ISO, host-unchanged listings (`findmnt`, `losetup -a` filtered to `.scratch/`, grubenv sha256)
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/P14.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/P14-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/P14.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: no mounts under `.scratch/` (`findmnt | grep /root/ngfw-wt/P14` empty), no loop device backed by your files, no `w<SLOT>-p14` container, build chroot removed, the ISO path + sha256 in P14.md (the ISO stays in `.scratch/` for the manager), processes stopped by PID
questions: docs/status/tasks/P14-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own · install, remove or upgrade a package on this host
