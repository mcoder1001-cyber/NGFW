# TASK ENVELOPE — F-unbound-chrony-syslog
id: F-unbound-chrony-syslog   branch: task/F-unbound-chrony-syslog   worktree: /root/ngfw-wt/F-unbound-chrony-syslog   base: main@<HEAD at spawn>   started: <at spawn>
title: Wave B (day 10-12): Unbound DNS, chrony NTP, syslog export + log explorer
prompt: prompts/features/F-unbound-chrony-syslog.md   (template: prompts/FEATURE-TEMPLATE.md)   wbs: D7.3, D7.4, D7.7
scope: Unbound DNS, chrony NTP, syslog export + log explorer — the RF-3 unbound/chrony and RF-4 rsyslog renderers, DF-8 dns descriptors and the services/management schema exist: projection, D-086 contract fields, state, dns-lookup action, log explorer, API, UI, docs
merged deps you can rely on: P08 (vertical slice: desired/subsystems/projection patterns), RF-3 (renderers/{unbound,chrony}), RF-4 (renderers/rsyslog + rfkit) (+ pre-wave merges TD-2, TD-3)
slot: 10 → VRX_SLOT=10 VRX_TEST_PREFIX=w10 VRX_HTTP_PORT=3000+100×10 VRX_WEB_PORT=5000+100×10 VRX_METRICS_PORT=9100+10×10+1 VRX_AGENT_SOCKET=/run/vrx-test/w10/agent.sock VRX_PG_DATABASE=vrx_w10 VRX_VALKEY_DB=10 VRX_VPP_TABLE_BASE=10000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env 10)"`; slots 1–11 only, 12 = CI; rsyslog test ports per RF-4: imtcp 3<N>14, collectors 3<N>15/3<N>16)
daemon-owner: unbound, chrony, rsyslog — slot test instances only (RF-3/RF-4 `TestPaths` under /run/vrx-test/w10/, loopback listen, own sockets/pidfiles). `chrony.service` and `rsyslog.service` are the HOST's own time and logging (active, enabled): never stop, restart, reload, reconfigure or disable them — the owner rule "leave the daemon stopped and disabled" applies to your test instances only; `unbound.service` stays disabled and stopped
files you own exclusively: apps/agent/internal/renderers/{unbound,chrony,rsyslog}/** apps/agent/internal/descriptors/dns/** docs/agent/renderers/{unbound,chrony,rsyslog}.md docs/agent/descriptors/dns.md apps/agent/internal/desired/{dns,ntp,syslog}*.go apps/agent/internal/subsystems/{unbound,chrony,rsyslog,dns}*.go apps/agent/internal/agent/rpc_{dns,ntp,logs}*.go apps/agent/internal/actions/unbound-chrony-syslog/** packages/schema/src/domains/ext/syslog*.ts packages/schema/src/semantic/unbound-chrony-syslog*.ts apps/api/src/features/unbound-chrony-syslog/** apps/api/test/e2e/unbound-chrony-syslog* apps/web/src/domains/services/unbound-chrony-syslog/** apps/web/src/locales/{en,fa}/unbound-chrony-syslog.json docs/user/services/unbound-chrony-syslog.md test/topology/unbound-chrony-syslog/** docs/status/tasks/F-unbound-chrony-syslog*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol): A1 apps/agent/internal/subsystems/subsystems.go (`Domains["services"]` — shared with F-rpf-adl-pbr (wave A) and F-kea-dhcp-relay: whoever lands first adds the key, the others append; `Domains["management"]` is new — you add it (syslog only); `dns.RegisterGlobals` only when `env.GlobalsOwner`; Register lines one per line) · A2 apps/agent/internal/agent/projection.go (one builder call in project(), one in assemble()) · A4 apps/agent/internal/agent/server.go (one Action case for the DNS lookup; state RPCs in your rpc_*.go) · C1 packages/schema/src/domains/management.ts (D-086 stand-ins via your ext/syslog.ts + one key line) and services.ts (only if needed) · C2 packages/schema/src/semantic/index.ts · C3 packages/schema/src/index.ts (export of the ext file) · C4 examples/fixtures (new `unbound-chrony-syslog-*.json` only) · C5 packages/proto/vrx/v1/dataplane.proto (SyslogTarget fields, DNS/NTP/log state rpcs + messages in a `// ----- F-unbound-chrony-syslog -----` section, one ActionRequest member) · C6 docs/contracts/proto.md · C7 generated files · P1 apps/api/src/app.module.ts · P3 `POST /actions/dns-lookup` as a static route in your own controller · P4 agent.client.ts · P5 fake-agent.ts · W1 router.tsx · W2 nav.ts (+nav.test.ts: BUILT_DOMAINS 'services') · W3 i18n.ts · services domain page + tab registry apps/web/src/domains/services/{ServicesPage.tsx,tabs.ts} (shared with F-kea-dhcp-relay; the manager seeds it before spawn — if it is not on main, do NOT create it (two parallel creators = add/add conflict): register your screen as its own route + NavItem under W1/W2 and the manager folds it into the shell at merge) · apps/agent/internal/renderers/ALLOWLIST.md (one row: `/usr/bin/journalctl` fixed argv for the log explorer) · A7 docs/vpp-code-track.md
contract numbers (wave-A-hotspots §2, binding since D-109 e): **SyslogTarget 6 `facilities`, 7 `format`, 8 `queue_size`, 9 `tls`** (D-086 stand-ins; `tls{caRef, certRef, keyRef, authMode, permittedPeers}` is a new message), **ActionRequest 7 `dns_lookup`**, DNS/NTP/log state rpcs (or one services-state rpc; no number) — nothing else: ManagementConfig 5–6 are F-dashboard-prom-alarms' and 7–8 F-backup-restore's; never "next free"; commit them first as separate `contract(schema|proto): …` commits on YOUR branch (P08 pattern; never a contract/ branch — workers never create branches, 00-CONTEXT) + docs/status/tasks/F-unbound-chrony-syslog-contract.md; tell the manager in the questions file and keep building
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc/vpp, /root/vpp, apps/agent/binapi (P04/manager-owned), /etc/{unbound,chrony,rsyslog.conf,rsyslog.d}, the host's /dev/log (always `logger -u <slot log.sock>`), the system units, apps/agent/internal/renderers/{kea,frr,strongswan,snmpd,keepalived,vppstartup,rfkit}/** and renderers/{renderer.go,helpers_*} (other tasks / shared — a need goes to the questions file), apps/agent/internal/descriptors/dhcp/** (F-kea-dhcp-relay), apps/agent/internal/agent/{agent,service,state,ifstate}.go (A5), tools/ci.sh, tools/lab
host facts: unbound 1.24.2, chrony 4.8, rsyslog 8.2512 installed; no DNS query tool on the host (`dig`, `drill`, `host`, `nslookup` all missing) → Go `net.Resolver` pinned to the slot address (RF-3 test pattern) · `journalctl` present (default log-explorer source: RF-4 emits only `omfwd`, no local files) · VPP's dns plugin is VPP-global: `dns_resolve_name` works only where the globals owner enabled it — on a slot the lookup is tested through the fake · unbound TestPaths are loopback-only
obligations accumulated from decisions (all must be honoured and listed in F-unbound-chrony-syslog.md with evidence): D-079 unbound listen/port/interface changes → persisted restart request + convergence check; pending requests survive until acted on (chrony M2 pattern); never restart a system unit from a test slot · D-086 RF-4 stand-ins move into the contract now; rsyslog TLS stays built but untested until the driver is packaged (P10) · D-050 NTP lives only in `services.ntp` · D-063/D-076/D-071 `dns.name-server`/`dns.enable` are write-only VPP globals: registered only in the globals owner (slot agents VRX_GLOBALS_OWNER=0 require, never set) · D-082 the opt-in VPP dns host test holds `flock -x /run/lock/vrx-globals.lock` · RF-4 review M2/M3: no restart for an unchanged rendering; respect a host that already loads impstats
coordination: (1) renderer stage — no renderer is called by the agent today (`service.go` runs only the scheduler); default = one singleton scheduler descriptor per renderer under `Domains["services"]`/`["management"]` (the F-host-acl-nftables default (a)) unless the manager has put a shared renderer stage on main; log the choice with options · (2) secret channel API→agent does not exist (P11 default owner): syslog TLS keys and chrony auth keys are refused with a clear DryRun error until it lands — say so · (3) F-kea-dhcp-relay (parallel) shares `Domains["services"]` and the services page; F-snmp later uses RF-4's snmpd renderer (not yours) · (4) the agent persists only implemented domains (`agent/state.go` `mergeDomains`) — adding `management` makes its other leaves `agent.unsupported-field`, check restart-safety with that in mind
SAFETY: no data-plane packets are needed; if you run the opt-in VPP dns host test (globals lock, manager window only), paste `systemctl show vpp -p NRestarts` before/after and stop if it rises; one host test package at a time (D-087); never touch the host's chronyd/rsyslogd processes (kill only PIDs you started)
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b (kept outside the repo) for screenshots
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-unbound-chrony-syslog.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-unbound-chrony-syslog-wip.md current
CI: `TMPDIR=/tmp/g-w10 tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-unbound-chrony-syslog.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
questions: docs/status/tasks/F-unbound-chrony-syslog-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own

MANAGER ADDENDA: insert only under your `wave-BC: F-unbound-chrony-syslog` anchors. You are the first to add `Domains["management"]` (F-dashboard-prom-alarms depends on you and reuses it). Daemons unbound + chrony + rsyslog run only as slot-local instances in your own netns/pathspace (D-119 M3); units stay disabled; never touch /etc/unbound, /etc/chrony, /etc/rsyslog* or the host's resolver/time. RF-3/RF-4 renderers (chrony, rsyslog, unbound) are merged — use them. rsyslog TLS keys wait for PENDING-secret-channel (fixture resolver). D-128: never `show trace`. GIT RULE: git only inside your own worktree, NEVER in /root/ngfw. CI: main's tools/ci.sh copy if your branch copy fails in the contract guard (D-127).
