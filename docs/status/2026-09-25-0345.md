# Status 2026-09-25 03:45 (+0330) — manager cycle 19 (ngfw-46)

## خلاصه (فارسی)
- مرج شدند: TD-20، TD-8 (seamهای ایجنت) و TD-7. TD-11b (ایمنی claimها) تأیید شد و الان در حال مرج است.
- لیمیت پنج‌ساعتهٔ نشست ساعت ۰۰:۳۷ همهٔ ایجنت‌ها را متوقف کرد. ساعت ۰۳:۴۱، بعد از ریست، هر ۱۷ ایجنت با حفظ context از همان نقطه ادامه دادند.
- بازبینی‌ها: WEB-1 تأیید شد. F-bonding مشروط تأیید شد و به دور اصلاح رفت. F-vrf-static-ecmp دور اصلاح را تمام کرد و بازبینی متمرکزش در جریان است.
- VPP از ۱۸:۴۱ پایدار است (NRestarts=1)؛ بار سرور ~۱۰.
- Overall: 34.1% by hours, 44/124 tasks merged

## Detail
Merged since cycle 18: TD-20 a54b493f, TD-8 8a633616, TD-7 bd205c13; board sync 0f228367 (TD-23/TD-8b/TD-21/TD-22 rows, D-134).
Merge queue (merger «بررسی پروژه»): TD-11b (in progress) → TD-4 → F-vlan-qinq → WEB-2 (at 4b421fe2+) → WEB-1 (55b2f8fa) → F-object-model.
Resumed after the limit stop (03:41, SendMessage, context intact): TD-9, F-kea-dhcp-relay (+ DHCP UI fork), F-acl, F-nat44-ed-sessions, F-nat44-ei-64-66-nptv6, TD-10a, TD-10b, TD-23, F-loopback-bvi-gso-lldp-span, F-host-acl-nftables, F-unbound-chrony-syslog, F-wireguard, P12 (mid main merge), TD-11c, WEB-3, WEB-2.
New: F-bonding fix round 1 (F1 dropPhantomOptionals, F2 D-132 polling, F4 non-Ethernet members, F5, F6, Q1 drop broadcast — manager decision); F-vrf-static-ecmp focused verify.
Rule added for every wave row (TD-11b M1): each registered descriptor declares RecordsNoOwnership() or CheckPersistent(); the agent refuses to start otherwise.
Process: an hourly manager cron (:17) now starts with usage-limit recovery (resume every stopped agent + tell the peer sessions) — product-owner order 2026-09-25.
Risks: the 5-hour usage window, not slots, is the binding limit (the full fleet used it up in ~2.8 h); P12 resumes mid-merge.
Next: after the in-flight wave drains, start TD-8b, TD-22, TD-13, UI-domain-editor, P11 (secret steps still PENDING-secret-channel).
