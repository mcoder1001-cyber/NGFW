# TASK ENVELOPE — F-ospf
id: F-ospf   branch: task/F-ospf   worktree: /root/ngfw-wt/F-ospf   base: main@<BASE>   started: <STARTED>
title: Wave B (day 10-12): OSPFv2/v3 via FRR
prompt: prompts/features/F-ospf.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed on task/prep-rest against main, task/P08, task/W-seed and the P12 envelope)   wbs: D3.4, D3.5
scope: OSPFv2 on the existing `routing.ospf` model + OSPFv3 as a new `routing.ospf6` key + v2 MD5 interface auth (contract) — FRR `ospf`/`ospf6` sections plugged into P12's FRR singleton, neighbour/interface/LSDB state + neighbour events, API, UI (OSPF page with a v3 tab), docs; learned routes reach the VPP FIB through linux-nl (P12's test pattern) — the agent never programs them
merged deps you can rely on: P08, P12, W-seed (+ the wave-B/C anchor pass, docs/status/wave-BC-numbers.md "Pack rules")
  - P08: desired/ + subsystems/ + projection patterns, `routing` domain = core static routes, the "routing protocols … RF-1" warning hunk in projection.go (seam S3)
  - P12: the FRR singleton scheduler descriptor that renders every registered RF-1 section (read its name in P12.md), the linux-cp mapper (`internal/lcpmap`, injected with `frr.WithInterfaceMapper`), the LCP pair model, the routing/BGP state RPC and routing EventKinds 14/15, the linux-nl test plan (P12-questions.md), `renderers/frr/policy` (route maps your `redistribute` lines name), the `proto` filter on F-vrf-static-ecmp's `/state/routes` FIB browser
  - through P12: RF-1 (`RegisterSection` / `RegisterStateReader` / `RegisterPoller`, `rc.Secret` + redaction incl. `ip ospf message-digest-key`, `rc.MapInterface`, `frrtest`), DF-8 (`lcp.itf-pair`), F-vrf-static-ecmp (`ListRoutes`, `viaFrr` + its selector)
  - also on main: TD-2, TD-3 (V19 sanitizer + cmd/vrx-vpp-preflight), TD-5 (agent af_packet quiesce) if merged
read first: prompts/features/F-ospf.md · docs/status/wave-BC-numbers.md (Pack rules, section F-ospf) · docs/status/wave-A-hotspots.md (§0 rules; ids A1 A2 C1–C7 P1 P4 P5 P6 W1–W3 A7) · apps/agent/internal/renderers/frr/README.md · docs/agent/renderers/frr.md · docs/status/tasks/P12.md + P12-questions.md · docs/vpp-code-track.md V1 · docs/decisions/LOG.md D-045, D-051, D-070, D-071, D-072, D-086, D-087, D-089, D-094, D-095, D-101, D-109 · docs/decisions/PENDING-secret-channel.md
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100·<SLOT> VRX_WEB_PORT=5000+100·<SLOT> VRX_METRICS_PORT=9100+10·<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock
  - source of truth: `eval "$(tools/lab env <SLOT>)"`
  - rig prefix w<SLOT> → 10.<SLOT>.{1,2}.0/24; announced test prefixes inside 10.<SLOT>.0.0/16 (+ one slot-unique IPv6 /48 for OSPFv3); any kernel VRF/table id in <SLOT>000–<SLOT>999
  - test agents run with VRX_GLOBALS_OWNER=0 (D-071)
  - slots 1–11 only; 12 is CI
daemon-owner: frr — slot test instances only through `frrtest` (pathspace w<SLOT>, /run/vrx-test/w<SLOT>/frr, symlink /run/frr/w<SLOT>, lock /run/vrx-test/w<SLOT>/frr.lock; `Daemons: ospfd, ospf6d` on top of mgmtd/zebra/staticd); `frr.service` stays disabled and stopped; nothing under /etc/frr is written; other FRR tasks may run in their own slots at the same time only after the manager has logged per-slot-instance FRR ownership (wave-BC-launch-queue.md M3; until then one FRR task at a time). A zebra in the ROOT netns (linux-nl FIB proof) is host-wide in any case: one slot at a time, manager window (P12 coordination 4)
obligations:
  - D-104: use, do not rebuild — RF-1's framework and `frrtest`, P12's singleton/mapper/LCP pairs/state RPC, F-vrf-static-ecmp's FIB browser. The rules `routing.ospf-area-exists`, `routing.interface-exists`, `routing.route-map-exists` already exist: test them, extend the same checks to `ospf6` in YOUR semantic file, never edit semantic/routing.ts
  - D-051 + D-072 (second half): MD5 keys are `password/<name>` refs resolved only through `rc.Secret`; a unit test proves the key is `<redacted>` in DryRun, errors and Retrieve; OSPF MD5 keys are ≤ 16 characters
  - PENDING-secret-channel: nothing carries secrets API→agent yet — the end-to-end auth step waits; build and test with a slot-local fixture resolver (`frr.WithSecretResolver`) and test literals `VRXTPSKospf<n>` (D-086 precedent for length-capped keys); never key material in status files or logs
  - RF-1 review M3: scoped readers only (`show ip ospf neighbor json`, `show ip ospf interface json`, `show ipv6 ospf6 neighbor json`); the LSDB is paged from a scoped command and never part of Retrieve; no full-RIB dumps
  - render FRR's canonical `show running-config` form (the framework's convergence check fails otherwise); interface lines on Linux names from `rc.MapInterface`; an unmapped interface is an error, never skipped silently
  - seam S2 (per-interface lines): use P12's interface-lines hook if it exists; else render your own `interface X … exit` block and prove with frrtest that frr-reload's DryRun is empty after Apply — if it is not, questions file (never edit `renderers/frr/*.go`)
  - D-072: OSPF never programs VPP; its routes arrive through linux-nl. `redistribute static` covers only `viaFrr` routes (FRR-owned); document it
  - D-071: nothing VPP-global; never `lcp default netns`, never `lcp_itf_pair_replace_*`
files you own exclusively:
  - apps/agent/internal/renderers/frr/ospf/** (both sections `ospf` and `ospf6`: orders 440/450 per wave-BC-numbers.md), docs/agent/renderers/frr-ospf.md
  - apps/agent/internal/subsystems/ospf*.go (blank import of renderers/frr/ospf so its init() runs, any Wiring glue), apps/agent/internal/desired/ospf*.go (only if a projection is needed), apps/agent/internal/agent/rpc_ospf*.go (only with the `OspfState` RPC)
  - packages/schema/src/domains/ext/ospf*.ts, packages/schema/src/semantic/ospf*.ts (rule ids `routing.ospf-…`, never the existing `routing.ospf-area-exists`), packages/schema/examples/ospf-*.json, packages/proto/test/fixtures/ospf-*.json
  - apps/api/src/features/ospf/** (index.ts exports {controllers, providers}; `OspfController`; real fake behaviour in fake.ts), apps/api/test/e2e/ospf*
  - apps/web/src/domains/routing/ospf/**, apps/web/src/locales/{en,fa}/ospf.json
  - docs/user/routing/ospf.md, test/topology/ospf/**, docs/status/tasks/F-ospf*
shared hotspots (append-only, conflicts resolved by the manager at merge):
  - protocol: docs/status/wave-A-hotspots.md §0. Insert only directly below your own anchor `// wave-BC: F-ospf` (seeded by the manager's wave-B/C anchor pass); if it is missing, say so in the questions file and insert at the end of that anchor block. In dataplane.proto keep the one-blank-line framing. Never reorder or reformat other lines. List every hunk under "Shared hunks" in docs/status/tasks/F-ospf.md
  - A1 apps/agent/internal/subsystems/subsystems.go: at most one line at the end of `Register()` (nothing if your subsystems/ospf.go needs no call)
  - A2 apps/agent/internal/agent/projection.go: drop `ospf` from the "routing protocols … RF-1" warning per seam S3 (one line if P12 made it table-driven; otherwise do not edit — questions file, the manager edits at merge)
  - C1 packages/schema/src/domains/routing.ts: `RoutingSchema.ospf6` and `OspfInterfaceSchema.auth` key lines (sub-schemas in ext/ospf.ts) · C2 packages/schema/src/semantic/index.ts (import + spread) · C3 packages/schema/src/index.ts · C4 new fixture files only
  - C5 packages/proto/vrx/v1/dataplane.proto: `RoutingConfig` 13, `OspfInterface` 9, `EventKind` 20, the `OspfState` rpc under the service anchor only if needed; new messages in `// ----- F-ospf -----`
  - C6 docs/contracts/proto.md (`### F-ospf: …` for the event kind / RPC) · C7 generated, never hand-edited: apps/agent/gen/**, packages/proto/gen/ts/**, packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md (`packages/proto/gen.sh`, `pnpm gen`, `make -C apps/cli gen docs`)
  - P1 apps/api/src/app.module.ts · P4 apps/api/src/agent/agent.client.ts + P5 apps/api/src/testing/fake-agent.ts (only with a new RPC) · P6 apps/api/src/infra/bus.ts (`ospf.events`) + apps/api/src/telemetry/relay.service.ts (EventKind 20 case)
  - W1 apps/web/src/router.tsx · W2 apps/web/src/nav/nav.ts + nav.test.ts (an OSPF NavItem in the routing group, labelKey in `ospf:`; `routing` in BUILT_DOMAINS is F-vrf-static-ecmp's) · W3 apps/web/src/i18n.ts
  - A7 docs/vpp-code-track.md: `### V-new (F-ospf)` only if linux-cp drops OSPF multicast; the manager numbers it
  - allocated numbers (docs/status/wave-BC-numbers.md, a merge blocker if reused): **RoutingConfig 13 `ospf6`**, **OspfInterface 9 `auth`**, **EventKind 20** — nothing else (OspfInterface 10 is F-bfd-redistribution's, RoutingConfig 14 F-isis-rip's)
contract: commit `contract(schema): routing ospf6 + ospf md5 auth` and `contract(proto): ospf6, ospf auth, ospf neighbour event` as separate commits on YOUR branch first (the ci.sh contract guard checks the subject), plus docs/status/tasks/F-ospf-contract.md; tell the manager in the questions file and keep building. Never a `contract/` branch (workers never create branches)
files you must not touch:
  - everything else
  - never: /root/ngfw (main), other worktrees, /etc (incl. /etc/vpp and /etc/frr), /root/vpp, the frr/vpp units
  - apps/agent/binapi + tools/binapi-gen.sh (P04/manager-owned), tools/lab, tools/ci.sh, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
  - apps/agent/internal/renderers/frr/*.go, frr/frrtest/**, frr/templates/**, frr/{bgp,policy}/** (RF-1/P12 framework — a need → questions file); frr/{isis,rip,bfd,redistribute,ldp,pim}/** (sibling tasks)
  - P12's files: apps/agent/internal/descriptors/lcp/**, apps/agent/internal/lcpmap/**, desired/{bgp,lcp}*.go, subsystems/{frr,lcp}*.go, agent/rpc_{bgp,routing}*.go, apps/api/src/features/bgp/**
  - agent core (A5): apps/agent/internal/agent/{agent,service,state,ifstate}.go, apps/agent/cmd/**; apps/agent/internal/desired/interfaces.go (A3); packages/schema/src/domains/routing.ts and semantic/routing.ts beyond the anchored key lines; apps/api/src/state/**; F-vrf-static-ecmp's files
host facts:
  - FRR 10.7.1 installed (`/usr/lib/frr/ospfd`, `ospf6d` present), `frr.service` disabled; frrtest refuses the root netns and anything with ens192
  - linux_cp/linux_nl loaded since D-060; no `linux-cp { default netns }` → linux_nl listens in the ROOT netns: reuse P12's linux-nl test plan (slot kernel VRF/table in <SLOT>000–<SLOT>999); OSPF routes never land in the root netns main table (management path on ens192)
  - source (VPP 26.06 lcp_router.c): linux-cp installs (*,224.0.0.0/24) and (*,ff00::/8) accept mfib entries for LCP pairs, and OSPF (IP proto 89) is not claimed by VPP, so it reaches the tap through the ip4-punt arc — confirm on the host that 224.0.0.5/6 and ff02::5/6 hellos arrive; if not: V-new + the adjacency test netns↔netns with the reason
  - no data NICs bound (D-026): af_packet rig only (D-010); LCP pairs only on slot interfaces, host taps `w<SLOT>-…` (≤ 15 chars), never ens*
coordination: (1) F-isis-rip (parallel) — sibling FRR sections, the same S2/S3 hunks; keep to your own blocks · (2) F-bfd-redistribution depends on you: `bfd: true` renders only `ip ospf bfd`; BFD profiles and OspfInterface 10 are theirs · (3) P12's EventKinds 14/15 are routing change / BGP neighbour; OSPF neighbour state is 20 · (4) redistributing `ospf6` into bgp/isis/rip is out of scope (F-bfd-redistribution's matrix shows it)
V19/V24 SAFETY (D-095, D-101): before ANY traffic through the rig, run TD-3's preflight (`go -C apps/agent run ./cmd/vrx-vpp-preflight`) on the rig interfaces' sw_if_index and never send packets through an unchecked interface; every af_packet delete with its veth down; one host test package at a time (D-087); `systemctl show vpp -p NRestarts` before/after every host run pasted — stop host runs and write it down if it rises
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots (en + fa/RTL: OSPF page, areas/interfaces, neighbours grid, v3 tab); paste the harness's `vtysh --vty_socket <run dir> -N w<SLOT> -c "show ip ospf neighbor"` and `vppctl show ip fib table <id>` / `show ip6 fib table <id>` counts
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-ospf.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-ospf-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-ospf.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite; FRR daemons through the harness Stop) by PID · lab lock released · vrx_w<SLOT> dropped · no w<SLOT> LCP pairs, tables or routes left in VPP (Retrieve + `show lcp` + `show ip fib table <id>` pasted) · no ns-w<SLOT>-* netns left · rig down · dist/ and apps/agent/bin removed
questions: docs/status/tasks/F-ospf-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
