// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: vrx/v1/dataplane.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "vrx.v1";

/**
 * Contract between vrx-api (Node.js) and vrx-agent (Go). Frozen after task P03 is approved;
 * changes require a PR labelled `contract`. Field numbers are never reused.
 */

export interface HealthRequest {
}

export interface HealthResponse {
  /** Agent build version. */
  agentVersion: string;
  /** True when the VPP binary API is connected. */
  vppConnected: boolean;
  /** VPP version string as reported by show_version, empty when disconnected. */
  vppVersion: string;
}

function createBaseHealthRequest(): HealthRequest {
  return {};
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(_: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHealthRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): HealthRequest {
    return {};
  },

  toJSON(_: HealthRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<HealthRequest>): HealthRequest {
    return HealthRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<HealthRequest>): HealthRequest {
    const message = createBaseHealthRequest();
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return { agentVersion: "", vppConnected: false, vppVersion: "" };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentVersion !== "") {
      writer.uint32(10).string(message.agentVersion);
    }
    if (message.vppConnected !== false) {
      writer.uint32(16).bool(message.vppConnected);
    }
    if (message.vppVersion !== "") {
      writer.uint32(26).string(message.vppVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHealthResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.agentVersion = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.vppConnected = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.vppVersion = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): HealthResponse {
    return {
      agentVersion: isSet(object.agentVersion)
        ? globalThis.String(object.agentVersion)
        : isSet(object.agent_version)
        ? globalThis.String(object.agent_version)
        : "",
      vppConnected: isSet(object.vppConnected)
        ? globalThis.Boolean(object.vppConnected)
        : isSet(object.vpp_connected)
        ? globalThis.Boolean(object.vpp_connected)
        : false,
      vppVersion: isSet(object.vppVersion)
        ? globalThis.String(object.vppVersion)
        : isSet(object.vpp_version)
        ? globalThis.String(object.vpp_version)
        : "",
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.agentVersion !== "") {
      obj.agentVersion = message.agentVersion;
    }
    if (message.vppConnected !== false) {
      obj.vppConnected = message.vppConnected;
    }
    if (message.vppVersion !== "") {
      obj.vppVersion = message.vppVersion;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthResponse>): HealthResponse {
    return HealthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthResponse>): HealthResponse {
    const message = createBaseHealthResponse();
    message.agentVersion = object.agentVersion ?? "";
    message.vppConnected = object.vppConnected ?? false;
    message.vppVersion = object.vppVersion ?? "";
    return message;
  },
};

/** Dataplane is the privileged agent's northbound API, served on a unix socket. */
export type DataplaneService = typeof DataplaneService;
export const DataplaneService = {
  /** Health reports agent and VPP connectivity. Cheap; polled by the API. */
  health: {
    path: "/vrx.v1.Dataplane/Health" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HealthRequest): Buffer => Buffer.from(HealthRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): HealthRequest => HealthRequest.decode(value),
    responseSerialize: (value: HealthResponse): Buffer => Buffer.from(HealthResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthResponse => HealthResponse.decode(value),
  },
} as const;

export interface DataplaneServer extends UntypedServiceImplementation {
  /** Health reports agent and VPP connectivity. Cheap; polled by the API. */
  health: handleUnaryCall<HealthRequest, HealthResponse>;
}

export interface DataplaneClient extends Client {
  /** Health reports agent and VPP connectivity. Cheap; polled by the API. */
  health(
    request: HealthRequest,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
}

export const DataplaneClient = makeGenericClientConstructor(DataplaneService, "vrx.v1.Dataplane") as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): DataplaneClient;
  service: typeof DataplaneService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
