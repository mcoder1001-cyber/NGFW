# TASK ENVELOPE — F-licensing
id: F-licensing   branch: task/F-licensing   worktree: /root/ngfw-wt/F-licensing   base: main@<HEAD at spawn>   started: <at spawn>
title: S5: simple licence/entitlement (offline, Ed25519-signed file, enforced at commit validation)
prompt: prompts/features/F-licensing.md   (template: prompts/FEATURE-TEMPLATE.md; refreshed against main in wave-B/C prep)   wbs: D12.2
scope: API + UI only (no agent, no VPP, no schema/proto): `.vrxlic` format + dependency-free `vrx-license` CLI (keygen/issue/verify/inspect), `PUT /api/v1/system/license`, `GET /api/v1/state/license`, a `license` validation stage at commit (pointer to the first unlicensed node, grace period, community set), expiry event in `system_event`, licence page + banner, docs
merged deps you can rely on: P06 (commit pipeline `commit/validation.service.ts`, `common/problem.ts`, `db/schema.ts` + Drizzle migrations, `auth/` roles + RBAC, `audit/` interceptor + `system-events.service.ts`), P08 (the running stack end to end), P07b (system screens, `system/users` / `system/revisions` routes and nav items) (+ on main by then: TD-2 (and TD-4 if merged) own apps/api/src/{auth,users}/** — read-only for you; W-seed)
read first: prompts/features/F-licensing.md · docs/status/wave-A-hotspots.md (§0 rules; P1 W1–W3 C7) · docs/status/wave-BC-numbers.md § F-licensing and "DB migrations" · apps/api/src/commit/validation.service.ts · apps/api/src/audit/system-events.service.ts · apps/api/src/db/schema.ts · docs/decisions/LOG.md D-001, D-040, D-046, D-059, D-091, D-097, D-100, D-102
slot: <SLOT> → VRX_SLOT=<SLOT> VRX_TEST_PREFIX=w<SLOT> VRX_HTTP_PORT=3000+100×<SLOT> VRX_WEB_PORT=5000+100×<SLOT> VRX_METRICS_PORT=9100+10×<SLOT>+1 VRX_AGENT_SOCKET=/run/vrx-test/w<SLOT>/agent.sock VRX_PG_DATABASE=vrx_w<SLOT> VRX_VALKEY_DB=<SLOT> VRX_VPP_TABLE_BASE=<SLOT>000 VRX_LAB_LOCK=/run/lock/vrx-lab.lock  (source of truth: `eval "$(tools/lab env <SLOT>)"`; slots 1–11 only, 12 = CI; test agents run with VRX_GLOBALS_OWNER=0, D-071; test signing keys are generated at test time under /run/vrx-test/w<SLOT>/license/ (0700) and deleted in cleanup — never inside the repo)
daemon-owner: none
files you own exclusively: tools/license/** apps/api/src/features/licensing/** apps/api/test/e2e/licensing* apps/web/src/domains/system/licensing/** apps/web/src/locales/{en,fa}/licensing.json docs/user/system/licensing.md test/topology/licensing/** docs/status/tasks/F-licensing*
shared hotspots (append-only, conflicts resolved by the manager at merge; ids from docs/status/wave-A-hotspots.md, same anchor protocol — insert only directly below your anchor `// wave-BC: F-licensing` (seeded by the manager's wave-B/C anchor pass; if it is missing in a file, append at the end of that block, mark the hunk "unanchored" under Shared hunks in docs/status/tasks/F-licensing.md and say so in the questions file); never reorder or reformat other lines): P1 apps/api/src/app.module.ts (features/licensing/index.ts exports {controllers, providers}) · commit validation stage apps/api/src/commit/validation.service.ts (P06; `'license'` added to the `ValidationTier` union + one call between the secret-existence check and the agent DryRun; the logic lives in your features/licensing/ — no other wave-B/C task edits this file) · DB apps/api/src/db/schema.ts + apps/api/migrations/** (only if you store the licence in PostgreSQL: tables under your anchor, `pnpm -C apps/api db:generate --name licensing`, regenerated by the manager at merge if main has a newer migration — wave-BC-numbers.md DB protocol; F-dashboard-prom-alarms and F-aaa may add tables in parallel) · banner slot apps/web/src/shell/AppShell.tsx (one `<LicenseBanner />` line beside `ConfirmBanner`/`SyncBanner`; task/ui-nav-collapse edits the nav part of this file) · W1 apps/web/src/router.tsx (`system/licensing` route) · W2 apps/web/src/nav/nav.ts + nav.test.ts (one `system` NavItem `licensing`, labelKey in your namespace; F-aaa / F-backup-restore / F-restconf-yang add their own system items) · W3 apps/web/src/i18n.ts · C7 generated files (regen, never hand-merged: packages/api-client/src/generated/**, apps/cli/internal/api/operations_gen.go, docs/user/cli/reference.md — `pnpm gen`, `make -C apps/cli gen docs`)
contract numbers: none — docs/status/wave-BC-numbers.md § F-licensing: nothing licence-related crosses the API↔agent boundary (D-040) and the licence is not part of the config document; no schema/proto change (if you find you need one, stop and write the question)
files you must not touch: everything else; never /root/ngfw (main), other worktrees, /etc (incl. /etc/machine-id — read-only), apps/agent/** (no agent/VPP work at all), packages/schema/**, packages/proto/**, apps/api/src/{auth,users,secrets,datastore,audit,state,actions,telemetry}/** (TD-2/TD-4/P06 — use the services, do not edit them), apps/api/src/commit/** beyond the one validation-stage hunk, apps/api/src/config.ts (keep the public-key config in your feature dir), pnpm-workspace.yaml, pnpm-lock.yaml, apps/*/package.json dependency lists, apps/agent/go.{mod,sum} (a new dependency or workspace package → questions file; the manager changes them on main), apps/cli/** except the regenerated table, tools/ci.sh, tools/lab, plan/tasks.yaml, docs/decisions/LOG.md, docs/status/PROGRESS.md, packages/ui-kit/** (a need → questions file)
host facts: Node 22 `node:crypto` Ed25519 (`generateKeyPairSync('ed25519')`, `crypto.sign/verify(null, …)`) — no extra package · `tools/` is not a pnpm workspace member and has no Go module: the CLI stays dependency-free and its tests run from the API's vitest suite so `tools/ci.sh` covers them · vrx-a is a VMware VM: `/etc/machine-id` is cloned with VM templates, DMI serial is VMware's — host binding is optional and flagged (open question) · no VPP is needed except the one "running config stays applied" check
obligations accumulated from decisions (all must be honoured and listed in F-licensing.md with evidence): D-040 nothing licence-related goes to the agent; the data plane is never degraded by licensing — an expired licence rejects **new** unlicensed features at commit, the running config and the agent's Retrieve stay unchanged (pasted) · secrets rule 10 / 00-CONTEXT: no private key in the repo, fixtures, logs or status files (`git grep -n "PRIVATE KEY"` pasted; gitleaks if installed); the signature is never logged or returned by GET · D-046/D-091-style minimisation: `GET /state/license` returns status, entitlements, days left and the customer **name** only · RBAC: upload admin-only (P06/TD-2 roles), every mutation audited (DoD 9) · RFC 9457 problem+json with the `pointer` of the first offending node (403/422 as the prompt says; tampered file 400) · D-059 have-not items stay out (online activation, call-home, dongles, tamper-resistance, VDOM licences)
coordination: (1) F-aaa, F-restconf-yang, F-backup-restore (S5, may run in parallel) also touch app.module/router/nav/i18n and possibly DB tables — anchors + DB protocol; F-backup-restore decides whether the licence is part of a backup (question for it, not you) · (2) F-dashboard-prom-alarms may raise an alarm from your `system_event` (subsystem `licensing`); do not add a bus topic · (3) P13 CLI enforces nothing itself: the API rejects; regenerate the CLI table for your routes
SAFETY: only the "running config stays applied" check touches the slot agent/VPP: commit one prefixed w<SLOT> object (e.g. a `loop<SLOT>xx` loopback), paste Retrieve before/after expiry, delete it in cleanup; `systemctl show vpp -p NRestarts` before/after that run pasted; hold `flock -s` on the lab lock only during the run (D-094)
evidence: Playwright is not installed — use the headless Chrome approach from P07a/P07b/P08 (kept outside the product code) for screenshots of the licence page and the grace banner (en + fa/RTL)
time box: 15 h — when exceeded: stop, commit WIP, write docs/status/tasks/F-licensing.md with what is left
WIP: commit at least every 45 min; keep docs/status/tasks/F-licensing-wip.md current
CI: `TMPDIR=/tmp/g-w<SLOT> tools/ci.sh --base main` — short TMPDIR (unix socket paths ≤ 108 chars); no host-wide CI lock: golangci-lint serializes itself since main fc0fe68 (D-106 rejected serialising whole gates). Ports 3000/8080/9101 and /run/vrx/agent.sock belong to the running product stack (tools/app) — never touch them
finish: `tools/ci.sh --base main` green in the worktree · docs/status/tasks/F-licensing.md with pasted real output · everything committed · final message = 10-line summary (branch, last commit, CI result, evidence, open questions, decisions taken with options)
cleanup: stop every process you started (API/agent/vite), by PID · test keys and licences under /run/vrx-test/w<SLOT>/license/ deleted · your w<SLOT> objects removed (Retrieve pasted) · lab lock released · vrx_w<SLOT> dropped · dist/ removed
questions: docs/status/tasks/F-licensing-questions.md — write and keep going; never wait for a human
never: merge · restart/kill VPP · Docker · pkill · secrets in files · edit files you do not own
